# Moved

Active papers moved to [`../../papers/`](../../papers/).

This directory remains temporarily so older links resolve during the docs
reorganization; each `.md` file here is now a redirect stub to its new home
in `papers/`.

The frozen [`3.14.5/`](./3.14.5/) snapshot and `CDD-OVERVIEW.pdf` stay in
place (frozen by policy; see [`../../archive/`](../../archive/README.md)).
