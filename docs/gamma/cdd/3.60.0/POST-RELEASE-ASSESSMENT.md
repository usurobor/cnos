## Post-Release Assessment — 3.60.0

> Cycle **#235** (B2: `cn build --check` validates command entrypoints + skill `SKILL.md` presence). PR #276 squash-merged at `d814e166`; release commit `cce905e4` by β; tag `3.60.0` operator-pushed (β-step-8 deferral on harness-403; tag observed on origin during this PRA's authoring at `git ls-remote --tags origin` → `cce905e44375744681f8a53909269dc40659461c refs/tags/3.60.0`); α close-out at `.cdd/releases/3.60.0/alpha/CLOSE-OUT.md` (`1bb5e2f3`); β close-out at `.cdd/releases/3.60.0/beta/CLOSE-OUT.md` (`b03939aa`). PRA + step-12a CDD.md §Tracking patches authored by γ in this commit.

### 1. Coherence Measurement

- **Baseline:** 3.59.0 — α A-, β A, γ A-, **level L6 (cycle cap; diff L7).** Intervening 3.59.1 / 3.59.2 / 3.59.3 are δ post-cycle disconnects (spec/skill/doc patches only — no Go kernel or binary changes; no triadic scoring per CDD §9 mandatory-assessment scope).
- **This release:** 3.60.0 — α A, β A, γ A-, **level L5 (cycle cap; diff L6).** β's CHANGELOG provisional score (A/A/A-/L6 diff/L5 cycle cap) is **confirmed by this assessment without revision.**
- **Delta:**
  - **α** improved to A. Zero findings against the diff. R1 APPROVED at `f7d27b4`. Pre-implementation peer enumeration named the three authority surfaces (build-time validator in `internal/pkgbuild/build.go`, runtime command dispatch in `internal/discover/discover.go`, runtime skill activation in `internal/activation/index.go`) before writing code; new validator consumes `pkg.ParseFullManifestData` + `pkg.ClassSkills` — the canonical full-manifest parser already used by `discover.go:60` and the canonical skills-class constant already used by `activation`. No parallel parser introduced — `eng/go §2.17` "one parser per fact" + diff-local generalization (sharpened in 3.59.0 PRA per #274 F1) was applied successfully on first pass. AC2's "top-level subtree contains SKILL.md" rule was tightened mid-implementation when the first version false-positived on legitimate resource subdirs (`cnos.core/skills/naturalize/references/`, `cnos.eng/skills/eng/code/references/`); refinement was caught by live-package check, not by tests. AC4 verification path (copy-repo-to-tmpdir + inject-deliberate-break + run-binary + observe-exits) gave end-to-end CI-routing proof in ~30s without touching the working tree. **One operational miss:** α treated `mcp__github__subscribe_pr_activity` as primary and polling as one-shot; webhook did not deliver β's R1 review event; α did not re-poll `get_reviews` until operator prompted. CDD §Tracking already names polling as primary and webhook as complement — α's session inverted the priority. The miss did not affect the diff; the cycle cap (see γ scoring + §4b) is where it lands.
  - **β** held A. Review walked the full `cdd/review/SKILL.md` discipline: §2.0 (issue contract + AC↔evidence map) → §2.1 (mechanical scans + diff-internal coherence) → §2.2 (sibling/context audit incl. §2.2.1a input enumeration, §2.2.5 cross-ref, §2.2.8 authority surface, §2.2.9 module-truth, §2.2.10 contract-implementation confinement, §2.2.13 design constraints, §2.2.14 architecture spot-check) → §2.3 (verdict). Findings table empty; four observations N1–N4 explicitly framed as deferred-to-γ rather than findings against the diff (pre-existing parallel parser debt; `BuildOne` not gating on `CheckOne`; `os.Stat` symlink semantics; issue body's stale `docs/alpha/INVARIANTS.md` path). Architecture check 6/7 yes (1 n/a, 0 no). β-step-8 tag-push HTTP 403 handled cleanly per CDD §1.4 — addendum logic in close-out, no blocking on closure; tag has since landed via δ. **One operational miss, self-named in close-out:** on the new-branch transition for `claude/alpha-tier-3-skills-M8Vce`, β's first sync PR check used `mcp__github__list_pull_requests state=open head=usurobor:claude/alpha-tier-3-skills-M8Vce` and trusted the `[]` result; PR #276 was already open at exactly that head ref. Three filter forms attempted, all silently dropped the match: (a) MCP `head=owner:branch` filter (slash-encoding bug on in-repo branches); (b) `gh pr list --search 'closes:#235 OR refs:#235 OR fixes:#235'` (`closes:`/`refs:`/`fixes:` are not valid GitHub search qualifiers — query always returns empty); (c) the polling Monitor's 60s window (would have caught it on next tick). Operator caught the gap within one tick. β explicitly framed this as a §9.1 trigger fired against the §Tracking surface — the gap is per-event handling vs. session-start handling. Same root-class as α's miss, opposite direction.
  - **γ** held A-. Issue-quality gate held: #235 had all 11 γ §2.4 fields present at dispatch time (gap, evidence, numbered ACs AC1–AC4 independently testable, non-goals, Tier 3 skills, active design constraints with named DESIGN-CONSTRAINTS sections + INVARIANTS rows, related artifacts, priority P1, work-shape small single triad, sequencing AC1→AC4 in one PR, dependencies blocked-by:none/blocks:#216,#101). Dispatch was right-sized (one validator pass + tests + CI verify). PRA-first observation per the new γ ownership pattern. Selection ratified per §3.3 (3.59.0 PRA next-MCA #235) + §3.5 (MCI freeze; #235 in stale set). The A- (rather than A) captures three γ-side process gaps: (a) **issue body's stale doc-path** — #235 cited `docs/alpha/INVARIANTS.md` (canonical: `docs/alpha/architecture/INVARIANTS.md`) — same shape as 3.59.0 PRA §7's deferred-stale-paths in eng/go §2.17/§2.18 + pkg.go:134; (b) **§Tracking patch landed in 3.59.0 PRA was underspecified for the per-event surface** — the synchronous-baseline rule was named only for session-start; β's miss + α's miss are both per-event-handling shape, not session-start absorption shape; the patch closed the previous-cycle's gap but did not generalize one step further; (c) **harness branch-naming drift** continues from 3.59.0 (`claude/alpha-tier-3-skills-M8Vce` matches `claude/alpha-tier-3-skills-IZOsO` shape — non-canonical per CDD §4.2; harness-side, not agent-controllable). Patches in this commit close (a)'s class (issue-body doc-path drift is a γ §2.4 issue-quality post-flight to add) and (b) (per-event baseline rule + invalid-qualifier warning + MCP `head=` slash-encoding warning + missing `get_reviews` row in PR-comments table). (c) remains harness-side and operator-deferred.
  - **Level — L5 (cycle cap), L6 (diff).** The diff is L6: cross-surface coherence preserved (validator + activation + discover all consume the same authority via `pkg.ParseFullManifestData` + `pkg.ClassSkills`); architecture check 6/7 yes; T-002 + T-004 + INV-001 + INV-003 preserved-tightened; DESIGN-CONSTRAINTS §1 (single source of truth) + §2.2 (source/artifact/installed clarity) + §6.3 (degraded-path visibility) tightened. The cycle cap is L5 because two roles' polling discipline reached operator-correction in the same cycle — the cycle's autonomous coherence required two operator unblocks before β saw the PR and before α saw the review. Per CDD §9.1 lowest-miss rule, cycle caps at L5. β's CHANGELOG provisional score (L6 diff; L5 cycle cap) is **confirmed without revision**.
- **Coherence contract closed?** Yes for the substantive intent. Pre-cycle two failure classes shipped silently: (i) `cn build --check` passed even when `commands.X.entrypoint` pointed at a missing file (broken tarball); (ii) `cn build --check` passed even when `skills/X/` had no `SKILL.md` anywhere in subtree (silently invisible at activation time). Post-cycle both fail at `cn build --check` with exit 1, routed through I1's `coherence-build-check` job's existing exit-code gate; α verified via deliberate-break dry-run on a copy of `cnos.cdd`. The cycle did not introduce new CI workflow surface — the existing I1 gate already routed the new error classes. AC1 wording was earned beyond strict text (path-traversal + non-regular-file rejection added on top of "regular file under the package root") to make the AC's claim mechanically defensible.

### 2. Encoding Lag

| Issue | Title | Type | Design | Impl | Lag |
|-------|-------|------|--------|------|-----|
| #235 | B2 `cn build --check` validates entrypoints + skill paths | feature/process | converged | **shipped this release** | none |
| ~~#230~~ | (B1 distribution chain honesty — version-skip + release-bootstrap smoke) | — | — | shipped 3.59.0 | — |
| ~~#238~~ | (smoke release bootstrap compatibility) | — | — | subsumed by #230; shipped 3.59.0 | — |
| ~~#268~~ | (CDD package convergence from audit) | — | — | shipped 3.58.0 rollup | — |
| #266 | dist/ out of git (L7 MCA) | feature/process | converged | shipped 3.58.0 | none |
| #264 | Deterministic package tarball builds | bug | converged | shipped 3.58.0 | none |
| #262 | `cn pack` packlist derivation | feature | converged | partially shipped 3.58.0 (slice landed; AC1 rename + AC5 `--list`/`--dry-run` + AC3 root-file narrowing + `.gitignore` honoring deferred) | low |
| #249 | .cdd/ Phase 1 audit trail | feature | converged | partially shipped (`.cdd/releases/<v>/{α,β,γ}/` pattern in steady use across 3.57.0 / 3.58.0 / 3.59.0 / 3.60.0) | low |
| #256 | CI as triadic surface | feature | converged | not started | growing |
| #255 | CDD 1.0 master | tracking | converged | in progress (3.60.0 advances polling discipline per-event handling + invalid-qualifier warning + `get_reviews` clarification via this PRA's step-12a §Tracking patches) | low |
| #244 | kata 1.0 master | tracking | converged | in progress (Tier-1 + Tier-2 + framework shipped 3.54.0–3.55.0; M0/M4 work depends on #243/#245) | low |
| #245 | cdd-kata 1.0 (prove CDD adds value) | feature | converged | stubs only | growing |
| #243 | cnos.cdd.kata cleanup (M0/M4 + remove runtime + honest stubs) | feature | converged | partially done | low |
| #242 | .cdd/ directory layout, lifecycle, and retention | design | converged | not started (but `.cdd/releases/{version}/` move is steady-state adjacent partial impl) | low |
| #241 | DID: triadic identity | design | converged | not started | growing |
| #240 | CDD triadic protocol (git transport, .cdd/ artifacts) | design | converged | partial (Phase 1 in 3.55.0 + .cdd/releases/ moves in 3.58.0–3.60.0) | low |
| #218 | cnos.transport.git design | design | converged | not started | growing |
| #216 | Migrate non-bootstrap kernel commands to packages | feature | converged | not started | growing |
| #199 | Stacked v3.39.0 + v3.40.0 PRAs | docs | converged | not started | growing |
| #193 | Orchestrator `llm` step execution | feature | converged | not started | growing |
| #192 | Runtime kernel rewrite (OCaml → Go) | feature | converged | Phase 4 complete; remaining: orchestrator + wider port | low |

(Older items #190 / #189 / #186 / #182 / #181 / #180 / #175 / #170 / #168 / #162 / #156 / #154 / #153 / #149 / #148 / #142 / #141 / #138 / #135 / #132 / #124 remain open and stale across multiple cycles; not promoted to the active lag table this release. Per CDD §3.4, a stale-backlog re-evaluation cycle is overdue and should be slotted when no §3.3-default MCA forces a different next move; documented in §7 Next Move below — same disposition as 3.59.0 PRA.)

**Growing-lag count:** 7 (vs 8 in 3.59.0 — closed: #235; opened: 0). Net direction is converging: one growing item closed, none added; the remaining 7 are unchanged in design state and represent the durable backlog that has carried across 3+ cycles. **#235 was the smallest-clear-fix-path P1 growing item per 3.59.0 PRA's stack.**

**MCI/MCA balance:** **Freeze MCI continues** — 7 growing-lag items, well over the 3-issue threshold. No new substantial design work until the MCA backlog drops below threshold.

**Rationale:** The freeze pattern continues to deliver. Four cycles since 3.57.0 have followed the rule: 3.57.0 (#261 skill activation), 3.58.0 (rollup: #266 L7 + #264 + #262 partial + 15 CDD skill patches), 3.59.0 (#230 distribution chain honesty L7 diff), 3.60.0 (#235 build-check validation L6). Each picked from the stale set per §3.3 (PRA commitment) + §3.5 (MCI freeze). The next cycle's natural pick is **#262 AC1/AC5** (`cn pack` rename + `--list`/`--dry-run` + AC3 root-file narrowing + `.gitignore` honoring) — it finishes the 3.58.0 partial slice, sits at low-lag (in-progress), and composes cleanly with the install-authority chain that 3.59.0 hardened and the build-time validator that 3.60.0 just added (`cn pack` is the build-side counterpart to `cn deps restore`, both consuming the same content-class authority). After #262 AC1/AC5, candidates rank: **#218** (cnos.transport.git design — P1 growing) → **stale-backlog re-evaluation** per §3.4 (older #190/etc. set is now 4+ cycles deferred and overdue for descope/consolidate/commit triage).
