# CDD

## Source of truth

The canonical CDD specification lives in the package:

**`src/packages/cnos.cdd/skills/cdd/CDD.md`**

This file is a pointer. Do not add method content here.

`SKILL.md` in the same directory is the package-visible loader entrypoint. `CDD.md` is the normative source.

## Historical artifacts

Version-specific CDD artifacts (assessments, reviews) live in subdirectories here (`3.54.0/`, `3.55.0/`, etc.) for historical cycles. New cycles use `.cdd/releases/<version>/`.
