# β Close-Out — Cycle #250 (PR #259)

## Cycle summary

| | |
|---|---|
| Issue | #250 — `cn deps lock` vendors every package in index, ignores `deps.json` pins |
| PR | #259 — squash-merged to main as `4f860f1` |
| Ships in | 3.56.2 (release commit `0dedbc9`; tag `3.56.2` push pending transient 403 retry — see below) |
| Review rounds | **1** (APPROVED on Round 1, zero findings, straight to merge — at best-case target) |
| Findings total | 0 |
| Mechanical ratio | 0% (0/0) — undefined with zero findings; recorded for ledger consistency |
| Diff shape | **L7** (per α close-out — silent-success fallback removed; malformed manifests and stale pins surface at lock time rather than being masked into over-installs; friction class eliminated) |
| Cycle process level | **L6** (per α close-out — Go code L5-clean on first push; two pre-review CI iterations surfaced three bug-masked test-harness defects, all fixed in the same PR; β review clean Round 1) |

**Scoring convergence note:** β originally scored this cycle L5 at release time (CHANGELOG ledger row written before α close-out existed). After reading α close-out at `.cdd/releases/3.56.2/alpha/250.md`, β concurs with α's L6 cycle level — the pre-review CI iterations prevented a clean L7 cycle earn, but the diff itself did move a system boundary (silent-success fallback → explicit-error contract). CHANGELOG row revised from L5 to L6 in the same correction commit that ships the `eng/go` §2.12 addendum.

## Findings

### Finding A — skill stack worked as written

Every loaded skill performed its job without gaps:

- **Review §2.0 Issue Contract gate** — walked all four ACs before reading the diff; each AC mapped to a test name.
- **Review §2.2.9 module-truth audit** — enumerated every `idx.Packages` iteration and every `idx.Lookup` site across `src/go`; no stale "dump the index" assumption remained.
- **Review §2.2.1b sibling-fallback audit** — identified the removed silent fallback (index-dump → manifest-filter) and confirmed `Restore`'s `lockfile missing → nil, nil` is a documented no-op rather than a sibling fabrication.
- **Review §2.2.8 authority-surface conflict** — verified Go runtime (`GenerateLockFromIndex`), OCaml runtime (`lockfile_for_manifest`), `cn setup` default-manifest writer, and issue #250 ACs all agree on the same array-schema `.cn/deps.json` shape and exact-match pin resolution.
- **`eng/go` §2.17 Parse/Read purity boundary** — α wrote `ParseManifest([]byte)` in `pkg/` (pure) and `ReadManifest(path)` in `restore/` (IO). β grepped `src/go/internal/pkg/pkg.go` for `os.` imports; clean.
- **`eng/go` §2.5 error policy, §2.13 determinism** — every failure path wraps with `%w`; output stays sorted by `(name, version)`.
- **`eng/testing` §3.10** — test names encode ACs or failure classes (`_FiltersByManifest`, `_NameAppearsAtMostOnce`, `_RestoreInstallsOnlyPinned`, `_MissingManifest`, `_PinNotInIndex`, `_DefaultProfile`).

No skill patch needed. Recording this as a positive example.

### Finding B — α's cycle scope was precise

The PR body named the fix (single function rewrite), the two latent test-harness errors the bug had been masking (Tier-1 accepted `≥1 installed`, Tier-2 used object-syntax `packages`), explicit out-of-scope lines (semver, transitive deps, schema evolution, `cn setup` default-version policy), and the known debt (binary-version pin in dev/CI). No overclaiming, no underclaiming. The "discovered by PR #248, β review F3" cross-link back to where the bug was originally filed is preserved in issue #250; the PR closes the loop.

Triage: **drop** — no review action required. Recording this as an example of precise MCA scope for future cycles that bundle a fix with its masked downstream errors.

### Finding C — tag push hit the known 403 pattern

The 3.56.1 assessment §3 friction log flagged this exact pattern: `HTTP 403 curl 22 … Everything up-to-date` on `git push origin <tag>`, with `Everything up-to-date` being misleading (the tag has *not* in fact pushed; `git ls-remote --tags origin <tag>` is the authoritative verification surface). This cycle's tag push hit the same pattern. β applied the 3.56.1 guidance cleanly — did not trust the `Everything up-to-date` line, verified independently via `git ls-remote` after each retry, and observed the tag did not reach the remote through 4 exponential-backoff retries (2s, 4s, 8s, 16s). A background retry loop `b39vzm337` is running `until git push origin 3.56.2 lands`.

**Disposition:** this is now the **second consecutive cycle** hitting this pattern. If it recurs a third time, file as a tooling-gap issue (candidate MCA: either a longer release-skill retry budget or a harness-level diagnostic for the underlying 403 class). One more data point needed before filing — a 2/3 rate could still be environmental and transient. No skill patch shipped this cycle.

Triage: **watch** — one more recurrence triggers filing.

### Finding D — no D-level findings. No C-level findings. No B-level findings. No A-level findings.

Zero findings across the full A/B/C/D severity range. Review body body cites the audits β ran so this closure claim is reproducible by a third party.

## §9.1 Triggers

None fired:

- [ ] review rounds > 2 — **no**, 1 round
- [ ] mechanical ratio > 20% — **no**, 0/0 (undefined; recorded as 0%)
- [ ] avoidable tooling/environmental failure delayed the cycle — **no**; the tag-push 403 is the known environmental pattern from 3.56.1, handled in-process via background retry loop; it did not delay review, merge, or release commit
- [ ] loaded skill failed to prevent a finding — **no**; every loaded skill worked as written

Cycle Iteration section is therefore **omitted** per CDD §9.1 "If the cycle went cleanly (no trigger fires), cycle iteration may be omitted."

## γ reports

**One request to γ:**

- **Close issue #250.** The PR title referenced `(#250)` but did not include the `Closes/Fixes/Resolves` keyword that triggers GitHub's auto-close. Issue #250 therefore remains open after merge. β closed the coherence contract (all ACs met, tests cover the reproducer, CI green); γ or operator should mechanically close the GitHub issue once the release tag lands. Alternatively β can close it as part of the closeout commit — deferring to γ's preference on who mechanically clicks the close button. (Candidate MCA for the release skill: "on release commit, close all referenced issues" — but that is a process-economics question for γ, not β.)

**α's proposed MCA for this cycle — shipped as immediate output:** α close-out proposed a one-line addendum to `eng/go` §2.12 (Schema and compatibility) extending the schema-audit scope to sibling shell test harnesses producing JSON for the same parsers. This was the authoring-time skill gap that allowed `cnos.kata/lib.sh` `write_deps_json`'s object-schema output to survive to CI discovery rather than authoring-time audit. β concurs and shipped the addendum in the same correction commit as this close-out revision. The addendum adds a "sibling harnesses are contract surfaces too" rule with a mechanical grep form and a derivation cite to this cycle (`src/packages/cnos.eng/skills/eng/go/SKILL.md` §2.12).

**No other pending MCA proposals from β this cycle.** The row 7a peer-enumeration addendum proposed by α in the 3.56.1 close-out remains pending γ triage; this cycle did not exercise it (no canonical-form table edits in the diff) but also did not invalidate it. β's prior concurrence stands: the addendum is narrow, mechanical, CDD-evolvable, and a valid MCA candidate for the next F1-shaped finding.

## Next cycle

β concurs with this assessment's §7 commitment: **#230** (`cn deps restore` version upgrade skip) is the next MCA. Rationale in §7 of the assessment — adjacency to this cycle's subsystem, completes the manifest→lock→installed authority chain, P1 bug with a narrow fix path, and the 3.56.1 assessment already queued it.

Signed: β (beta@cdd.cnos) · 2026-04-16 · release commit `0dedbc9` · assessment `docs/gamma/cdd/3.56.2/POST-RELEASE-ASSESSMENT.md`
