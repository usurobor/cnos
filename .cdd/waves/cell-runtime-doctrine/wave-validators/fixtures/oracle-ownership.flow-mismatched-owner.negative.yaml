# NEGATIVE fixture (cnos#671 R16) — FLOW-style registry entry whose (owner,predicate) matches NO child.
# The child declares (wc-x, alpha_mechanical); the registry (flow style) instead carries
# (wc-z, alpha_mechanical) — a PHANTOM (registry -> no child) AND the child's alpha_mechanical is MISSING.
# R11's parser could collapse flow entries to empty fields and spuriously agree; R12 sees both. MUST exit non-zero.
# Run:  go run ../oracle_ownership_bijection.go oracle-ownership.flow-mismatched-owner.negative.yaml  -> exit 1
child_predicates: [
  {owner: "wc-x", predicate: "alpha_mechanical"},
]
assurance: [
  {owner: "wc-z", predicate: "alpha_mechanical", classification: "mechanically-verifiable", checker: "check_alpha.go"},
]
