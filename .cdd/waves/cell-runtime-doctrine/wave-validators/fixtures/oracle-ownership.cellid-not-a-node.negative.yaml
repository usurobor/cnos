# NEGATIVE fixture (cnos#671 R16) — a child owner that is NOT an owning wave node.
# `wave_nodes` lists the legal owner namespace (the wave-dir analog of contract cell.id ∈ wave nodes).
# The child declares owner `wc-ROGUE`, absent from wave_nodes, so ownership cannot be attributed to a
# real node: FAIL CLOSED (exit 2). Pins Codex's "contract cell.id inconsistent with the owning wave node".
# Run:  go run ../oracle_ownership_bijection.go oracle-ownership.cellid-not-a-node.negative.yaml  -> exit 2
wave_nodes: ["wc-x", "wc-y"]
child_predicates: [
  {owner: "wc-ROGUE", predicate: "alpha_mechanical"},
]
assurance: [
  {owner: "wc-ROGUE", predicate: "alpha_mechanical", classification: "mechanically-verifiable", checker: "check_alpha.go"},
]
