# NEGATIVE fixture (cnos#671 R16) — FLOW-style child predicate with NO registry entry.
# `beta_cognitive` is declared as a CUE-valid flow list item; the registry omits it. R11's ad-hoc
# parser dropped flow items and would false-pass; R12 normalizes via cue export, sees the predicate,
# and reports it MISSING (child -> no registry). MUST exit non-zero.
# Run:  go run ../oracle_ownership_bijection.go oracle-ownership.flow-missing-registry.negative.yaml  -> exit 1
child_predicates: [
  {owner: "wc-x", predicate: "alpha_mechanical"},
  {owner: "wc-x", predicate: "beta_cognitive"},
]
assurance: [
  {owner: "wc-x", predicate: "alpha_mechanical", classification: "mechanically-verifiable", checker: "check_alpha.go"},
]
