# NEGATIVE (must FAIL #CellContract) — a sibling_output ref in inputs.optional.
# §2: inputs.optional is external-locator refs ONLY; a sibling_output can never be optional.
schema: cn.cell.contract.v1
cell:
  id: "neg"
  class: "working"
  mode: "doctrine"
  protocol: "cds"
  matter_domain: "doctrine"
scope:
  repo: "usurobor/cnos"
  wave: null
  parent_cell: null
intent_ref:
  schema: "cn.intent.v1"
  id: "intent-neg"
  carrier:
    kind: "github_issue"
    ref: "cnos#671"
inputs:
  required:
    - ref_kind: "external"
      locator:
        kind: "repo_artifact"
        repo: "usurobor/cnos"
        commit: "6e40d93497589afd96e6c891e94851cdabe2ef3a"
        path: "docs/architecture/CELL-RUNTIME.md"
  optional:
    - ref_kind: "sibling_output"   # VIOLATION: optional refs are external-only
      producer: "wc-2"
      output_id: "coherence-measurement-contract"
requested_output:
  id: "o"
  kind: "artifact"
  path: "docs/architecture/X.md"
acceptance:
  predicates:
    - "p"
constraints:
  allowed_paths:
    - "docs/architecture/X.md"
  forbidden_paths: []
  non_goals: []
gates:
  operator_authorization_required: false
  operator_acceptance_required: false
  reason: null
doctrine_affecting: false
stop_conditions: []
