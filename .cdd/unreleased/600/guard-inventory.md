# Guard-surface inventory — final (cycle #600)

Every row below is α's final classification, produced by re-reading the current file state directly (not inherited from γ's hypothesis without re-verification). Where α's conclusion differs from γ's starting hypothesis, the difference and reason are stated.

| # | Surface | γ's hypothesis | **α's final classification** | Reason / evidence |
|---|---|---|---|---|
| 1 | `scripts/ci/check-dispatch-repair-preflight.sh` (cnos#516) | NARROW (open question: could the scope of `need` checks be reduced) | **KEEP** | Full read (69 lines) shows this script was *already* presence-of-contract only — no live wake execution, no FSM read, no behavior check. γ's hypothesis of "narrow-worthy" was based on an open question, not a confirmed excess; on full read there is nothing left to narrow out. The behavioral half of the invariant (does `changes -> todo` actually require repair context) is proven live by `transitions.json`'s `repair_contract_present` guard (lines 225-240) and `issuesfsm_test.go`'s `TestAC6_ChangesWithoutRepairContextBlocked` / `TestAC6_ChangesWithRepairContextEnablesRepairPass` (both confirmed PASS). Header comment updated to state this explicitly (AC5). No functional change; `bash -n` clean; script re-run, exit 0. |
| 2a | `scripts/ci/check-dispatch-closeout-integrity.sh` — presence-of-contract half | NARROW (same open question as #516) | **KEEP** | Same reasoning as row 1 — already narrow by construction. Header rewritten to cite the specific Go tests + `transitions.json` guard that now prove the behavioral half (AC5). Script re-run post-edit, exit 0. |
| 2b | `scripts/ci/check-dispatch-closeout-integrity.sh` — `--self-test` / `closeout_violation()` shell predicate | Split hypothesis: FOLD the self-test | **FOLD (removed; replacement cited)** | The 4-case hand-rolled shell predicate (`(review,no,no)→violation`, `(review,yes,no)→violation`, `(review,yes,yes)→ok`, `(in-progress,no,no)→ok`) duplicated an invariant already proven against the REAL FSM transition table by `TestAC3_EmptyReviewBlocked`, `TestApply_EmptyReviewStateBlocked`, `TestAC574_ReviewPartialEvidenceBlocked`, `TestAC574_ReviewWithPRStillValid` (all confirmed to exist, all confirmed PASS both before and after this edit). Grepped the entire repo for `--self-test` invocations outside this script (γ's friction note 2): every hit is either a per-cycle β/α verification-evidence log from an already-closed cycle (`.cdd/unreleased/{524,569}/*`, historical/append-only) or the unrelated `validate-skill-frontmatter.sh --self-test` (a different script, still live in `build.yml:272`, untouched). No operator runbook or living doc depends on invoking this script's `--self-test` as a standing procedure. Removed the `closeout_violation()`/`self_test()` functions and the `--self-test` CLI branch; the script is now presence-of-contract only. `dispatch-protocol/SKILL.md` §2.9/§4.9/D12 updated in the same commit to stop claiming the CI guard "self-tests the empty-review detector" (a sibling-doc-drift the fold would otherwise leave stale) and instead cite the Go tests directly. |
| 3 | cnos#574 review-guard tightenings in `transitions.json` (`in-progress`, `review` states) | KEEP (the replacement target, not a removal candidate) | **KEEP, confirmed** | Read `transitions.json` in full for the relevant blocks. `in-progress -> review` rule (lines 136-151) confirmed `all_true: [review_request_present, pr_exists, pr_has_commits]` exactly as γ described. `review` state valid rule (lines 206-211) confirms the same tightening. `_doc_phase2` block (lines 66-90) explains the cnos#569/#574 tightening and its backward-compat claim. Checked for a dead/superseded rule variant alongside these — none found. No edit made; this file does not appear in `git diff origin/main..HEAD --stat`. |
| 4 | `cell_kind` seam (`snapshot.go`, `fetch.go`, `decision.go`, `issuesfsm_test.go`, `CELL-KINDS.md`) | KEEP as-is / narrow-language-check only | **KEEP, confirmed — no edit** | Full read of all cell_kind-specific comments in `snapshot.go` (lines 96-125), `fetch.go` (lines 149-218), `decision.go` (lines 92-96), and `CELL-KINDS.md`'s "Observation, not enforcement" (156-158) and "Future typed field" (154) sections. All read as confident, intentional, forward-pointing prose — none read as forgotten/loose scaffold (resolves γ's friction note 3: judged the "Future typed field" paragraph as NOT misleading, an honest deferred-scope statement). `TestSeam_CellKindNotEnforced` read in full (issuesfsm_test.go:810-854): 10 fixtures × 5 cell_kind values, byte-identical-decision assertion, confirmed PASS, untouched. `table.go` (full 217-line read) confirmed zero rules consume `CellKind`. |
| 5 | Prompt-presence doctrine (`dispatch-protocol/SKILL.md` §2.8/§2.9/§4.8/§4.9, D11/D12) | KEEP the doctrine prose; classification is about the scripts | **KEEP, with a targeted edit** | §2.8/§4.8/D11 (repair re-entry) — unchanged, still accurate, no dependency on anything folded this cycle. §2.9/§4.9/D12 (closeout integrity) — edited to remove the now-stale claim that the CI script "self-tests the empty-review detector" (see row 2b), replaced with a direct citation of the Go FSM tests that now carry that invariant. This is the sibling-surface update the fold in row 2b required per α's peer-enumeration duty (`alpha/SKILL.md` §2.3) — found via grepping `self-test` across `dispatch-protocol/SKILL.md`, `cds-dispatch/SKILL.md`, its golden, and the live workflow: only `dispatch-protocol/SKILL.md` (3 sites) referenced it; the SKILL/golden/live-workflow render chain does not (confirmed via targeted grep, zero hits in those three files), so no golden re-render was required. |
| 6 | TODO/scaffold comments (#568/#570/#575/#591) across `issuesfsm.go`, `scan.go`, `table.go` | Preliminary: no dead TODO found via grep, but full-file reads not yet done | **No dead scaffold found — confirmed via full-file reads, negative result** | Read all three files in full (286 + 217 + 397 lines respectively — the exact files γ flagged as not yet read in full). Every comment referencing #568/#569/#570/#575/#591/#593 describes the still-current architecture accurately; none describe a superseded pre-Sub-B/C state. Widened grep (`todo|fixme|hack|scaffold|temporary|stopgap|will be replaced|until .* lands|superseded`) across the three files returned only false positives (the literal GitHub label `status:todo`, and file-path references to `.cdd/unreleased/{N}/gamma-scaffold.md`). One self-declared, intentional duplication was found and explicitly NOT removed: `scan.go:197-204`'s "Defensive re-check" comment, which re-verifies the `activeScanStates` filter already applied in `liveListActiveIssues` (lines 316-326) — this protects against a real concurrent-transition race (a claim firing between listing and observing), not strand-era guard residue, and removing it would reintroduce exactly the kind of blind spot the STOP conditions warn against. Left unmodified. |
| 7 | `cn cell finalize` (#591) / `cn issues fsm scan` (#593) | Not audit targets — replacements | **Not audit targets, confirmed** | Cited as AC2/AC3 replacement proof only (see rows above); no changes made or needed. |

## Discretionary item considered and explicitly declined (see self-coherence.md §Debt)

An `install-wake-golden` byte-identity redundancy was identified during the audit (the "LIVE" leg of both scripts' presence-check loops is logically redundant with `install-wake-golden`'s sha256 byte-identity gate between the live workflow and its golden) but was **deliberately not acted on** this cycle — see §Debt for the full reasoning (the redundancy claim depends on `install-wake-golden`'s path-filtered trigger always firing whenever LIVE/GOLDEN could diverge, which is plausible but not exhaustively proven, and this surface is outside cnos#600's named scope). This is not a blank/TBD cell — rows 1 and 2a above are fully classified KEEP regardless of this open question; the open question is about a *further, optional* narrowing beyond what this cycle resolves, not about the classification of the rows themselves.
