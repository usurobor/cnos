# γ scaffold — cycle #600

**Issue:** [cnos#600](https://github.com/usurobor/cnos/issues/600) — "cds/runtime: consolidate strand-era guards and scaffold now that the mechanical lifecycle is closed (Sub D of #583)"
**Mode:** `design-and-build` (per the issue's own `**Mode:**` field, confirmed against the fetched issue body: `**Mode:** design-and-build`)
**cell_kind:** `audit` — this cycle's matter is a classification inventory + narrow removal/narrowing diff, not a green-field feature. Per `CELL-KINDS.md`'s taxonomy this is the closest named kind (`audit` — "inventory / drift report / proof table"; `status:todo -> status:review`, PR optional, report artifact is the matter). Recorded here as the canonical recording point per `CELL-KINDS.md` §"Observation, not enforcement" — **observed-only**, does not gate any FSM transition (see AC4 below; do not read this line as new cell_kind enforcement).
**Parent:** #583 (Sub D). **Preconditions (all merged, per issue body):** #584, #575, #591, #593.
**Base SHA:** `1c5dd993fcd25b5bdca3843555d240895e3212ee` (verified via `git fetch origin main && git rev-parse origin/main` at branch-creation time).
**Branch:** `cycle/600`, created from `origin/main` at the base SHA above, pushed to `origin/cycle/600`.

---

## Guard-surface inventory (γ's evidence-grounded starting hypothesis — NOT the audit's conclusion)

Every row below was produced by actually reading the named file (or grepping the named symbol) — not asserted from the issue text alone. α does the real classification and must cite replacement proof per AC2; this table is the concrete starting point so α does not have to rediscover the surface set from scratch.

| Surface | Current role (from reading it) | Preliminary classification hypothesis | Why |
|---|---|---|---|
| `scripts/ci/check-dispatch-repair-preflight.sh` (cnos#516) | Presence-of-contract guard only — greps `dispatch-protocol/SKILL.md` §2.8, `cds-dispatch/SKILL.md`, its golden, and the live workflow for the literal strings `Repair re-entry preflight`, `REPAIR-PLAN`, `repair_evidence`, `run_class`, `first_pass`, `repair_pass`. It does **not** run a wake and does not touch the FSM. It guards prompt/doc text presence, not FSM behavior. | **NARROW** (hypothesis) — the *behavioral* invariant (changes→todo repair re-entry requires repair context) is now proven live by `transitions.json`'s `changes` state rules (lines ~229/239, "repair contract / prior beta-delta findings present; changes -> todo repair re-entry enabled" / "blocked: no repair contract... cnos#516") and locked by `issuesfsm_test.go`'s `TestAC6_ChangesWithoutRepairContextBlocked` / `TestAC6_ChangesWithRepairContextEnablesRepairPass`. What this script *uniquely* still protects is that the **prompt itself still tells the worker to load repair context** — a doc-drift guard, not a behavior guard. α should confirm whether that prompt-presence fact is still worth a dedicated CI job or folds into a lighter doc-integrity check. Do not REMOVE outright: the FSM enforces the state-transition invariant but nothing currently enforces that the *prompt text* keeps instructing the worker to look at repair context before dispatch. |
| `scripts/ci/check-dispatch-closeout-integrity.sh` (cnos#524 W4) | Two jobs: (1) presence-of-contract grep (same shape as the #516 script, targeting `dispatch-protocol/SKILL.md` §2.9, `cds-dispatch/SKILL.md`, golden, live workflow for `Closeout integrity`, `deliverable_evidence`, `commits beyond its base`, `No-deliverable rule`, `status:review`); (2) `--self-test` — a **pure-function empty-review detector** (`closeout_violation(status, has_pr, has_commits)`) exercising 4 hand-written cases (empty review / PR-no-commits / PR+commits / non-review). This self-test does not read live GitHub state; it is a logic-shape unit test embedded in bash. | **Split hypothesis: FOLD the self-test, NARROW the presence guard.** The empty-review invariant this self-test encodes is now the FSM's own live behavior: `transitions.json`'s `in-progress` review rules (lines ~142/150) require `review_request_present && pr_exists && pr_has_commits` (all_true, tightened by cnos#574 from any_true), and `issuesfsm_test.go`'s `TestAC3_EmptyReviewBlocked` / `TestApply_EmptyReviewStateBlocked` / `TestAC574_ReviewPartialEvidenceBlocked` exercise the identical logical cases the bash self-test hand-rolls, against the real transition table rather than a re-implemented `closeout_violation` shim. α should assess whether the bash self-test is now a **redundant re-implementation** of a fact the Go FSM tests already prove (candidate FOLD: delete the bash self-test, cite the Go tests as replacement) while the presence-of-contract half (does the prompt still say the words) may still be NARROW-worthy for the same doc-drift reason as the #516 script above. |
| cnos#574 review-guard tightenings in `transitions.json` (`in-progress`, `review` states) | Live, current, exercised. `_doc_phase2` / rule `reason` fields at lines 142/150/210/217 document the any_true→all_true tightening (REVIEW-REQUEST.yml + open PR + commits beyond base, all required). Tests: `TestAC574_ReviewRequestAloneNoLongerValid`, `TestAC574_ReviewPartialEvidenceBlocked`, `TestAC574_ReviewWithPRStillValid`, `TestAC574_InProgressBranchOnlyNoLongerProposesReview`, `TestAC574_InProgressWithMatterStillProposesReview`, `TestAC574_InProgressRunActiveNonGatingPreserved`. | **KEEP** (hypothesis) — this is itself the *replacement* the two CI scripts' invariants fold into, not a strand-era leftover to prune. Out of scope for removal; in scope as a citation target for AC2. α should confirm no dead/superseded rule variant remains alongside it (I did not find one — the `_doc_phase2` block explicitly states these rules change no Phase-1 fixture outcome). |
| `cell_kind` seam (`snapshot.go`, `fetch.go`, `decision.go`, `issuesfsm_test.go`, `CELL-KINDS.md`) | Fully wired as **observed-only**: `FactSnapshot.CellKind{Observed,Source,DefaultedTo}` (`snapshot.go:96-132`); `fetch.go`'s `assembleLive` parses the `cell_kind:` line from `gamma-scaffold.md` via `parseCellKind`/`cellKindLinePattern` (`fetch.go:149-219`) and sets `Source: "cdd_artifact"`; `decision.go:92-96` prints it in the decision block "so the defaulting is operator-visible rather than silent"; `TestSeam_CellKindNotEnforced` (`issuesfsm_test.go:810-850`) evaluates 11 fixtures under 5 different `cell_kind` values each and asserts byte-identical `(Outcome, TargetState, Action)` — i.e. the non-enforcement invariant AC4 requires is already locked by a real test, not prose. `CELL-KINDS.md` §"Observation, not enforcement" already states plainly: "This is **observation only** — no transition rule in `table.go` consumes `CellKind`... Consuming `cell_kind` in transition guards... is deferred to a future, explicitly scoped FSM Phase 2 issue." | **KEEP as-is / narrow-language-check only.** I did **not** find literal "TODO" language, nor "forgotten"/"loose"-reading phrasing, in `snapshot.go`, `fetch.go`, `decision.go`, or `CELL-KINDS.md`'s current prose (`grep -rn "TODO" ... | grep -iE "568|570|575|591|cell_kind"` returned no hits in these files). The comments already read as intentional and confident ("observed but not consumed by any transition rule. Shown so the defaulting is operator-visible rather than silent"). **Friction note for α:** re-verify this yourself before declaring AC4 trivially satisfied — my grep was not exhaustive of every comment in the vicinity (e.g. `CELL-KINDS.md`'s "Future typed field (deferred, Phase B)" paragraph uses hedged language — "could define," "explicitly out of scope for this doctrine-first cell" — that reads as forward-looking design, not forgotten scaffold, but α should judge whether any phrase there qualifies as the "misleading TODO language" AC4 asks to remove). Do not weaken/delete `TestSeam_CellKindNotEnforced` under any classification. |
| Prompt-presence guards for the strand class generally (`dispatch-protocol/SKILL.md` §2.8 "Repair re-entry detection and preflight," §2.9 "Closeout integrity," and §4.8/§4.9 verify-checklist rows) | Doctrine prose defining the two invariants the CI scripts above check for presence of. §2.9 explicitly names the CI guard by path in its own text ("A CI guard (`scripts/ci/check-dispatch-closeout-integrity.sh`) asserts the rendered dispatch surface carries this contract"). | **KEEP the doctrine prose; classification of the guard scripts (above) is the actual audit surface.** α should not conflate "the doctrine section exists" (clearly still load-bearing prose naming the invariant) with "the CI presence-check script is the right mechanism at this granularity" (the actual audit question, addressed in the two script rows above). |
| TODO/scaffold comments from #568/#570/#575/#591 | Targeted `grep -rn "TODO"` across `src/`, `scripts/` filtered for `568|570|575|591|strand|cell_kind` returned **zero hits**. Broader `grep -rn "TODO" -i` across the fsm/dispatch-protocol/cds-dispatch/golden/live-workflow surfaces also returned zero hits. | **Preliminary: no dead TODO scaffold found at the surfaces I checked.** This is a negative result, not a guarantee — α's own peer enumeration (γ/SKILL.md §2.2a analog for α) should re-run the grep with a wider net (e.g. `rg -i "todo|fixme|scaffold" src/packages/cnos.issues/commands/issues-fsm/ src/go/internal/cell/ src/packages/cnos.cds/skills/cds/fsm/ src/packages/cnos.cdd/skills/cdd/CELL-KINDS.md`) before asserting AC1's "every strand-era guard is classified" is vacuously complete on this axis. Item 4 of the issue's Scope §1 ("dead scaffold: stale TODOs · dead comments · redundant fixture scaffolding · obsolete guard docs") may still surface something in files I did not read in full (e.g. `issuesfsm.go`, `scan.go`, `table.go` bodies beyond the greps I ran). |
| `cn cell finalize` (#591, `src/go/internal/cell/cell.go` + `cell_test.go`) | The finalizer: parses return/resume/finalize args, drives label transitions (converge/iterate/reject), checkpoint + PR-open. ~35 tests (`TestReturner_Return_*`, `TestResumer_Resume_*`, `TestParseFinalizeArgs_*`) already exercise atomicity, preflight-wrong-status, label-drift-on-failure, issue/verdict-mismatch rejection. | Not itself an audit target (it's a *replacement*, per the issue's Context section: "Finalizer... owns matter checkpoint + PR-open"). **Citable as AC2 replacement proof** for any guard whose invariant is "checkpoint/PR-open happened correctly." |
| `cn issues fsm scan` (#593, `scan.go` + `scan_test.go`) | The reconciler: dead-run sweep with 9 tests covering live-run-no-op, dead-no-matter→requeue, dead-with-matter-no-PR→finalize+comment, dead-with-PR-no-review-request→no-op, review-request-with-matter→apply-review, stale-review-no-proof→blocked, idempotence, no-apply dry-run, and the AC1/Sub593 wiring test. | Not itself an audit target — a *replacement*. **Citable as AC2 replacement proof** for any guard whose invariant is "stranded/dead-run recovery happened correctly." |

---

## AC oracle approach

| AC | Concrete check |
|---|---|
| **AC1** — guard inventory exists, every strand-era guard classified | The table above, extended by α to a final version with every row assigned exactly one of KEEP/REMOVE/FOLD/NARROW (not left as a "hypothesis"). Oracle: the final table in α's `self-coherence.md` (or a dedicated `.cdd/unreleased/600/guard-inventory.md`) has no blank/TBD classification cell across all rows named in the issue's Scope §1 plus any additional surface α's own peer enumeration turns up. |
| **AC2** — no guard removed without a cited live replacement | For every row classified REMOVE (or the REMOVE half of a split FOLD), the row names a specific test function or transition-table rule (e.g. `TestAC574_ReviewPartialEvidenceBlocked`, `transitions.json` lines ~142-150) as the replacement. Oracle: `grep` the named test/rule actually exists and actually exercises the claimed case (α re-runs `go test ./src/packages/cnos.issues/commands/issues-fsm/...` and points at the specific `-run` pattern proving it green). |
| **AC3** — strand fixtures still pass | Run `go test ./src/packages/cnos.issues/commands/issues-fsm/... ./src/go/internal/cell/...` post-consolidation. Minimum named fixtures/tests to verify explicitly green: empty-review (`TestAC3_EmptyReviewBlocked`, `TestApply_EmptyReviewStateBlocked`), repair re-entry (`TestAC6_ChangesWithoutRepairContextBlocked`, `TestAC6_ChangesWithRepairContextEnablesRepairPass`), matter-without-PR (`TestScan_DeadWithMatterNoPR_FinalizesAndComments`), dead-run-no-matter (`TestScan_DeadNoMatter_RequeuesToTodo`, `TestAC4_DeadInProgressNoMatterProposesRequeue`), dead-run-with-matter (`TestAC5_DeadInProgressWithCommitsProposesDeltaRecovery`, `TestScan_DeadWithPRNoReviewRequest_FinalizeNoOpNoComment`), review-request guarded transition (`TestAC569_InProgressReviewRequestWithMatterProposesReview`, `TestAC569_InProgressReviewRequestNoMatterBlocked`, `TestAC574_ReviewRequestAloneNoLongerValid`). |
| **AC4** — `cell_kind` resolved | `TestSeam_CellKindNotEnforced` still present and unweakened (byte-diff check against pre-cycle version, or at minimum: still asserts all 11 fixtures × 5 kinds byte-identical decision); `CELL-KINDS.md` §"Observation, not enforcement" still states non-enforcement plainly; grep for "TODO" across the cell_kind surfaces returns no misleading hits (see friction note above — this needs α's own fresh grep, not just mine); no new transition rule in `table.go` reads `CellKind`. |
| **AC5** — prompt-presence guards narrowed if kept | For each of the two CI scripts kept in any form, the row/PR description states explicitly what it still protects post-consolidation (e.g. "protects prompt-text presence only; the behavioral invariant is proven by transitions.json + Go tests cited above"). Oracle: the script's own header comment (or a companion doc) says this in so many words — not left implicit. |
| **AC6** — no lifecycle invariant weakened | Diff review: no `transitions.json` rule changed shape (schema), no test deleted without a citation of the equivalent replacement coverage, no CI gate removed from `build.yml` without the invariant it protected being independently proven elsewhere. If any ambiguity exists on this axis, STOP per the issue's STOP conditions (see Scope guardrails below) rather than guessing. |
| **AC7** — all gates green | Named gates from the issue: I1, I2, I4, I5, I6, install-wake-golden, dispatch-repair-preflight, dispatch-closeout-integrity, Go, Package, Binary — all green on the merge-candidate SHA, verified via `gh run list` / CI status per β's pre-merge gate. |

---

## Empirical anchor citations (from the issue body — no new anchors invented)

- cnos#516 — dispatch repair re-entry preflight (Pass 4D / cnos#514 failure mode) — audited by `scripts/ci/check-dispatch-repair-preflight.sh`.
- cnos#524 — dispatch closeout-integrity (W4 RCA: empty-review false-complete) — audited by `scripts/ci/check-dispatch-closeout-integrity.sh`.
- cnos#574 — review-guard tightening (any_true → all_true) — the audit's central "this is now the replacement" citation target.
- cnos#568/#570 — `cell_kind` seam origin (taxonomy + observation-only doctrine) — resolved per AC4, not enforced.
- cnos#391/#392/#393 — not directly referenced by any surface I read in this scaffold's peer enumeration; carried forward only if α's own audit surfaces a direct dependency. Not asserted here.
- cnos#583 (master), #584 (Sub A doctrine), #575 (FSM authority), #591 (Sub B finalizer), #593 (Sub C reconciler), #566 (reaper idea, closed superseded) — context anchors per the issue's own "Related" section; not independently re-verified beyond what the peer-enumeration greps above already touched (`#575`/`#591`/`#593` surfaces were read directly; `#584`/`#566` were not opened as part of this scaffold — cite from the issue text only).

---

## Expected diff scope (coarse — not line-by-line)

- `scripts/ci/check-dispatch-repair-preflight.sh` — likely narrowed (scope of `need` checks reduced) or left as-is with an explicit doc-comment update stating what it now uniquely protects; unlikely to be deleted outright given AC5's "narrow, don't silently drop" framing.
- `scripts/ci/check-dispatch-closeout-integrity.sh` — likely split: the `--self-test` shell function is the more plausible FOLD/removal candidate (citing Go FSM tests as replacement); the presence-of-contract half likely narrowed similarly to the #516 script.
- `.github/workflows/build.yml` — only if a guard's invocation step changes (e.g. a flag added to a `run:` line); **do not** touch `.github/workflows/*.yml` beyond what's strictly required by a scoped script change, per this cycle's own constraint on γ (which also binds the spirit of the audit — avoid `.github/workflows/` churn).
- `src/packages/cnos.cds/skills/cds/fsm/transitions.json` — likely **no shape change** (per the issue's own AC2 replacement-citation framing, this file is the *reference*, not the audit target); at most a doc-comment (`_doc*` array) clarification if α finds the cnos#574 rationale prose itself needs updating to reflect the consolidation. Schema must not change (per this scaffold's α-prompt implementation contract below).
- `src/packages/cnos.issues/commands/issues-fsm/*.go` + `*_test.go` — possible: removing a redundant bash-reimplemented test case in favor of citing the existing Go test, or adding a doc comment clarifying `cell_kind`'s non-enforcement more explicitly. Unlikely to need new test *logic* since the invariants are already covered — mostly comment/doc-level clarification plus whatever the bash-script FOLD requires on the Go side (probably nothing, since the Go tests already exist independently).
- `src/packages/cnos.cdd/skills/cdd/CELL-KINDS.md` — possible small prose edit if AC4's "no misleading TODO" turns up something on a closer read than mine.
- `src/packages/cnos.core/skills/agent/dispatch-protocol/SKILL.md` — possible small doc-comment update to §2.8/§2.9 if the guard-script narrowing changes what those sections should say the CI guard actually checks.
- No new Go packages, no new CLI subcommands, no new labels, no new FSM states/transitions.

---

## Scope guardrails

Restated by reference only (do not restate the full text — see the issue body directly):

- **Out of scope** — issue §"Out of scope" (no new labels; no new FSM behavior unless required only to preserve an existing invariant; no weakening guards without replacement proof; no Demo 0; no #564; no #585/#586 unless directly referenced; no `cell_kind` enforcement; no new cell-kind transition semantics).
- **STOP conditions** — issue §"STOP conditions" (guard removal would weaken protection; an invariant only protected by prompt prose — surface it, don't rely on it silently; `cell_kind` enforcement becomes necessary; new FSM behavior required beyond consolidation; deleting a guard creates a blind spot; CI cannot prove the replacement; Demo 0 would start). **α/β: escalate on any STOP hit — do not work around.**

---

## α prompt

**Branch:** `cycle/600` (already created and pushed from `origin/main` at `1c5dd993fcd25b5bdca3843555d240895e3212ee`; switch to it, do not create a new branch.)

**Task:** Execute the audit-first consolidation named in cnos#600. Start from the guard-surface inventory table above — treat it as a verified starting hypothesis, not settled fact. For each row: (1) confirm or correct the preliminary classification with your own reading of the current file state (do not trust the hypothesis blindly — re-grep, re-read); (2) for every REMOVE/FOLD, cite the specific test function(s) or `transitions.json` rule(s) that now carry the invariant, and prove they're green; (3) for every KEEP/NARROW, state explicitly (in the script's own header comment or an adjacent doc) what it still protects post-consolidation, per AC5. Resolve `cell_kind` per AC4 exactly as scoped (observed-only, non-enforced, `TestSeam_CellKindNotEnforced` intact and unweakened, remove only misleading TODO language if any exists on closer inspection — do not enforce, do not add cell-kind transition semantics). Run the full strand-fixture suite named under AC3's oracle before declaring review-ready. If any STOP condition fires, halt and surface to γ/δ rather than working around it.

**## Implementation contract**

| Axis | Pin |
|---|---|
| Language | Bash (guard scripts under `scripts/ci/`) + Go (FSM tests under `src/packages/cnos.issues/commands/issues-fsm/`, finalizer tests under `src/go/internal/cell/`) + Markdown (doctrine comments in `CELL-KINDS.md`, `dispatch-protocol/SKILL.md`). No new language is introduced. |
| CLI integration target | N/A — no new CLI surface. This cycle narrows/removes/documents existing guard scripts and Go tests; it does not add a `cn` subcommand or any new binary entry point. |
| Package scoping | Existing paths only: `scripts/ci/`, `src/packages/cnos.issues/commands/issues-fsm/`, `src/packages/cnos.cds/skills/cds/fsm/`, `src/go/internal/cell/` (read/cite only, not the audit target), `src/packages/cnos.cdd/skills/cdd/CELL-KINDS.md`, `src/packages/cnos.core/skills/agent/dispatch-protocol/SKILL.md`. No new package directories. |
| Existing-binary disposition | Coexist, modify in place. `check-dispatch-repair-preflight.sh` and `check-dispatch-closeout-integrity.sh` are edited/narrowed in place, not replaced by a new binary/script. The `cn issues fsm` and `cn cell` binaries are unchanged in surface (read/cited as replacement proof, not modified unless a doc-comment clarification is needed). |
| Runtime dependencies | None new. No new external tool, library, or service dependency. |
| JSON/wire contract | `transitions.json` schema (the `states`/`transitions`/`rules`/`guard` shape) MUST NOT change shape — only rule *content* may change per a guard's classification, and only if AC2's replacement-citation obligation is otherwise unmet without it (expected: no shape change at all, per this scaffold's diff-scope estimate). |
| Backward compat | All AC3 strand fixtures (empty-review, repair re-entry, matter-without-PR, dead-run-no-matter, dead-run-with-matter, review-request guarded transition — full list + test-function names in the AC oracle table above) MUST still pass unmodified in their asserted outcome. `TestSeam_CellKindNotEnforced` MUST remain unweakened. |

---

## β prompt

**Oracle list:** AC1–AC7 exactly as stated in the issue body and expanded in this scaffold's "AC oracle approach" table above. β verifies each AC's concrete check, not a paraphrase of it.

Additional gates:
- Standard `beta/SKILL.md` §"Pre-merge gate" (full row set — canonical-skill staleness re-check, merge-commit close-keyword check, etc.).
- **Rule 7 (implementation-contract conformance)** is binding: verify the diff matches every pinned axis in the α prompt's `## Implementation contract` table above — no new language, no new CLI surface, no new package, no new binary, no new runtime dependency, no `transitions.json` shape change, and every AC3 fixture still green. A behaviorally-correct consolidation that nonetheless changes `transitions.json`'s schema shape, or introduces a new Go package/binary, or silently drops a strand fixture from the test suite, fails Rule 7 even if AC1–AC7's behavioral checks otherwise pass.
- Confirm no `.github/workflows/*.yml` file was touched beyond what a scoped guard-script change strictly required (per this cycle's narrow diff-scope estimate — flag as a finding, not necessarily an automatic REQUEST CHANGES, if a workflow file changed and the reason isn't self-evidently minimal).
- Confirm the guard inventory table (wherever α lands its final version — `self-coherence.md` or a dedicated file) has zero blank/TBD classification cells (AC1's oracle).

---

## Friction notes (open questions for α/β — not resolved here, do not guess past them)

1. **#516/#524 script scope is genuinely ambiguous at the margin.** I classified both as NARROW-leaning rather than REMOVE, because each still protects a fact (prompt text presence) that no other surface currently checks. α may find, on closer reading of `dispatch-protocol/SKILL.md` §2.8/§2.9's exact wording, that even the prompt-presence check is now redundant with something else (e.g. `install-wake-golden`'s byte-identity check already implicitly proves the golden matches the SKILL.md source, which is most of what these scripts check for the golden/live-workflow rows). If so, a stronger REMOVE case may exist for the presence-guard half too — but only with a named replacement citation per AC2. I did not verify this myself; flagging as a genuine open question rather than asserting either way.
2. **The `--self-test` bash function in `check-dispatch-closeout-integrity.sh` looks like the clearest FOLD/REMOVE candidate I found** (a hand-rolled 4-case pure-function check duplicating logic the Go FSM already tests against the real transition table), but I did not verify whether any downstream tooling or documentation independently depends on `--self-test` being invokable as a standalone CLI entry point (e.g. a runbook telling an operator to run `./scripts/ci/check-dispatch-closeout-integrity.sh --self-test` directly, outside CI). α should grep for that invocation pattern before removing it.
3. **CELL-KINDS.md's "Future typed field (deferred, Phase B)" paragraph** uses forward-looking hedge language ("could define," "out of scope for this doctrine-first cell") that I judged as *not* meeting AC4's "misleading TODO language" bar — it reads as an honest deferred-scope statement, not forgotten scaffold. This is a judgment call γ is flagging, not deciding; α/β should form their own view rather than inherit mine uncritically.
4. **I did not open `issuesfsm.go`, `scan.go`, or `table.go` in full** (only grepped for `TODO` and specific symbols) — Scope item 4 ("dead scaffold: stale TODOs · dead comments · redundant fixture scaffolding · obsolete guard docs") may still turn up something in those files' bodies that a targeted grep missed. α's own peer enumeration must cover full-file reads of the surfaces the issue names, not just my grep sweep.
5. **I did not verify cnos#391/#392/#393 relevance** — the issue's own "Related" section does not explicitly chain them to #600 (they appear only in my task brief, not in the fetched issue body), so I have not cited them as anchors above. If α finds a genuine dependency, name it explicitly rather than assuming my omission was deliberate scoping.
