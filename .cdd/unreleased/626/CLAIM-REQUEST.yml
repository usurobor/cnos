claimed_by: cds-dispatch/local-run-20260709-ac4
protocol: cds
requested_at: 2026-07-09
issue: 626
note: >
  Continuation claim for the bounded AC4-only scope named in the operator/
  CAP comment "AC4 dispatched — proof-first (operator directive via
  kappa/CAP, 2026-07-08)" posted 2026-07-09T00:42:46Z, which superseded
  the prior "reverting to status:ready" note and flipped
  status:ready -> status:todo. AC1/AC2 (PR #635) and AC3 (PR #637,
  e5a3abe5) are already merged on this same cycle/626 branch, which the
  operator's comment confirms was reset to current main ("clean-base
  start"); neither AC1/AC2 nor AC3 is being redone here.

  run_class: same taxonomy gap the prior AC3 round's CLAIM-REQUEST.yml
  already flagged -- this does not cleanly match any of {first_pass,
  resumed_from_matter, repair_pass} as currently named in
  cds-dispatch/SKILL.md Sec"Repair re-entry preflight" Step A. No
  operator-rejection evidence exists (rules out repair_pass); no
  "MECHANICAL reversion" scanner audit-note comment exists (rules out
  resumed_from_matter per delta/SKILL.md Sec9.11's detection rule);
  branch content is verified identical to main modulo a trivial
  board-map diff (git diff origin/main origin/cycle/626 --stat: 2 files,
  board-data.json + index.html only), consistent with the operator's
  "clean-base start" characterization. Treated operationally as
  equivalent to first_pass for the NEW scope (AC4 only). This is the
  SAME fourth shape (tentatively "scope_continuation") the AC3 round's
  claim already named as a doctrine gap for cds-dispatch/SKILL.md +
  delta/SKILL.md Sec9 to formally adopt; not re-litigated here, just
  reconfirmed as still open.

  Round numbering: this is R2 on cycle/626 (R0 = AC1/AC2 doctrine +
  activation-scope change; R1 = AC3 sparse-checkout capability removal;
  R2 = this round, AC4 write-fence retirement decision, proof-first).
