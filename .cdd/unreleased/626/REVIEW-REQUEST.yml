issue: 626
protocol: cds
cycle_branch: cycle/626
head_commit: e0ad5230eefa0ce2e1deee261d780a168b7b1b81
requested_by: delta (cds-dispatch wake, wake-invoked mode)
converge_verdict_ref: .cdd/unreleased/626/beta-review.md
note: >
  Requesting the in-progress -> review transition per beta's Round 2
  converge verdict (round 2: converge, zero findings). This round is a
  bounded continuation of the same issue/branch: AC1/AC2 already merged
  via PR #635 (R0, untouched here) and AC3 already merged via PR #637
  (R1, untouched here); this round's scope is AC4 only, per the
  operator/CAP "AC4 dispatched — proof-first" comment posted
  2026-07-09T00:42:46Z.

  AC4 (write-fence retirement decision) is implemented and independently
  re-derived by beta -- not merely read from alpha's self-coherence.md
  narrative: beta independently re-ran the two new realCheckpoint tests,
  built a THIRD scratch repo without the sparse-checkout exclusion to
  sabotage-test that the new tests actually discriminate on the
  mechanism (negative control: the same write DOES persist without the
  AC3 boundary), independently re-rendered both manifests from a clean
  state and recomputed sha256, and independently observed real CI green
  (not just local reproduction) on both `Build` (all 10 jobs, including
  I4/I5 which are not runnable in this session for lack of the
  lychee/cue binaries) and `install-wake golden`.

  Outcome B selected: the old post-run
  dispatch_activation_log_write_violation write-fence is retired
  specifically for role=="dispatch" manifests (narrowed by role, not by
  activation_log_writer alone, so a hypothetical future non-dispatch
  non-writer wake without the sparse-checkout boundary keeps the old
  fence). AC10 (live-fire validation after the change) is correctly and
  explicitly NOT claimed this round -- structurally deferred to the next
  real cds-dispatch firing after this PR merges, per the same v0
  single-firing constraint (delta/SKILL.md §9.7) R1's own AC3 round
  already established as precedent. Named explicitly in
  gamma-closeout.md's §R2 amendment, not glossed over.

  run_class for this continuation does not cleanly match
  cds-dispatch/SKILL.md's current taxonomy (first_pass /
  resumed_from_matter / repair_pass) -- named explicitly in
  self-coherence.md §R2 "run_class note" rather than silently
  mislabeled; the same fourth shape (scope_continuation) R1's round
  already flagged, reconfirmed rather than re-filed as a new issue.

  All R2 closeout artifacts (self-coherence.md §R2, beta-review.md §R2,
  alpha-closeout.md / beta-closeout.md / gamma-closeout.md §R2
  amendments) are present on this branch as of this request.
  Pull request #638 (https://github.com/usurobor/cnos/pull/638) is open,
  head commit e0ad5230 (3 commits beyond the cycle/626 base
  8e0ed7cf), real CI green (Build: all 10 jobs; install-wake golden).
  `cn issues fsm evaluate --issue 626 --apply` is delta's next action
  following this request.
