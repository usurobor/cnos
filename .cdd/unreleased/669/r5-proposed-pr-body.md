# Proposed PR body after R5

Closes #669. Draft pending fresh exact-SHA R5 β/γ/CC review.

This publishes the recursive-cell CM as a source-checkout-only, zero-standing
candidate and ships the deterministic #662 State-A route described by the
methodology:

- atomically emits the exact eight declared `emission/...` prompt/manifest
  paths and documents the assessment and path-base commands as executable,
  self-contained routes;
- validates exact standard TSC v3.2.4 witnesses, including six required
  defect-card fields, optional strictly validated `secondary_axes`, and exact
  typed nonempty `next_fixes` entries;
- snapshots all emitted and external inputs, runs coh only from response
  snapshots, retains six reports, computes the unweighted L0-L4 aggregate, and
  carries the selected canonical TSC bottleneck axis;
- CUE-validates the aggregate and digest-binding marker before atomically
  creating immutable `publication/`, with reuse, races, symlinks, mutation, and
  partial failure all fail-closed;
- binds the complete authority, target, input, report, result, and caller-time
  provenance surface; and
- passes deterministic CI plus a separate pinned `coh 0.12.0 (26aab50)` route
  whose nonempty card omits optional `secondary_axes`.

The real-coh route is compatibility evidence over synthetic tracked witnesses,
not a semantic sample. State-B arbitrary loading and installed activation
remain unshipped. #662 remains a design/calibration source, not held out, and
no independent, admissibility, consistency, ratification, hosting-independent,
or off-house standing is claimed.

External β R4 comment `5016473801` requested these two narrow repairs over
exact SHA `8baf9ead0ff773a23812db1153d1bcfa27bedf2f`. R5 is a focused successor
requiring fresh exact-SHA β review, then only on convergence fresh γ binding and
CC re-adjudication. This PR remains draft; no merge or control-plane transition
is authorized.
