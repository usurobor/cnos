# Proposed PR body after R3

Closes #669. Draft pending fresh R3 β/γ/CC review.

This publishes the recursive-cell CM as a source-checkout-only, zero-standing
candidate and now ships the deterministic #662 State-A route described by the
methodology:

- emits all six registered prompts (system and L0-L4), digest manifest, and a
  separate all-prompt-bound H01-H13 assessment prompt;
- ingests six exact standard TSC v3.2.4 witnesses, retains six canonical hybrid
  reports, and emits one CUE-validated `recursive-cell-run.json`;
- computes the unweighted geometric mean of L0-L4 canonical `C_sigma`, applies
  all thirteen typed invariant outcomes, and selects exactly one deterministic
  bottleneck and disposition;
- binds actual runner/coh/schema/instruction/assessment/registry/preflight/
  manifest, prompt/response/report, target, and time bytes while labeling caller
  Git revisions as declared provenance;
- adds deterministic no-live-model CI for emission, ingestion, digests, math,
  gates, scoped failures, schema, provenance, and fail-closed cases;
- separately passes local six-target emit/ingest compatibility against actual
  pinned `coh 0.12.0 (26aab50)` using synthetic tracked witnesses (not a model
  sample or calibration claim);
- preserves the prior real pinned-engine self evidence but marks its same-author
  semantic N=1 superseded after R3 and ineligible for future k=3.

State-B arbitrary methodology/target loading and installed activation remain
unshipped. #662 remains a design/calibration source, not a held-out target, and
this PR claims no independent, admissibility, consistency, held-out, hosting-
independent, or off-house standing.

The prior R2 β convergence and γ closeout apply only through
`c8d680bf822b46b9f931d62b07d68f50ade1b07e`. CC addendum
`5016069237` supersedes the earlier proceed recommendation. R3 requires a fresh
exact-SHA β review, then fresh γ binding and CC re-adjudication. No merge or
control-plane transition is authorized by this draft.
