# α R3 Self-Coherence and Repair Receipt — cnos#669

## Binding and authority

- R3 implementation base: `c8d680bf822b46b9f931d62b07d68f50ade1b07e`
- Superseded reviewed matter: `9cc9c3bbe97b11a94dbab6e47bdc95a2fb0a418c`
- Superseding CC addendum ID: `5016069237`
- Superseding CC addendum URL:
  `https://github.com/usurobor/cnos/pull/670#issuecomment-5016069237`
- R3 head binding: the immutable α PR comment posted after the single R3 commit
  (a tracked file cannot contain its own commit SHA without circularity)

This is α's same-author implementation receipt, not a β review, γ closeout, CC
judgment, or independent warrant. It closes no review loop and grants no CM
standing.

## Coherence and repair account

The repair converts the documented one-target manual route into a deterministic
two-phase State-A runner. `emit` materializes six TSC prompts, their digest
manifest, and a separate invariant-assessment prompt bound to all six digests.
`ingest` refuses changed/incomplete prompts or witnesses, validates exact
standard TSC response objects, runs each target through hybrid ingestion,
retains six reports, computes the declared L0-L4 aggregate, applies H01-H13,
selects one stable bottleneck, and emits one typed disposition/provenance object.

Self-repair caught and corrected these coherence defects before freeze:

- an initial CUE comprehension left row constraints incomplete; it was replaced
  by explicit six-target, five-level, and ordered H01-H13 typed tuples;
- failed-invariant correlation initially searched any systemic card; it now
  requires the declared level/system witness, primary axis, severity, and Hxx
  marker;
- the invariant assessment initially bound only the system prompt; its generated
  prompt and JSON now bind all six prompt digests;
- caller revision strings were ambiguous provenance; result keys now identify
  them as declared values while actual executable and path-framed authority
  bytes are hashed, and active/target-copy runner mismatch refuses;
- the strict fake CLI initially tolerated incompatible invocations; it now
  requires existing registry/instruction/root paths and exact llm/hybrid flags.
- a local actual-coh run exposed absolute authority arguments being joined to
  `--root`; the runner now supplies root-relative paths and the fake explicitly
  rejects absolute authority args so CI guards the real CLI contract.
- the first pushed R3 candidate `a25a711f4b5cea9b263f3e3cd35aad39a7cdcdc6`
  failed I5 in push run `29692168284` and PR run `29692169452`: the shallow job
  had fetched receipt `a0d3929...` but the newly exercised preflight also needs
  exact matter `2d6b93c...`. The same R3 commit was amended so I5 fetches and
  verifies both exact objects at depth one. A fresh depth-one clone with only
  those two explicit fetches passed the complete runner test before repush.

The six standard witnesses remain unextended. H01-H13 outcomes live only in the
separate typed assessment. `unknown` is nonaccepting; a failed item must match a
systemic defect card at the declared level and primary axis. The system report
is retained but excluded from the unweighted L0-L4 geometric mean. Ties resolve
by L0→L4 and α→β→γ. With zero standing, even all-pass yields `hold`.

## Evidence boundary

Deterministic CI fixtures cover orchestration, schema, aggregate math, gates,
disposition, provenance, and refusal behavior without a live model. They are
not proof against the real pinned TSC binary and are not semantic calibration.
The reusable methodology validator also has a positive State-A execution
fixture plus missing-runner and non-executable-runner refusal fixtures.
Separately, α ran the finished route locally against actual pinned
`coh 0.12.0 (26aab50)` at
`26aab5023f03dc7d0abf82e5fdba20134fc6adad`: real prompt emission resolved
`30/4/4/8/12/11` files, and real hybrid ingestion validated all six tracked
synthetic standard-witness fixtures and produced the typed aggregate. This is
CLI-compatibility evidence only; outputs were temporary and it created no model
response or semantic sample.
The earlier v0.2 self receipt separately preserves real pinned-coh prompt,
mechanical N=3, and external-response ingestion evidence. Its same-author
semantic N=1 is now explicitly superseded after the instrument change and is
ineligible for any later k=3 claim. R3 generated no semantic sample.

Final exact test commands and the R3 commit SHA are supplied in the immutable
PR α receipt after commit. Fresh β, γ, and CC actions remain required.
