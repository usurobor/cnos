# Proposed PR body after R6 provenance recovery

Closes #669. Draft pending a fresh exact-SHA R6 β→γ→CC chain.

This publishes the recursive-cell coherence methodology as a
source-checkout-only, zero-standing candidate and ships the deterministic #662
State-A route described by the tracked methodology:

- atomically emits the exact six registered prompts, prompt digest manifest,
  and separate H01-H13 assessment prompt;
- ingests six exact standard TSC v3.2.4 witnesses plus the typed assessment,
  snapshots and binds all inputs, retains six canonical reports, and emits one
  CUE-validated publication record;
- validates exact defect-card and `next_fixes` shapes, computes the unweighted
  L0-L4 aggregate, carries the selected canonical bottleneck axis, and applies
  all thirteen invariant gates;
- refuses changed, missing, reused, raced, or partially published inputs and
  outputs; rejects a symlink in the output root or any existing output-path
  component before creation, and requires all response/invariant inputs to be
  regular files with no symlink component before snapshot;
- binds the active runner, pinned coh executable, schema, instruction,
  assessment, registry, preflight, manifests, prompts, responses, reports,
  target, and caller-declared revisions and time; and
- passes deterministic no-live-model validation plus pinned
  `coh 0.12.0 (26aab50)` six-target compatibility using tracked synthetic
  witnesses.

R6 repairs provenance without changing that product matter. It begins at
current-main base `e8ba9954764d58e7b808104d633504e25aa615cc`; γ's scaffold
`6e07b4cc42a851a3b35499212f624264ff49557a` is the first cycle commit;
and all six superseded matter patches were freshly committed with both author
and committer `alpha <alpha@cdd.cnos>`. Their replay boundary is
`b2aafc79e2c5952dc2ecf22038ae9dfcc1aaa544`; the old
`90287522..ccaf356` and reconstructed `e8ba9954..b2aafc79` binary patch
streams, excluding `.cdd/unreleased/669/**`, remain byte-identical with
SHA-256
`4c42826034a446e0953e0934085dbfd8ee6bb71febff39254c7a34c44a65d9c0`.

R6/R2 repairs β D1 additively. The current matter commit is
`db9ce95b1beb025d24f43fba2df58692665f80be`, tree
`2e4f08e1d2632a8a5fd008a08187a1b42d231152`. Its exact additive binary patch
stream from γ clarification `3144369b...` hashes to
`23749673b9fa678ad832b5b4f6915f5bed2b2865896555daf3504e2dd4e62796`.
The corrected full `e8ba9954..db9ce95b` cycle patch stream hashes to
`055c1b52ea5e83e64cca7a3dd8efe7c054b76f9e170c3fa90e9484a05d81c6a1`
and is byte-identical to the superseded-base replay after applying that exact
additive repair. These are patch-stream statements, not final-tree or direct-
diff equality claims.

The retained R2 and R5 β/γ records are historical snapshots only. Their
verdicts and closeouts do not authorize this reconstructed lineage. The exact
immutable R6/R2 α commit containing the updated canonical self-coherence
artifact and this proposed body requires a fresh context-isolated β review.
Only a fresh R6/R2 β convergence may precede new γ binding and CC
re-adjudication.

State-B arbitrary methodology/target loading and installed activation remain
unshipped. #662 remains a design/calibration source rather than a held-out
target. The pinned real-coh run is compatibility evidence over synthetic
tracked witnesses, not a semantic sample, calibration result, ratification,
or standing claim. The candidate's standing remains `none`.

PR #670 remains draft. This proposal authorizes no ready mark, merge, operator
acceptance, label change, standing promotion, or cell/wave FSM transition.
