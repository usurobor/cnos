# R3 Working Contract and Fresh Review Request — cnos#669

## Control-plane binding

- Issue: `#669`
- PR: `#670`
- Prior reviewed matter base: `9cc9c3bbe97b11a94dbab6e47bdc95a2fb0a418c`
- Prior receipt head / R3 implementation base: `c8d680bf822b46b9f931d62b07d68f50ade1b07e`
- Superseding CC addendum ID: `5016069237`
- Superseding CC addendum URL:
  `https://github.com/usurobor/cnos/pull/670#issuecomment-5016069237`

The addendum supersedes the earlier proceed recommendation and requires a new
round. The R2 β convergence and γ closeout remain historical evidence over the
exact `9cc9c3b..c8d680b` interval; neither applies to R3 matter.

## Canonical output and scope

This remains a WC. Its canonical output is the repository-native recursive-cell
measurement package. R3 is limited to making the already-declared #662 State-A
route executable, typed, tested, and accurately documented.

In scope:

1. emit all six registered prompts (`system`, L0-L4) and bind their bytes;
2. ingest six exact standard TSC v3.2.4 witnesses plus one separate typed
   H01-H13 invariant assessment;
3. run hybrid validation for all six targets, retain six canonical reports,
   compute the unweighted geometric mean of L0-L4 canonical `C_sigma`, gate all
   thirteen invariants, and emit one deterministic bottleneck/disposition;
4. bind actual runner, schema, instruction, assessment, registry/preflight,
   six manifests, coh executable, prompts, responses, reports, target, and time
   provenance in a CUE-validated aggregate;
5. exercise emission, ingestion, math, gates, scoped defect-card correlation,
   provenance, and refusals in CI without a live model;
6. mark the prior N=1 same-author semantic sample superseded and ineligible for
   later k=3 consistency after the instrument change.

Out of scope: arbitrary State-B methodology/target loading, installed-package
activation, a new semantic sample, independent calibration, standing promotion,
#662 ratification, merge, or any FSM/control-plane transition.

## Acceptance and review request

R3 is reviewable only when declarations, instructions, README commands, runner,
schema, fixture outputs, CI, package layout, and historical provenance agree;
all checks pass; unrelated #429 material remains untouched; and one immutable
R3 commit is pushed. The α PR receipt must name the exact R3 head SHA.

After α stops writing, request a fresh context-isolated β review of the exact
R3 head, followed only after β convergence by a fresh γ evidence binding and CC
re-adjudication. Shared OpenAI Codex hosting and GitHub identity must be
disclosed; functional separation is not hosting independence or off-house
standing. No earlier β/γ result may be reused as the R3 verdict.
