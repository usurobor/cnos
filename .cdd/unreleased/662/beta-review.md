# beta-review — cnos#662 (β, R0)

Independent re-walk of `docs/architecture/CELL-RUNTIME-CLASSES.md` against `gamma-scaffold.md §4` (cell-level AC1–AC7, note-level AC1–AC8), the pinned decisions D1–D10 (operator-authorization comment, 2026-07-13T19:14:52Z), and primary sources: issue #662 body (κ cohering pass + embedded draft §1–§20), the full comment thread (6 comments, read verbatim, not just the last one), `docs/architecture/CELL-RUNTIME.md`, `COHERENCE-CELL-NORMAL-FORM.md`, the shipped `src/packages/cnos.cds/skills/cds/fsm/transitions.json`, and the built `cn` binary (`go build ./cmd/cn`, `cn --help`, `cn issues fsm --help`, `cn cell finalize --help`). Read independently; self-coherence.md was read only after forming my own judgment on each item, then cross-checked.

## §R0 — AC / D-decision walk

### Cell-level ACs (gamma-scaffold.md §4, cell-level)

| AC | Verdict | Citation |
|---|---|---|
| AC1 | PASS | Note formalized at the correct path; status header parallels `CELL-RUNTIME.md`'s convention ("Proposed architecture note (realization layer). Not ratified."). Note's own AC1–AC8 walked below, all pass except note-AC6/AC8 nuance noted (still pass). |
| AC2 | PASS | §4 carries the operator's CC↔ε reconciliation text verbatim (compared word-for-word against the issue's §CC↔ε reconciliation block and the D3 comment text — identical); cites #530 as artifact owner, "does not redefine it, and it files no work against #530." |
| AC3 | PASS | §6 pins `cell_class` as post-claim routing/authority metadata; dispatch readiness stated as solely `dispatch:cell + protocol:{P} + status:todo`; contract-incompleteness routes to `status:blocked` + `degraded_reason: cell_contract_incomplete`. §2 restates the labels-vs-contract split. No contradiction with #640/#643 found (see finding F-3 below on the #643 citation itself — doesn't invalidate AC3, just a sourcing nuance). |
| AC4 | PASS | §8 states κ is a control-plane slot, not a role; "κ must not author the specification as α"; actor-collapse declared explicitly for #662's own execution. No sentence anywhere implies κ=α. |
| **AC5** | **FAIL** | §11.1–§11.2 correctly ground `cn cell return/resume/finalize`, `#500` (closed/shipped), `#504` (genuinely open) against the shipped FSM and the built binary — that part is solid and independently verified (see FSM cross-check below). **But** gamma-scaffold's AC5 text explicitly requires "CDR shipped #376; CDW illustrative" as part of "State A reflects shipped reality" (this is F4 from the issue's κ pass). The note contains **zero** occurrences of `CDR`, `CDW`, `#376`, `#403`, or the draft's §4.4 "Protocol Package" distinction (`cnos.cds` shipped / `cnos.cdr` shipped v0.1 / `cnos.cdw` illustrative). This half of AC5 is simply absent from the 364-line note. α's own self-coherence.md AC5 claim (line 13) quietly narrows the AC to only the `cn cell` command surface and #500/#504, omitting the CDR/CDW clause without flagging the narrowing — that is itself a self-coherence overclaim. |
| AC6 | PASS | §14 "Reconciliation map" is a genuine, per-surface cohering pass covering #627, #628, #530, #500, #504, #644, #654, #583, #584, the κ boundary, and the Go mechanical-orchestration target, each with an accurate open/closed/shipped characterization (spot-checked live via `gh issue view`, see below). |
| AC7 | PASS | §15 states the reflexive no-child-dispatch guard; `git log`/`git show --stat` on all three commits on `cycle/662` (claim marker, gamma-scaffold, alpha note) touch only `.cdd/unreleased/662/**` and the one new doc file — no `gh issue create`/`edit`, no label mutation beyond the cell's own lifecycle transitions (`status:todo→in-progress`, confirmed via the current label set on #662: no `cell_class:*` label present, matching the operator's explicit instruction not to add one). |

### Note-level ACs (gamma-scaffold.md §4, "Note-level ACs")

| AC | Verdict | Citation |
|---|---|---|
| AC1 | PASS | §1 + D1 restatement: WC/PC/CC as output-telos classes of one kernel, confirmed not re-derived. |
| AC2 | PASS | Actively tried to break this: scanned every place the note discusses "architecture note" (§3.3's PC-or-CC ambiguity), `matter_domain` vs `cell.class` (kept as distinct, orthogonal contract fields throughout §2, §3.1, §6), and `CELL-KINDS.md` (§1's table: "`cell_class` (this note) and `matter_domain` (`CELL-KINDS.md`) are orthogonal fields"). No sentence treats an artifact name (e.g. "architecture note," "coherence receipt") as itself determining cell class — telos is always the stated discriminator. |
| AC3 | PASS | §3.1/§3.2/§3.3 each give a distinct, example-backed contract; §3.2 additionally uses #662's own real gamma-scaffold YAML as a worked (not hypothetical) PC-D0 example. |
| AC4 | PASS | §7 states the CC→PC→CC→WC loop formally with the `Jₖ := CC.evaluate(...)` notation and both `CELL-RUNTIME.md` anti-patterns. |
| AC5 | PASS | §6 + §11.3 name the extension point precisely: promoting `FactSnapshot.CellKind` from the shipped *observation-only* seam (locked by `TestSeam_CellKindNotEnforced`) to an *evaluated* FSM dimension. This is a genuinely specific, checkable claim, not hand-waving — I did not independently locate `TestSeam_CellKindNotEnforced` in Go source to confirm the test name (out of scope of `--help`-level verification I ran; the claim is plausible and consistent with the shipped `transitions.json`'s own `_doc` block, which never mentions `cell_class`/`CellKind` — i.e. nothing in the shipped table contradicts "observation only, not yet evaluated"). Flagged as unverified-but-plausible, not a finding. |
| **AC6** | **PASS (verified independently, not just accepted)** | I read `transitions.json` in full and independently re-derived: declared `states: [ready, todo, in-progress, review, changes]` (`blocked` absent from the array, reachable only as a `target_state`) — matches §11.1 exactly, including the specific "`blocked` is a transition target, not an enum member" nuance. All 12 guard names in the note's §11.1 guard list match the shipped `guards` object exactly (`run_active`, `branch_exists`, `branch_has_commits`, `pr_exists`, `pr_has_commits`, `review_request_present`, `repair_contract_present`, `cdd_artifacts_present`, `checks_passing`, `claim_request_present`, `block_request_present`, `release_request_present`). The note's marker-file table (`CLAIM-REQUEST.yml`/`REVIEW-REQUEST.yml`/`BLOCK-REQUEST.yml`/`RELEASE-REQUEST.yml`, with their exact companion guards) matches the corresponding `rules[]` entries in the JSON rule-by-rule, including the `#574`-tightened all_true PR+commits requirement and the `#630` dead-run-with-matter distinction. This is the actual shipped mechanism, correctly distinguished from the draft's old illustrative FSM-events table, which the note explicitly relabels "illustrative-future" (§11.5) rather than presenting as shipped. This is real, not cosmetic, verification work by α. |
| AC7 | PASS | §12 lists all guards from the draft, ties the two D8-confirmed mechanically-checkable ones (PC no-auto-dispatch, CC no-implementation-surface) to their `V`-predicate citations in §5, and inherits the rest from CCNF's firebreak by citation rather than re-deriving. |
| AC8 | PASS | §11's explicit three-way partition (11.1–11.2 shipped / 11.3–11.4 specified-by-this-note / 11.5 illustrative-future) is State A/State B made explicit, with a legitimate third category the binary draft framing lacked. |

### D1–D10 (operator-authorization comment, 2026-07-13T19:14:52Z, read verbatim)

| D | Verdict | Citation |
|---|---|---|
| D1 | PASS | §1, confirmed not re-derived. |
| D2 | PASS | §8, matches verbatim substance, correctly generalized from "the Planning Cell's α" to "the cell's α" per gamma-scaffold's own instruction that this must also read as general doctrine. |
| D3 | PASS | §4, compared character-for-character against both the issue's §CC↔ε reconciliation block and the D3 line in the authorization comment — identical wording carried, no paraphrase, no redefinition of ε as an in-closure role (the exact CCNF-forbidden error was specifically hunted for and not found: §4's own gloss explicitly says "CC is not ε wearing a different hat — CC is a full cell at scope n+1 with its own α/β/γ/δ, for which εₙ's observation is one input, not the cell itself"). |
| D4 | PASS | §10, matches point for point: one generic runner per protocol, no separate PC/CC wake provider in v0, CC runs as a claimed cell in v0, scheduled pulse is future-only. |
| D5 | PASS | §9, reproduces the default/no-default gate lists, the typed-flag doctrine-gate mechanism, wave-boundary-not-per-child granularity, and the "operator must not become the scheduler for every child" line. |
| D6 | PASS | §6, `(status, protocol, cell_class)` triple stated correctly with the same three one-line glosses as the source comment. |
| D7 | PASS | §11.4, named as a subsection heading with the typed `wave_transition_request` shape. |
| D8 | PASS | §5 table reproduces the common floor + three class layers, including the two "mechanically checkable" structural invariants (no child auto-dispatched; no implementation surface modified) — these are the two items the operator's own *(confirm)* addenda (2026-07-12 comment) explicitly called mechanically checkable, not narrative aspiration invented by α. |
| D9 | PASS | §13 names all four schemas in the pinned dependency order, explicitly states none is implemented. |
| D10 | PASS | Grepped the whole note for `cn cell run`, `cn cell pulse`, `cell measure`, `cell bundle`, `cell act` — both occurrences of `run`/`pulse` are confined to §11.5 ("Illustrative-future... not shipped") and the §16 open-questions list, never presented as shipped. §11's whole structure is the D10 execution; §10 restates it for wake topology; §16 authoring note restates the bootstrap-calibration point again. |

**No D-decision is left as an open question anywhere in §16** — checked each of the five remaining open questions against D1–D10 and confirmed none re-litigates a settled decision (they're genuinely residual: wake-manifest cardinality, wave-dispatchability receipts, `cn cell pulse` vs #504 sequencing, exact schema field types, and wave-scope concurrency — none of these were pinned by the final D1–D10 comment).

## Cross-checks

**Issue-state spot checks** (`gh issue view <n> --json state,title`), compared against the note's §14 characterizations:

- #530 OPEN "codify scope-lift projection as a typed cell artifact" — note: "Open... depends on it, does not redefine it." Matches.
- #500 CLOSED "review-return..." — note: "Closed/shipped." Matches.
- #504 OPEN "stale-claim-recovery..." — note: "Open... genuinely open." Matches.
- #644 OPEN "pc: mechanize planning cell... (S6 of #627)" — note: "Open... this note specifies, #644 would build." Matches; the "S6 of #627" tag the note doesn't repeat but doesn't contradict either.
- #654 OPEN "Harden Planning Cell with customer-facing (D0) contract gate..." — note: "Open." Matches.
- #627 OPEN "wave: coherence-cell runtime..." — note: "Open. Cited as parent." Matches.
- #628 CLOSED "S1 (doctrine): supersede cell-kind ontology..." — note: "Landed as a proposed, not-yet-ratified architecture note (merged #629)." Careful, accurate wording — does not claim ratification despite the underlying issue being closed (correctly mirrors `CELL-RUNTIME.md`'s own still-"Not ratified" status header rather than inferring ratification from issue closure).
- #583/#584 CLOSED — note: "Closed/landed." Matches.
- **#643** — the note cites "#643 (shipped)" in §2 and §6 as the source of the labels-sole-source-of-truth-for-readiness rule. `gh issue view 643` resolves to a **pull request** ("cycle/640", MERGED, body "Refs #640" — the mechanical cell-finalizer's auto-opened PR for issue #640), not a standalone issue. This citation pattern (`#640/#643`) originates in the issue's own κ-authored F2 finding, not something α invented — so it is not an α-introduced error — but it is imprecise: "#643" names the *PR that shipped #640's fix*, not a second issue with independent content. Noting as a minor, inherited sourcing imprecision, not a blocking finding (does not affect AC3's substance, which is correct).

**Command-surface cross-check** (built `/tmp/cn`, ran `cn --help`, `cn issues fsm --help`, `cn issues dispatch --help`, `cn cell finalize --help`): the note's §11.2 shipped-command list (`cn cell return/resume/finalize`, `cn issues fsm evaluate --issue N [--apply]`, `cn issues fsm scan --protocol P [--apply]`, `cn issues dispatch`) matches the actual built binary's surface and help text; `run`/`pulse`/`measure`/`bundle`/`act` are absent from the shipped `--help` output, confirming §11.5's "not shipped" claim.

**Scope/non-goals check**: `git show --stat` on all three `cycle/662` commits confirms the only files touched are `.cdd/unreleased/662/CLAIM-REQUEST.yml`, `.cdd/unreleased/662/gamma-scaffold.md`, `.cdd/unreleased/662/self-coherence.md`, and `docs/architecture/CELL-RUNTIME-CLASSES.md` — exactly the allowed surface in gamma-scaffold §7. No Go source, no workflow file, no other `docs/architecture/*.md` touched. `#662`'s own labels show no `cell_class:*` label (per the operator's explicit instruction) and no evidence of any `gh issue create`/`edit` invocation.

## Findings

1. **(Blocking) AC5 (cell-level) partially unsatisfied — CDR/CDW state-truth omitted.** `gamma-scaffold.md §4` AC5 explicitly requires "State A reflects shipped reality (`cn cell return/resume/finalize`; **CDR shipped #376; CDW illustrative**; #504 open)." The note grounds the `cn cell` surface and #500/#504 correctly but contains no mention of `CDR`, `CDW`, `#376`, `#403`, or the draft's §4.4 "Protocol Package" distinction (`cnos.cds` shipped / `cnos.cdr` shipped v0.1 / `cnos.cdw` illustrative-only) anywhere in the 364-line document. This is a real, named piece of F4/AC5 that went missing, not a stylistic quibble — it's explicit content the AC oracle names by number. α's self-coherence.md AC5 entry (line 13) restates AC5 with this half quietly dropped, which means the self-assessment itself overclaims completeness rather than flagging the gap. **α must add a State-A citation of `cnos.cds` (shipped, #403) / `cnos.cdr` (shipped v0.1, #376) / `cnos.cdw` (illustrative, not yet a shipped package) to close this**, most naturally as a short addition to §11.2 alongside the existing `cn cell`/#500/#504 state-truth material, or as a restored, condensed version of the draft's §4.4 "Protocol Package" term with the κ-note's correction folded in.
2. **(Non-blocking, minor) Self-referential citation imprecision in §9.** §9 states the doctrine-gate flag is "`matter_domain: doctrine` / `doctrine_affecting: true`, as #662's own contract carries — §2," but the worked contract snippet actually shown in §2 (quoting `gamma-scaffold.md §1`) only carries `matter_domain: doctrine` — it does not include `doctrine_affecting: true`. That field appears only in the operator-authorization comment's own YAML block, not in §2's cited snippet. Not doctrinally wrong (the field genuinely is part of #662's contract per the operator's comment), but the citation to "§2" as the evidentiary source for that specific field is inaccurate. Cosmetic; worth a one-line fix (cite the authorization comment directly, or add the field to §2's snippet) but does not on its own justify iterate.
3. **(Not a finding, recorded for completeness)** The `#643` citation (§2, §6) names a merged PR rather than a distinct issue; inherited from the issue's own κ-authored F2 wording, not introduced by α, and does not change AC3's substance.

## Anti-rubber-stamp note

I actively tried to break: the CC↔ε wording (compared byte-for-byte against two independent source occurrences — found no drift); the FSM section (independently rederived the shipped `transitions.json` states/guards/marker-table from the raw JSON rather than trusting either the note or self-coherence.md, and it holds rule-for-rule); the D8 "mechanically expressible" claim (traced the two "mechanically checkable" predicates back to the operator's own *(confirm)* addenda rather than accepting them as α's assertion); every open-question item against D1–D10 for silent re-litigation (none found); every §14 issue-state citation against live `gh issue view` output (all correct except the #643 PR-vs-issue nuance, which is inherited, not introduced); and the non-goals/scope-guardrail claim against actual `git show --stat` diffs (holds). The one substantive gap that survived this pass is Finding 1 (AC5/CDR/CDW), which is real and traceable to the AC oracle's own explicit wording, not invented pickiness.

verdict: iterate
