schema: cn.cds.review-request.v1
issue: 662
cell_class: planning
mode: PC-D0
protocol: cds
requests: in-progress -> review
requested_by: cds-dispatch (delta wake-invoked mode; interactive cn-sigma substrate)
beta_verdict: pending          # R7 matter offered for exact-SHA external-β review; NO verdict bound yet
beta_round: R7
matter_sha_m: 2d6b93cc4e69e5b413a80bd8e352cb0a004da460   # immutable spec-only R7 matter, offered for exact-SHA external-β review

# External-β history SPLIT BY ROUND so that no verdict binds to a SHA it did not review.
# Each round points at its own review comment (or a clearly-marked ref), never a bare PR-root URL.
# The external β is a review entity OUTSIDE the Sigma lineage (#664); authority is findings + verdict
# only (not approval / ratification / dispatch / merge / issue-state).
external_beta:
  reviewer: external-beta (outside Sigma lineage, #664)
  authority: findings_and_verdict_only
  rounds:
    - round: R3
      sha: 014c75ff05570be877026b36e7f74c0abfa68d0e
      verdict: iterate
      findings: 6 (1 BLOCKER + 4 REQUIRED + 1 DISPUTED)
      review_comment_url: https://github.com/usurobor/cnos/pull/667#issuecomment-5012130583   # external-β R3 exact-revision review, names 014c75ff
    - round: R4
      sha: ce310a334081831fa52c287a342c527a5236c369
      verdict: iterate
      findings: 4 (1 BLOCKER + 3 REQUIRED)
      review_comment_url: https://github.com/usurobor/cnos/pull/667#issuecomment-5012790037   # external-β R4 exact-revision review, names ce310a33
    - round: R5
      sha: eb627874725647dae69ec6331d9bd9f597136ca3
      verdict: iterate
      findings: 2 (2 REQUIRED, no blocker)
      review_comment_url: https://github.com/usurobor/cnos/pull/667#issuecomment-5012900197   # external-β R5 exact-revision review, names eb627874
    - round: R6
      sha: 64d94e717d0ddfe790ba1dd21aae148e922b8847
      verdict: iterate
      findings: 3 (2 REQUIRED + 1 REFINEMENT, no blocker)
      review_comment_url: https://github.com/usurobor/cnos/pull/667#issuecomment-5012973128   # external-β R6 exact-revision review, names 64d94e71 (confirmed R6 resolved both R5 findings at the right altitude)
    - round: R7
      sha: 2d6b93cc4e69e5b413a80bd8e352cb0a004da460
      state: pending          # offered for exact-SHA external-β review on this SHA; NO verdict bound yet
      # no review_comment_url and no verdict until the external β reviews this exact SHA

disposition: >
  R7 repairs the external-β R6 ITERATE findings (PR #667 comment 5012973128; ITERATE with 2 REQUIRED
  + 1 REFINEMENT, no blocker — β confirmed R6 resolved both R5 findings at the right altitude):
  finding 1 [REQUIRED] the control_plane locator's `revision` must bind IMMUTABLE CONTENT (content
  hash + retrievable snapshot / immutable repository artifact containing the snapshot / a substrate
  revision that cannot be rewritten under the same identifier), and a bare comment/issue id (stable
  object identity, not content) is rejected as a revision — bare "comment id" removed from the
  acceptable immutable-revision examples, the worked-instance control_plane roots and the external-
  immutability fixtures updated (identity-only / edited-body / edited-comment negatives; a
  repeat-resolution / fails-stale positive) (§2, §13); finding 2 [REQUIRED] the unbackable
  inputs.optional.annotations behavioral-inertness claim is removed — inputs.optional is now a seq of
  external-locator refs only (immutable, content-bound, edge-free, never sibling_output), the contract
  carries only authoritative immutable refs, and any human presentation lives on the control-plane
  surface outside the typed contract (prompt-only prohibitions falsified in cnos, #583/#584;
  mechanism-over-cognition) (§2); finding 3 [REFINEMENT] the retired external `target` scalar is
  deleted from the §13 projection/round-trip invariant (`locator` only). Template↔worked-instance
  key-path identity re-verified after the inputs.optional reshape. Exact field storage types stay
  deferred to §16 Q4 (D9 named-not-implemented); §8 untouched.
beta_round_history: >
  R0 iterate -> R1 converge -> R2 (internal) -> operator-final-read ITERATE -> R3 operator-settled
  -> external-β R3 ITERATE (014c75ff, 6 findings) -> R4 repair -> external-β R4 ITERATE (ce310a33,
  4 findings) -> R5 repair -> external-β R5 ITERATE (eb627874, 2 findings, no blocker) -> R6 repair
  -> external-β R6 ITERATE (64d94e71, 2 REQUIRED + 1 REFINEMENT, no blocker) -> R7 repair (2d6b93cc,
  pending exact-SHA re-review). Convergence trajectory: R3 6 -> R4 4 -> R5 2 -> R6 2+1-refinement;
  no blocker since R4.
review_return: >
  external-β R6 ITERATE on PR #667 (2 REQUIRED + 1 REFINEMENT, no blocker, comment 5012973128;
  β confirmed R6 resolved both R5 findings at the right altitude); alpha repaired all three
  (control_plane revision binds immutable content, not object identity; unbackable annotations bucket
  removed — optional inputs are external-locator refs only; retired target deleted from the projection
  invariant) -> R7 spec-only matter at SHA_M 2d6b93cc, offered for exact-SHA re-review.
deliverable:
  matter: docs/architecture/CELL-RUNTIME-CLASSES.md
  closeout_artifacts:
    - CLAIM-REQUEST.yml
    - gamma-scaffold.md
    - self-coherence.md
    - operator-review.md
    - beta-review.md
    - beta-review-R2.md
    - alpha-closeout.md
    - beta-closeout.md
    - gamma-closeout.md
note: >
  R7 external-β repair pass. Bootstrap PC-D0 realization on the shipped generic CDS/CCNF runner
  (no cell_class-aware routing). A genuinely-independent external β (outside the Sigma lineage,
  #664) reviewed the R6 matter (64d94e71) and returned ITERATE on PR #667 with two REQUIRED findings
  and one REFINEMENT, no blocker (comment 5012973128; β confirmed R6 resolved both R5 findings at the
  right altitude); alpha repaired all three as spec-surface / receipt-projection repairs consistent
  with pinned D1-D10, and kept this review-request split per round so no verdict binds to a SHA it did
  not review (R3 -> 014c75ff/iterate/comment 5012130583; R4 -> ce310a33/iterate/comment 5012790037;
  R5 -> eb627874/iterate/comment 5012900197; R6 -> 64d94e71/iterate/comment 5012973128; R7 ->
  2d6b93cc/pending). The repair landed as a spec-only immutable matter commit SHA_M 2d6b93cc, offered
  back to the external β for exact-SHA re-review. The cell is NOT converged and NOT ratified;
  convergence is not claimed here. Merge stays gated on the operator exit sequence (§16): external-β
  re-review -> gamma binds SHA_M + review + evidence -> separate CC ratification -> operator-final-read
  -> merge -> separate PC-Wave. PR #667 stays DRAFT; alpha did not merge, invite the external beta,
  touch the PR body, or write gamma binding.
