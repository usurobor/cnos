schema: cn.cds.review-request.v1
issue: 662
cell_class: planning
mode: PC-D0
protocol: cds
requests: in-progress -> review
requested_by: cds-dispatch (delta wake-invoked mode; interactive cn-sigma substrate)
beta_verdict: pending          # R5 matter offered for exact-SHA external-β review; NO verdict bound yet
beta_round: R5
matter_sha_m: eb627874725647dae69ec6331d9bd9f597136ca3   # immutable spec-only R5 matter, offered for exact-SHA external-β review

# External-β history SPLIT BY ROUND so that no verdict binds to a SHA it did not review.
# Each round points at its own review comment (or a clearly-marked ref), never a bare PR-root URL.
# The external β is a review entity OUTSIDE the Sigma lineage (#664); authority is findings + verdict
# only (not approval / ratification / dispatch / merge / issue-state).
external_beta:
  reviewer: external-beta (outside Sigma lineage, #664)
  authority: findings_and_verdict_only
  rounds:
    - round: R3
      sha: 014c75ff05570be877026b36e7f74c0abfa68d0e
      verdict: iterate
      findings: 6 (1 BLOCKER + 4 REQUIRED + 1 DISPUTED)
      review_comment_url: https://github.com/usurobor/cnos/pull/667#issuecomment-5012130583   # external-β R3 exact-revision review, names 014c75ff
    - round: R4
      sha: ce310a334081831fa52c287a342c527a5236c369
      verdict: iterate
      findings: 4 (1 BLOCKER + 3 REQUIRED)
      review_comment_url: https://github.com/usurobor/cnos/pull/667#issuecomment-5012790037   # external-β R4 exact-revision review, names ce310a33
    - round: R5
      sha: eb627874725647dae69ec6331d9bd9f597136ca3
      state: pending          # offered for exact-SHA external-β review on this SHA; NO verdict bound yet
      # no review_comment_url and no verdict until the external β reviews this exact SHA

disposition: >
  R5 repairs the four external-β R4 ITERATE findings (PR #667 comment 5012790037): finding 1
  [BLOCKER] dependency-ref model made representable in cn.cell.contract.v1 (provenance-tagged
  external/sibling_output input union + stable requested_output.id; edges over sibling_output only;
  external roots create no edge and are accepted); finding 2 [REQUIRED] SHA-provenance made
  revision-neutral (exact matter-only review SHA_M, not a named superseded round); finding 3
  [REQUIRED] gates.reason made always-present scalar-or-null; finding 4 [REQUIRED] §16 Q1 subject
  moved to State-B/post-v0 with no open v0 manifest-count decision left in the artifact.
beta_round_history: >
  R0 iterate -> R1 converge -> R2 (internal) -> operator-final-read ITERATE -> R3 operator-settled
  -> external-β R3 ITERATE (014c75ff, 6 findings) -> R4 repair -> external-β R4 ITERATE (ce310a33,
  4 findings) -> R5 repair (eb627874, pending exact-SHA re-review)
review_return: >
  external-β R4 ITERATE on PR #667 (4 findings, comment 5012790037); alpha repaired all four
  (1 BLOCKER + 3 REQUIRED) -> R5 spec-only matter at SHA_M eb627874, offered for exact-SHA re-review.
deliverable:
  matter: docs/architecture/CELL-RUNTIME-CLASSES.md
  closeout_artifacts:
    - CLAIM-REQUEST.yml
    - gamma-scaffold.md
    - self-coherence.md
    - operator-review.md
    - beta-review.md
    - beta-review-R2.md
    - alpha-closeout.md
    - beta-closeout.md
    - gamma-closeout.md
note: >
  R5 external-β repair pass. Bootstrap PC-D0 realization on the shipped generic CDS/CCNF runner
  (no cell_class-aware routing). A genuinely-independent external β (outside the Sigma lineage,
  #664) reviewed the R4 matter (ce310a33) and returned ITERATE on PR #667 with four findings
  (comment 5012790037); alpha repaired all four as spec-surface / receipt-projection repairs
  consistent with pinned D1-D10, and split this review-request per round so no verdict binds to a
  SHA it did not review (R3 -> 014c75ff/iterate/comment 5012130583; R4 -> ce310a33/iterate/comment
  5012790037; R5 -> eb627874/pending). The repair landed as a spec-only immutable matter commit
  SHA_M eb627874, offered back to the external β for exact-SHA re-review. The cell is NOT converged
  and NOT ratified; convergence is not claimed here. Merge stays gated on the operator exit sequence
  (§16): external-β re-review -> gamma binds SHA_M + review + evidence -> separate CC ratification ->
  operator-final-read -> merge -> separate PC-Wave. PR #667 stays DRAFT; alpha did not merge, invite
  the external beta, touch the PR body, or write gamma binding.
