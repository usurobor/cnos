# self-coherence — cnos#662 (α, R0)

## §R0 — AC and pinned-decision walk

Target artifact: `docs/architecture/CELL-RUNTIME-CLASSES.md` (new file, only file besides this one written by this cell).

### Cell-level ACs (issue #662 body, "Acceptance criteria")

- **AC1** — Note formalized at `docs/architecture/CELL-RUNTIME-CLASSES.md`, sibling of `CELL-RUNTIME.md` (status header explicitly parallels it: "Proposed architecture note (realization layer). Not ratified."). Note's own AC1–AC8 (draft §20) held — see the note-level walk below.
- **AC2** — §4 "CC ↔ ε — settled (D3, F1)" carries the operator's reconciliation wording verbatim (the fenced block matches the issue's §CC↔ε reconciliation text word-for-word) and states "**#530 owns the typed scope-lift / ε-projection artifact.** This note depends on that lineage; it does not redefine it, and it files no work against #530."
- **AC3** — §6 "`cell_class` as an FSM dimension (D6, F2)" pins `cell_class` as post-claim routing/authority metadata; states dispatch readiness stays solely `dispatch:cell + protocol:{P} + status:todo` (the shipped #643 rule); states contract-incompleteness routes to `status:blocked` + `degraded_reason: cell_contract_incomplete`. No contradiction asserted with #640/#643; §2 also states this ("Labels vs typed contract").
- **AC4** — §8 "κ remains outside the cell (D2, F3)" states plainly "κ is a **control-plane** slot, not a cell role... κ must not author the specification as α" and the actor-collapse declaration rule. No section of the note implies κ=α; §16's authoring note explicitly narrates κ (issue author) and α (this file's author) as distinct cell instances.
- **AC5** — §11 "FSM — State A (shipped) vs specified vs illustrative-future (D10)" is the dedicated three-way partition: §11.1–§11.2 ground State A in `transitions.json` and the shipped `cn` command definitions (`cn cell return/resume/finalize`; `cn issues fsm evaluate/scan`; `cn issues dispatch`); §11.2 states #500 review-return is "closed/shipped" and #504 stale-claim recovery is "genuinely open"; §11.5 explicitly labels `cn cell pulse/run` etc. as "Illustrative-future" and "not shipped."
- **AC6** — §14 "Reconciliation map" is the recorded cohering pass; its rows explicitly cover #627, #628, #530, #500, #504, #644, #654, #583, #584, the κ boundary, and the "Go mechanical-orchestration target" row (partially shipped/partially specified, citing exactly which parts are which).
- **AC7** — §15 "Non-goals" states explicitly: "filing, labeling, or dispatching any child issue as a side effect of writing this note (§3.2's own guard applies reflexively to the cell that produced this note)." No `gh issue create`, `gh issue edit`, or label mutation was run by this cell; the only filesystem writes are this file and the architecture note.

### Note-level ACs (draft §20, must hold in the formalized doc)

- **AC1** — §1 "Relationship to CELL-RUNTIME.md and the kernel" table + D1 restated: WC/PC/CC are output-telos classes of one CCNF kernel, confirmed not re-derived.
- **AC2** — §3's per-class sections and §6 both distinguish artifact *shape* from cell *class* (e.g. §3.3 "Architecture note ... PC or CC" ambiguity is resolved by telos, not by filename) — no section treats an artifact name as a cell-class identifier.
- **AC3** — §3.1/§3.2/§3.3 give WC/PC/CC each a distinct, example-backed contract (§3.2 includes #662's own worked `cell.class: planning` YAML instance via §2; §3.3 gives the CC result shape and disposition vocabulary).
- **AC4** — §7 "The Coherence Loop" states the CC→PC→CC→WC loop formally (`Jₖ := CC.evaluate(...)`) and names both anti-patterns from `CELL-RUNTIME.md`.
- **AC5** — §6 and §11.3 name the FSM extension point (`cell_class` promoted from the shipped `FactSnapshot.CellKind` observation seam to an evaluated dimension) explicitly, citing `TestSeam_CellKindNotEnforced` as the current lock this note's extension targets.
- **AC6** — §11.1 verifies against the actual shipped `transitions.json` (declared states `ready, todo, in-progress, review, changes`; `blocked` as a target_state, not a declared state; the full shipped guard vocabulary and the request-marker-file table) rather than restating the draft's illustrative §11 table as shipped. §11.6-equivalent (folded into §11.5) explicitly relabels that draft table as illustrative-future.
- **AC7** — §12 "Mechanical guards against role/class collapse" lists all eight guards from the draft (PC self-dispatch, CC self-implementation, WC self-contract-mutation, κ editing cell-owned artifacts, α/β/γ/δ firebreak) and ties each to a `V` predicate or CCNF firebreak citation rather than leaving them narrative-only.
- **AC8** — §11's three explicit subsection groups (11.1–11.2 shipped; 11.3–11.4 specified; 11.5 illustrative-future) are State A and State B made explicit, plus a third "specified-but-unshipped" middle category the draft's binary State-A/State-B framing didn't separately name (needed because §11.3/§11.4 are neither shipped today nor merely illustrative — they are this note's own specification).

### D1–D10 (operator authorization comment, 2026-07-13T19:14:52Z)

- **D1** — §1 states it directly under a "**D1 (one CCNF kernel)**" heading; confirmed, not re-derived.
- **D2** — §8 "κ remains outside the cell (D2, F3)" carries this as its section title and body.
- **D3** — §4 carries the CC↔ε wording verbatim, as required.
- **D4** — §10 "Wake topology (D4)" states v0 = one generic runner per protocol, no separate PC/CC provider, CC runs as a claimed cell in v0, scheduled pulses are a future extension — matching D4's wording point for point.
- **D5** — §9 "Human gates (D5)" reproduces the default-gate list, the no-default-gate list, the doctrine-affecting typed-flag mechanism, and the wave-boundary-not-per-child authorization granularity, plus the "operator must not become the scheduler for every child" line verbatim in substance.
- **D6** — §6 states the `(status, protocol, cell_class)` triple, the label-vs-contract split, and that dispatch readiness remains solely label-gated.
- **D7** — §11.4 states "CC owns the wave-state judgment; the mechanical FSM owns the wave-state transition" as a named subsection heading and body, including the typed `wave_transition_request` shape.
- **D8** — §5 "Class-specific V" table reproduces the common floor plus WC/PC/CC additions exactly as pinned, including the two mechanically-checkable "structural invariant" predicates (no child auto-dispatched; no implementation surface modified).
- **D9** — §13 "Schema-first destination (D9)" names all four schemas in the pinned order and states explicitly that none is implemented; §2 and §11.4 give worked-but-unimplemented shapes for two of them.
- **D10** — §11's entire three-way structure is the D10 execution; §10 and §16 (authoring note) additionally restate the bootstrap-calibration requirement — that #662 itself proves a planning contract can run on the generic runner, not that class-aware routing already ships.

## Hard-STOP check

No irreconcilable contradiction was found between D1–D10, the issue draft, and shipped state. The one nuance requiring explicit exposition rather than a flat restatement — the shipped FSM's declared `states` array omitting `blocked` while `blocked` is still a reachable `target_state` — is resolved by exposition in §11.1, not by inventing a resolution; it is not a contradiction between the draft and shipped reality, only an imprecision in the draft's shorthand that shipped-state grounding corrects.

## §R0.1 — resume-time α adjustments (δ wake-invoked resume, same session)

The R0 matter above was committed by an earlier firing of this interactive bootstrap session (commit `1754cf4d`), before β ran. On δ-resume (per `delta/SKILL.md` §9.11 resumed-from-matter: read and continue existing `cycle/662` matter rather than re-scaffold), three binding-directive/honesty alignments were applied to the spec before independent β, and are recorded here rather than silently folded:

1. **Status header → `Draft`.** The κ dispatch contract for this cell fixes the artifact Status at **Draft** (bootstrap, pre-ratification). R0 had mirrored the sibling `CELL-RUNTIME.md`'s "Proposed… Not ratified" header; the self-referential status word is now `Draft (realization layer). Not ratified.` at the header (line 3) and in the §16 authoring note. The "Not ratified — separate CC review → operator-final-read → doctrine cell" exit framing is preserved unchanged. (The row describing the *sibling* `CELL-RUNTIME.md` as "proposed, not-yet-ratified" in §14 is left as-is; it reports that note's own status, not this one's.)
2. **State-A command-surface basis (§11.2, §16) → honest.** R0 wrote "Confirmed directly against the built binary (`cn --help`, this cycle)." No binary was actually invoked in this environment; the ground truth is the shipped command *definitions* (`src/go/internal/cli/cmd_cell.go`, `cmd_issues_dispatch.go`, `cmd_issues_fsm*.go`, and the `cmd_help_test.go` registry assertions), plus `cmd_cell.go`'s absence of any `cell-pulse`/`cell-run` command. §11.2 and the §16 authoring note now cite that source basis rather than a binary run — a D10/State-A honesty correction, not a content change (the shipped/unshipped partition is unchanged and independently re-verified: `states` array = `["ready","todo","in-progress","review","changes"]`; `blocked` a `target_state` only; `TestSeam_CellKindNotEnforced` present; `cell-pulse`/`cell-run` absent).
3. **Dangling `§11.6` cross-references → repointed.** R0 folded its intended §11.6 into §11.5 but left three "(§11.6)" pointers dangling (§11.1 once, §11.2 twice). All three now point to the correct subsection (§11.5 for the illustrative-future relabeling; §11.1 for the shipped-table grounding).

No AC-level claim in §R0 changed; these are directive-alignment and internal-consistency fixes on the same matter. Every §R0 AC citation above still holds against the adjusted file.

## §R1 — α repair against β R0 findings

β's reconciled R0 verdict was **iterate** (two findings; see `beta-review.md`). α repaired both:

- **F1 (AC5 / F4) — Protocol-Package State-A truth was absent.** The R0 spec carried the shipped `cn cell`/#500/#504 State-A truth but omitted the protocol-package half AC5/F4 explicitly requires (CDS shipped #403 / CDR shipped v0.1 #376 / CDW illustrative). My §R0 AC5 walk above had narrowed AC5 to the command-surface + #500/#504 half without flagging the omission — that narrowing was itself an overclaim, and I withdraw it. **Repair:** §11.2 now carries a "Protocol Package state truth (F4)" paragraph naming `cnos.cds` shipped (#403), `cnos.cdr` shipped v0.1 (#376), `cnos.cdw` illustrative-only (`CDD.md` v4.0.0 names `cdo`/`cdh` as future bindings), and that every worked example uses `protocol: cds`. AC5 now fully met (both halves).
- **F2 (§9 citation) — `doctrine_affecting: true` was mis-cited to §2.** §2's worked snippet quotes only `matter_domain: doctrine`; the full flag pair lives in the operator-authorization comment's contract block. **Repair:** §9 now cites that comment's contract block for the `doctrine_affecting` half.

No other AC/decision citation in §R0 changed; the R1 delta is scoped to F1/F2. AC5 is now satisfied in full.

## §R2 — α repair against κ's operator-final-read verdict on PR #667 (ITERATE NARROWLY)

κ's operator-final-read (issue comment 2026-07-16, transcribed verbatim in `operator-review.md`) returned **iterate narrowly** with six load-bearing blockers in the typed-contract surface. The architecture converges; the defects are precisely in the contracts. α repaired blockers 1–5 (blocker 6 is a fresh independent β, Step 4). **No settled architecture was re-opened; no unpinned decision was made; no operator-gate hold was required** — every fix is a typed-contract-surface repair consistent with the pinned D1–D10.

- **Blocker 1 — canonicalize `cn.cell.contract.v1` (§2).** The R1 §2 had a schema template and a worked instance that disagreed on four axes (`cell.id` vs `cell.issue`; `requested_output` object vs list; `constraints.non_goals` vs `cell.non_goals`; top-level `gates` vs `cell.gates`). **Repair:** one canonical envelope now governs, and the worked instance (#662's own contract) is rewritten to validate against it **verbatim** — same top-level keys, same nesting: `cell.id`; `requested_output: { kind, path }` (object); `non_goals` under `constraints`; `gates`/`stop_conditions` top-level. A closing sentence enumerates the four resolved disagreements so the "one shape, not two" property is explicit.
- **Blocker 2 — reconcile intent (§2 ↔ §13).** The R1 §2 envelope inlined `intent: { source: issue, … }`, treating the issue *as* intent, while §13 defines first-class `cn.intent.v1` produced before any cell exists. **Repair:** §2 now carries `intent_ref: { schema: cn.intent.v1, id, carrier: { kind: github_issue, ref } }`; a new paragraph states the issue is a **carrier/control-plane projection**, not intent identity, and cross-references §13. §13's `cn.intent.v1` and `cn.cell.contract.v1` bullets were strengthened to state the reference direction from both ends. One intent object; the issue is a carrier.
- **Blocker 3 — split PC result by mode (tagged union, §3.2).** The R1 result shape forced `wave_ref`+`graph` onto every PC result, D0 included. **Repair:** §3.2 now gives a tagged union by `mode` — PC-D0 → `{ class: planning, mode: d0, artifact_ref, readiness: ready_for_coherence_review, requires_operator_gate }`; PC-Wave → `{ class: planning, mode: wave, wave_ref, graph, readiness: ready_for_wave_review, requires_operator_gate }`. D0 carries no `wave_ref`/`graph`; #662 (a PC-D0) produces the `artifact_ref` form.
- **Blocker 4 — correct actor-collapse terminology (§8, §14, γ bootstrap declaration).** The R1 §8 called the single-Sigma-lineage situation "actor collapse." **Repair:** §8 now distinguishes **actor collapse** (one activation, two roles inside one cell boundary — the firebreak-defeating case) from **hosting-identity collapse** (separate activations sharing one account/model lineage, tracked by #664), and states plainly that #662 was **primarily hosting-identity collapse, not actor collapse** — **protocol-level κ≠α held; κ did not author the spec, α did.** §14's κ-boundary row and the γ bootstrap declaration (gamma-closeout §"Bootstrap calibration" item 4) are updated to match; the receipts stop calling every reuse of Sigma "actor collapse."
- **Blocker 5 — repair stale §17 / §17 Q6 references (§10, §11.5).** The document ends at §16 (five open questions); the two "§17" pointers were dangling. **Repair:** §10's wake-provider open-sub-decision now points to **§16 Q1** (Wake-provider realization); §11.5's pulse-sequencing pointer now points to **§16 Q3** (Sequencing of the illustrative command surface against #504). Both targets exist and match the referring content; this is a pure renumber — no missing content, no new policy, so it did **not** escalate to a blocker-6 architecture-decision STOP.

**§R2 Hard-STOP check.** No blocker required an unpinned architecture decision. All five are typed-contract-surface repairs within the pinned D1–D10. No operator-gate hold raised by α in R2. The "already good" parts κ named (WC/PC/CC classes, CC↔ε, κ-outside, State-A/specified/illustrative partition, grounded FSM + request-marker mechanics, CC-judgment/FSM-transition, wave-level authorization, mechanical class-specific V, disclosed bootstrap limitation) are preserved unchanged.

**§R2 note on earlier AC walks.** The §R0 AC4 walk described the note as stating "the actor-collapse declaration rule"; after the blocker-4 repair the note instead states the *actor-collapse vs hosting-identity-collapse distinction* and that #662 was hosting-identity collapse. The §R0 record stands as the R0 receipt; this §R2 note is the correction of that surface against the current file. All other §R0/§R1 AC citations still hold against the R2 file.

## §R3 — α repair: §8 reframed to operator-authorized-transitional (operator-settled)

**Matter SHA_M (this repair):** `014c75ff05570be877026b36e7f74c0abfa68d0e` (spec-only amend of the prior R3 spec commit `fdcf4233`; `docs/architecture/CELL-RUNTIME-CLASSES.md` only).

**Trigger — operator control-plane instruction (verbatim).** The operator directly settled the κ/α doctrine framing: *"You're K in sigma world executing the role of alpha of cnos planning cell since we haven't yet made planning cell operational. We're moving to the world of three cells agent operating in this GH autonomously. We're bootstrapping that three-cells agent."* This supersedes the earlier "disclosed nonconformance" framing that R3's first spec commit (and the external-β R3 review-input note) carried. Because this is a direct operator control-plane settlement, it is a mechanical reframe, not an α architecture decision — **no operator-gate hold raised by α; no unpinned decision made.**

**What changed (characterization only).** §8 (and its mirror statements in the Thesis, §1 table, §14 reconciliation-map rows, §15 non-goal, §16 authoring note) were rewritten from **"disclosed nonconformance / assurance limitation / not a valid topology"** to **"operator-authorized transitional bootstrap posture (State A)"**:

1. **State A (κ=α)** is now framed as the **operator-authorized transitional topology** — legitimate because the mechanical Planning-Cell runtime that would execute α does not exist yet, so the operator authorized κ (Sigma) to execute the α role during bootstrap. It is a designed, explicitly transitional stage retired at State B, **not** a nonconformance/violation, **not** a licensed permanent topology. It still carries a reduced *independent* warrant (hosting-identity, #664) requiring external-β discharge — authorized ≠ self-warranting.
2. **`CELL-KINDS.md:137`** reframed from "stale/wrong prose" to **"State-A-accurate, scoped, superseded at State B."** The downstream doctrine-migration issue **repoints/scopes** line 137, it does not "correct an error."
3. **Guard-consequence closing sentence** now reads "operator-authorized transitional posture disclosed on the receipts," not "disclosed nonconformance."

**Invariant claims held UNCHANGED under the reframe (identical either way):**

- κ is a control-plane slot, not a cell role; adds no new role letter.
- The **State-B invariant, unqualified**: κ supplies control-plane input; the cell's α owns and writes the spec/impl matter; κ does not author α matter, does not perform β/γ; role functions must not be silently collapsed; κ≠α is the unconditional target the three-cell agent is built toward — not softened, relativized, or made optional by the bootstrap.
- **Actor collapse** (one activation performing two cell roles inside one cell boundary — the firebreak-defeating case) remains **forbidden**; the bootstrap limitation is the weaker **hosting-identity collapse** (separate activations, one Sigma lineage), tracked by **#664** — distinction kept crisp, #664 pointer kept.
- **Provenance:** internal same-lineage passes do not constitute independent-β warrant; the independent warrant is the **external β (outside the Sigma lineage) bound to the matter-only R3 SHA it reviewed (§11.6)** + the later γ receipt binding that same SHA + external review.
- **This PC-D0 does not edit `CELL-KINDS.md`** (§14 dependency+owner; §15 non-goal); reconciling line 137 is bound to a bounded downstream doctrine-migration issue (PC-Wave / later doctrine wave).
- Guard consequence (§12): "κ editing cell-owned artifacts" forbidden because κ≠α/β/γ/δ.

**§R3 Hard-STOP check.** No unpinned architecture decision; the reframe is a direct operator control-plane settlement mechanically applied to the characterization surface only. Section length, structure, cross-references (#583/#584, #664, §11.6, §12, §14, §15), and doctrinal force preserved. `CELL-KINDS.md` not edited; only the spec (matter commit) and this receipt + `alpha-closeout.md` (this receipts commit) were written. The durable external-β record lives on **PR #667 comments** (not in the committed tree); its embedded "κ operator-directive note" recorded the earlier nonconformance framing and is superseded by this operator settlement — no fabricated new external review was written, and γ closeout is not written here (γ binds later, separately, after external β converges on SHA_M).

## §R4 — α repair against external-β ITERATE (R3 review on PR #667)

**Trigger.** A genuinely-independent **external β** (a review entity outside the Sigma lineage, #664) reviewed the R3 matter and returned **ITERATE** on PR #667. The durable external-β record is the **PR #667 review comments** (not a committed tree file). The verdict carried six findings; α repaired findings 1, 3, 4, 5 and (authoring-note/projections) 6, and hardened the disputed finding 2 without reversing operator framing. These are **repairs**, not re-openings: no pinned decision D1–D10 was re-opened, the kernel was not changed, no role letter was added, `CELL-KINDS.md` was not edited, and the operator-settled §8 authorized-transitional framing is preserved exactly.

**New immutable matter SHA_M (this repair):** `ce310a334081831fa52c287a342c527a5236c369` — spec-only (`docs/architecture/CELL-RUNTIME-CLASSES.md` only), on top of the R3 matter `014c75ff`.

**Findings and disposition:**

- **Finding 1 [BLOCKER] — single canonical wave dependency authority + explicit completion predicate — REPAIRED (Repair A).** §3.2/§13: the child contract's immutable input/output refs are the one canonical dependency authority; `cn.wave.v1` edges are their mechanical projection (or validated for exact parity); explicit *child-completion* predicate (requested output produced · acceptance predicates pass · `V` PASS · bound receipt) and *whole-wave-completion* predicate defined; the "child contract carries completion semantics as a field" overclaim removed (completion is a derived predicate, not a §2 field); positive/negative verification fixtures named (two-node A→B closes only after both bound child-completion predicates pass; reject missing edge / spurious edge / unresolved producer-input ref / issue-closed node lacking V+receipt).
- **Finding 2 [DISPUTED] — manifest-count / v0 D4 — HARDENED, NOT REVERSED (Repair E).** §10 already holds D4 for v0; the one-vs-several provider-manifest question is a State-B (post-v0) open sub-decision the operator explicitly left open (§16 Q1). The manifest-count is kept **open**; §10's State-B paragraph and §16 Q1 now carry an explicit scope tag — the manifest-count is a **State-B (post-v0) sub-decision only and does not reopen the v0 D4 decision** (v0 closed: one generic provider, route by `cell.class` after claim, no class-split provider manifest). Operator framing not reversed, question not settled.
- **Finding 3 [REQUIRED] — exactly four canonical schemas (D9) — REPAIRED (Repair B).** §13's canonical list and dependency chain are exactly four (`cn.intent.v1 → cn.cell.contract.v1 → cn.next-mca.v1 → cn.wave.v1`); the fifth `cn.cc-result.v1` removed as a named canonical schema; the tagged CC-disposition-union constraint kept and reframed as the **CC class-result shape/refinement** (how a CC result embeds `cn.next-mca.v1`), pinned during four-schema implementation; §3.3's closing sentence fixed accordingly.
- **Finding 4 [REQUIRED] — one mechanically decidable constraint model + gate invariants — REPAIRED (Repair C).** §2's false "identical types and cardinalities under an ordinary parser" claim narrowed to identical **key paths** + worked-instance conformance to a **normative type/cardinality constraint model** (a value-parse proves only key-path identity; types/cardinalities/unions enforced by a future CUE/Go constraint checker). Added a **decidable gate-invariant set** (`doctrine_affecting: true` ⟹ `gates.operator_acceptance_required: true`; `gates.reason` required-nonempty **iff** a gate boolean is true, else null/absent; `doctrine_affecting` authoritative, acceptance gate derived). Template YAML made internally consistent (`reason: null` when both gate booleans are false).
- **Finding 5 [REQUIRED] — real exit sequence + corrected successors + qualified CELL-KINDS relation — REPAIRED (Repair D).** Added an explicit **Exit sequence** block in §16 (external-β review → γ binds SHA_M + review + evidence → separate CC ratification → operator final-read → merge D0 → separate PC-Wave → CC wave review → operator wave authorization); repointed the header (line 3), §3.2, and the §16 authoring note to it; corrected the immediate post-merge successor to **separate PC-Wave** (the `CELL-KINDS.md:137` doctrine-migration is a downstream PC-Wave-owned dependency, not the post-merge step); qualified the §1 `CELL-KINDS.md` relationship row from "Unaffected" to "Domain vocabulary unaffected; its κ/α role note (`:137`) carries the State-A→State-B migration dependency (§8, §14)."
- **Finding 6 — durable-assurance projections must not present R2-converged as current — REPAIRED (this receipts commit + Repair F).** This §R4 records the external-β ITERATE, the six findings, and the new SHA_M; the phantom `external-beta-review-R2.md` reference is replaced with the PR #667 durable record; `REVIEW-REQUEST.yml` rebound from `converge`/`R2` to the external-β round on SHA_M with the pending/ITERATE→R4 state; supersede-banners added atop `gamma-closeout.md` and `beta-review.md`; the §16 authoring note records R4 (Repair F). The stale "fresh independent β R2 / converge / No R3 needed" signals are corrected here and below.

**Invariants held (unchanged under R4):** D1–D10 pinned decisions intact; kernel untouched; no new role letter; `CELL-KINDS.md` not edited; §8 operator-settled authorized-transitional framing (State A transitional / State-B κ≠α unconditional) preserved exactly; actor collapse forbidden, only hosting-identity collapse carried (#664); external-β-bound-to-SHA_M + later γ remain the independent warrant (§11.6). Matter is spec-only at SHA_M; γ closeout is **not** written here (γ binds later, after external-β re-review converges on SHA_M); convergence is **not** claimed; nothing merged or dispatched.

**§R4 Hard-STOP check.** No unpinned architecture decision; all repairs are spec-surface / receipt-projection repairs consistent with pinned D1–D10 and settled doctrine (the external β itself flagged them as mechanically repairable — "no new operator policy decision needed"). No operator-gate hold raised by α.

## §R5 — α repair against a second external-β ITERATE (R4 review on PR #667)

**Trigger.** The genuinely-independent **external β** (a review entity outside the Sigma lineage, #664) reviewed the **R4 matter (SHA_M `ce310a334081831fa52c287a342c527a5236c369`)** and returned **ITERATE** on PR #667 — its **second** ITERATE (the first was the R3 review of `014c75ff`). The durable external-β record is the **PR #667 review comment `#issuecomment-5012790037`** (naming R4 SHA `ce310a33`), distinct from the R3 review comment `#issuecomment-5012130583` (naming `014c75ff`). The R4 verdict carried **four findings — 1 BLOCKER + 3 REQUIRED, none disputed this round** — all mechanically repairable (the β itself: "No new operator policy decision is needed"). These are **repairs**, not re-openings: no pinned decision D1–D10 was re-opened, the kernel was not changed, no role letter was added, `CELL-KINDS.md` was not edited, and the operator-settled §8 authorized-transitional framing is preserved exactly (only the line-297 revision-neutral edit within §8).

**New immutable matter SHA_M (this repair):** `eb627874725647dae69ec6331d9bd9f597136ca3` — spec-only (`docs/architecture/CELL-RUNTIME-CLASSES.md` only), on top of the R4 matter `ce310a33`.

**Findings and disposition (all four valid, all repaired):**

- **Finding 1 [BLOCKER] — dependency authority not representable in `cn.cell.contract.v1` — REPAIRED (Repair A).** The R4 spec claimed child input/output refs are the sole dependency authority and `cn.wave.v1` edges are derived by matching `inputs.required` to `requested_output`, but §2's envelope could not support it (`inputs.required` opaque `seq<scalar>`; `requested_output` just `{kind, path}` with no stable output identity, mutable `path`, no external-vs-sibling tag), so the negative fixture would wrongly reject legitimate **external root inputs** — including #662's own four. **Repair:** `inputs.required[]` is now a typed, provenance-tagged **reference union** — `{ ref_kind: external, target }` (no wave edge; validated as a resolvable immutable external ref) or `{ ref_kind: sibling_output, producer, output_id }` (creates edge `producer → this`); `requested_output` gains a stable logical `id`; edge derivation restated to run **only over `sibling_output` refs** (external refs create no edge), and the receipt/evidence chain binds `requested_output.id` → immutable produced bytes/SHA at completion (§2 template + normative model + input reference-model paragraph, §3.2, §13 dependency-authority paragraph). The §13 fixtures were corrected: positive (external input → no edge, accepted; B's `sibling_output` ref → exactly `A→B`; resolved output bound to immutable evidence); negative (missing producer, `output_id` mismatch, missing/spurious edge, ambiguous/untyped ref) — **while accepting a valid external root input.** The §2 worked instance now carries all four inputs as `ref_kind: external` (a PC-D0 root consuming external roots) plus `requested_output.id`; both YAML blocks still normalize to **identical key paths** (verified by parse+diff).
- **Finding 2 [REQUIRED] — revision-neutral provenance — REPAIRED (Repair B).** §8 (~line 297) and §14 (~line 464) hardcoded "matter-only **R3** SHA" while the review target had moved to R4/R5. Both are now **revision-neutral** — "the exact matter-only review SHA (the immutable SHA_M the external β reviewed)" — with no specific superseded round named. §8's operator-settled framing is otherwise unchanged. The receipt-side split (per-round review-request) is the receipts-commit half of this finding.
- **Finding 3 [REQUIRED] — gate.reason always-present nullable — REPAIRED (Repair C).** §2 previously allowed `gates.reason` to be "absent or null" when both gate booleans are false, which changes the canonical key-path set and contradicts the exact-key-path claim. **Repair:** `gates.reason` is now **always a present key**, type **`scalar-or-null`**; invariant restated as **null iff both gate booleans are false, nonempty scalar iff either is true**; all "absent" language removed; the key-path list marks `gates.reason` always-present; gate-reason fixtures added (accept `false/false/null`, `any-true/nonempty`; reject missing `reason`, `false/false/non-null`, `any-true/null-or-empty`). Template `reason: null` and worked `reason: <nonempty>` both carry the key.
- **Finding 4 [REQUIRED] — §16 Q1 subject is State-B/post-v0, not v0 — REPAIRED (Repair D).** §16 Q1's first sentence still framed the manifest-count as a "whether v0's 'one generic runner' …" question, contradicting §10's v0 closure in the same paragraph. **Repair:** Q1's subject is now the **State-B / post-v0 wake-provider realization** (single manifest vs several provider profiles/manifests); it states **v0 is settled** (one generic provider per protocol, route by `cell.class` after claim, §10) and that any several-manifest alternative **begins only after the settled v0 boundary**; no open v0 manifest-count decision remains anywhere in the artifact. The State-B count is left **open** (operator's call), not decided.

**Prior finding-2 dispute vindicated.** The external β's own R4 finding 4 confirms "§10 now clearly settles v0 as one generic provider per protocol … and correctly leaves only State-B manifest count open" — i.e. **β agreed v0 is settled**; only §16 Q1's phrasing needed tightening. This **vindicates** the R4 α disposition that hardened (not reversed) the earlier disputed finding-2 (manifest-count / v0 D4): the operator framing held, and this round no finding disputed it.

**Invariants held (unchanged under R5):** D1–D10 pinned decisions intact; kernel untouched; no new role letter; `CELL-KINDS.md` not edited; §8 operator-settled authorized-transitional framing (State A transitional / State-B κ≠α unconditional) preserved exactly (only the line-297 revision-neutral edit); actor collapse forbidden, only hosting-identity collapse carried (#664); external-β-bound-to-SHA_M + later γ remain the independent warrant (§11.6); the canonical schema set stays exactly four (`cn.intent.v1 → cn.cell.contract.v1 → cn.next-mca.v1 → cn.wave.v1`). Matter is spec-only at SHA_M `eb627874`; γ closeout is **not** written here (γ binds later, after external-β re-review converges on SHA_M); convergence is **not** claimed; nothing merged or dispatched.

**§R5 Hard-STOP check.** No unpinned architecture decision; all four repairs are spec-surface / receipt-projection repairs consistent with pinned D1–D10 and settled doctrine (the external β itself flagged them mechanically repairable — "no new operator policy decision is needed"). No operator-gate hold raised by α.

## §R6 — α repair against a third external-β ITERATE (R5 review on PR #667)

**Trigger.** The genuinely-independent **external β** (a review entity outside the Sigma lineage, #664) reviewed the **R5 matter (SHA_M `eb627874725647dae69ec6331d9bd9f597136ca3`)** and returned **ITERATE** on PR #667 — its **third** ITERATE (R3 review of `014c75ff`, R4 review of `ce310a33`, now R5 review of `eb627874`). The durable external-β record is the **PR #667 review comment `#issuecomment-5012900197`** (naming R5 SHA `eb627874`), distinct from the R3 comment `#issuecomment-5012130583` (`014c75ff`) and the R4 comment `#issuecomment-5012790037` (`ce310a33`). The R5 verdict carried **two findings — both REQUIRED, no blocker, none disputed** — and explicitly **confirmed the R4/R5 sibling-dependency model is now sound** ("sibling dependencies are now representable and edge-derivable, valid external roots no longer imply sibling edges"). Both are **semantic** repairs at PC-D0 architecture altitude, mechanically repairable (the β itself: "No operator decision required; Claude α can repair mechanically"). These are **repairs**, not re-openings: no pinned decision D1–D10 was re-opened, the kernel was not changed, no role letter was added, `CELL-KINDS.md` was not edited, and the operator-settled §8 authorized-transitional framing is preserved **exactly** (§8 untouched this round).

**New immutable matter SHA_M (this repair):** `64d94e717d0ddfe790ba1dd21aae148e922b8847` — spec-only (`docs/architecture/CELL-RUNTIME-CLASSES.md` only), on top of the R5 matter `eb627874`.

**Findings and disposition (both valid, both repaired):**

- **Finding 1 [REQUIRED] — external refs must ENCODE the immutability they claim — REPAIRED (Repair A).** The R5 `external` variant was `{ ref_kind: external, target: <scalar> }`, required to be a "resolvable immutable external ref," but a single scalar overloaded materially different locator classes (`repo/path@sha`, `issue#`, prior-receipt id) and a bare `issue#` or bare repo path pinned no revision — the §2 worked instance claimed conformance while using bare `docs/architecture/CELL-RUNTIME.md`, `cnos#627`, `cnos#530`, `cnos#644`. **Repair:** the overloaded `target` scalar is replaced by a **locator-classed external union** where each class encodes its immutable revision component: `repo_artifact` (repo + immutable commit/tree/blob + path; bare path insufficient), `control_plane` (issue/comment/receipt + immutable revision: durable snapshot / comment id / content hash; bare `issue#` rejected), `prior_receipt` (receipt identity + immutable artifact/hash binding, content-addressed or otherwise revision-bound). Normative rule stated: every `external` ref carries `{ kind, <class-specific immutable revision component> }`; a free overloaded scalar does not satisfy it (a validated canonical URI grammar is one acceptable *implementation*, exact field storage types deferred to §16 Q4 / D9). The §2 worked instance now carries all four external roots as revision-pinned illustrative values (repo artifact = repo+commit+path; the three `cnos#…` inputs = control_plane locator + revision/snapshot); both YAML blocks still normalize to **identical key paths** (verified by parse+diff — template exhibits `repo_artifact` + `control_plane` under `inputs.required[]` and a `prior_receipt` under `inputs.optional.refs[]`; set-dedup makes template ≡ worked). §13 gains external-immutability fixtures — positive (each kind resolves to immutable evidence; repeated resolution → same content identity); negative (bare repo path, moving branch/tag, unversioned issue ref, mutable PR-root URL, receipt id without binding), with the #662 worked instance passing the same checker. The `sibling_output` variant and the required/sibling edge-derivation rules are kept **exactly** as R5 left them (β confirmed them sound).
- **Finding 2 [REQUIRED] — close the canonical ref/output vocabulary across optional inputs and the CC handoff — REPAIRED (Repair B).** R5 canonicalized `inputs.required[]` and `requested_output.{id,kind,path}`, but `inputs.optional` was still `seq<scalar>` and `cn.next-mca.v1` named `requested_output`/`input_refs` without saying they reuse the canonical shapes — the same concept had a typed form, an untyped form, and an undefined form. **Repair:** (a) `inputs.optional` semantics pinned (§2) — optional inputs must **not** create readiness/dependency edges or scheduling-dependent behavior; any sibling-produced value execution needs is a **required `sibling_output` ref**, never optional; `inputs.optional` becomes `{ refs, annotations }` where `refs` reuses the **same external locator union** (provenance-tagged + immutable, `external` only, edge-free) and `annotations` is an explicitly **non-authoritative** `seq<scalar>` that cannot affect readiness/behavior; the opaque `seq<scalar>` optional form is retired. (b) Vocabulary closed at `cn.next-mca.v1` (§13, §3.3) — `input_refs` and `requested_output` **reuse the exact canonical `cn.cell.contract.v1` shapes** (the locator-classed input-ref union + stable-`id` requested-output), or next-MCA references a complete proposed `cn.cell.contract.v1`; one canonical vocabulary across the four-schema surface, no second ref schema at the handoff. (c) **Projection/round-trip invariant** named (§13, §3.3) — transforming a CC `next_mca` into a `cn.cell.contract.v1` preserves input `ref_kind`, immutable `locator`/`target`, `producer`+`output_id`, and `requested_output.id`; edge parity unchanged through the CC→contract→wave chain; negative fixtures reject an untyped optional scalar, an optional `sibling_output` dependency bypassing the graph, a next-MCA input using a second shape, and a next-MCA requested output lacking the stable `id`.

**Convergence trajectory.** External-β findings per round: **R3: 6 → R4: 4 → R5: 2**; the R5 verdict carried **no blocker** and vindicated the sibling-dependency model. This round's two findings are both REQUIRED (not blocker), both semantic, both mechanically repaired without touching settled architecture.

**Invariants held (unchanged under R6):** D1–D10 pinned decisions intact; kernel untouched; no new role letter; `CELL-KINDS.md` not edited; **§8 operator-settled authorized-transitional framing (State A transitional / State-B κ≠α unconditional) preserved exactly — §8 not touched this round**; actor collapse forbidden, only hosting-identity collapse carried (#664); external-β-bound-to-SHA_M + later γ remain the independent warrant (§11.6); the canonical schema set stays exactly four (`cn.intent.v1 → cn.cell.contract.v1 → cn.next-mca.v1 → cn.wave.v1`) — the ref-union and vocabulary closure add no fifth schema; exact field storage types stay deferred to §16 Q4 (D9 named-not-implemented). Matter is spec-only at SHA_M `64d94e71`; γ closeout is **not** written here (γ binds later, after external-β re-review converges on SHA_M); convergence is **not** claimed; nothing merged or dispatched; PR #667 body untouched, external β not invited.

**§R6 Hard-STOP check.** No unpinned architecture decision; both repairs are spec-surface / receipt-projection repairs consistent with pinned D1–D10 and settled doctrine (the external β itself flagged them mechanically repairable — "No operator decision required"). No operator-gate hold raised by α.

## §R7 — α repair against a fourth external-β ITERATE (R6 review on PR #667)

**Trigger.** The genuinely-independent **external β** (a review entity outside the Sigma lineage, #664) reviewed the **R6 matter (SHA_M `64d94e717d0ddfe790ba1dd21aae148e922b8847`)** and returned **ITERATE** on PR #667 — its **fourth** ITERATE (R3 of `014c75ff`, R4 of `ce310a33`, R5 of `eb627874`, now R6 of `64d94e71`). The durable external-β record is the **PR #667 review comment `#issuecomment-5012973128`** (naming R6 SHA `64d94e71`), distinct from R3 `#issuecomment-5012130583`, R4 `#issuecomment-5012790037`, and R5 `#issuecomment-5012900197`. The R6 verdict carried **three findings — 2 REQUIRED + 1 REFINEMENT, no blocker, none disputed** — and explicitly **confirmed R6 resolved both R5 findings at the correct architecture altitude**. All three are **semantic/narrow** repairs at PC-D0 architecture altitude (the β itself: no operator decision required, α can repair mechanically). These are **repairs**, not re-openings: no pinned decision D1–D10 was re-opened, the kernel was not changed, no role letter was added, `CELL-KINDS.md` was not edited, and the operator-settled §8 authorized-transitional framing is preserved **exactly** (§8 untouched this round).

**New immutable matter SHA_M (this repair):** `2d6b93cc4e69e5b413a80bd8e352cb0a004da460` — spec-only (`docs/architecture/CELL-RUNTIME-CLASSES.md` only), on top of the R6 matter `64d94e71`.

**Findings and disposition (all three valid, all repaired, none disputed):**

- **Finding 1 [REQUIRED] — `control_plane` revision must bind immutable CONTENT, not object identity — REPAIRED (Repair A).** R6's `control_plane` locator listed "comment id" among acceptable immutable-revision examples, but a GitHub issue/PR **comment id is stable object identity, not content** — comments and issue bodies are editable under the same id (update-comment API), so a comment-id "revision" does not pin the bytes consumed. **Repair:** the issue/comment/receipt **identity** stays the carrier `ref`, but `revision` must now **bind immutable content** — a content hash + retrievable snapshot, an immutable repository artifact containing the snapshot, or another substrate revision **that cannot be rewritten under the same identifier**; bare "comment id" (and any identity-only token) is **removed** from the acceptable examples, and the semantic rule that an identity-only token is **rejected as a revision** is stated (§2 input-reference model, template comment + placeholder). The §2 worked-instance `control_plane` roots (`cnos#627/#530/#644`) now carry an illustrative content-hash + snapshot `revision`. §13 external-immutability fixtures gained: negative — reject an identity-only `revision` (bare comment/issue id), an **edited issue body**, and an **edited comment retaining the same object ID**; positive — a content-hash / immutable-snapshot locator resolves to **identical bytes on repeat** or **fails stale**. Exact storage fields stay §16 Q4.
- **Finding 2 [REQUIRED] — remove the unbackable `annotations` behavioral-inertness claim — REPAIRED (Repair B, clean-removal path).** R6 added `inputs.optional.annotations` (non-authoritative scalars) claiming they "cannot affect readiness **or behavior**." Readiness/edges can mechanically ignore a field, but model **behavior** cannot be proven independent of text visible in the contract/context, and cnos treats **prompt-only prohibitions as empirically falsified**, requiring structural enforcement (mechanism-over-cognition, #583/#584); a prose guarantee cannot back behavioral non-influence. **Repair:** `inputs.optional.annotations` is **removed entirely**; `inputs.optional` becomes a **seq (0+) of external-locator refs only** (the same `{ ref_kind: external, locator }` form as required external inputs — immutable, content-bound per Repair A, edge-free, never `sibling_output`); there is **no** non-authoritative-annotation bucket in the typed contract. The rationale is stated where `inputs.optional` is defined, and human presentation/notes are pointed to the **control-plane surface** (issue/PR), mechanically outside the cell's typed contract/matter (§8/§12). Every "annotations cannot affect behavior" / non-authoritative-annotation claim was removed from the matter; the normative constraint model, template, worked instance, and the "both blocks carry the same key paths" paragraph were all updated. **Key-path identity between template and worked instance was re-verified** after this change (parse + normalized key-path diff → identical, 56 paths each).
- **Finding 3 [REFINEMENT] — delete the retired `target` from the projection invariant — REPAIRED (Repair C).** §13's projection/round-trip invariant said it preserves the immutable "`locator` (or `target` component)"; the overloaded external `target` scalar was retired in R6. **Repair:** "or `target` component" deleted — `locator` only. A whole-matter grep confirmed no other lingering retired external `target` field reference remains (the `carrier.ref`, `intent_ref`, State-A/B "target", `target_state`, and the R6 authoring-note's historical "replacing the overloaded `target` scalar" are unrelated and correctly untouched).

**Repair D — authoring note.** The §16-area authoring note records R7 succinctly (external-β R6 ITERATE → control_plane revision binds immutable content not object identity; annotations bucket removed, optional inputs are external-locator refs only; stale `target` deleted from the projection invariant), and the R6 note's present-tense `annotations` parenthetical was neutralized to point at the R7 removal.

**Convergence trajectory.** External-β findings per round: **R3: 6 → R4: 4 → R5: 2 → R6: 2 REQUIRED + 1 REFINEMENT**; **no blocker since R4**. The R6 verdict confirmed R6 resolved both R5 findings at the right altitude. This round's three findings are all semantic/narrow (2 REQUIRED + 1 REFINEMENT), all mechanically repaired without touching settled architecture.

**Invariants held (unchanged under R7):** D1–D10 pinned decisions intact; kernel untouched; no new role letter; `CELL-KINDS.md` not edited; **§8 operator-settled authorized-transitional framing (State A transitional / State-B κ≠α unconditional) preserved exactly — §8 not touched this round**; the `sibling_output` model, the four-schema set (`cn.intent.v1 → cn.cell.contract.v1 → cn.next-mca.v1 → cn.wave.v1`), and the locator-union structure kept exactly as R6 left them except where Repairs A–C changed them; actor collapse forbidden, only hosting-identity collapse carried (#664); external-β-bound-to-SHA_M + later γ remain the independent warrant (§11.6); exact field storage types stay deferred to §16 Q4 (D9 named-not-implemented). Matter is spec-only at SHA_M `2d6b93cc`; γ closeout is **not** written here (γ binds later, after external-β re-review converges on SHA_M); convergence is **not** claimed; nothing merged or dispatched; PR #667 body untouched, external β not invited.

**§R7 Hard-STOP check.** No unpinned architecture decision; all three repairs are spec-surface / receipt-projection repairs consistent with pinned D1–D10 and settled doctrine (the external β itself flagged them mechanically repairable — no operator decision required). No operator-gate hold raised by α.

## Review-ready signal

`docs/architecture/CELL-RUNTIME-CLASSES.md` is written, self-verified against every cell-level AC1–AC7, every note-level AC1–AC8, and every D1–D10 decision above, adjusted per §R0.1 to the binding directive, repaired per §R1 (β R0 findings), §R2 (κ's operator-final-read six-blocker contract), §R3 (operator-settled §8 authorized-transitional reframe), §R4 (external-β ITERATE on `014c75ff` → six-finding repair; SHA_M `ce310a33`), §R5 (second external-β ITERATE on `ce310a33` → four-finding repair; SHA_M `eb627874`), §R6 (third external-β ITERATE on `eb627874` → two-finding repair, no blocker; SHA_M `64d94e71`), and **§R7 (fourth external-β ITERATE on `64d94e71` → three-finding repair, 2 REQUIRED + 1 REFINEMENT, no blocker; new SHA_M `2d6b93cc`)**. Convergence trajectory of external-β findings: **R3: 6 → R4: 4 → R5: 2 → R6: 2+1-refinement** (no blocker since R4). **Current state: the R7 matter (SHA_M `2d6b93cc4e69e5b413a80bd8e352cb0a004da460`) is offered back to the external β for exact-SHA re-review; the cell is NOT converged and NOT ratified.** The remaining exit sequence (§16) is: external-β re-review → γ binds SHA_M + review + evidence → separate CC ratification → operator-final-read → merge → separate PC-Wave → CC wave review → operator wave authorization.
