# α close-out — cycle/558

**Status:** pre-merge close-out, authored on `cycle/558` at head `ecae165a3c3b96380fe6be315a673d32815fa3bc` (β's R0 APPROVE commit). This cycle is a docs-only cell (AC8: no code changes); per the hub's engineering-persona doctrine, the β-α-collapse-on-δ exception for skill/docs-class cycles applies (AC oracle is mechanical: file existence, grep counts, CI status; review-independence risk is structurally low). One actor authored all three role close-outs in sequence rather than waiting for a γ-requested post-merge re-dispatch. Standard re-dispatch close-out timing (`alpha/SKILL.md` §2.8, "Re-dispatch path") does not apply here for that reason — this is the collapsed-role equivalent, filed pre-merge because merge/PR are explicitly δ's next step, not this pass's.

## Summary

Issue #558 asked for a refresh of `docs/reference/governance/GLOSSARY.md` (stale relative to current CDD/CDS/wave vocabulary, wake-as-skill migration, and issue-taxonomy work) plus glossary links from 4 docs entrypoints. R0 converged cleanly:

- `docs/reference/governance/GLOSSARY.md` rewritten (413 → 561 lines; 213 lines changed in the diff): stale `docs/alpha|beta|gamma` path citations removed (replaced with an explicit retirement note), package paths corrected to carry the `src/` prefix, the stale `C_Σ ≥ 0.80` TSC threshold corrected to `Θ ≥ 0.75` (cited to the 2026-06-23 v3.0.1 errata), and 22 required terms (cell, wave, matter, receipt, review request, projection, α, β, γ, δ, ε, V, MCA, MCI, TSC, trust claim, coherence witness, wake, SKILL.md, golden, issue, dispatch:cell, effort/*) each given a dedicated heading with a full definition paragraph.
- 5 required term-pair distinctions added (review request vs verdict; trust claim vs coherence witness; signature vs attestation; projection vs role-renaming; cell vs wave), each with an explicit contrast sentence rather than two adjacent definitions.
- 4 one-line glossary cross-links added: `docs/README.md`, `docs/reference/README.md`, `docs/development/README.md`, `docs/development/issues/TAXONOMY.md`.
- Diff confined to `docs/` plus this cycle's own `.cdd/unreleased/558/` artifact channel (AC8 guard verified empty on `git diff --stat origin/main...cycle/558 -- . ':!docs' ':!.cdd/unreleased/558'`).

One fix-round occurred within R0: the first `self-coherence.md` commit (`02bf3627`) used `§`-prefixed section headings (`## §Gap`, etc.) that failed the I6 CDD-artifact-ledger-validation CI job, which requires an exact-line match on `## Gap` / `## Skills` / `## ACs` / `## CDD Trace`. Root cause: the artifact-writing convention was not cross-checked against the ledger validator's exact-match requirement before the first push. Fixed in `ad47430c` by dropping the `§` prefix from all six headings; CI green after re-push. This was a same-session fix, not a review round — β's round-1 review (head `57e44f47`) was against the corrected headings.

## Findings / patterns (factual, no recommended disposition — triage is γ's)

- **Pattern: source-of-truth citations drift faster than the glossary that cites them.** Beyond the 4 stale-reference classes γ's scaffold named up front (retired `docs/alpha|beta|gamma`, missing `src/` prefix, root `threads/` ambiguity, stale TSC threshold), 3 additional dead citations were found and corrected during implementation that the scaffold's source-of-truth table did not enumerate: the "DUR" entry's pointer to `COGNITIVE-SUBSTRATE.md §7.3` (section no longer exists there), the "Wake-up" entry's pointer to `FOUNDATIONS.md §6` (content moved), and the "Coherence delta" entry's pointer to `docs/development/cdd/CDD.md §3.4/§9.5` (that file is now a 3-line pointer, not the algorithm). All three were caught by the same `test -f` + `grep -n "^## "` verification discipline applied to the scaffold-named citations, not by a separate check — same class, wider net than the scaffold pre-computed.
- **Pattern: 3 pre-existing staleness items found incidental to this cell's verification pass, outside this cell's pinned surface.** Surfaced while cross-checking the TSC-threshold and package-list citations, not sought out: (1) `CHANGELOG.md` still cites the superseded `≥0.80 = PASS` threshold; (2) `lychee.toml`'s header comment names `.github/workflows/coherence.yml` as the I4 job's home, but the job is actually defined in `.github/workflows/build.yml`; (3) `docs/reference/governance/GLOSSARY.md`'s own header (`# Glossary – cnos v3.6.0`) does not follow the file's own stated document-versioning convention (`GLOSSARY v2.0.0`-style). None of these three are inside the AC8-pinned surface (GLOSSARY.md content + 4 entrypoint links); none block any AC.
- **Finding surfaced by β, not α: "Package" glossary entry is not exhaustive** (β `beta-review.md` Finding 1, severity A, non-blocking). The rewritten "Package" entry lists 5 packages under `src/packages/` (`cnos.core, cnos.eng, cnos.cdd, cnos.cds, cnos.handoff`) but the tree has 9 (`ls src/packages/` confirms `cnos.cdr`, `cnos.cdd.kata`, `cnos.issues`, `cnos.kata` are also present). "Package" is not one of AC2's 22 required terms; the entry is a net improvement over the pre-existing entry (which cited the now-defunct `cnos.pm`), but the tightened "Current packages under..." framing reads as exhaustive when it is not — a name-overpromise pattern (`beta/SKILL.md` §6b class).

## Debt disclosed at review-readiness (self-coherence.md §Debt, restated here for γ triage)

1. No local `lychee` binary was available in the implementation environment; AC7's binding evidence is the CI job result on the pushed head, not a pre-push local run. A manual Python link-resolution script served as a pre-push sanity check (0 broken links found); CI confirmed green after push.
2. Glossary header version-string format inconsistency (item 4 above) — pre-existing, no AC requires fixing it, left untouched to avoid scope creep.
3. `CHANGELOG.md` stale threshold citation (item 2 above) — outside AC8's pinned surface, left untouched.
4. `lychee.toml` comment/reality mismatch (item 3 above) — outside AC8's pinned surface, left untouched.

## Calibrated success claim

R0 converged with zero D/C/B-severity findings and one A-severity non-blocking finding (Package-entry incompleteness). All 8 ACs independently re-verified by β against code/tree, not taken on trust from `self-coherence.md`. CI green across all 10 jobs on the reviewed head SHA and on the current branch head. This is a clean, single-round convergence for a docs-only cell — not evidence about implementation quality on code-bearing cycles, where the review-independence risk this cell's collapsed-role exception relies on does not hold.

Branch head at close-out time: `ecae165a3c3b96380fe6be315a673d32815fa3bc`.
