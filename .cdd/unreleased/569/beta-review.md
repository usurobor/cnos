# β review — cycle/569

manifest:
  planned_sections: [Verdict, Contract Integrity, Issue Contract, Diff Context, Architecture, Findings]
  completed: [Verdict, Contract Integrity, Issue Contract, Diff Context, Architecture, Findings]

## §R0 — round 0

**Base:** `origin/main` @ `0520235e1285c078eb3bc9d7eeba191b0413c53b` (re-fetched synchronously at review time; no drift from γ's branch-point). **Head:** `cycle/569` @ `c1bd863ad6f0629c3018d64dedb4cbd3317091b5`. **γ artifact present:** `.cdd/unreleased/569/gamma-scaffold.md` — yes, on `origin/cycle/569`. **α identity:** `alpha@cdd.cnos` on every commit (verified via `git log`).

### Verdict

**CONVERGE.** All 5 ACs pass on independent re-verification (code re-run, not doc-trust). Scope guardrail holds. 7-axis implementation contract confirmed. One I6 anomaly was investigated and determined to be a transient cycle-staging artifact, not a regression — documented below with reproduction so it isn't silently dropped.

### AC-by-AC findings (independently re-executed, not read off self-coherence.md)

**AC1 — `--apply` guard-gated + idempotent: PASS.**
`cd src/packages/cnos.issues/commands/issues-fsm && go build ./... && go vet ./... && go test ./... -race -v` → all 27 top-level/subtest lines PASS, 0 failures, race-clean. Read `TestApply_ReviewTransitionAppliesOnGuardPassAndIsIdempotent` (issuesfsm_test.go:335) and `TestApply_RequeueTransitionAppliesOnGuardPassAndIsIdempotent` (:402) directly: both issue a **real second `Run()` call** against a distinct post-mutation fixture (`review-with-pr.json`, `todo.json`) through an `httptest`-style fake server, and assert zero requests reached the fake server on the second call (`n != 0` fails the test) — idempotence is exercised, not merely asserted. Negative case `TestApply_BlockedReviewRequestRefusesAndMutatesNothing` (:465) asserts a `calls` counter stays 0; `TestApply_EmptyReviewStateBlocked` (:494) goes further — the fake server's handler itself calls `t.Fatalf` if any request arrives, the strongest available "zero writes" assertion. Both assert nonzero exit. Manually ran `cn issues fsm evaluate --issue 601 --fixture testdata/review-empty.json` (no `--apply`, byte-identical to Phase 1 output — see backward-compat check below) and confirmed the negative fixture's shape independently.

**AC2 — workers no longer own status labels: PASS.**
`git diff origin/main...cycle/569 -- src/packages/cnos.cds/orchestrators/cds-dispatch/SKILL.md src/packages/cnos.cdd/skills/cdd/delta/SKILL.md src/packages/cnos.core/skills/agent/dispatch-protocol/SKILL.md` re-read directly (not via self-coherence's summary): step 7 of cds-dispatch now reads "...requests the ... transition ... runs `cn issues fsm evaluate --issue {N} --apply` ... rather than writing the label directly"; the "Closeout integrity preflight" and "Lifecycle transitions" table were updated consistently, explicitly calling out that claim/hard-block/release-back-to-queue remain direct label writes (correctly out of AC2's scope) while only the β-converge→review event is routed through FSM-apply. `delta/SKILL.md` §9.6's `status:review` row and `dispatch-protocol/SKILL.md` §2.9 both updated consistently. Post-diff grep for `writes the label transition|then writes the label|sets .status:review.` across all three touched files: one surviving hit, `dispatch-protocol/SKILL.md` D12 ("cell sets `status:review`") — read in context, this is a historical-failure-symptom description (the cnos#524 W4 RCA narrative), immediately followed by "_Fix:_ §2.9" which carries the corrected mechanism; not a normative instruction to δ. §2.4 confirmed untouched (`git diff` on that section is empty) and does not itself assert a label-write mechanism, so it does not contradict AC2.

**AC3 — empty-review structurally impossible: PASS.**
Ran the new negative fixture (`testdata/in-progress-review-request-no-matter.json`, `review_request_present=true`, no PR/commits/branch) through `--apply` via the test suite: nonzero exit + zero label-write calls confirmed (see AC1 evidence — same test doubles as AC3's oracle). Pre-existing `review-empty.json` (status:review already set, no evidence) also confirmed blocked under the new `--apply` path via `TestApply_EmptyReviewStateBlocked`'s `t.Fatalf`-in-handler assertion. `./scripts/ci/check-dispatch-closeout-integrity.sh --self-test` run directly: `cnos#524 closeout-integrity self-test: empty-review detector correct` (exit 0) — the independent shell-level detector is untouched and still green, confirming α's choice not to duplicate/weaken it.

**AC4 — no broad label redesign: PASS.**
`git diff origin/main...cycle/569 | grep -E "^\+" | grep -iE "gh label create|label-doctrine|status:[a-z_-]+\"|protocol:[a-z_-]+\""` re-run independently: every hit is (a) the AC4 oracle text itself quoted into the γ-scaffold/self-coherence artifacts, (b) `url.PathEscape("status:in-progress")` test-assertion literals, or (c) existing label names inside the three new `testdata/*.json` fixtures. Zero `gh label create` additions, zero edits to any labels-definition surface. `git diff --stat` confirms no `label-doctrine`-path files touched.

**AC5 — current gates remain green: PASS, with one investigated anomaly (see Findings §1).**
- `go build ./... && go vet ./... && go test ./... -race` run in all 4 go.work modules (`src/go`, `cdd-verify`, `issues-map`, `issues-fsm`): all green, race-clean.
- I1 (`cn build --check`, built from cycle/569 HEAD): `✓ All packages valid.`
- I2 (`diff docs/reference/schemas/protocol-contract.json tests/fixtures/protocol-contract.json`): no diff.
- `./scripts/ci/check-dispatch-closeout-integrity.sh --self-test` and `./scripts/ci/check-dispatch-repair-preflight.sh`: both green, run directly.
- Golden/live workflow render: re-ran `cn-install-wake cds-dispatch` (default + `--out .github/workflows/cnos-cds-dispatch.yml`) myself; `diff` against the committed golden and committed live workflow is empty in both cases (not just trusted from self-coherence — independently reproduced), and the working tree was left clean afterward (no drift).
- I5 (skill-frontmatter-check): `cue` CLI absent in this sandbox too — could not run directly, matching α's own disclosed limitation. Confirmed structurally: every edited SKILL.md's diff hunks land well past its frontmatter block. CI must confirm on the actual PR.
- I6: see Findings §1 — investigated, determined non-blocking (self-resolves once this file lands).
- go.mod/go.sum: `git diff origin/main...cycle/569 -- '**/go.mod' '**/go.sum'` is empty — confirms no new runtime dependency, independent of self-coherence's claim.
- Backward-compat invariant (7-axis contract row 7): built both `origin/main` and `cycle/569` into separate binaries and ran `issues fsm evaluate --issue 601 --fixture testdata/review-empty.json` (no `--apply`) on both — `diff` of stdout is empty. Byte-identical, independently reproduced, not just asserted by the test suite.

### Scope guardrail (operator-mandated, hard constraint)

**PASS.** `git diff origin/main...cycle/569 | grep -in "cell_kind\|cellkind"` shows zero hits outside prose (γ-scaffold/self-coherence text quoting the constraint itself) and the `TestSeam_CellKindNotEnforced` fixture-list extension. The new `transitions.json` rules gate only on `review_request_present`, `pr_exists`, `pr_has_commits`, `branch_has_commits` — no `cell_kind` read anywhere in the two new rules. `TestSeam_CellKindNotEnforced`'s assertion body (issuesfsm_test.go:678-709) is byte-unchanged from `origin/main`; only its fixture list gained the two new fixtures, and the test (re-run, PASS) confirms the new rules' decisions are unaffected by `cell_kind` value across `implementation/issue_authoring/wave/cleanup/recovery`. `FactSnapshot.CellKind`-touching code (`fetch.go` observation, `snapshot.go` struct/normalize) is unchanged by this diff — confirmed via diff hunk boundaries.

### 7-axis implementation contract

All 7 axes confirmed against the diff directly (not read off self-coherence's table): (1) Go — no other language present in the diff. (2) `--apply` is a flag on the existing `evaluate` verb (`issuesfsm.go`'s `Run` dispatch switch still has exactly one case, `"evaluate"`; no new subcommand). (3) Package scoping matches exactly the three pinned paths. (4) `cn issues fsm` remains the single compiled-in kernel command — `cmd_issues_fsm.go`'s diff is comment-only, `SourceKernel`/`TierKernel` untouched. (5) No new runtime deps (go.mod/go.sum diff empty; `fetch.go`'s new code uses only `bytes`, `net/http`, `net/url`, `encoding/json`, all stdlib). (6) `FactSnapshot`/fixture JSON shape unchanged; `Decision` gains `ApplyAttempted`/`Applied` fields additively, and `Render` only emits the new `applied:` line when `ApplyAttempted` is true. (7) Backward-compat empirically reproduced above (byte-identical evaluate-without-`--apply` output, main vs. cycle/569).

### Findings

**1. [Investigated, non-blocking] I6 (`cn cdd verify --unreleased --exceptions .cdd/exceptions.yml`) is NOT currently byte-identical between `origin/main` and `cycle/569` — self-coherence.md's AC5 claim of "byte-identical warning/error output" does not hold under the actual CI invocation.**

Reproduction: built `cn` from both `origin/main` (worktree) and `cycle/569` HEAD and ran the exact CI command (`.github/workflows/build.yml`'s `cdd-artifact-check` job: `./cn cdd verify --unreleased --exceptions .cdd/exceptions.yml`):
- `origin/main`: exit 0, `125 passed, 0 failed, 90 warnings`.
- `cycle/569`: exit 1, `126 passed, 1 failed, 91 warnings` — new failure: `❌ self-coherence.md sections — missing required sections: Skills/Mode ACs/AC Coverage CDD Trace` on issue #569 itself.

α's self-coherence.md AC5 evidence ran the check *without* `--exceptions` and reported "byte-identical... the single failure (#512)... pre-existing" — that comparison undercounted: without `--exceptions`, `origin/main` already shows the #512 failure (expected — it's the known, exception-backed debt), but α's own diff of "before/after" apparently didn't surface the *second*, `cycle/569`-only failure on issue #569 itself, which only appears when `#569`'s own directory is scanned (impossible on `origin/main`, since `.cdd/unreleased/569/` doesn't exist there) — an artifact-count asymmetry the "byte-identical" framing glossed over.

**Root cause, traced in `src/packages/cnos.cdd/commands/cdd-verify/ledger.go`:** `classifyCycleType()` (line 424) classifies a cycle "small-change" (hard-fail section validation, `forUnreleased=false`) whenever `self-coherence.md` exists but `beta-review.md` does not (line 439-444) — which is exactly cycle/569's current state (β's review, this file, didn't exist yet on the branch at the time I started). Once `beta-review.md` exists in the same directory, `classifyCycleType` returns `"triadic"`, which validates sections with `forUnreleased=true` (line 457, 473) — a mere ⚠️ WARNING, not a ❌ FAIL, for the identical missing-header pattern. Confirmed by comparison: sibling cycle #570's `self-coherence.md` uses the **exact same `## §Gap`/`## §Skills`/`## §ACs`/`## §CDD Trace` heading convention** as #569's (I diffed the header lists directly), yet #570 registers only a ⚠️ warning — because #570 already has `beta-review.md` present and classifies "triadic". This is not an α regression in the mutated Go/CDS surfaces; it is a property of the multi-role artifact-authoring sequence (β's own artifact was, correctly, not yet on the branch when α signaled review-readiness) colliding with a pre-existing (not introduced by #569) strictness asymmetry between the "small-change" and "triadic" classification paths in `cdd-verify`.

**Verification that this self-resolves:** re-ran `./cn cdd verify --unreleased --exceptions .cdd/exceptions.yml` against the working tree *after* writing this very `beta-review.md` file (which flips #569's classification to "triadic" the moment it's committed): exit code and failure count return to parity with `origin/main`'s exceptions-applied baseline (0 hard failures attributable to #569; the section-header gap becomes the same class of ⚠️ warning #570 already carries). I am not filing this as an α fix-round finding — by the time δ opens the PR (after this converge verdict, per the dispatch contract: "PR not opened by α... δ opens it after β converges"), `beta-review.md` will already be present in the same commit, so the CI run that actually gates the PR will see the "triadic" classification and the lenient path, matching every other in-flight sibling cycle. Recorded here per Role Rule 6 (code-first anchoring: I re-ran the actual gate rather than trusting the doc's "byte-identical" claim) so the discrepancy in α's self-coherence.md wording is not silently absorbed — α should tighten the AC5 evidence language in a future cycle to run the check with `--exceptions` (the real CI invocation) and to account for the small-change/triadic classification transition rather than asserting flat "byte-identical."

**2. [Observation, not a finding] `## §`-prefixed section headers are a growing but not-yet-tool-recognized convention.** Both #569 and #570's self-coherence.md use `## §Gap`/`## §Skills`/etc., which `cdd-verify`'s `sectionPresent()` (exact-prefix match) does not recognize for the `Skills`/`ACs`/`CDD Trace` checks (it only accidentally tolerates it for `Gap` via a duplicate plain "## Gap statement" heading, and for `Self-check`/`Debt` via case-insensitive substring matching). This is pre-existing tool debt unrelated to #569's scope (the CDS FSM/label-authority surfaces) — out of scope to fix here, but worth a follow-up issue so the "small-change" hard-fail path doesn't keep tripping on a heading convention the "triadic" path already tolerates as a warning.

### Pre-merge gate readiness

Row 1 (identity truth): asserted `beta@cdd.cnos` this session. Row 2 (canonical-skill freshness): `origin/main` re-fetched synchronously at review start; HEAD matches γ's branch-point (`0520235e`) — no advance since, no re-load needed. Row 4 (γ artifact completeness): confirmed present. Row 5 (close-keyword presence): to be satisfied at merge-commit-subject time (β's next step, not this artifact). Row 3 (non-destructive merge-test in a throwaway worktree) and the merge itself are the next β action after this verdict is committed.
