# γ close-out — cnos#575

## Cycle summary

**Issue:** [usurobor/cnos#575](https://github.com/usurobor/cnos/issues/575) — "cds/fsm: route claim, hard-block, release-back-to-queue through the FSM (Phase 3 — Sub 2 of #583)". **Parent:** #583 (master wave — mechanical dispatch-cell architecture). **Precondition:** #584 (Sub 1 — mechanism/cognition boundary doctrine), landed and CLOSED before this cycle started.

The CDS issue-state FSM (`cn issues fsm`, #568/#569, hardened #574) owned exactly one status transition (`in-progress → review`) before this cycle. Claim (`todo → in-progress`), hard-block (`→ status:blocked`), and release-back-to-queue (`in-progress → todo`) remained direct wake label writes — so "the FSM owns status labels" was still aspirational, and the mechanical PR-open/recovery runtime planned for Subs 3–4 of #583 could not be built on that foundation.

This cycle routed all three remaining transitions through the FSM via new marker-file-gated rules (`CLAIM-REQUEST.yml`, `BLOCK-REQUEST.yml`, `RELEASE-REQUEST.yml`), mirroring the `REVIEW-REQUEST.yml` pattern #569 established: new `FactSnapshot` fields, new `guardFuncs` entries, new `fetch.go` live-path wiring, additive `transitions.json` rules (2 new `todo`-state rules, 3 new `in-progress`-state rules), and doctrine updates to `cds-dispatch/SKILL.md` (all "direct label write" prose removed for the three transitions) and a scoped override paragraph in `dispatch-protocol/SKILL.md`. 14 new TDD-driven tests (`TestAC575_*`) plus 6 new fixtures; the full pre-existing 37-test suite passes unmodified. Implementation: α at `dd3e966` (+2 CI-fix commits, `167410e`). Review: β converged R0 at `c6ac67f`, no findings, all five ACs PASS, implementation-contract CONFORMS, scope guardrails HELD.

**Merge state at close-out authoring time:** PR [#588](https://github.com/usurobor/cnos/pull/588) is OPEN and MERGEABLE (not yet merged); issue #575 is OPEN. This `gamma-closeout.md` is therefore the pre-merge process-gap audit and readiness declaration for this sub — distinct from the full multi-artifact CDD.md §"Closure" cycle-close (which additionally requires post-merge `alpha-closeout.md`/`beta-closeout.md`, `RELEASE.md`, cycle-directory move, hub-memory update, and branch cleanup). Those post-merge steps apply once #588 merges and are not claimed here.

**CI on reviewed head (`b91f4ba`, includes β's `beta-review.md` commit on top of α's `c6ac67f`):** all named check-runs `success` (Package verification, Binary verification, SKILL.md frontmatter validation (I5), Package/source drift (I1), Protocol contract schema sync (I2), CDD artifact ledger validation (I6), Dispatch closeout-integrity guard (#524), Dispatch repair-preflight guard (#516), Repo link validation (I4), Go build & test, Re-render + diff per-package goldens). PR merge-state is `MERGEABLE`; `mergeStateStatus: UNSTABLE` reflects PR-level review/branch-protection bookkeeping (e.g. awaiting the merge action itself), not a CI failure — no check-run reports non-success.

## Process-gap audit (γ→α→β pipeline)

### Base-SHA discrepancy at branch-creation time — handled correctly

δ handed γ a pinned main SHA (`a075666...`) that had already drifted two commits behind `origin/main` HEAD (`8f97b60...`) by the time γ created `cycle/575`. γ named the discrepancy explicitly in `gamma-scaffold.md` §"Base-SHA discrepancy" rather than silently absorbing it, ran `git diff --stat` between the two SHAs, confirmed the delta touched only `.cn-sigma/logs/20260704.md` and `docs/development/board/*` (agent-admin heartbeat + board-map regen — non-overlapping with any surface this cycle touches: `transitions.json`, the Go FSM engine, `cds-dispatch/SKILL.md`, `dispatch-protocol/SKILL.md`, or CDD role skills), and created the branch from actual `origin/main` HEAD per the branch-creation instruction's literal wording ("from `origin/main`", not from the stale pin). No re-load of canonical skills was triggered, correctly, because the delta was non-overlapping with anything γ had loaded skill-content from. This is exactly the discipline `gamma/SKILL.md`'s Load Order staleness-check section calls for: name the drift, characterize its overlap, decide re-load necessity on the merits rather than by default. **Disposition: handled correctly, no corrective action needed.**

A second, structurally identical drift recurred mid-cycle during α's work: `origin/main` advanced by one more commit (`8f97b60` → `4876278`, another non-overlapping agent-admin heartbeat) while α was authoring `self-coherence.md`. α rebased cleanly, re-ran the full test suite (green), and re-stamped SHA citations (`self-coherence.md` §Review-readiness row 1) — the α-side analogue of the same discipline. Both instances of drift were caught and named rather than silently ignored; neither required operator escalation.

### Six friction notes deliberately left for α — all resolved coherently per β's independent verification

γ's `gamma-scaffold.md` named six friction notes as real design calls for α to resolve, not blockers requiring γ pre-decision (per `gamma/SKILL.md` §2.5 Step 5's "γ may not... author the implementation fix inside the review loop" and the general principle that γ names the tension without pre-deciding it when the decision is genuinely α's implementation call):

1. **Claim guard framing** ("no competing active run" — new run-id-comparing fact vs. structural reliance on the existing claim sequence). α resolved as a hybrid neither of γ's two offered framings anticipated exactly: reused `run_active` unmodified (its branch-scoped definition already structurally excludes the requesting wake's own pre-branch execution) plus a new `claim_request_present` marker guard. β independently re-derived this from the `transitions.json` diff (not from α's narrative) and confirmed it doesn't reopen the #569 `run_active`-gating tension, since `run_active` is not referenced in the review/hard-block/release rules' `all_true` clauses.
2. **`dispatch-protocol/SKILL.md` scope** (protocol-agnostic file vs. cds-only FSM). α chose option (a): a scoped "Protocol-specific FSM override" paragraph in §2.2, leaving the generic step-4 direct-write code block untouched. β confirmed via direct diff read (2-line addition only) rather than accepting α's description.
3. **`delta/SKILL.md` §9.6** (same "direct write" prose pattern, not in AC4's named surface list). α read it in full and left it unedited, with reasoning recorded in `self-coherence.md` §Debt. β confirmed `git diff` on this file is empty and judged the non-edit a defensible non-goal given the issue's explicit surface list. See "Deferred work" below — γ now closes this triage row explicitly by filing a follow-up.
4. **Test-function naming collision risk** (`TestAC1_*` etc. already claimed by #568's own historical numbering). α used `TestAC575_N_*` prefixes throughout; β spot-checked for duplicate symbols, none found.
5. **Fixture naming convention.** α followed the existing `<state>-<condition>.json` pattern for all six new fixtures; β spot-checked filenames and contents directly.
6. **Rule ordering** (first-match-wins; new rules must not shadow the existing dead-run reconciliation rules). α positioned the three new `in-progress` rules before the dead-run rules and proved non-shadowing with a dedicated test plus a full-suite re-run. β independently re-verified via manual `jq` rule-order inspection rather than trusting the dedicated test alone.

β's `beta-review.md` §"Friction-note resolution verdict" states explicitly: **"ALL SIX RESOLVED, ALL COHERENT"**, with each resolution independently re-confirmed against the actual diff, not paraphrased from α's self-coherence report. This is the pipeline working as designed: γ names real design tensions without collapsing role separation by pre-deciding them; α resolves them and records the reasoning; β independently re-derives and checks the resolution rather than trusting the narrative. **Disposition: no gap — the friction-note mechanism functioned correctly across all six instances.**

### A minor, self-corrected friction: two CI failures surfaced on first push

α's first pushed head commit failed two CI checks — I4 (lychee link validation: a relative-path `../` off-by-one in the new `dispatch-protocol/SKILL.md` paragraph) and I6 (CDD artifact ledger validation: a section-heading form mismatch in `self-coherence.md`). Both were fixed at root cause in commit `167410e`, re-verified green, and documented in `self-coherence.md`. Neither reached β as a review-round finding — α caught and fixed both before signaling review-readiness, so no formal §"Cycle iteration triggers" review-churn trigger fired (there was exactly one β round, R0, converge). This is not a role-skill failure requiring a patch: both are mechanical, easily-caught-post-push errors of a class no loaded skill specifically warns about (relative-path arithmetic in nested SKILL.md files; section-heading-form conventions enforced by CI but not locally lintable pre-push in this environment). **Disposition: one-off — recorded here per the silence rule (§3.8) is not "noted and dropped without disposition"; the disposition is explicit no-patch, because the cost was one self-corrected round with zero role-boundary crossing and no operator involvement, below the bar for a new gate.**

### Scope discipline — held

Per β's independent "Scope guardrail verdict: HELD": no Sub 3/4 work (no PR-open/checkpoint/recovery runtime code in the diff); `cell_kind` untouched for enforcement (`rg` zero-match in `transitions.json`, `TestSeam_CellKindNotEnforced` unmodified assertion); no new status labels or taxonomy (the four transitions use the existing `status:todo`/`in-progress`/`blocked`/`review` set only); no Demo 0; no touches to #585/#586; `transitions.json`'s `review`/`changes` state blocks are additive-only elsewhere; `delta/SKILL.md` untouched (empty diff, confirming Friction-note-3's claimed non-edit); workflow execution semantics (`permissions`/`on:`/`runs-on`/`concurrency`) in the rendered GitHub Actions workflow are byte-identical to `main` — only the embedded doctrine prose changed, which is the expected downstream consequence of the `SKILL.md` edit. γ independently confirms this reading from `beta-review.md` rather than re-deriving it: β's verdict is itself independently-derived (diff-based, not narrative-trusting), which is the property this triage step relies on.

## Independent γ process-gap check (`gamma/SKILL.md` §2.9)

- **Did this cycle reveal a recurring friction?** No new recurring friction beyond the two already-tracked classes: (a) `origin/main` heartbeat/board-map commits drifting the base SHA mid-cycle — occurred twice this cycle, handled correctly both times by explicit-naming-plus-overlap-check, and is already the documented discipline in `gamma/SKILL.md`'s Load Order section; no patch needed, the existing discipline is working. (b) mechanical CI failures surfacing post-push rather than pre-push (I4 link-path, I6 heading-form) — a known class, not new to this cycle, self-corrected within budget, below the bar for a new gate per the analysis above.
- **Was any gate too weak or too vague?** No. The pre-dispatch scaffold gate (§2.5 "Pre-dispatch γ scaffold check"), the friction-note mechanism, and β's independent-verification discipline (Rule 7 implementation-contract conformance, diff-based re-derivation rather than narrative-trust) all functioned exactly as specified and caught what they were designed to catch (nothing to catch here — R0 converge — which is itself evidence the gates are calibrated, not evidence they're untested).
- **Did a role skill fail to prevent a predictable error?** No. Both CI failures were of a class (relative-path arithmetic, heading-form drift) that is inherently a "look at CI" class, not a "the skill should have told you this in advance" class — no loaded skill's omission caused either.
- **Did coordination burden show a better mechanical path?** No excess coordination burden observed: zero γ-side unblocking actions were required mid-cycle (no `gamma-clarification.md` was needed — the six friction notes were resolved entirely within α's session, as designed), zero RC rounds, single β pass to converge. This is the cycle working at its most mechanically efficient shape for a substantial 5-AC cycle.

**Conclusion:** no `cnos.cds/skills/cds/CDS.md` §"Assessment" → §"Cycle iteration triggers" trigger fired (review rounds = 1, not > 2; findings = 0, not ≥ 10; no avoidable tooling failure blocked the cycle — the two CI failures were caught and fixed within the same session, not "blocking"; no loaded-skill miss identified). Per §2.9's explicit instruction for the no-trigger-fired case: stated here — no skill/spec patch is required this cycle; the friction-note mechanism and the base-SHA-drift discipline both performed as designed.

## Deferred work

**`delta/SKILL.md` §9.6 prose gap (Friction note 3).** α's resolution left `delta/SKILL.md` §9.6 unedited: two of its three return-tokens-table rows ("status:blocked + reason", "claim release (race)") still describe δ-observable transitions without the FSM-request phrasing the "status:review" row already carries, one layer up the doctrinal stack from the `cds-dispatch/SKILL.md` prose this cycle did fix. This is real and future-relevant — a reader of `delta/SKILL.md` alone could infer δ performs the write directly for hard-block/claim-release — but it was correctly out of scope for #575 (not in the issue's named AC4 surface list, not in its "Out of scope" list either; editing it here would have been an unscoped addition per α's own reasoning in `self-coherence.md` §Debt).

**Disposition: filed as a follow-up issue, not landed as an immediate patch and not dropped as a non-goal.** Filed [usurobor/cnos#589](https://github.com/usurobor/cnos/issues/589) — "delta/SKILL.md §9.6: align hard-block/claim-release prose with FSM-request phrasing (post-#575)" — referencing #575 and #583, labeled `kind/process` + `area/cdd` (no `dispatch:cell`/`status:todo`/priority labels — this is a doctrine-consistency note for a future pass, not an immediately-dispatchable cell). Rationale for filing rather than declaring a non-goal: the gap is concrete (two specific table rows, one specific target file), low-effort to fix (prose-only, no runtime surface), and the risk of leaving it silently un-tracked is that a future doctrine-stack reader trusts `delta/SKILL.md` in isolation and gets the write-vs-request model wrong — exactly the kind of one-off finding CAP triage (`gamma/SKILL.md` §2.7) routes to "Project MCI → file / update project issue" rather than "drop."

No other deferred outputs were identified. All five ACs are MET per β's independently-verified §R0; no known debt beyond this one item and the already-disclosed (and since-superseded) local-CI-proxy caveat in `self-coherence.md` §Debt (superseded by β's own live CI/PR check-run verification, both confirmed green above).

## Close-out triage table

| Finding | Source | Type | Disposition | Artifact / commit |
|---|---|---|---|---|
| Base-SHA discrepancy at branch-creation (2 instances: pre-dispatch + mid-cycle) | γ scaffold / α self-coherence | process | No action — existing staleness discipline handled it correctly both times | `gamma-scaffold.md` §"Base-SHA discrepancy"; `self-coherence.md` §Review-readiness row 1 |
| Six friction notes (claim-guard framing, dispatch-protocol scope, delta/SKILL.md §9.6, test naming, fixture naming, rule ordering) | γ scaffold → α resolution → β verification | design/process | Resolved in-cycle; verified independently by β | `self-coherence.md` §Debt; `beta-review.md` §"Friction-note resolution verdict" |
| Two CI failures on first push (I4 link path, I6 heading form) | α (self-caught pre-review) | mechanical/tooling | One-off, self-corrected same session; no gate patch warranted | commit `167410e` |
| `delta/SKILL.md` §9.6 direct-write prose (one layer above the fixed surface) | Friction note 3 | doctrine-consistency | Project MCI — follow-up issue filed | [usurobor/cnos#589](https://github.com/usurobor/cnos/issues/589) |
| Scope discipline (no Sub 3/4, no `cell_kind` enforcement, no new taxonomy) | β independent verification | scope guardrail | Held — no action | `beta-review.md` §"Scope guardrail verdict: HELD" |

Silence rule (§3.7/§3.8) check: every row above carries an explicit disposition; none are left at "noted."

## Scope confirmation

This cycle stayed within its pinned bounds:
- **No Sub 3/4 work** (mechanical PR-open/checkpoint/recovery runtime) — confirmed absent from the diff by β's independent scan.
- **No `cell_kind` enforcement** — the seam remains observed-only; `TestSeam_CellKindNotEnforced`'s assertion is byte-identical to pre-cycle, only its fixture-iteration list was extended to cover the six new fixtures.
- **No new status-label taxonomy** — all four FSM-owned transitions (claim, hard-block, release-back-to-queue, review) use the pre-existing `status:todo`/`in-progress`/`blocked`/`review` label set.
- **No Demo 0, no #585/#586 touches.**

## Closing statement

Cycle #575 (Sub 2 of #583) is process-complete: α implemented against a fully-specified γ scaffold, resolved all six named friction notes with recorded, independently-verifiable reasoning, and β converged at R0 with zero findings after independently re-deriving every AC oracle, the implementation-contract conformance verdict, and the scope-guardrail verdict from the diff itself rather than from narrative. One follow-up item (`delta/SKILL.md` §9.6 prose alignment) has been triaged and filed as [usurobor/cnos#589](https://github.com/usurobor/cnos/issues/589) rather than left unaddressed.

**This sub is ready to be marked complete under master #583 once PR [#588](https://github.com/usurobor/cnos/pull/588) merges.** The remaining steps (post-merge `alpha-closeout.md`/`beta-closeout.md`, issue-close-state assertion, hub-memory update, branch cleanup, and — if this is the release boundary — `RELEASE.md` + cycle-directory move) are δ/β/γ post-merge obligations per `gamma/SKILL.md` §2.6/§2.10 and are not claimed as done by this document; this document is the pre-merge process-gap audit and readiness declaration for the sub itself.
