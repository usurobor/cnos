## Protocol exemption

**Scope of exemption:** rule 3.11b γ-artifact-of-record (`review/SKILL.md` §3.11b), satisfied here via **recovery path (b)** — this explicit `## Protocol exemption` section — in place of a per-cycle `.cdd/unreleased/671/gamma-scaffold.md` or a §5.2 wave manifest inside the reviewed matter.

**Reason — bootstrap Planning Cell.** #671 authors the **cell-runtime doctrine itself**: the cell classes, the `cn.cell.contract.v1` envelope, the cell/wave FSMs, the shipped→specified migration, and the integration seal. That doctrine, once implemented (#627 S2–S8), is the machinery that mechanically constitutes cells and separates cell roles. #671 therefore **predates the runtime that would constitute it** — it cannot be scaffolded by a runtime it is defining. Per `delta/SKILL.md` §9.1, **bootstrap-δ role-collapse is a doctrine-acknowledged empirical mode** (γ-driver fused with δ-orchestrator, roles spawned as sub-sessions), explicitly "an empirical case, not the destination architecture." #671 ran under such collapse (α authorship fused with the κ/δ control plane; β genuinely external). This exemption **names that collapse rather than hiding it.** The exemption does **not** make κ-performed γ valid; γ is performed by a durably-provenanced activation distinct from κ (below).

**Why not the §5.2 wave-manifest path (c).** The canonical wave-manifest-as-γ-artifact route (`operator/SKILL.md` §10.2) would place `manifest.md`/`status.md` inside `.cdd/waves/cell-runtime-doctrine/` — the **frozen reviewed matter** whose content bindings the external-β CONVERGE depends on. Mutating that dir would break the freeze. Path (b) keeps the matter byte-identical and locates the cell's constitution + receipts under `.cdd/unreleased/671/`.

**Auditable role receipts (this exemption's discharge).** Committed on `wave/671-cell-runtime-doctrine`:
- `.cdd/unreleased/671/self-coherence.md` — α record + role-provenance ledger (names the bootstrap collapse as debt).
- `.cdd/unreleased/671/beta-review-source-5076109763.md` — the **external** β review captured **byte-exact** (raw-body sha256 `75cdb9b6…`, 9,894 bytes; verdict CONVERGE at reviewed SHA `614829a4`), with κ capture metadata in the separate envelope `.cdd/unreleased/671/beta-review.md`.
- `.cdd/unreleased/671/gamma-dispatch.md` — the durable γ **dispatch/activation record**: the exact frozen inputs, the activation identity, and the γ output hash.
- `.cdd/unreleased/671/gamma-closeout.md` — γ closure authored by a **γ activation distinct from κ** (independent re-verification), binding the frozen matter + β evidence. **Git author metadata is not the proof of identity** — the dispatch record + input/output binding is; κ only **transports** (commits/pushes) the artifact.

**This exemption is itself revision-bound.** Because constitution now depends on this mutable issue-body section, its exact text is snapshotted and content-hashed at `.cdd/unreleased/671/protocol-exemption.md` (with this issue's identity + observed `updated_at`). The pre-authorization gate **fails stale** if the live issue-body exemption diverges from that committed snapshot — so an operator can prove the exemption authorized is the one reviewed.

**Firebreak correction (R17).** The earlier γ closeout was signed by κ ([PR #672 comment 5076124652](https://github.com/usurobor/cnos/pull/672#issuecomment-5076124652)) — a violation of the κ-must-not-perform-γ firebreak. It is **retracted as void**; γ is re-performed by an activation distinct from κ, with durable dispatch/authorship provenance (not a git-author string). A fresh external-CC process judgment runs against the new receipt head **before** the operator authorization gate. The reviewed matter under `.cdd/waves/cell-runtime-doctrine/` remains **frozen** at `614829a4`