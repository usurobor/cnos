# α self-coherence — cnos#640

## Gap

- **Issue:** [usurobor/cnos#640](https://github.com/usurobor/cnos/issues/640) — "process/dispatch: the 'Not dispatched' body-hold vs `status:todo` label defer trap keeps recurring — and the meta-gap that lets it."
- **Mode:** design-and-build (process doctrine + a small mechanism), per `.cdd/unreleased/640/gamma-scaffold.md`.
- **Gap being closed:** a design-first issue's body can assert a hold state ("Not dispatched — status:ready ... dispatch on explicit operator authorization") while its `status:*` label independently drifts (e.g. flipped to `status:todo` by hand without the body being rewritten). The two sources of truth can disagree, and a dispatch wake or δ that reads the body sees the contradiction and defers — this happened at cnos#614 (stranded ~1 day) and recurred in the same session at cnos#633 (~1h lost, three no-op firings) because the cnos#614 learning was captured (an RCA comment) but never mechanized. This cell mechanizes the fix: (1) doctrine naming labels as the sole source of truth for dispatch readiness, (2) a `cn issues dispatch --issue N` primitive that performs the label flip and the legacy-body cleanup as one caller-visible operation, and (3) an honest accounting of the observe→capture→detect-recurrence→mechanize→verify-non-recurrence loop cnos#640's own meta-comment names.
- **Governing constraints (restated, not re-derived):** Kernel §1.2 "MCA over MCI — 'Won't repeat' without a mechanism is not a fix" and Kernel §2.2 "one source of truth per fact" (`src/packages/cnos.core/doctrine/KERNEL.md`). Scope guardrails per `gamma-scaffold.md` §5: do NOT weaken operator hold authority; do NOT add new status labels; do NOT change `transitions.json`'s transition rules; do NOT change #630's recovery behavior; do NOT touch #626, #618, #639 (do not dispatch); do NOT start "Demo 0". The new primitive is operator/human-invoked only — no dispatch wake or scan/reconciler calls it on its own initiative.
- **Base SHA:** γ's scaffold pinned `cycle/640` from `59e7fdb7` (per δ's claim comment citing `2114d649`, with γ's own friction note 6 recording the one-commit drift to `59e7fdb7`). `origin/main` advanced two further commits during α's session, to `2eeb4032` (`87c60c28` board-map regen + `2eeb4032` a sigma agent-admin heartbeat commit) — neither touches any surface this cell changes (`src/packages/cnos.issues/`, `src/go/`, the three doctrine files, or the issue template). Per α SKILL.md §2.6 rule 1, α rebased `cycle/640` onto `origin/main` at `2eeb4032` before requesting review (`git fetch origin main && git rebase origin/main && git push --force-with-lease origin cycle/640`); the rebase was conflict-free.
- **Branch:** `cycle/640`.

## Skills

- **Tier 1:** `CDD.md` (canonical lifecycle) + `cnos.cdd/skills/cdd/alpha/SKILL.md` (this role's surface). No β/γ role skills loaded.
- **Tier 2:** `eng/go` (implicit — new Go package mirroring an existing sibling's shape; no new toolchain).
- **Tier 3 (per `gamma-scaffold.md` §3 α prompt):**
  - `src/packages/cnos.core/skills/write/SKILL.md` — applied to every doctrine paragraph written (front-load the point, one fact one home, cut throat-clearing).
  - `src/packages/cnos.core/skills/agent/dispatch-protocol/SKILL.md` — read in full before editing; §1.2 "Human dispatch gate" is the edit site for AC1/AC2; §1.3/§5's D-numbered failure-mode catalogue is where the new D13 entry landed (see §ACs AC2 for why D13 and not the D8/D9 slot γ's friction note 2 flagged as free — that premise was checked against the live file and found incorrect).
  - `src/packages/cnos.core/skills/agent/label-doctrine/SKILL.md` — read in full; got a one-line cross-reference (not a restatement) in §7.
  - `src/packages/cnos.cdd/skills/cdd/CELL-KINDS.md` — read in full; the AC3 loop doctrine landed as a new section extending "Mandatory terminal learning section", per γ's landing-candidate guidance.
  - `src/packages/cnos.core/doctrine/KERNEL.md` §1.2, §2.2 — cited directly (not restated) in both the dispatch-protocol doctrine addition and the CELL-KINDS.md loop section, per the prompt's "cite, do not restate" instruction.
- **Also loaded (not in the Tier-3 list but load-bearing for the implementation contract):** `src/packages/cnos.issues/commands/issues-fsm/fetch.go`, `terminal.go`, `terminal_test.go`, `issuesfsm.go` — read in full to mirror the injectable-`*Options`-struct test pattern, the `ghRequest`/`ghAddLabel`/`ghRemoveLabel`/`ghGetJSON` HTTP-primitive shapes, and the `withFakeGitHub` httptest-server test idiom, per `gamma-scaffold.md` §2's explicit pointer.
- **Implementation contract (per α SKILL.md §3.6, all 7 axes pinned by γ, none required escalation):**
  - Language = Go (the CLI primitive) + Markdown (the doctrine changes). No other language used.
  - CLI integration target = `cn issues dispatch` as a `cn` subcommand, sibling of `cn issues fsm`/`cn issues map`, same registry/routing (verified live: `cn issues` group listing shows all three siblings; `cn issues dispatch --issue N` resolves via noun-verb routing to the `"issues-dispatch"` registry key — see §ACs AC1 for the transcript).
  - Package scoping = `src/go/internal/cli/cmd_issues_dispatch.go` (thin wiring only — 34 lines, delegates to `issuesdispatch.Run`) + `src/packages/cnos.issues/commands/issues-dispatch/` (domain logic, own `go.mod`), registered in `src/go/cmd/cn/main.go` beside `IssuesMapCmd`/`IssuesFsmCmd`.
  - Existing-binary disposition = additive; nothing replaced or deprecated.
  - Runtime dependencies = GitHub REST API only, via the same dependency-free `net/http` idiom as `issues-fsm/fetch.go`; the one new call is `ghEditIssueBody` (PATCH), modeled on `ghAddLabel`'s shape as instructed.
  - JSON/wire contract preservation = N/A/additive — new command, no existing wire contract touched.
  - Backward-compat invariant = `status:*` label semantics, `transitions.json`'s transition rules, and existing claim/dispatch mechanics are unchanged (verified: `transitions.json` byte-identical to `origin/main`, see §ACs AC4).

## ACs

Implementation SHA: `cb861886` (last implementation commit; `84165e17`/`545cb18f` are this artifact's own §Gap/§Skills commits and carry no further code/doctrine changes).

**AC1 — dispatching a design-first issue can no longer leave body-hold and label state contradictory.**

- **Fixture reconstructing the exact #614/#633 shape (positive case).** `TestDispatch_ContradictoryShape_BodyCorrectedLabelUntouched` (`src/packages/cnos.issues/commands/issues-dispatch/dispatch_test.go`) builds an issue with `contradictoryBody` (a body ending in `"**Not dispatched** — \`status:ready\`. Design-first; dispatch on explicit operator authorization."`, preceded by a `---` separator — the live #614/#633 shape) plus labels `["dispatch:cell","protocol:cds","status:todo"]` (the label already authorized, body still stale — the exact drifted contradiction, not the "about to dispatch" case). Running `Dispatch(ctx, opts)` with `Apply: true` asserts: `res.BodyChanged == true`, exactly 1 `EditBody` call, the stored body equals `contradictoryBodyWant` (the hold paragraph and its preceding separator stripped), and **zero** label-mutation calls (the label was already correct — a corrected, not a redundant, result). This is an automated unit test with an injectable GitHub-API double (`fakeIssueStore`), not a live-network manual check.
- **Clean issue is a safe no-op (negative case).** `TestDispatch_AlreadyCleanAndTodo_SafeNoOp` builds an issue with no hold phrase and `status:todo` already set. `Dispatch` returns `res.NoOp == true`, and the fixture's mutation counters (`edits`, `removed`, `added`) are all zero — proven, not just claimed.
- **Pure-function coverage of the same two cases**, independent of any GitHub double: `TestReconcileBody_StripsHoldPhrase_BoldWithSeparator` / `_PlainNoSeparator` (positive, two phrasing variants) and `TestReconcileBody_CleanBodyIsSafeNoOp` (negative) in `reconcile_test.go`.
- **Idempotency oracle.** `TestDispatch_Idempotent_SecondRunIsNoOp`: runs `Dispatch` twice against the same stateful fixture (`status:ready` + hold phrase). The first run applies both actions; the second run — observing the post-first-run state — reports `NoOp == true` and issues zero further HTTP-shaped calls. Body, labels, edit count, and label-mutation counts are asserted identical before/after the second run.
- **"One caller-visible operation, no half-applied state" oracle.** `TestDispatch_BodyEditFails_LabelNeverFlipped`: injects an `EditBody` failure via `fakeIssueStore.editErr`. `Dispatch` returns an error whose text contains `"label not touched"`, and the fixture asserts **zero** label-mutation calls occurred and `status:ready` remains present (never flipped to `status:todo`) — i.e. a body-edit failure can never produce the exact contradiction (`status:todo` label + un-cleaned hold phrase) this primitive exists to fix, even though the underlying operation is still two separate HTTP calls (`ghEditIssueBody` then `ghRemoveLabel`+`ghAddLabel`) executed body-first by construction (`dispatch.go`'s `Dispatch`, comment at the body-edit call site).
- **Live-wiring proof (caller-path trace).** `TestLiveDefaults_RouteThroughSharedPrimitives` (`fetch_test.go`) exercises `Dispatch` with a zero-value `Options{}` (no injected fakes) against a real `httptest.Server`, proving the default wiring (`liveGetIssue` → `ghEditIssueBody` → `ghRemoveLabel`/`ghAddLabel`) actually reaches the HTTP layer: exactly 1 GET, 1 PATCH, 1 DELETE, 1 POST captured.
- **Candidate (3), the optional contradiction gate, was NOT built as a separate mechanism.** The detection logic `ReconcileBody` computes internally is the same logic a standalone gate would need; γ's scaffold explicitly allowed "add it if cheap given what you've already built." α judged a separate gate to be net-negative complexity for this cycle: `cn issues dispatch` itself already performs the detection-and-correction on every operator invocation, and a same-cell CI/reconciler gate would need its own selector loop over open issues (a second, wake-adjacent mechanism the scope guardrails' "operator/human-invoked only" framing does not obviously extend to) which was not part of the pinned implementation contract. This is disclosed as an open design call in §Debt below, not silently dropped.

**AC2 — the wake no longer has to decide "body says held, label says go."**

- `dispatch-protocol/SKILL.md` §1.2 "Human dispatch gate" now carries a new paragraph, "Labels are the sole source of truth for dispatch readiness (cnos#640)," stating in one place that the `status:*` label alone determines dispatch readiness, that a body-hold sentence is non-authoritative, and that a wake/δ finding one MUST NOT defer — plus two new ❌/✅ example pairs and a pointer to `cn issues dispatch`. Diff: `src/packages/cnos.core/skills/agent/dispatch-protocol/SKILL.md`, commit `cb861886`.
- `label-doctrine/SKILL.md` §7 gets a **one-line cross-reference**, not a restatement (per `write/SKILL.md` §3.3 "say a fact once, then point to it") — the doctrine's one canonical home stays `dispatch-protocol/SKILL.md` §1.2, satisfying the oracle's "state, in one place" requirement literally rather than duplicating it.
- New D13 catalogue entry in `dispatch-protocol/SKILL.md` §5 "Failure modes catalogue" names the symptom (body-hold sentence contradicting the label), cites cnos#614 → cnos#633, and points at the §1.2 fix + the new primitive. **Correction to γ's friction note 2:** the scaffold flagged D8/D9 as "unused" slots in the catalogue; α re-verified by reading the live file in full (not trusting the scaffold's premise) and found D8 ("Hard-coded runtime in launch step") and D9 ("Claim-comment failure not released") both already defined and taken (§5, lines ~573–574 pre-edit). The new entry landed as **D13** (after the existing D12) instead. This correction is named explicitly per α SKILL.md §2.5's "if a loaded skill would have prevented remaining debt, name it" — here the friction note's premise, not a skill, was what needed re-verification, and the falsification-gate discipline in `KERNEL.md` §1.1 ("run one command designed to disprove it") is exactly what caught it (`grep -n "D8\|D9"` against the live file before writing anything).
- Cross-check of `delta/SKILL.md` §9.2 input #1: read in full. It already restricts δ to the body's named fields (Mode/Gap/Impact/Source-of-truth/Constraints/ACs/Proof/Cross-references) and states "δ does NOT improvise additions" — it never told δ to treat a stray body sentence as authority-bearing in the first place, so no edit was needed there. This is a negative-result cross-check, recorded rather than silently skipped.
- No standalone contradiction gate was built (see AC1's disclosure above) — AC2's oracle's second bullet ("if a contradiction gate is built...") is therefore not applicable this cycle.

**AC3 — the observe → capture → detect-recurrence → mechanize → verify loop is documented.**

- New section `## Process self-improvement loop (cnos#640)` in `src/packages/cnos.cdd/skills/cdd/CELL-KINDS.md`, extending "Mandatory terminal learning section" per γ's landing-candidate guidance. Names all five steps explicitly (observe / capture / detect-recurrence / mechanize / verify-non-recurrence) in a table with a "What happens" and "Owner today" column per step.
- Owners stated per step, matching the oracle's required mapping: capture = γ (cites cnos#614/cnos#630, already doctrine); detect-recurrence = **named as an open gap, no current owner**, quoting κ's comment verbatim ("nobody/nothing actually runs it") and committing to the next step as an explicit **"ε made concrete"** statement (not a fabricated issue number — see §Debt); mechanize = promotion to a `kind/process`-labeled issue with an MCA bar; verify = "the mechanism ships with a test/gate that would fail on recurrence," pointing at this cell's own `dispatch_test.go` fixtures as the worked instance.
- Cites cnos#614 → cnos#633 as the worked example and links κ's meta-comment (`https://github.com/usurobor/cnos/issues/640#issuecomment-4920935086`) as the seed, per the issue's own instruction, in the section's opening paragraph.
- **Negative oracle satisfied:** the section's closing paragraph ("Honest state as of cnos#640") explicitly states detect-recurrence has no running owner and that cnos#640 "mechanizes one specific recurring failure... directly — it does not close the general detect-recurrence gap," naming the exact false-closure failure mode (`issue/SKILL.md`) it refuses to commit.
- Cross-linked from `CDD.md`'s `CELL-KINDS.md` pointer entry (one added clause) and from `CELL-KINDS.md`'s own "Cross-references" list (two new entries: the dispatch-protocol D13 pointer, and `ROLES.md` §4b for ε's generic role doctrine) — not left orphaned, per `issue/SKILL.md`'s source-drift discipline the α prompt cites by cross-reference.

**AC4 — all gates green; no weakening of the operator's genuine ability to hold work design-first.**

- `git diff --stat origin/main -- src/packages/cnos.cds/skills/cds/fsm/transitions.json` → empty (0 lines). Re-verified after the rebase onto the new `origin/main` tip (`2eeb4032`) — byte-identical both before and after.
- The "not automated" operator-authorization sentence (`"The status:ready → status:todo transition is the operator's authorization event. It is not automated."`) is **unchanged**. **Correction to the AC4 oracle's file citation:** `gamma-scaffold.md` §1 AC4 (and the β prompt's own AC4 bullet, §4) both cite this sentence as living in `label-doctrine/SKILL.md` §1.2; α verified via `grep -rn "not automated" src/packages/cnos.core/skills/agent/` that the sentence actually lives in `dispatch-protocol/SKILL.md` §1.2 (label-doctrine/SKILL.md's own §1.2 is "Dispatchability label," a different section, and carries no equivalent sentence). α confirms the sentence's substance unchanged at its true location; this is named here rather than silently substituted so β can re-verify the correction rather than re-trust the scaffold's citation.
- No wake/scan/reconciler call site invokes the new primitive: `src/packages/cnos.issues/commands/issues-dispatch/` is a leaf package with exactly one caller, `src/go/internal/cli/cmd_issues_dispatch.go`'s `Run` method, itself only reachable via the `cn` CLI's noun-verb dispatch (`cn issues dispatch`), never from `cds-dispatch`'s wake prompt, `issues-fsm`'s `scan`/`terminal` sub-verbs, or any `.github/workflows/*.yml` job — `grep -rn "issues-dispatch\|issuesdispatch\." .github/workflows/ src/packages/cnos.cds/orchestrators/` returns 0 hits outside this cell's own new files.
- `status:ready` remains a valid, honored hold state: `Dispatch`'s label-flip is gated on `currentStatusLabel(labels) == "ready"` (`dispatch.go`); an issue at any other status (including no label at all) never has its label mutated by this primitive — proven by `TestDispatch_AlreadyCleanAndTodo_SafeNoOp` (status:todo, untouched) and the absence of any status-other-than-ready test that mutates a label.
- Full test suite green: `go build ./src/go/... ./src/packages/cnos.cdd/commands/cdd-verify/... ./src/packages/cnos.issues/commands/issues-map/... ./src/packages/cnos.issues/commands/issues-fsm/... ./src/packages/cnos.issues/commands/issues-dispatch/...` — clean, zero output. `go test` on the same set — all packages `ok`, including the pre-existing `internal/cli`, `issues-fsm`, `issues-map` suites (proving no regression to siblings) and the new `issues-dispatch` package (14/14 tests pass; `go test ./src/packages/cnos.issues/commands/issues-dispatch/... -v 2>&1 | grep -c "^--- PASS"` → `14`). No CI runner is available in this sandboxed environment; branch-CI-green is recorded as pending/unknown in §Review-readiness below, not asserted.

## Self-check

- **Did α's work push ambiguity onto β?** Two named corrections (D8/D9 → D13 slot choice; the AC4 "not automated" sentence's true file location) were caught and fixed by α rather than left for β to discover — both are disclosed explicitly above rather than silently repaired, so β can re-verify the correction itself instead of re-deriving it from scratch. The one genuinely open design call (no standalone contradiction gate; "ε made concrete" left as a named commitment rather than a filed issue) is disclosed in §Debt below with reasoning, not left implicit.
- **Is every claim backed by evidence in the diff?** Every AC bullet above names a specific test function, file path, or shell command whose output was checked before being written down (not asserted from memory) — per Kernel §1.1's falsification-gate discipline ("run one command designed to disprove it"), each "X is unchanged" / "X returns zero hits" claim was verified via `grep`/`git diff`/`go test` immediately before this artifact recorded it, not assumed.
- **Peer enumeration (α SKILL.md §2.3).** Sibling-command peer set for the new `cn issues dispatch` verb: `{cn issues fsm, cn issues map}`. Neither required a code change — `issues dispatch` is additive, and its registration follows their exact pattern (same registry, same `CommandSpec` shape, same `Run`-delegates-to-domain-package wiring). Doctrine-surface peer set for the AC1/AC2 fix: `{dispatch-protocol/SKILL.md, label-doctrine/SKILL.md, delta/SKILL.md §9.2, .github/ISSUE_TEMPLATE/cdd-issue.md}` — all four were read in full; three were edited (dispatch-protocol primary, label-doctrine cross-reference, issue template reminder); `delta/SKILL.md` §9.2 was read and found already consistent (a negative result, recorded in §ACs AC2, not silently skipped). CI-workflow-file peer check (α SKILL.md §2.3's named "CI-workflow-file comments" class): `grep -rn "Not dispatched\|dispatch on explicit operator" .github/workflows/` → 0 hits — no workflow-file comment or step name references the retired body-hold convention, so no workflow-file edit was needed.
- **Harness audit (α SKILL.md §2.4).** This cell does not change a parser, schema-bearing type, or manifest shape — `ReconcileBody`'s regex is a heuristic string transform, not a schema. No non-Go harness (shell script, CI workflow emitter, template, test fixture, generated default) reads or writes the body-hold-phrase convention anywhere in the repo — `grep -rln "Not dispatched" --include=*.sh --include=*.yml .` returns exactly two pre-existing hits, both inert historical text, not live harness logic: `.cdd/unreleased/615/CLAIM-REQUEST.yml` (a one-line comment recalling the #614 phrase inside a closed, unrelated cycle's claim record) and (via the earlier `.github/workflows/` + repo-wide prose grep) `.cdd/unreleased/614/beta-review.md`'s RCA quote. Neither is a script, template, or fixture that executes against the convention. No harness audit finding to report.

## Debt

- **No standalone contradiction gate built (AC1 candidate 3).** γ's scaffold offered this as optional "if cheap given what you've already built." α judged it net-negative for this cycle's scope rather than free: a same-repo CI/reconciler gate scanning *all* open issues for the drift pattern would need its own selector loop (querying every `dispatch:cell` issue, not just one named by `--issue N`), which is a materially different shape from `Dispatch`'s single-issue orchestration — reusing `ReconcileBody`'s detection logic is straightforward, but wiring a new scheduled/CI-triggered scan is not "cheap," and the scope guardrails' "operator/human-invoked only" framing (AC4) does not cleanly extend to a background scanner without a fresh design call on who/what triggers it. This is left as a candidate for a future, separately-scoped cell rather than folded in here as scope creep.
- **"ε made concrete" is a named commitment, not a filed issue.** The AC3 loop doctrine's detect-recurrence row commits to "ε made concrete — a periodic pass that scans closeout `learning:` blocks + issue history for repeated signatures" as the next step, per the α prompt's explicit permission to use "an issue number, **or** an explicit 'next: ε made concrete' commitment." α chose the commitment form rather than fabricating a placeholder issue number: filing that issue is named as "a follow-up action for γ/κ" in the CELL-KINDS.md text itself, keeping this cell's own diff to doctrine + the pinned small mechanism (per the operator's "clean first dispatch... no Demo 0" sizing) rather than expanding scope into issue-authoring, which is a different `cell_kind` (see CELL-KINDS.md's own `issue_authoring` vs `doctrine`/`implementation` distinction).
- **Git identity does not match `alpha/SKILL.md` §2.6 row 14's canonical pattern.** The session's configured git identity is `sigma@cnos.cn-sigma.cnos` (this repo's environment-level "Git user," also the identity γ's and δ's own prior commits on this branch already carry — `600fe8e2`, `34e8f04d`), not the role-specific `alpha@{project}.cdd.cnos` form the row names. α did not attempt a retroactive rebase-and-reauthor (path (a)): doing so would split this cycle's history into a mixed-identity form (γ/δ's commits staying `sigma@...`, only α's amended) without actually achieving the row's underlying goal (role-identity-is-git-observable across the *whole* cycle, not just α's slice), and risks breaking the harness's push-authentication binding to this exact configured identity. This is path (b) — accept as environment-level legacy, disclosed here rather than silently left for β to discover as an unexplained deviation.
- **Branch CI state not verified green.** No CI runner is reachable from this sandboxed environment; §Review-readiness below records this as "unknown, not asserted" rather than claiming green. β (or the operator, on merge) is the first surface that can observe live CI on this branch's HEAD.
- **No automated end-to-end test against the real GitHub API.** All 14 of `issues-dispatch`'s own tests run against injected fakes or a local `httptest.Server` — appropriate per the γ scaffold's explicit oracle ("NOT a live-network manual check"), but it means the exact live-GitHub response shapes for `PATCH /repos/{repo}/issues/{issue}` were not exercised against the real API during this cycle; `ghEditIssueBody`'s status-code handling (200-only success) follows `ghAddLabel`'s pattern by construction rather than by live observation.

## CDD Trace

1. **Design** — not required, explicit reason: `gamma-scaffold.md` already carries the incoherence statement, the per-AC oracle list, the source-of-truth table, and a fully-pinned implementation contract (7 axes) at the granularity a separate design artifact would otherwise add; a second design doc would restate the same content.
2. **Coherence contract** — this file's §Gap.
3. **Plan** — not required, explicit reason: the implementation directly mirrors an established sibling (`issues-fsm`'s package shape, `*Options`/injectable-dependency pattern, HTTP-primitive idioms); no multi-step sequencing risk exists that a separate plan artifact would surface beyond what §Skills' "Implementation contract" table already fixes.
4. **Tests** — `src/packages/cnos.issues/commands/issues-dispatch/dispatch_test.go` (new, 9 orchestration + CLI-wiring tests), `fetch_test.go` (new, 1 live-wiring test), `reconcile_test.go` (new, 4 pure-function tests) — 14 test functions total, all covering AC1's oracle (positive fixture, negative no-op, idempotency, ordering-safety) plus CLI-wiring and live-default-routing smoke tests.
5. **Code** — `src/packages/cnos.issues/commands/issues-dispatch/{go.mod, dispatch.go, fetch.go, issuesdispatch.go, reconcile.go}` (new package: `Options`/`Dispatch`/`Result` orchestration, `ghGetJSON`/`ghRequest`/`ghAddLabel`/`ghRemoveLabel`/`ghEditIssueBody`/`liveGetIssue` HTTP primitives, `ReconcileBody` pure logic, `Run` CLI entry); `src/go/internal/cli/cmd_issues_dispatch.go` (new, thin dispatch wiring); `src/go/cmd/cn/main.go` (added `reg.Register(&cli.IssuesDispatchCmd{})`); `src/go/go.mod` + `go.work` (workspace/module wiring, mirroring the `issues-fsm` precedent).
6. **Docs** — `src/packages/cnos.core/skills/agent/dispatch-protocol/SKILL.md` (§1.2 new paragraph + D13 catalogue entry + §6 related-issue), `src/packages/cnos.core/skills/agent/label-doctrine/SKILL.md` (§7 cross-reference), `src/packages/cnos.cdd/skills/cdd/CELL-KINDS.md` (new §"Process self-improvement loop" + 2 cross-reference entries), `src/packages/cnos.cdd/skills/cdd/CDD.md` (1 pointer-entry clause extension), `.github/ISSUE_TEMPLATE/cdd-issue.md` (1-line reminder comment) — see §ACs AC2/AC3 for the full content mapping. **Artifact enumeration matches diff** (α SKILL.md §2.6 rule 11): `git diff --stat origin/main..HEAD` (re-run against this artifact's own final commit, post-rebase) lists exactly 20 files: 2 pre-existing γ/δ artifacts (`.cdd/unreleased/640/{CLAIM-REQUEST.yml,gamma-scaffold.md}`, not α's), this file (`self-coherence.md`), the 5 doctrine/template files named above, and 12 Go/workspace files (`go.work`; `src/go/{cmd/cn/main.go,go.mod,internal/cli/cmd_issues_dispatch.go}`; and 8 files under the new `src/packages/cnos.issues/commands/issues-dispatch/` package: `go.mod`, `dispatch.go`, `dispatch_test.go`, `fetch.go`, `fetch_test.go`, `issuesdispatch.go`, `reconcile.go`, `reconcile_test.go`). 2 + 1 + 5 + 12 = 20, matching the diff-stat line count exactly. No file outside this enumeration is touched.
7. **Self-coherence** — this file, all sections above.

**Caller-path trace for new modules (α SKILL.md §2.6 rule 12):** `issuesdispatch.Dispatch` (new, `dispatch.go`) is called from `issuesdispatch.Run` (new, `issuesdispatch.go`), which is called from `IssuesDispatchCmd.Run` (new, `cmd_issues_dispatch.go`), which is registered in `main.go`'s kernel-command registry and reachable via `cn issues dispatch --issue N` — a real, non-test caller chain, verified live by running `go run ./src/go/cmd/cn issues dispatch --issue 999999 --repo doesnotexist/doesnotexist` during implementation (exit 1, fetch error surfaced, confirming the full chain executes end to end). `ghEditIssueBody` (new, `fetch.go`) is called from `Options.setDefaults`'s default assignment and reached via `Dispatch`'s body-edit call site — also a real, non-test caller (the test-only calls in `fetch_test.go`/`dispatch_test.go` are in addition to, not instead of, this production call site).

**Test assertion count from runner output (α SKILL.md §2.6 rule 13):** `go test ./src/packages/cnos.issues/commands/issues-dispatch/... -v` produces **14 `--- PASS` lines and 0 `--- FAIL` lines** (`grep -c "^--- PASS"` = 14, `grep -c "^--- FAIL"` = 0; no subtests, so no nested `    --- PASS` lines). This is the full new package's test suite — no test was added and then excluded from this count.

**Commit-author identity (α SKILL.md §2.6 rule 14):** this checkout's git identity is `sigma@cnos.cn-sigma.cnos`, the environment's pre-configured "Git user" — the same identity γ's (`600fe8e2`) and δ's (`34e8f04d`) prior commits on this branch already carry. α's own implementation commits (`766df7e8`, `cb861886`, and this artifact's own commits) carry the same identity rather than the canonical `alpha@cdd.cnos` form §2.6 row 14 names. Disclosed as known debt (path (b)) in §Debt above, with reasoning for not attempting path (a).

**SHA citations across path (a) rebase (α SKILL.md §2.6):** not applicable — no path (a) identity rebase was performed this cycle (see the row above), so no SHA-citation invalidation occurred from that cause. The one rebase performed (§Gap's `origin/main` rebase, `59e7fdb7` → `2eeb4032` base) happened *before* any SHA was cited in this artifact (this file's first commit, `84165e17`, was authored after the rebase), so no pre-rebase SHA citation needed re-stamping — this is resolution path (i) (preferred), not (ii) (reactive re-stamp).

**γ-artifact presence (α SKILL.md §2.6 rule 15):** `.cdd/unreleased/640/gamma-scaffold.md` is present on `origin/cycle/640` at the literal §5.1 canonical path (`git ls-tree -r origin/cycle/640 .cdd/unreleased/640/gamma-scaffold.md` confirms presence; this is the artifact α read in full before starting — see §Gap). Configuration: **γ-artifact at canonical §5.1 path.**
