# β review — cnos#570

## §R0 (round 0)

**Reviewed:** `origin/main` (base `3a5112...4dd` per γ scaffold; re-verified current at review time via `git rev-parse origin/main`) → `origin/cycle/570` head `0bacd989` (commit "cdd: self-coherence.md for #570 (cell-kind taxonomy cell)").
**Scope of review:** full diff (`git diff origin/main..origin/cycle/570`), γ's scaffold AC-oracle table, issue #570's authoritative AC1–AC11 text, and both issue comments (design-note + wave-note). Independently re-ran build/vet/test rather than trusting `self-coherence.md`'s claims (per `beta/SKILL.md` Role Rule 6: code first, doc second).

### Per-AC verdict table

| AC | Verdict | Evidence checked |
|---|---|---|
| AC1 | **met** | `src/packages/cnos.cdd/skills/cdd/CELL-KINDS.md` (new, 166 lines) exists on the branch. `### 1.`–`### 10.` headers name exactly: `issue_authoring`, `implementation`, `repair`, `recovery`, `wave`, `cleanup`, `audit`, `release`, `doctrine`, `experiment` — the full 10-kind set the issue body requires, verbatim naming. |
| AC2 | **met** | Read all 10 subsections directly (not just grepped labels). Every one carries all six required fields: **Purpose**, **Required input**, **Matter produced**, **Review surface**, **Closeout / projection**, **FSM implication** — labels present verbatim in each of the 10 blocks (CELL-KINDS.md lines 32–119). No section is missing a field. |
| AC3 | **met** | CELL-KINDS.md §1 `issue_authoring`: "**The issue itself is the matter**" (line 36), cross-referencing `issue/SKILL.md`. `issue/SKILL.md` gained (diff, +2 lines after "A coherent issue lets an engineer..."): "**An issue can itself be a cell's matter.**" naming the α-drafts/β-reviews-for-executability/γ-closes-at-`status:ready` lifecycle explicitly, and distinguishing the authoring cell from the later `implementation` cell. Matches AC3's Positive case exactly. |
| AC4 | **met** | CELL-KINDS.md §2 `implementation`: matter = "branch, commits, PR, changed files, validation evidence, receipts"; explicit line "Maps directly to existing CDS dispatch (`cnos.cds/skills/cds/CDS.md` §"Development lifecycle")" (line 47). Distinguished from `issue_authoring` by matter shape, not by prose assertion alone. |
| AC5 | **met** | CELL-KINDS.md §5 `wave` (line 73): "child receipts *project* upward into the wave receipt — **projection, not role-renaming** (`CDD.md` §"Scope-lift"...)" — the exact framing the issue's own AC5 oracle demands (Positive: "cites projection, not role-renaming"; Negative avoided: not described as "merely a long issue"). |
| AC6 | **met** | CELL-KINDS.md §6 `cleanup` (lines 76–83): matter = "a TSC report, a drift inventory, a cleanup PR, or cleanup issues"; FSM implication states it "may create `issue_authoring` or `implementation` cells as children; **must not bypass** issue/review/receipt for the cleanup work itself" — satisfies both the Positive (can spawn issue-authoring children) and Negative (cannot bypass review) cases verbatim. |
| AC7 | **met** | CELL-KINDS.md §"FSM awareness: `cell kind -> valid matter -> allowed transition request`" (lines 121–136) — a 10-row table, one row per cell kind, each naming valid matter and an allowed-transition-request shape distinct per kind (e.g. `issue_authoring` → `selected gap -> issue draft -> issue reviewed -> status:ready`, not the implementation-shaped `todo->in-progress->review`). |
| AC8 | **met** | Doctrine (CELL-KINDS.md §"Recording point (AC8)", lines 138–154) names `.cdd/unreleased/{N}/gamma-scaffold.md`'s `cell_kind:` line as canonical. **Verified the doctrine text matches the actual code mechanism**, not just asserted: `fetch.go`'s `assembleLive` (diff lines 90–101) reads `gamma-scaffold.md` from the same relative path (`.cdd/unreleased/{issue}/gamma-scaffold.md`) the doctrine names, via the new `parseCellKind` regex (`cellKindLinePattern`, lines 149–166), populating `snap.CellKind.Observed` / `.Source = "cdd_artifact"` before `normalizeCellKind()` runs. I independently ran `parseCellKind` against the cycle's own real `gamma-scaffold.md` file (not just the unit-test fixtures) and confirmed it correctly extracts `"doctrine"` with no false-positive match against the prose elsewhere in that same file that also mentions `cell_kind:` in running text (the `^` multiline anchor requires the token at line-start, which the prose sentences don't satisfy). `TestParseCellKind` (4 subtests) and `TestAssembleLive_ObservesCellKindFromGammaScaffold` both pass (`go test ./... -v`, independently run). |
| AC9 | **met** | `grep -l 'CELL-KINDS.md' CDD.md issue/SKILL.md GLOSSARY.md` → all three match. Read each diff hunk directly: `CDD.md` +1 line in the "Pointers" list; `issue/SKILL.md` +2 lines (the AC3 paragraph, which also carries the `../CELL-KINDS.md` link); `GLOSSARY.md` +5 lines under §"Cell" naming the typed-kind refinement and linking to `CELL-KINDS.md`. All three are genuine cross-references, not just filename mentions. |
| AC10 | **met — rigorously verified** | `git diff origin/main..origin/cycle/570 -- '**/table.go'` → **empty output**, confirmed directly. `git diff --stat` shows exactly 8 files touched, none of them a dispatch/renderer/wake file (`.cdd/unreleased/570/{gamma-scaffold,self-coherence}.md`, `GLOSSARY.md`, `CDD.md`, `CELL-KINDS.md`, `issue/SKILL.md`, `fetch.go`, `issuesfsm_test.go`). I independently diffed `TestSeam_CellKindNotEnforced`'s function body (`issuesfsm_test.go`) against `origin/main` byte-for-byte using `awk '/^func TestSeam_CellKindNotEnforced/,/^}/'` on both refs piped to `diff` — **output: `IDENTICAL`**, no textual difference at all (not just "passes," the literal bytes are unchanged). I then ran the full test suite myself in a fresh worktree: `go test ./... -v` in `src/packages/cnos.issues/commands/issues-fsm` → `TestSeam_CellKindNotEnforced` PASS, 20/20 top-level tests PASS, 0 failures. |
| AC11 | **met (locally observable subset)** | Independently ran in a fresh `git worktree` off `origin/cycle/570` (not trusting the cycle's own sandbox claims): `go build ./...` → clean, `go vet ./...` → clean, `gofmt -l .` → no output (clean), `go test ./... -v` → 20/20 PASS, in `src/packages/cnos.issues/commands/issues-fsm`. Also independently built/tested the sibling `issues-map` module (clean) and built the root `src/go` module (clean). `./scripts/ci/validate-skill-frontmatter.sh` could not run in this sandbox (missing `cue` binary) but is moot here: the diff modifies `issue/SKILL.md`'s body only, not its YAML frontmatter (confirmed by reading the diff — the added paragraph is well after the frontmatter block), and `CELL-KINDS.md` is a plain doctrine doc with no SKILL.md frontmatter at all. GitHub Actions' full named gate list (I1, I2, I4, I5, I6, install-wake-golden, dispatch-repair-preflight, dispatch-closeout-integrity, Go, Package, Binary) is not directly observable from this sandbox — same disclosed limitation `self-coherence.md` §Debt already names. This is a residual pre-merge action for whoever runs the actual merge-tree gate (`beta/SKILL.md` pre-merge-gate row 3), not a defect in the diff; nothing in the local subset suggests it would fail. |

### Findings

**Non-blocking (nits, no action required before merge):**

1. **Cosmetic markdown inconsistency in CELL-KINDS.md §4.** `### 4. `recovery`` is immediately followed by the `- **Purpose:**` bullet with no blank line, while every other numbered section (`### 3`, `### 5`, etc.) has a blank line between the header and the first bullet. Purely a rendering nit (extra blank line), zero content or doctrine impact. `file: src/packages/cnos.cdd/skills/cdd/CELL-KINDS.md`, around line 59–60.
2. **`CELL-KINDS.md` (166 lines) carries no large-file section-manifest header**, per `cnos.cds/skills/cds/CDS.md`'s "Large-file authoring rule" (nominally for files >50 lines authored incrementally). `self-coherence.md` §Debt already discloses this and notes the file was authored in a single pass (not resumed across a session boundary) and that its doctrine sibling `CDD.md` (161 lines) also lacks one — consistent existing precedent, not a new gap this cycle introduces. No action needed; naming it here only so it's visible to γ/δ if a future retrofit sweep targets doctrine files.
3. **Full CI gate list (I1/I2/I4/I5/I6/install-wake-golden/dispatch-repair-preflight/dispatch-closeout-integrity/Go/Package/Binary) not directly observed green from this sandbox** — same limitation self-coherence.md discloses. Locally reproducible subset (build/vet/gofmt/test across all Go modules the diff touches) is clean. Recommend confirming actual CI status on the branch head commit as part of the merge action, per `beta/SKILL.md` pre-merge-gate row 3, but nothing in the diff gives reason to expect a regression (docs + one small, well-tested observation-only Go change).

**No blocking findings.** Scope discipline holds: no CUE file touched, no `table.go`/dispatch/renderer/wake file touched, no backfill requirement introduced, no Demo 0 work started — diff is exactly the 8 files γ's scaffold's "Expected diff scope" predicted. The `fetch.go` regex addition is minimal, follows the file's existing idiom (same `os.ReadFile` + ignore-error-on-absence pattern used two blocks above it for `beta-review.md`'s "iterate" scan), is anchored/bounded (no ReDoS risk), degrades safely to `""` on malformed/absent input (verified via `TestParseCellKind`'s "absent" case and my own real-file check), and does not alter any pre-existing `FactSnapshot` field's behavior — confirmed by the byte-identical `TestSeam_CellKindNotEnforced` diff against `origin/main`.

### Overall verdict

```
verdict: converge
```

All 11 ACs are genuinely met with independently re-verified evidence (not just self-coherence.md's claims). AC10, the highest-stakes guardrail, holds under the strictest test I could apply: `table.go` is byte-absent from the diff, and the seam-lock test's body is byte-identical to `origin/main`. AC11 holds for every locally-runnable gate; the remaining CI surface is a normal pre-merge confirmation step, not a defect. No blocking findings. The two non-blocking nits (§4 formatting, missing large-file manifest header) are cosmetic and already disclosed by α — no repair round is needed before merge.
