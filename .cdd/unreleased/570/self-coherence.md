# self-coherence — cnos#570

## §Gap

**Issue:** [#570](https://github.com/usurobor/cnos/issues/570) — cdd/cds: codify cell kinds and the matter each cell produces.
**Mode:** design-and-build (doctrine-first; issue's own body pins CUE/verifier/FSM-enforcement/UI/wave-autonomy/cleanup-implementation/Demo-0 as **Deferred**).
**Branch:** `cycle/570`, based on `origin/main` at `3a511121c4cf50fa4024494ae00f7bca6fae47dd` (γ scaffold's recorded base SHA).
**cell_kind:** `doctrine` (per `.cdd/unreleased/570/gamma-scaffold.md`) — this cell itself is the worked example `CELL-KINDS.md` cites for the `doctrine` kind.

The gap: CDD names one generic recursive coherence-cell kernel, but the operational vocabulary in use across issues, waves, and the in-flight issue-FSM work (#567/#568/#569) already implies several distinct cell kinds (worker/implementation, issue-authoring, repair, recovery, wave, cleanup) without a canonical taxonomy naming their matter, review surface, closeout/projection, and FSM implication. This cell closes that doctrine gap and wires an **observation-only** recording point into the existing FSM fact model, per the operator's wave-note comment pinning AC8 to the `CellKind` seam already shipped in #568/#571.

## §Skills

- **Tier 1:** `CDD.md` (canonical kernel, loaded per alpha load order), `alpha/SKILL.md` (this role's contract).
- **Tier 2:** none beyond the always-applicable doc-authoring discipline; no `eng/{language}` Tier 2 bundle applies to the doctrine surfaces. `eng/go` conventions (gofmt/go vet/go test) applied to the `fetch.go` + test changes even though the issue's own "Skills to load" section did not name a language-specific Tier 2 explicitly — the fetch.go touch is small enough not to require a separate bundle beyond standard toolchain discipline.
- **Tier 3 (per issue body §"Skills to load"):** `cdd`, `cdd/issue`, `cdd/issue/contract`, `cdd/issue/proof`, `cdd/issue/constraints`, `eng`, `eng/evolve`, `eng/process-economics`, `write`. Loaded conceptually: `cdd/issue/SKILL.md` was read and edited directly (AC9); the contract/proof/constraints sub-skills informed how `CELL-KINDS.md`'s per-kind fields (purpose/input/matter/review-surface/closeout/FSM-implication) were structured as a proof-bearing table rather than free prose.
- γ scaffold (`.cdd/unreleased/570/gamma-scaffold.md`) — read first; its "Surfaces α is expected to touch," "AC oracle approach," "Scope guardrails," and "Empirical anchor" sections directly shaped the diff boundary.
- Two most recent issue comments on #570 (design-note on the future typed `cell_kind` CUE substrate; wave-note pinning AC8 to the existing `FactSnapshot.CellKind` seam) — both read in full; the design-note is cited as informational/deferred inside `CELL-KINDS.md`'s "Future typed field (deferred, Phase B)" subsection, not implemented.

## §ACs — Round 0 (R0)

| AC | Requirement | Evidence |
|---|---|---|
| AC1 | `CELL-KINDS.md` exists; defines ≥10 named kinds | `src/packages/cnos.cdd/skills/cdd/CELL-KINDS.md` created. Contains all 10 kinds named in the issue body: `issue_authoring`, `implementation`, `repair`, `recovery`, `wave`, `cleanup`, `audit`, `release`, `doctrine`, `experiment` (§"The 10 cell kinds", numbered 1–10). `grep -c '^### [0-9]\+\.' CELL-KINDS.md` → 10. |
| AC2 | each kind entry has purpose/input/matter/review-surface/closeout/FSM-implication | Every one of the 10 subsections in `CELL-KINDS.md` carries all six fields as bullet items: **Purpose**, **Required input**, **Matter produced**, **Review surface**, **Closeout / projection**, **FSM implication** (field labels verbatim per subsection; verified by reading each of the 10 blocks). |
| AC3 | issue-authoring cell defined, issue is matter | `CELL-KINDS.md` §1 `issue_authoring`: "The issue itself is the matter" with a cross-ref to `issue/SKILL.md` §"An issue can be a cell's matter"; that section states the α/β/γ lifecycle explicitly (α drafts, β reviews for executability not a diff, γ closes at `status:ready`). Also satisfies AC9's "issue can be matter" requirement in `issue/SKILL.md` directly. |
| AC4 | implementation cell distinguished, maps to CDS dispatch | `CELL-KINDS.md` §2 `implementation`: matter = "branch, commits, PR, changed files, validation evidence, receipts"; explicit line "Maps directly to existing CDS dispatch (`cnos.cds/skills/cds/CDS.md` §"Development lifecycle")." |
| AC5 | wave cell defined as parent cell | `CELL-KINDS.md` §5 `wave`: "parent β reviews wave coherence across children... **projection, not role-renaming** (`CDD.md` §"Scope-lift"...)" — explicit projection-not-role-renaming statement with a citation into `CDD.md`'s existing scope-lift doctrine. |
| AC6 | cleanup cell defined | `CELL-KINDS.md` §6 `cleanup`: matter = "a TSC report, a drift inventory, a cleanup PR, or cleanup issues"; FSM implication states it "may create `issue_authoring` or `implementation` cells as children; must not bypass issue/review/receipt for the cleanup work itself." |
| AC7 | FSM-awareness section: `cell kind -> valid matter -> allowed transition request` table | `CELL-KINDS.md` §"FSM awareness: `cell kind -> valid matter -> allowed transition request`" — a 10-row Markdown table, one row per cell kind, naming valid matter and allowed transition request per kind (e.g. `issue_authoring` → `selected gap -> issue draft -> issue reviewed -> status:ready`; `recovery` → the cnos#368-safe split between no-matter requeue and matter-present delta-recovery). |
| AC8 | recording point chosen | `CELL-KINDS.md` §"Recording point (AC8)" names `.cdd/unreleased/{N}/gamma-scaffold.md`'s `cell_kind:` line as canonical, citing this very cell's own scaffold as the worked example. Machine-readability is demonstrated, not just asserted: `fetch.go`'s `assembleLive` now parses that exact line via the new `parseCellKind` helper and populates `FactSnapshot.CellKind.Observed` / `.Source = "cdd_artifact"`. New tests `TestParseCellKind` (4 subtests: bold-markdown form with trailing prose, plain form, embedded-in-larger-body, absent) and `TestAssembleLive_ObservesCellKindFromGammaScaffold` (temp-dir + `os.Chdir`, no network) prove the parse path is exercised end-to-end, not just documented. |
| AC9 | links from CDD.md, issue/SKILL.md, GLOSSARY.md | `grep -l 'CELL-KINDS.md' src/packages/cnos.cdd/skills/cdd/CDD.md src/packages/cnos.cdd/skills/cdd/issue/SKILL.md docs/reference/governance/GLOSSARY.md` → all three files match. `CDD.md` §"Pointers" → "Generic doctrine" gained one list item citing `CELL-KINDS.md`. `issue/SKILL.md` §"Core Principle" gained a paragraph "An issue can itself be a cell's matter" citing `../CELL-KINDS.md` and the `issue_authoring` kind. `GLOSSARY.md` §"Cell" gained two sentences pointing to `CELL-KINDS.md` for the typed-kind refinement. No file was restructured — each addition is a small paragraph/list-item in an existing section. |
| AC10 | no behavior change to dispatch/renderer/FSM-enforcement | Diff review: `git diff --stat origin/main..HEAD` (see §CDD Trace step 6) shows exactly 5 files touched: 1 new doctrine file, 3 doc files gaining a cross-reference each, `fetch.go` gaining an observation-only parse block, and `issuesfsm_test.go` gaining tests. `table.go` (transition logic) is untouched — confirmed via `git diff origin/main..HEAD -- src/packages/cnos.issues/commands/issues-fsm/table.go` producing no output. No dispatch label, renderer YAML, or wake-behavior file appears in the diff. `TestSeam_CellKindNotEnforced` (pre-existing, unmodified) passes on HEAD — see §Self-check for the exact run. |
| AC11 | CI gates green | Not directly runnable from this sandbox (no access to the repo's CI runners for I1/I2/I4/I5/I6/install-wake-golden/dispatch-repair-preflight/dispatch-closeout-integrity/Go/Package/Binary workflows). Locally reproducible subset run and green: `gofmt -l .` (no output), `go build ./...`, `go vet ./...`, `go test ./...` — all clean on `src/packages/cnos.issues/commands/issues-fsm` and the sibling `issues-map` module. This is disclosed as a review-readiness gap in §Debt: β/CI must confirm the full named gate list is green on the branch head commit before merge, per pre-review-gate row 10. |

## §Self-check

- **Did α's work push ambiguity onto β?** No new ambiguity was introduced deliberately. One judgment call is flagged for β's attention: the `cellKindLinePattern` regex is intentionally permissive (matches `cell_kind:` with 0–2 leading `*` and an optional backtick, then any word/underscore/hyphen token, ignoring trailing prose on the line) rather than a strict schema-validated parse — this matches the issue body's own framing ("a regex or line-scan is fine," "no mandatory verifier enforcement yet") and the scaffold's explicit deferral of CUE/verifier work, but β should confirm this permissiveness is acceptable for an observation-only, non-enforced seam.
- **Is every claim backed by evidence in the diff?** Yes — every AC row above cites either a file path + grep-verifiable string, a named test function, or a specific diff-scope command (`git diff --stat`, `git diff -- table.go`). No AC row asserts "done" without a concrete pointer.
- **Peer enumeration (alpha/SKILL.md §2.3):** the family here is "doc files that should point at new doctrine." The three named peers (`CDD.md`, `issue/SKILL.md`, `GLOSSARY.md`) are the complete peer set per the issue body's own AC9 and the scaffold's "Doctrine links (AC9)" list — no other file in the repo is named by the issue or scaffold as a required linking site. A `grep -rl 'cell_kind\|cell kind' docs/ src/packages/cnos.cdd/` sweep (see §CDD Trace step 6) surfaced no additional doctrine surface that discusses cell kinds and was missed.
- **Harness audit (alpha/SKILL.md §2.4):** `fetch.go`'s `assembleLive` is the sole live producer/consumer of `.cdd/unreleased/{N}/gamma-scaffold.md` content within this package; `LoadFixture` (the fixture path in `snapshot.go`) intentionally does not read `gamma-scaffold.md` — fixtures are JSON, not markdown, and no fixture in `testdata/` needed updating since none of them exercise the new code path (confirmed: `grep -l gamma-scaffold testdata/*.json` → no matches; the new behavior is additive and covered by the new dedicated test instead). No shell harness, CI workflow emitter, or template in the repo writes or reads a `cell_kind:` line — `grep -rn 'cell_kind:' --include='*.sh' --include='*.yml' --include='*.yaml' .` (excluding this cell's own new files) returned no hits.
- **Commit-message closure claims (alpha/SKILL.md §2.3):** the commit message accompanying this diff names the exact file set changed and does not claim FSM enforcement, verifier, or CUE work — matching the issue's own Deferred list.

## §Debt

- **CI gate list not directly observed green (AC11).** Only the local Go toolchain subset (`gofmt`, `go build`, `go vet`, `go test`) was run in this sandbox; the full named gate list (I1, I2, I4, I5, I6, install-wake-golden, dispatch-repair-preflight, dispatch-closeout-integrity, Go, Package, Binary) requires the branch to run through actual CI. β/δ must confirm green on the head commit before merge (pre-review-gate row 10).
- **`CELL-KINDS.md` carries no large-file section-manifest header.** `cnos.cds/skills/cds/CDS.md`'s "Large-file authoring rule" names a section-manifest HTML-comment convention for files >50 lines authored incrementally across a session. `CELL-KINDS.md` (166 lines) was authored in a single pass in this sandboxed session, not resumed across a session boundary, so no manifest was needed for resumption; its sibling doctrine doc `CDD.md` (161 lines) also carries no such header. If a future β round requires retrofitting one, that is a small mechanical addition, not a content change.
- **`cell_kind -> allowed transition request` table (AC7) is doctrine only.** Per the issue's own scope and this cell's guardrails, `table.go` was deliberately left unconsuming; a future, separately-scoped FSM issue (#569 or successor) will decide whether/how to turn the table's "allowed transition request" column into an enforced guard. This is named explicitly in `CELL-KINDS.md`'s own "Observation, not enforcement" section, not hidden.
- **No CUE schema, no verifier enforcement, no UI display, no automatic cell-kind inference, no wave/cleanup execution engine, no backfill of `cell_kind` onto existing cells, no Demo 0.** All match the issue body's own "Deferred" list verbatim; none were attempted.

## §CDD Trace

1. **Gap** — see §Gap above; sourced from issue #570 body + γ scaffold.
2. **Mode** — design-and-build, doctrine-first, per issue body header and scaffold line 4.
3. **Design** — not a separate artifact; the issue body itself carries the converged design (taxonomy, design rule, FSM-awareness table shape) and the γ scaffold names the exact surfaces to touch. No additional design doc was warranted (single doctrine file + 3 link additions + one observation-only Go change — no impact graph beyond what the scaffold already named).
4. **Plan** — not a separate artifact; scaffold's "Expected diff scope" list functioned as the plan (1 new doctrine file, 3 doc cross-refs, 1 Go observation change, optional Go test, this self-coherence.md). Followed in that order.
5. **Tests** — `src/packages/cnos.issues/commands/issues-fsm/issuesfsm_test.go` gained `TestParseCellKind` (4 subtests) and `TestAssembleLive_ObservesCellKindFromGammaScaffold`. Full package test run: `go test ./... -v` → 20 top-level test functions pass (18 pre-existing + the 2 new: `TestParseCellKind`, `TestAssembleLive_ObservesCellKindFromGammaScaffold`; verified via `go test ./... -v 2>&1 | grep -cE '^--- PASS: Test[A-Za-z0-9_]+ '` → 20), 0 failures. `TestSeam_CellKindNotEnforced` passes unmodified — confirmed byte-for-byte: file diff of that test function against `origin/main` is empty (`git diff origin/main..HEAD -- src/packages/cnos.issues/commands/issues-fsm/issuesfsm_test.go` shows only additions after the existing test, no changes inside `TestSeam_CellKindNotEnforced`'s body).
6. **Code / docs** — full file list from `git diff --stat origin/main..HEAD`:
   - `docs/reference/governance/GLOSSARY.md` (+5) — AC9 link.
   - `src/packages/cnos.cdd/skills/cdd/CDD.md` (+1) — AC9 link.
   - `src/packages/cnos.cdd/skills/cdd/issue/SKILL.md` (+2) — AC9 link + AC3 "issue can be matter" statement.
   - `src/packages/cnos.cdd/skills/cdd/CELL-KINDS.md` (new, 166 lines) — AC1/AC2/AC3/AC4/AC5/AC6/AC7/AC8.
   - `src/packages/cnos.issues/commands/issues-fsm/fetch.go` (+33) — AC8 machine-readability, `parseCellKind` + call site in `assembleLive`.
   - `src/packages/cnos.issues/commands/issues-fsm/issuesfsm_test.go` (+93) — AC8 test coverage, AC10 non-enforcement re-confirmation.
   - **Caller-path trace for new code (alpha/SKILL.md §2.6 row 12):** `parseCellKind` (new function, `fetch.go`) is called from `assembleLive` at the point immediately after the existing `.cdd/unreleased/{N}/` artifact scan — a non-test caller exists in the same file. `assembleLive` itself is the pre-existing live-fact-assembly entrypoint invoked by `Run` in `issuesfsm.go` (unchanged call site).
7. **Self-coherence** — this file.

## Review-readiness

round 0 (initial submission) | base SHA: `3a511121c4cf50fa4024494ae00f7bca6fae47dd` | implementation SHA: see the commit immediately preceding this file's own commit on `cycle/570` | branch CI: not observed from this sandbox (see §Debt) | γ-artifact: present at canonical §5.1 path — `.cdd/unreleased/570/gamma-scaffold.md` exists on `origin/cycle/570` (committed prior to this cell's dispatch) | ready for β, pending β's own CI-green confirmation on the head commit before merge.
