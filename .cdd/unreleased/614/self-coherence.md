# self-coherence — cnos#614

## §Gap

**Issue:** [#614](https://github.com/usurobor/cnos/issues/614) — cdd: amend CELL-KINDS (#570) — add `planning` cell kind + mandatory terminal learning / ε-observations section.
**Mode:** design-and-build (doctrine). **cell_kind:** `doctrine` (per `.cdd/unreleased/614/gamma-scaffold.md`) — this cycle is itself a `doctrine` cell per the pre-amendment taxonomy, not a `planning` cell, since it produces doctrine text directly rather than a plan/issue/wave contract (see gamma-scaffold.md's "Cell-kind self-reference note").
**Branch:** `cycle/614`, rebased onto `origin/main` at `3d3b929f281a0ac72837288ff124228b3a08085d`.
**Amends:** #570 (closed/shipped `CELL-KINDS.md`). Filed as a new doctrine cell because #570's own receipt is honestly "done"; this cell carries the delta #570 did not cover.

The gap: #570 shipped a 10-kind cell taxonomy. Operator review (2026-07-06) surfaced two gaps: (1) planning — filing issue/wave contracts — is itself work and deserves its own cell kind, not an ad-hoc fit into `issue_authoring` or `wave`; (2) terminal cell closeouts do not systematically capture learning, so execution does not convert into process improvement.

**Resume history:** a prior firing (2026-07-07T05:03) claimed this cell, produced `cycle/614` + draft PR #623 (the `gamma-scaffold.md` commit), then hit a write-fence false-positive (root cause: a concurrent `agent-admin` heartbeat commit misattributed to the worker; fixed in #625). Operator flipped the issue back to `status:todo` and posted an explicit "Dispatch authorized" comment (2026-07-08) directing resumption from the existing branch/scaffold as valid prior matter, not a conflict. This firing resumed from that scaffold: refreshed `CLAIM-REQUEST.yml`, rebased `cycle/614` onto current `main` (73 commits behind at time of resume — rebase was clean, no conflicts), then implemented Amendments 1–3.

## §Skills

- **Tier 1:** `CDD.md` (canonical kernel), `alpha/SKILL.md` (this role's contract).
- **Tier 2:** none — doctrine-only surfaces; no `eng/{language}` bundle applies (the one `.cue` touch is a comment-only addition, verified with `cue vet`, not new CUE logic).
- γ scaffold (`.cdd/unreleased/614/gamma-scaffold.md`) — read first; its source-of-truth table, per-AC oracle, α prompt, and β prompt directly shaped the diff boundary. Followed verbatim.
- `.cdd/unreleased/570/self-coherence.md` — read as the sibling `doctrine`-kind cell's worked example for this file's own shape (this is a peer amendment cell to #570).

## §ACs — Round 0 (R0)

| AC | Requirement | Evidence |
|---|---|---|
| AC1 | `planning` cell kind defined in `CELL-KINDS.md` with purpose / matter / lifecycle(α/β/γ) / role-vs-model note / FSM implication | `grep -n "^### 11\. \`planning\`" CELL-KINDS.md` → line 121. The new `### 11. \`planning\`` section carries **Purpose**, **Required input**, **Matter produced**, **Lifecycle (α/β/γ)**, **Role-vs-model note**, **Relationship to neighbors**, **Worked example**, **FSM implication** — mirrors the field shape of the existing 10 entries (verified by side-by-side read of §10 `experiment` immediately above it). The "10 cell kinds" heading was bumped to "The 11 cell kinds" (line 30). The FSM-awareness table (§"FSM awareness") gained a `planning` row naming valid matter and allowed transition request, mirroring the other 10 rows' shape. |
| AC2 | the mandatory terminal `learning` / `epsilon_observations` section is stated as doctrine with the field schema, framed as ε-captured-by-γ, for **all** cell kinds | `grep -n "learning:" CELL-KINDS.md` → line 173 (new `## Mandatory terminal learning section`, placed between "Recording point (AC8)" and "Observation, not enforcement"). Contains the exact YAML field schema from the issue body verbatim: `observations`, `process_deltas`, `reusable_patterns`, `followups`, `operator_burden`. States explicitly "a general rule across **all** cell kinds, not just `planning`" and frames it as "ε captured by γ: ε observes patterns across runs; γ binds the observation into the receipt at closeout." Carries the required "required by doctrine; mechanical verifier enforcement deferred to a follow-up" caveat, citing this document's own "document first, enforce later" stance and #570's original framing. |
| AC3 | γ's role text updated: γ binds learning into the receipt | `grep -n "binds" gamma/SKILL.md` → three hits: frontmatter `description:` (appended clause, existing content preserved, not replaced), `## Core Principle` (new sentence: "γ's closeout responsibility explicitly includes binding the cell's learning/ε-observations into the receipt... this is a clarification of the existing closeout obligation, not new scope"), and §2.7's `gamma-closeout.md` authoring bullet (new sentence requiring the mandatory `learning`/`epsilon_observations` section with its field list, stating "γ binds this section into the receipt; it is not optional narrative"). All three are unambiguous that γ (not α, not β) performs the binding. |
| AC4 | `CELL-KINDS.md` ↔ `CDD.md` linkage updated; no orphan doctrine | `CDD.md`'s `CELL-KINDS.md` pointer line (line 94) now lists `planning` alongside the other 10 kinds in the enumeration, and adds a clause naming the mandatory learning-section rule with a cross-reference to `CELL-KINDS.md` §"Mandatory terminal learning section" and cnos#614. `receipt.cue` gained a documentation-only comment near `#ProtocolGapRef` cross-referencing the same section (satisfies the gamma-scaffold's "link from the receipt schema surface" instruction without a typed field — see AC5). No new file was created; both new `CELL-KINDS.md` sections are reachable via the existing "Cross-references" section's implicit same-document reachability (no separate file to orphan) and via `CDD.md`'s updated pointer. |
| AC5 | doctrine-only; no behavior change; gates green | `git diff --stat origin/main HEAD` (base `3d3b929f281a0ac72837288ff124228b3a08085d`) touches exactly 6 files: 2 pre-existing cell artifacts from the prior firing (`CLAIM-REQUEST.yml` refresh, `gamma-scaffold.md`), and this cell's own 4 doctrine/schema touches (`receipt.cue`, `CDD.md`, `CELL-KINDS.md`, `gamma/SKILL.md`) — all `.md`/`.cue`/`.yml`; **zero** `.go`, `transitions.json`, CI workflow, or label files (`git diff --name-only origin/main HEAD \| grep -vE '\.(md\|cue\|yml)$'` → empty). The `receipt.cue` change is comment-only: `cue vet -c -d '#Receipt' schemas/cdd/contract.cue schemas/cdd/boundary_decision.cue schemas/cdd/receipt.cue schemas/cdd/fixtures/valid-generic-receipt.yaml` → exit 0; `cue vet -c -d '#CDSReceipt' schemas/cds/receipt.cue schemas/cds/fixtures/valid-receipt.yaml` → exit 0 (both dispatch-table schema/fixture pairs still vet clean post-change, confirming no field/type was added). CI gate list: see §Debt — full named gate list (I1, I2, I4, I5, I6, install-wake-golden, dispatch-repair-preflight, dispatch-closeout-integrity, Go, Package, Binary) requires the branch to run through actual GitHub Actions; the locally-reproducible subset (gofmt, go build, go vet, go test on the touched-adjacent Go packages, plus the cue vet runs above) is green — see §CDD Trace step 5. |

## §Self-check

- **Did α's work push ambiguity onto β?** No deliberate ambiguity. One judgment call for β's attention: the `planning` worked example (in the new §11 entry) cites this very cell's own origin (#570's operator review surfacing two gaps) as the illustrative example, per the γ scaffold's explicit instruction to "cite a real one when available" rather than invent a fictitious example — β should confirm this self-referential example reads clearly rather than as circular.
- **Is every claim backed by evidence in the diff?** Yes — every AC row cites a `grep`/`git diff`/`cue vet` command and its output, or a specific line number.
- **Peer enumeration:** the family here is "doc/schema files that should point at or reflect the new doctrine." The four named surfaces (`CELL-KINDS.md`, `gamma/SKILL.md`, `CDD.md`, `receipt.cue`) are exactly the gamma-scaffold's source-of-truth table — no other file in the repo was named by the issue or scaffold as a required touch site. A supplementary sweep, `grep -rln "10 cell kind\|ten cell kind" --include="*.md" src/ docs/`, found only the one heading in `CELL-KINDS.md` itself (already updated to "11"); no other doc asserts a stale "10 cell kinds" count.
- **Harness audit:** no shell script, CI workflow, or Go source references a `cell_kind` enumeration list that would need updating for the new `planning` value — confirmed via `grep -rn "cell_kind" --include="*.go" src/packages/cnos.issues/` showing only the pre-existing, unmodified observation seam (`fetch.go`'s `assembleLive`/`parseCellKind`, `snapshot.go`'s `CellKind` struct), neither of which enumerates or validates against a closed kind list (per #570's `TestSeam_CellKindNotEnforced` invariant, unchanged and still true here — no transition rule consumes `cell_kind`, so adding an 11th named kind cannot change any FSM decision).
- **Commit-message closure claims:** the implementation commit names the exact four doctrine/schema files touched and does not claim FSM enforcement, verifier, or CUE schema-behavior changes — matching the issue's own Scope/Out-of-scope and STOP conditions.

## §Debt

- **CI gate list not directly observed green on GitHub Actions (part of AC5).** Only the locally-reproducible subset (gofmt/go build/go vet/go test on Go packages near the touched surfaces, plus `cue vet` against both dispatch-table schema/fixture pairs) was run in this sandbox. The full named gate list (I1, I2, I4, I5, I6, install-wake-golden, dispatch-repair-preflight, dispatch-closeout-integrity, Go, Package, Binary) requires the branch to run through actual CI once pushed. β/δ must confirm green on the head commit before merge.
- **No CUE schema, no verifier enforcement, no `cell_kind` FSM guard.** Matches the issue body's own Out-of-scope / STOP conditions verbatim; none were attempted. `table.go` (`cnos.issues/commands/issues-fsm`) is untouched — confirmed via `git diff origin/main HEAD -- src/packages/cnos.issues/commands/issues-fsm/table.go` producing no output.
- **This cell's own `gamma-closeout.md` will be authored under the doctrine it is landing** (a `learning` section as a dogfooding demonstration, per the gamma-scaffold's friction note) — not mechanically required by any gate yet, but consistent with the spirit of the change. See γ closeout.

## §CDD Trace

1. **Gap** — see §Gap above; sourced from issue #614 body + γ scaffold + the operator's 2026-07-08 "Dispatch authorized" resume comment.
2. **Mode** — design-and-build, doctrine, per issue body header and gamma-scaffold line 3.
3. **Design** — not a separate artifact; the issue body itself carries the converged design (planning-kind field shape, learning-section YAML schema, γ role-text clarification) and the γ scaffold names the exact four surfaces to touch. No additional design doc warranted.
4. **Plan** — γ scaffold's α prompt (four numbered steps) functioned as the plan; followed in that order: (1) `CELL-KINDS.md`, (2) `gamma/SKILL.md`, (3) `CDD.md`, (4) `schemas/cdd/receipt.cue`.
5. **Tests** — no new Go test required (doctrine/comment-only diff; `table.go` and the `cell_kind` observation seam are untouched, so `TestSeam_CellKindNotEnforced` and `TestParseCellKind`/`TestAssembleLive_ObservesCellKindFromGammaScaffold` from #570 remain valid unmodified). Verification instead ran: `cue vet -c -d '#Receipt' schemas/cdd/{contract,boundary_decision,receipt}.cue schemas/cdd/fixtures/valid-generic-receipt.yaml` → exit 0; `cue vet -c -d '#CDSReceipt' schemas/cds/receipt.cue schemas/cds/fixtures/valid-receipt.yaml` → exit 0 (confirms the `receipt.cue` comment addition changed no schema behavior for either the generic or CDS-unified definition).
6. **Code / docs** — full file list from `git diff --stat origin/main HEAD` (this cell's own contribution, excluding the 2 pre-existing artifacts from the prior firing):
   - `src/packages/cnos.cdd/skills/cdd/CELL-KINDS.md` (+36/-0 net, existing file) — AC1, AC2.
   - `src/packages/cnos.cdd/skills/cdd/gamma/SKILL.md` (+6/-3) — AC3.
   - `src/packages/cnos.cdd/skills/cdd/CDD.md` (+1/-1) — AC4.
   - `schemas/cdd/receipt.cue` (+10/-0) — AC4 (receipt-schema-surface link), AC5 (comment-only, verified via `cue vet`).
7. **Self-coherence** — this file.

## Review-readiness

round 0 (initial submission, resumed cell) | base SHA: `3d3b929f281a0ac72837288ff124228b3a08085d` | implementation SHA: see the commit immediately preceding this file's own commit on `cycle/614` | branch CI: pending push (see §Debt) | γ-artifact: present at canonical §5.1 path — `.cdd/unreleased/614/gamma-scaffold.md` exists on `cycle/614` (committed by the prior firing, preserved across resume) | ready for β, pending β's own CI-green confirmation on the head commit before merge.
