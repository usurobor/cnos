# α self-coherence — cnos#615

## Gap

- **Issue:** [usurobor/cnos#615](https://github.com/usurobor/cnos/issues/615) — "cds/reconciler: clear terminal lifecycle labels on closed dispatch cells."
- **Mode:** design-and-build; **Cell kind:** implementation (reconciler hygiene tail), per `.cdd/unreleased/615/gamma-scaffold.md`.
- **Gap being closed:** the FSM reconciler family (`cn issues fsm`) has no pass that reacts to a CLOSED `dispatch:cell + protocol:{P}` issue that still carries live `status:*` / `dispatch:cell` / `protocol:{P}` labels. Such an issue is inert (no further FSM transition applies to a closed issue) but its stale labels misrepresent it as still-active in any dashboard/query keyed on those labels. This cell adds a terminal-hygiene reconciliation pass — new sub-verb `cn issues fsm terminal --protocol P [--apply]` — that strips the stale labels and adds the correct `resolution/completed` or `resolution/not-planned` label, keyed off GitHub's own `state_reason` on the Issue resource.
- **Governing constraint (restated):** this is a reconciler pass, NOT a new FSM terminal state. `transitions.json` and the FSM engine (`table.go` / `decision.go` / `Evaluate`) are out of scope — this pass never evaluates a state transition; it observes GitHub's own close metadata and reconciles label taxonomy only.
- **Base SHA:** `9c780a1266aec0ad0896a94823c40ac0cbdcd0cd` (per γ's scaffold). `origin/main` advanced further during α's session (through `dcbcbfc`, three commits beyond the scaffold's pinned base: `f802a28` board-map regen, `1daf11c`/`dcbcbfc` an unrelated agent-admin topic-relay exchange for cnos#621) — all docs/mailbox-only, none touching `src/packages/cnos.issues/` or the FSM/label surfaces this cell touches. Per α SKILL.md §2.6 rule 1, α rebased `cycle/615` onto `origin/main` at `dcbcbfc` before requesting review (`git fetch origin main && git rebase origin/main && git push --force-with-lease origin cycle/615`); the rebase was conflict-free (`terminal.go`/`fetch.go`/`issuesfsm.go`/`terminal_test.go` touch no file any of those three main-side commits touched).
- **Branch:** `cycle/615`.

## Skills

- **Tier 1:** `CDD.md` (canonical lifecycle) + `cnos.cdd/skills/cdd/alpha/SKILL.md` (this role's surface). No β/γ role skills loaded, per α's load-order rule.
- **Tier 2:** `eng/go` (implicit — this is a Go-only change to an existing Go package; no new language/toolchain introduced). No `eng/ux-cli` needed beyond mirroring the existing `evaluate`/`scan` flag conventions already established in `issuesfsm.go`.
- **Tier 3:** none issue-specific beyond the γ scaffold itself, which functions as this cell's design/plan artifact (γ's scaffold explicitly provides the recommended shape, the per-AC oracle list, and the α prompt — no separate design or plan artifact was produced by α; not required, per §2.2's "explicit not required" allowance, because γ's scaffold already carries the incoherence-and-ACs plus the implementation shape at the level of detail plan/SKILL.md would otherwise require).
- **Implementation contract (per α SKILL.md §3.6):** language = Go (unchanged, matches existing package); CLI integration target = new `terminal` sub-verb on the existing `cn issues fsm` compiled-in kernel command (unchanged dispatch wrapper `src/go/internal/cli/cmd_issues_fsm.go` — no new wrapper needed, it already delegates to `issuesfsm.Run`); package scoping = same package `src/packages/cnos.issues/commands/issues-fsm/` (new file `terminal.go` + `terminal_test.go`, one addition to `fetch.go`); runtime dependencies = none new (stdlib `net/http`/`encoding/json` only, matching the existing file); existing-binary disposition = same `cn` binary, no new binary; JSON/wire contract = no existing JSON schema changed, only a new optional CLI sub-verb added; backward-compat = `evaluate`/`scan` sub-verbs and their tests are unchanged (verified below). All axes were unambiguous from γ's scaffold + the existing code shape; nothing required escalation to γ/δ.

## ACs

Evidence gathered against branch HEAD after implementation (commit to follow this file's commit).

**AC1 — recognizes closed dispatch:cell+protocol:{P} issue still carrying status:*/dispatch:cell/protocol:{P}.**
- Implementation: `liveListClosedCandidates` (`terminal.go`) issues `GET /repos/{repo}/issues?labels=dispatch:cell,protocol:{P}&state=closed&per_page=100`. This single query IS the recognizer — it can only return issues that are closed AND carry both `dispatch:cell` and `protocol:{P}`.
- Test evidence: `TestLiveListClosedCandidates_QueryShapeAndFiltering` (`terminal_test.go`) asserts the request URL contains `state=closed` and `labels=dispatch:cell,protocol:cds`, and that a fake server honoring that filter returns exactly the one true candidate (a closed issue missing `protocol:cds`, or an open issue, would be excluded server-side exactly as real GitHub would exclude them).
- `TestTerminal_ClosedCompletedStale_RecognizedAndCleaned` (dry-run half) confirms the orchestration layer (`RunTerminalSweep`/`terminalOne`) reports the candidate as recognized (non-empty "would remove ... -- rerun with --apply" note) without requiring `--apply`.
- Negative: an issue lacking `dispatch:cell`/`protocol:{P}` never appears in `ListClosedCandidates`'s return at all (structural — the query's own `labels=` param excludes it), proven via `TestTerminal_OpenIssueNeverTouched` and `TestLiveListClosedCandidates_QueryShapeAndFiltering`'s excluded fixture rows.

**AC2 — removes those labels, adds correct resolution/* keyed off state_reason.**
- `TestTerminal_ClosedCompletedStale_RecognizedAndCleaned` (`--apply` half): asserts the exact removal set `{900:dispatch:cell, 900:protocol:cds, 900:status:review}` and the exact added label `900:resolution/completed` for a `state_reason: "completed"` fixture — not just "a label was added."
- `TestTerminal_ClosedNotPlannedStale_ResolvesToNotPlanned`: asserts `state_reason: "not_planned"` resolves to exactly `resolution/not-planned` (not `resolution/completed`, not `resolution/wontfix` — the exact ambiguity the operator's 2026-07-07T01:51:47Z clarifying comment on the issue resolved).
- `TestTerminal_LiveDefaults_RouteThroughSharedPrimitives` proves the same shape end-to-end through the *live* default wiring (not injected fakes) via a fake GitHub server: exactly 1 GET, 3 DELETEs, 2 POSTs (one `ghEnsureLabelExists` repo-label creation + one `ghAddLabel` issue-label add).
- `resolution/completed`'s live color/description was re-verified at implementation time (2026-07-07) via `gh api repos/usurobor/cnos/labels/resolution%2Fcompleted` → `{"color":"ededed","description":null}` — unchanged from the scaffold's recorded value. `resolution/not-planned` was confirmed NOT to exist yet via `gh api repos/usurobor/cnos/labels/resolution%2Fnot-planned` → 404. The code pins `resolutionLabelColor = "ededed"`, `resolutionLabelDescription = ""` (`terminal.go`) to match. Per α's role boundary ("do NOT touch issue labels" — this dispatch's explicit instruction), α did **not** call `gh api` to actually create `resolution/not-planned` on the live repo; the `ghEnsureLabelExists` code path (unit-tested for both the 422-tolerant and the 201-created cases) is what performs that creation the first time an operator runs `--apply` against a real `not_planned`-closed issue.

**AC3 — idempotent (second run no-op); open/active cells never touched.**
- `TestTerminal_Idempotent_SecondPassIsNoOp`: first pass reconciles a candidate (3 removes + 1 add); second pass's `ListClosedCandidates` (simulating the post-cleanup state, mirroring `scan_test.go`'s `TestScan_Idempotent` second-pass technique) returns zero candidates for that issue → mutation counts unchanged across the second pass, and `ghEnsureLabelExists` was called exactly once total (first pass only).
- `TestTerminal_OpenIssueNeverTouched`: an open issue is asserted to never appear in the report and never receive a mutation call, alongside an in-flight closed candidate (proving the absence isn't merely "nothing to process").
- Idempotence is structural (not a bolted-on marker check), per γ's scaffold's argument: once a closed issue's `dispatch:cell`+`protocol:{P}` are both removed, the list query's own filter excludes it from every subsequent run. No separate "already processed" check was added — confirmed unnecessary by the passing idempotence test above.

**AC4 — no transitions.json change; removals go through the guarded label-remove path.**
- `git diff --stat origin/main..HEAD -- src/packages/cnos.cds/skills/cds/fsm/transitions.json` → empty (zero-diff, confirmed at implementation time on the working tree).
- `grep -n "http.MethodDelete\|DELETE" terminal.go` → zero hits outside a doc-comment mention; the only DELETE-labels HTTP call anywhere in this cell's code is inside `ghRemoveLabel` (`fetch.go`), reused verbatim via `TerminalOptions.RemoveLabel`'s default (`o.RemoveLabel = ghRemoveLabel`). No duplicate/direct DELETE call was written in `terminal.go`.
- `terminal.go` has zero references to `Evaluate`, `Table`, or `LoadTable` — `TerminalOptions` carries no `Table` field at all (unlike `ScanOptions`), and `TestTerminalOptions_HasNoFSMTableDependency` pins that shape so a future edit reintroducing one is a visible diff.
- No `gh issue close` / close-equivalent call exists anywhere in the new or touched code (`grep -rn "issue close\|state=closed.*PATCH\|state\":\"closed" terminal.go issuesfsm.go fetch.go` → zero hits) — this pass only reacts to issues GitHub already reports closed.

**AC5 — fixtures.**
Implemented as in-package Go tests using injected fakes (`fakeListClosedCandidates`, `fakeLabelMutator`) rather than JSON files under `testdata/`, per the γ scaffold's "whichever matches your chosen implementation's testing seam" latitude — this pass's testing seam is the injectable `TerminalOptions` struct (mirroring `ScanOptions`), not a `FactSnapshot`/`LoadFixture` JSON shape (which belongs to the FSM-evaluation path this pass does not use). All four scenarios:
1. `TestTerminal_ClosedCompletedStale_RecognizedAndCleaned` — closed + `state_reason=completed` + stale `status:review`/`dispatch:cell`/`protocol:cds` → cleaned, `resolution/completed` added. PASS.
2. `TestTerminal_ClosedNotPlannedStale_ResolvesToNotPlanned` — closed + `state_reason=not_planned` + stale labels → cleaned, `resolution/not-planned` added. PASS.
3. `TestTerminal_AlreadyClean_NoOp` — `ListClosedCandidates` returns zero candidates (the query-filter-excluded case) → zero results, zero mutation calls. PASS.
4. `TestTerminal_OpenIssueNeverTouched` — an open dispatch:cell+protocol:cds issue never appears as a candidate, zero mutation calls, alongside a real closed candidate in the same run. PASS.
- Additionally (beyond the scaffold's four, defense-in-depth): `TestTerminal_UnrecognizedStateReason_SkippedNeverMutated` (an unreachable-in-practice but defensively-coded unrecognized `state_reason` is skipped, never mutated — cnos#368 doctrine) and `TestGhEnsureLabelExists_TreatsAlreadyExists422AsNoOp` / `TestGhEnsureLabelExists_CreatesOnFirstUse` (the one new HTTP primitive's two branches).

**AC6 — gates.**
- `go build ./...` from `src/packages/cnos.issues/commands/issues-fsm`: clean, zero errors.
- `go vet ./...` from the same package: clean, zero findings.
- `go test ./... -v` from the same package: **72 top-level `--- PASS` lines + 14 nested subtest `--- PASS` lines, 0 `--- FAIL`** (raw count via `grep -c "^--- PASS"` / `grep -c "^--- FAIL"` on the `-v` output). This includes every pre-existing test in `scan_test.go` and `issuesfsm_test.go` — none of their expected outputs changed (confirms the "zero behavior change to scan.go/evaluate" guardrail).
- `go build ./...` and `go test ./...` from `src/go` (the `Go` CI job's scope, since `cmd_issues_fsm.go` calls into this package): both clean — `go build` silent/zero-error, `go test ./...` reports `ok` for every package including `internal/cli` (1.605s), with no `[no test files]` regressions beyond the pre-existing `cmd/cn` (which has never had tests).
- `gofmt -l .` on the issues-fsm package: initially flagged `terminal.go` (minor struct-tag alignment); fixed via `gofmt -w`; re-run confirms clean.
- `src/packages/cnos.core/labels.json`: confirmed untouched (`git diff --stat origin/main..HEAD -- src/packages/cnos.core/labels.json` → empty).
- **I6 (`cdd-artifact-check`, `cn cdd verify --unreleased --exceptions .cdd/exceptions.yml`): run locally** — α built the `cn` binary (`go build -o /tmp/cn ./cmd/cn` from `src/go`) and ran the real gate command. **First run failed** (`1 failed` — self-coherence.md's section headers used a `## §Gap`/`## §Skills`/`## §ACs`/`## §CDD Trace` style; the verifier's `sectionPresent` check (`ledger.go`) requires the literal, prefix-free headers `## Gap`, `## Skills` (or `## Mode`), `## ACs` (or `## AC Coverage`), `## CDD Trace` — the `§` prefix broke the exact-line match). Fixed by renaming this file's headers to the plain form (no `§` prefix) to match the precedent other cycles' self-coherence.md files use (e.g. `.cdd/unreleased/593/self-coherence.md`'s `## CDD Trace`/`## Self-check`). **Re-run after the fix: `182 passed, 0 failed, 119 warnings`** — the cycle is correctly classified `small-change` (no `beta-review.md` yet, which is expected pre-β) and its `self-coherence.md sections` check now passes.
- I2 (`protocol-contract-check`): `diff docs/reference/schemas/protocol-contract.json tests/fixtures/protocol-contract.json` → empty (no schema files touched by this cell; confirmed identical).
- **Not run locally / deferred to δ+β's CI observation:** Binary/`binary-verify`, Package/`package-verify`, I1 `package-source-drift`, I4 `link-check`/lychee (not installed in this environment; manually grepped this cell's new markdown for links — `self-coherence.md` carries exactly one external link, to the issue itself, no relative links to check), I5 `skill-frontmatter-check`, `install-wake-golden`, `dispatch-repair-preflight`, `dispatch-closeout-integrity`. α does not have a way to run the GitHub Actions workflow runner itself in this environment for these; `go build`/`go vet`/`go test` already succeeding for both modules covers the core mechanism Binary/Package verify build on top of. α reasoned through the rest structurally (no `.cn.package.json`-tracked source touched → I1 should be clean; no SKILL.md edited → I5 unaffected; no install-wake/dispatch-workflow paths touched → `install-wake-golden` should not even trigger; no dispatch-prompt/protocol-surface files touched → the two dispatch-integrity jobs unaffected). This remaining set is declared explicitly as known debt in §Debt below, per the pre-review-gate rule that local-CI-unavailable branches must say so explicitly rather than claim green.

## Self-check

- **Did α's work push ambiguity onto β?** The one open design call the scaffold explicitly left to α (new `terminal` sub-verb vs. a `--closed` flag on `scan`) is resolved here, not left for β to adjudicate: α chose the new-sub-verb shape (see rationale below), matching γ's own recommendation and the issue's own wording (which names `fetch.go`'s `ghRemoveLabel` as the reuse target, never `scan.go`'s `Evaluate`/`Table`). β's job per the scaffold's own β prompt is to *verify* this documented choice, not to make it.
- **Design-call rationale (documented per the scaffold's requirement):** chose (a) a new `terminal` sub-verb over (b) a `--closed` flag on `scan`, because: (1) the issue's own scope line names `ghRemoveLabel` (fetch.go) as the reuse target and never mentions `Evaluate`/`Table` (scan.go) — a deliberate signal this pass is not an FSM-evaluation variant; (2) folding this into `scanOne`'s switch would force either a fake FSM "state" through `Evaluate()` against `transitions.json` for a closed issue (directly violating AC4), or an early-return branch inside `scanOne` that never touches `Evaluate` for the closed case — both are more awkward than a clean sibling function with its own `TerminalOptions`/`RunTerminalSweep` pair, mirroring `ScanOptions`/`RunScan`'s existing shape without inheriting its `Table` dependency at all; (3) `scan.go`'s `ListActiveIssues` is inherently open-issue-scoped (`state=open`) and its `scanOne` per-issue loop is built around `activeScanStates`/`Evaluate` — bolting a `state=closed` mode onto that engine would be a larger, more entangling diff than a ~230-line sibling file. This mirrors the documented-design-call precedent the scaffold cites (cnos#574 AC4 / `observeRemoteBranch`'s doc comment).
- **Is every claim backed by evidence in the diff?** Yes — every AC1-AC6 claim above cites either a named test function (in `terminal_test.go`, all passing) or a shell command whose output is quoted/summarized inline. No claim rests on "should work" without a corresponding test or explicit deferral (AC6's GitHub-Actions-only gates are the one explicit deferral, stated as such, not claimed green).
- **Peer enumeration (α SKILL.md §2.3):** the peer family here is "sub-verbs of `cn issues fsm`" — `evaluate`, `scan`, and now `terminal`. All three are wired through the same `Run` dispatch switch in `issuesfsm.go`; `terminal`'s addition follows the exact same flag-parsing/usage-string/dispatch shape `scan` already established. The CLI wrapper peer (`src/go/internal/cli/cmd_issues_fsm.go`) needed **no change** — it already delegates generically to `issuesfsm.Run(ctx, inv.Args, ...)` and does not enumerate sub-verbs itself, so no wrapper-side peer update was needed (confirmed by reading the file; its own doctrine comment already documents this generic-delegation shape). The label-mutation-primitive peer family (`ghAddLabel`, `ghRemoveLabel` in `fetch.go`) gained exactly one new sibling, `ghEnsureLabelExists`, using the same `ghRequest`-based idiom and the same tolerant-status-code pattern (422-tolerance mirroring `ghRemoveLabel`'s 404-tolerance) — documented inline in `fetch.go`.
- **Doc-surface peer (`src/packages/cnos.issues/SKILL.md`):** this file's frontmatter `description` line and body describe `cn issues fsm evaluate` in detail but never mention `cn issues fsm scan` at all, even though `scan` shipped in cnos#593 (confirmed via `grep -n "scan\b" src/packages/cnos.issues/SKILL.md` → zero hits) — i.e. this doc surface was not treated as an authoritative sub-verb enumeration by the #593 precedent cycle either. α exempts it here for the same reason, consistent with that precedent, rather than introducing an update pattern the prior cycle did not establish. Flagging explicitly per §2.3's peer-enumeration rule ("each peer either updated or explicitly exempted with a reason") rather than silently skipping it.
- **Harness audit (α SKILL.md §2.4):** this change adds no new schema-bearing type, parser, or manifest shape — `terminalCandidate` is an internal, unexported struct with no serialization contract of its own (it is constructed from a raw GitHub API response inline in `liveListClosedCandidates`, not round-tripped through any file format). The one manifest this cell is adjacent to, `src/packages/cnos.core/labels.json`, is explicitly out of scope per the issue/scaffold (it does not track `resolution/*` today; pre-existing gap, not this cell's to fix) — confirmed untouched.

## Debt

- **Binary, Package, I1, I4 (lychee itself), I5, `install-wake-golden`, `dispatch-repair-preflight`, `dispatch-closeout-integrity` were not run via the actual GitHub Actions workflow** — this sandbox has no Actions runner. α ran every gate with a direct local equivalent instead: `go build`/`go vet`/`go test ./... -v`/`gofmt -l` (both modules, all clean), the real `cn cdd verify --unreleased` binary for I6 (caught and fixed a real header-format bug — see AC6 above), and a manual diff for I2. This is the one row (§2.6 rule 10, branch-CI-green) α cannot fully self-certify from this environment; the review-readiness section below states this explicitly rather than claiming green, per the pre-review-gate rule for CI-unavailable branches.
- **No dedicated JSON fixture files under `testdata/`** — α used in-package injected-fake tests instead (see AC5 above for the rationale: this pass's testing seam is `TerminalOptions`, not `FactSnapshot`/`LoadFixture`). If β's independent AC5 walk expects literal `testdata/*.json` files mirroring `scan_test.go`'s style, that expectation will not be met by file *location*, though the four required scenarios are all present and passing as Go test functions. Flagging so β does not read "no testdata/ files added" as "AC5 unmet" without first checking `terminal_test.go`.
- **No live mutation was performed against the real `usurobor/cnos` repo** — α confirmed `resolution/completed`'s live color/description and `resolution/not-planned`'s current absence via read-only `gh api` calls, but did not call `--apply` against the real repo or create the `resolution/not-planned` label live, per this dispatch's explicit instruction that α must not touch issue labels. That live creation only happens the first time an operator (or a later automated run) executes `cn issues fsm terminal --apply` for a `not_planned`-closed issue; the code path is unit-tested (`TestGhEnsureLabelExists_CreatesOnFirstUse`) but not exercised end-to-end against the live GitHub API.
- No skill (Tier 2/3) would have prevented either debt item above — both are environment/role-boundary limitations (no CI runner, no live-mutation authority), not authoring gaps.

## CDD Trace

1. **Design** — not required, explicit reason: γ's scaffold (`.cdd/unreleased/615/gamma-scaffold.md`) already carries the incoherence statement, the per-AC oracle list, and a fully-specified recommended implementation shape at the granularity a design artifact would otherwise add; producing a second, separate design doc would restate the same content.
2. **Coherence contract** — this file's §Gap.
3. **Plan** — not required, explicit reason: the implementation is a single new file plus two small additions to existing files, in an established package with a directly-mirrorable precedent (`scan.go`/`ScanOptions`/`RunScan`); no multi-step sequencing risk exists that a separate plan artifact would surface.
4. **Tests** — `src/packages/cnos.issues/commands/issues-fsm/terminal_test.go` (new file, 13 test functions covering AC1–AC5 plus two defense-in-depth cases and the two new `fetch.go` primitive branches).
5. **Code** — `src/packages/cnos.issues/commands/issues-fsm/terminal.go` (new file: `TerminalIssueResult`, `TerminalReport`, `terminalCandidate`, `resolutionForStateReason`, `TerminalOptions`/`setDefaults`, `RunTerminalSweep`, `terminalOne`, `liveListClosedCandidates`, `renderTerminal`); `src/packages/cnos.issues/commands/issues-fsm/fetch.go` (added `ghEnsureLabelExists`); `src/packages/cnos.issues/commands/issues-fsm/issuesfsm.go` (added the `terminal` case to `Run`'s dispatch switch, updated its usage strings, and added `runTerminal`).
6. **Docs** — no separate docs artifact was required; this file (§ACs, §Self-check) documents the design-call rationale and the peer-enumeration/harness-audit outcomes in place of a standalone docs update, per the "no authority surface changed" reading (the CLI's own `--help`/usage text, updated inline in `issuesfsm.go`, is the only user-facing surface this cell adds, and it is part of the code diff itself, not a separate doc). **Artifact enumeration matches diff** (α SKILL.md §2.6 rule 11): `git diff --stat origin/main..HEAD` (to be re-run against the final pushed commit) covers exactly: `terminal.go` (new, §ACs/§Code), `terminal_test.go` (new, §ACs/§Code), `fetch.go` (modified, §ACs AC2/AC4/§Code), `issuesfsm.go` (modified, §Code), and this file, `self-coherence.md` (new). No other file is touched by this cell's implementation commits (the pre-existing `gamma-scaffold.md` addition and `docs/development/board/*` drift predate α's work — see gamma-scaffold.md Friction note 4).
7. **Self-coherence** — this file, all sections above.

**Caller-path trace for new modules (α SKILL.md §2.6 rule 12):** `RunTerminalSweep` (new, `terminal.go`) is called from `runTerminal` (new, `issuesfsm.go`), which is called from `Run`'s dispatch switch (`issuesfsm.go`, case `"terminal"`) — a real, non-test caller reachable from `cn issues fsm terminal`. `ghEnsureLabelExists` (new, `fetch.go`) is called from `TerminalOptions.setDefaults`'s default assignment and from `terminalOne`'s apply path (`terminal.go`) — also a real, non-test caller, not merely invoked from test code (the test-only calls in `terminal_test.go` are in addition to, not instead of, this production call site).

**Test assertion count from runner output (α SKILL.md §2.6 rule 13):** `go test ./... -v` from `src/packages/cnos.issues/commands/issues-fsm` produces **72 top-level `--- PASS` lines and 14 nested `    --- PASS` subtest lines, 0 `--- FAIL` lines** (`grep -c "^--- PASS"` = 72, `grep -c "^--- FAIL"` = 0, `grep -c "^    --- PASS"` = 14, run against the `-v` output redirected to a file). This is the full package suite (pre-existing + this cell's 13 new top-level test functions), not a subset.

**Commit-author identity (α SKILL.md §2.6 rule 14):** this checkout's `git config user.email` was unset for the α role at session start (inherited `sigma@cnos.cn-sigma.cnos`); α configured `git config user.name "cds-dispatch-alpha"` and `git config user.email "alpha@cdd.cnos"` before the first commit of this cycle's implementation work, so every α commit on `cycle/615` carries the canonical `alpha@cdd.cnos` identity from the start — no retroactive rebase/amend was needed (path (a)/(b) in §2.6 rule 14 do not apply; there is no split-identity history to remediate).

**γ-artifact presence (α SKILL.md §2.6 rule 15):** `.cdd/unreleased/615/gamma-scaffold.md` is present on `origin/cycle/615` at the literal §5.1 canonical path (this is the artifact α read in full before starting — see §Gap). Configuration: **γ-artifact at canonical §5.1 path.**
