# γ living record — cycle #277

**Issue:** [#277](https://github.com/usurobor/cnos/issues/277) — Rewrite SOUL.md to skill form with UIE-V agent loop
**Branch (γ-side):** `claude/migrate-soul-skill-form-t7sIN` (operator-designated)
**Cycle scope:** sigma's `spec/SOUL.md` migrated to skill form per skill-skill (Define → Unfold → Rules → Verify); generic template at `src/packages/cnos.core/templates/SOUL.md` explicitly out of scope (issue AC12, Non-goal #1).
**Mode:** MCA candidate (skill-form authority + UIE-V Verify primitive both already present in the system; migration applies them to the existing soul artifact).
**Role:** γ
**γ session env:** Claude Code on the web. MCP-only for `gh` operations (scoped to `usurobor/cnos`); local `git` reachable via proxy; `Monitor` available; `Bash` available; cn-sigma repo **not in MCP scope** (see §6).

This file is γ's per-cycle living record per the convention established by #278's γ close-out (`.cdd/unreleased/gamma/278.md`). It accumulates intake observations, dispatch-readiness findings, dispatch artifacts, mid-cycle observations, close-out triage, §9.1 cycle iteration, and PRA pointer. Sections are appended as the cycle progresses.

---

## 1. Tracking reachability preflight (CDD §Tracking)

Probed each query form synchronously before starting the polling loop:

| Surface | Form probed | Result | Use for |
|---|---|---|---|
| Issue body / state | `mcp__github__issue_read get` | ✅ returned full issue #277 | Issue-text-of-record |
| Issue comments | `mcp__github__issue_read get_comments` | ✅ returned `[]` (zero comments at intake) | Mid-cycle issue activity |
| PR list | `mcp__github__list_pull_requests state=open` | ✅ returned `[]` (no PR for #277 at intake) | α PR open detection |
| Branch list | `mcp__github__list_branches` | ✅ returned 3 branches (see §2) | α/β branch detection |
| Git fetch | `git fetch origin` | ✅ silent success; remote tracking refs updated | Branch-head SHA polling |
| `gh` CLI | n/a | ❌ not installed in this environment | unavailable |
| `api.github.com` curl | n/a | not probed; demoted by #278 T2 in MCP-reachable envs | unavailable per #278 cycle property |
| cn-sigma read | `mcp__github__get_file_contents owner=usurobor repo=cn-sigma` | ❌ **out of MCP scope** per session restriction | **gap — see §6 / Q1** |

**Wake-up mechanism:** `Monitor`-wrapped `until` loops are available; the canonical wake-up shape per CDD §Tracking applies. For this cycle, however, MCP `*_read` calls are themselves the in-conversation polling channel and are not run inside `Monitor` (per CDD §Tracking environment constraints: "MCP tools require in-conversation invocation — they cannot run inside `Monitor` or background shell loops"). Branch-SHA polling via `git fetch` + `git rev-parse origin/{branch}` may run under `Monitor` for transition-only stdout; PR/issue activity polling runs synchronously between cycle steps.

**Stdout filter discipline:** any `Monitor` loop emits only on transition (new branch, new SHA). Synchronous-baseline pull (§2) owns the past; the `Monitor` loop owns the future.

## 2. Synchronous baseline pull (CDD §Tracking)

Read at intake so pre-existing state is not absorbed silently by transition-only polling:

- **Issue #277:** open, P1, labels `docs process P1`, created 2026-04-27, no updates, no comments. Body in full at `mcp__github__issue_read get`.
- **PRs open referencing #277:** none.
- **Branches at intake:**
  - `main` @ `9dbb107` (head: "docs: mark WRITER-PACKAGE.md as superseded by post-SOUL.md redo" — note: prior writer-package design lock at `b65329b` was marked superseded in this commit; relevant to §3 below)
  - `claude/migrate-soul-skill-form-t7sIN` @ `9dbb107` (γ branch — operator-designated; identical head to main; no cycle commits yet)
  - `claude/cnos-issue-278-Xh37v` @ `fce672b` (closed cycle #278, retained pending δ disposition)
  - `claude/load-cdd-phase-6-I1I0I` @ `80b684a` (prior cycle remnant)
- **No α or β branch yet** for #277. α has not started; β has not started.
- **Today:** 2026-04-28 (γ intake one day after issue creation 2026-04-27).
- **Most recent main commit:** `9dbb107` marks `docs/alpha/design/WRITER-PACKAGE.md` (cycle #278's design lock) as **superseded by post-SOUL.md redo**. This is δ's encoding that #277's outcome will redo the writer-package design — i.e. #277 is upstream of #278's design lock by current operator commitment, not just dependency-graph adjacency. Cycle inheritance is therefore directional: #277 ships, then writer-package redo follows.

## 3. Cycle inheritance from #278 — MCA verification

The dispatch brief instructs: "Verify cycle inheritance from #278. The writer-package cycle's MCA-1 (alpha skill §2.1 step 4: enumerate inline-prose-named artifacts) and MCA-2 (issue skill §2.5.3: parallel-dependency references must link with paths) are now in force."

Verified by inspection at HEAD `9dbb107`:

- ✅ **MCA-1 in force.** `src/packages/cnos.cdd/skills/cdd/alpha/SKILL.md` §2.1 step 4 reads: "enumerate every artifact the issue names — both `## Related artifacts` linked entries **and** any artifact named in inline prose ... Read each before drafting; if a named artifact has no path, search the repo to resolve it. Surface unavailability to the operator before treating an artifact as skipped." Provenance line `*Derives from: #278 F1 ...*` present at line 117.
- ✅ **MCA-2 in force.** `src/packages/cnos.cdd/skills/cdd/issue/SKILL.md` §2.5.3 reads: "Link parallel-dependency and constraint documents explicitly ... When the issue body names a cross-cutting reference (parallel-dependency document, related-but-not-load-bearing reference, constraint spec drafted alongside this work), link it with a path, not just a description." Provenance line `*Derives from: #278 F1 ...*` present at line 147.

Both patches are loaded by α's standard load order (CDD.md §4.4 Tier 1 + α's §2.1 dispatch intake step 5). Cycle is running against the current skill set.

**Self-test against MCA-1 (γ-side application):** issue #277 names `cn-sigma/spec/SOUL.md`, `cn-sigma/threads/adhoc/20260427-self-verification-gap.md`, `src/packages/cnos.core/templates/SOUL.md`, `src/packages/cnos.core/skills/skill/SKILL.md`, `src/packages/cnos.core/skills/write/SKILL.md`, `src/packages/cnos.core/skills/design/SKILL.md`, `docs/alpha/ctb/CTB-v4.0.0-VISION.md` (with §4.2, §8.5.2 inline qualifier), and `docs/alpha/doctrine/`. Of these, the cnos-internal ones are reachable from this clone; the two `cn-sigma/...` ones are not (see §6 / Q1). Per MCA-1's "surface unavailability before treating as skipped" rule, this γ-side note surfaces it now and α's dispatch prompt will surface it again before α's first action.

**Self-test against MCA-2 (γ-side application):** issue #277's `## Related Artifacts` block lists each artifact by path (good — no inline-only references). The `## Active Constraints` block names "Continuity rule (current SOUL §3.6)" — this is an inline reference to the current sigma SOUL §3.6 (which γ cannot read directly per §6). Path is partial (`cn-sigma/spec/SOUL.md` named in `## Related Artifacts`), so MCA-2 is technically satisfied. The deeper question (whether "current SOUL §3.6" can be resolved by α from text α can read) flows into Q1 below.

## 4. Issue #277 dispatch-readiness verification (CDD §2.2 + γ §2.4 issue-quality gate)

Verified against `gamma/SKILL.md` §2.4 issue-quality gate:

| Gate item | Verdict | Evidence |
|---|---|---|
| Satisfies `issue/SKILL.md` shape | ✅ | Problem 4 lines; Acceptance Criteria 12 numbered; Non-goals 4; Tier 3 Skills 4; Active Constraints 3; Related Artifacts 8; Priority P1; Work Shape "Substantial". |
| ACs numbered and independently testable | ✅ (mostly) | AC1–AC12. AC1–AC8 are concretely testable against the migrated artifact. AC9 is a "preserve identity / optimization order / memory surfaces / write triggers / boundary invariants / continuity rule / vibe" check — testable but vibe-bearing; β can verify presence-of-each-element from the diff. AC10 ("CTB-compatible structure ... reads as a function signature ... would translate to CTB without loss") is testable but requires β to load LANGUAGE-SPEC and run a translation thought-experiment; the test is well-defined but heavier than AC1–AC8. AC11 (no duplicate paragraphs / no orphaned derivations) is concrete. AC12 (template not modified) is grep-testable. |
| Every noun in scope | ✅ | "skill form", "frontmatter", "governing question", "coherence formula", "failure mode", "Verify phase", "UIE-V loop" — all defined in named related artifacts (skill, design skills) or in the issue itself. "PLUR" appears in AC11 ("PLUR above all" as an example of a possibly-orphaned derivation candidate); γ does not know what PLUR resolves to from cnos-side artifacts (not in skill skill, not in CDD.md, not in doctrine essays under cnos/docs/alpha/doctrine/). It is presumably defined in the current sigma SOUL or in CBP/EFA references. **Tracked under Q1: cross-repo readability.** |
| Non-goals explicit | ✅ | 4 non-goals named (rewriting generic template, CTB syntax, re-deriving doctrine, length-for-its-own-sake). |
| Tier 3 skills named explicitly | ✅ | `skill`, `compose`, `write`, `design`. Unqualified, but Related Artifacts resolve them to `src/packages/cnos.core/skills/{skill,write,design}/SKILL.md`. `compose` is at `src/packages/cnos.core/skills/compose/SKILL.md` per `find` (not relisted in Related Artifacts but discoverable). γ's α dispatch prompt will name fully-qualified paths to remove any ambiguity. |
| Active design constraints linked | ✅ | Continuity rule (sigma SOUL §3.6) — inline; Sigma-specific (do not merge with template) — explicit; doctrine inheritance (CFA, EFA, JFA) — Related Artifacts links `docs/alpha/doctrine/` (where all three essays live, plus IFA which is the fourth doctrine essay shipped in #278's prior cycle). |
| Related artifacts linked or absent | ✅ | 8 entries linked. Note: doctrine essays now number **four** (IFA shipped in #278 / 3.60.0); issue lists "doctrine triad: CFA, EFA, JFA" but Related Artifacts links the whole `docs/alpha/doctrine/` directory, which captures IFA implicitly. The AC7 phrasing ("Doctrine triad (CFA, EFA, JFA) inherited by reference") says triad explicitly; γ surfaces this as Q3 (whether IFA inheritance is in scope or out). |
| Priority stated | ✅ | P1. |
| Work-shape stated | ✅ | "Substantial. Requires α draft, β review, operator approval gate." Note the **operator approval gate** is named — see Q4 below. |
| Dependency notes when blockers/sequencing real | ✅ (implicit) | The branch `claude/migrate-soul-skill-form-t7sIN` is operator-designated; `9dbb107` marks the writer-package as superseded by post-SOUL.md redo, encoding directional sequencing (#277 ships first). |
| No prompt-only constraints hiding outside the issue | partial | The dispatch brief I received (this conversation) frames the cycle's *meta* purpose more broadly than the issue text (UIE-V as constitutive structure of every cnos agent, with α/β/γ as visible roles of UIE-V phases). The issue is sigma-specific. **Q5 below.** |

**Verdict:** issue body itself is dispatchable per the issue-quality gate. Five clarifying questions accumulated below (§7) — none of them are "the issue is wrong"; all of them are "the cycle has cross-cutting structural ambiguities that γ should resolve with δ before dispatch rather than dispatch silently and risk drift."

## 5. SOUL.md location and loader-skill landscape

The dispatch brief said: *"Artifact target: src/agent/SOUL.md (current location; migration may relocate per α's design)"* and asked γ to identify *"any runtime/loader skill that governs how SOUL.md is loaded by cnos agents — surface the gap to δ if no such skill exists yet."*

Verified by inspection of this clone:

- **`src/agent/SOUL.md` does not exist.** No `src/agent/` directory at all in cnos. The brief's path is not findable.
- **`src/packages/cnos.core/templates/SOUL.md`** exists (149 lines; the generic SOUL template). Issue AC12 forbids modifying this file in this cycle. Issue Non-goal #1 forbids "Rewriting the generic SOUL template (separate cycle)".
- **`cn-sigma/spec/SOUL.md`** is the issue's stated "Current artifact" — sigma's specific soul. **Not in this cnos clone**; lives in the separate `usurobor/cn-sigma` repository (verified via `mcp__github__search_repositories`). **Not in this session's MCP scope** (system prompt restricts MCP tools to `usurobor/cnos`).
- **Hub convention** per `cnos.core/skills/agent/self-cohere/SKILL.md` §Structure:
  ```
  workspace/
  ├── SOUL.md    → cn-*/spec/SOUL.md   ← per-agent hub holds sigma's actual SOUL
  ├── USER.md    → cn-*/spec/USER.md
  ├── AGENTS.md  → cnos/spec/AGENTS.md
  ├── cn-*/      ← hub: identity + state
  └── cnos/      ← template: skills + mindsets
  ```
  Per-agent hubs (`cn-sigma/`, `cn-rho/`, etc.) hold each agent's actual SOUL.md; cnos.core's template provides the default that materializes into a hub on fresh install.

**Loader-skill landscape.** No single skill is named "the SOUL.md loader". Two skills compose the loading and writing surface:

| Skill | Path | Role wrt SOUL.md |
|---|---|---|
| `self-cohere` | `src/packages/cnos.core/skills/agent/self-cohere/SKILL.md` | Reads `cn-*/spec/SOUL.md` at intake; if missing, materializes default from `cnos/spec/SOUL.md` (template) into the hub. Symlinks `workspace/SOUL.md → cn-*/spec/SOUL.md`. **This is the runtime intake path.** |
| `configure-agent` | `src/packages/cnos.core/skills/agent/configure-agent/SKILL.md` | Writes `spec/SOUL.md` after operator interview + confirmation. **Constitutive self rule (§3.1):** "Outside configuration mode, [the agent] may read, may explain, may propose, may not write." This is the only mode in which the agent may write its own SOUL.md. |
| `clp` | `src/packages/cnos.core/skills/agent/clp/SKILL.md` | Lists SOUL.md as a configuration change requiring CLP gate before confirmation. |

**Implication for #277.** The migration changes SOUL.md content shape (skill form + UIE-V Verify), not its loading mechanism. `self-cohere` does not parse SOUL.md as a skill — it just reads it as a markdown surface. So the migration to skill form is content-shape only; no `self-cohere` patch is required for the runtime to keep working. **But** if SOUL.md acquires frontmatter (per AC1: `name`, `artifact_class`, `governing_question`, `kata_surface`), and if any future tooling (e.g. the `skill` skill's verifier, or `cn cdd-verify`) treats SOUL.md as a skill artifact, those tools would need to be aware that SOUL.md is a skill at the agent layer. **No such verifier currently treats SOUL.md as a skill** per `find` and `grep` of cnos.core skills. The generic template (out of scope this cycle per AC12) would normally become the second copy of the new shape — but the cycle is sigma-only, so the template stays in its current pre-skill-form prose.

**Gap to surface to δ:** The brief asked γ to identify "any runtime/loader skill that governs how SOUL.md is loaded by cnos agents". The answer is: `self-cohere` is the runtime intake path; `configure-agent` is the writer. Neither parses SOUL.md as a skill. If the brief intended a "loader" in the sense of "skill that knows how to interpret SOUL.md as a skill artifact and validate it as one", **no such skill exists yet** — and on the issue's narrow sigma-only scope, none is required this cycle. Tracked under Q2 below.

## 6. Cross-repo readability — the cn-sigma barrier

The issue's Current artifact (`cn-sigma/spec/SOUL.md`) and Evidence (`cn-sigma/threads/adhoc/20260427-self-verification-gap.md`) are in a separate GitHub repository (`usurobor/cn-sigma`).

Verified barriers in **this** environment:

- **No local `cn-sigma` clone.** `find /home/user -maxdepth 3 -name 'cn-sigma' -type d` returns empty.
- **Out of MCP scope.** System-prompt restriction: *"Your GitHub MCP tools are restricted to the following repository: `usurobor/cnos`. Calls targeting repositories outside this list will be denied."* `mcp__github__get_file_contents owner=usurobor repo=cn-sigma path=spec/SOUL.md` would be denied.
- **WebFetch (deferred tool, available on demand)** could in principle fetch the raw GitHub content for a public file (cn-sigma is public per `mcp__github__search_repositories` result). γ did not invoke WebFetch at intake to keep the question on the table for δ rather than route around it silently.

α's environment may differ from γ's (separate Claude session under a different harness; the operator may have provisioned α with a cn-sigma clone, broader MCP scope, or explicit WebFetch instructions). γ does not know without δ saying. The dispatch prompt must either:
1. **(a)** instruct α to read `cn-sigma/spec/SOUL.md` from a locally-mounted path that the operator has provisioned, or
2. **(b)** instruct α to fetch the file content via WebFetch from the public GitHub raw URL, or
3. **(c)** instruct α that the current SOUL.md content is provided **inline** in the dispatch prompt (γ would need to read it on δ's behalf and embed it), or
4. **(d)** clarify that α should treat AC9 ("identity, optimization order, memory surfaces, write triggers, boundary invariants, continuity rule, and vibe preserved from current SOUL") by working from the **generic template** (`cnos.core/templates/SOUL.md`) as the structural basis and treating sigma-specific content as α's design decision — but this would partially undercut AC9's "preserve from current SOUL" wording.

γ's view: option (a) or (b) is structurally clean — α reads sigma's actual SOUL.md and writes the migration. Option (c) makes γ a transcription channel and risks omission. Option (d) substitutes the template for the artifact AC9 names, which conflicts with AC9.

**This is Q1 below — the most blocking question.**

## 7. Open questions surfaced to δ before dispatch

Per `gamma/SKILL.md` §2.4 ("Do not compensate for a weak issue by making the prompt longer. Fix the issue instead.") and CDD §3.7 (no closure with unresolved triage applied earlier in the cycle as no dispatch with unresolved structural ambiguity), γ holds dispatch and surfaces the following:

### Q1 — How does α read `cn-sigma/spec/SOUL.md`? (BLOCKING)

The artifact named in the issue's `## Related Artifacts` as Current artifact, and the artifact AC9 requires α to preserve identity / optimization order / etc. *from*, sits in a repo outside this MCP scope and is not present in this clone. α's environment may differ. Pick one of (a)/(b)/(c)/(d) from §6 and γ will write α's dispatch prompt accordingly. Default proposal pending δ: option (a) — operator pre-mounts cn-sigma clone at a known path (e.g. `/home/user/cn-sigma/`) and γ names the path in α's dispatch prompt; if the path is not present, α must surface unavailability to δ before drafting (per MCA-1 / α §2.1 step 4).

### Q2 — Does any new "loader" or "verifier" skill need to ship in this cycle? (LIKELY NO)

Per §5, no current cnos.core skill parses SOUL.md as a skill; the runtime intake (`self-cohere`) treats it as opaque markdown. The brief asked γ to surface a gap "if no such skill exists yet". γ's reading: on the issue's narrow sigma-only scope, no such skill is required this cycle; SOUL.md gaining skill-form frontmatter does not break runtime intake. The operator brief did flag downstream consequences ("cnos.cdd role-skill reconciliation, cnos.writer redo are NOT in scope for this cycle"). γ defers to δ on whether a stub `cnos.core/skills/agent/soul/SKILL.md` (a verifier-style skill that reads SOUL.md and asserts skill-form well-formedness) should ship as part of this cycle or as a separate follow-on. **Default proposal pending δ: defer; out of scope this cycle.**

### Q3 — Is IFA inheritance in scope, or only the original triad (CFA, EFA, JFA)?

Issue AC7: *"Doctrine triad (CFA, EFA, JFA) inherited by reference, not re-derived."* The fourth doctrine essay (Inheritance for Agents — IFA) shipped in cycle 3.60.0 / #278's prior cycle and now sits in `docs/alpha/doctrine/inheritance-for-agents/`. The issue body says "triad" (three) but Related Artifacts links the whole `docs/alpha/doctrine/` directory which contains four essays. The doctrine README (`docs/alpha/doctrine/README.md`) is the canonical hub. **γ's reading:** the issue AC7 phrasing predates IFA being framed as a doctrine essay (or treats IFA as something other than triad); α should inherit *all four* doctrine essays by reference (CFA, EFA, JFA, IFA), since IFA is now part of the canonical doctrine surface and the migration is asked to be retro-coherent (operator brief: "doctrine retro-coherence risk"). **Default proposal pending δ: α inherits all four doctrine essays by reference per the doctrine README hub; "triad" in AC7 is read as "the set of doctrine essays that govern agent behavior" rather than as a literal three-cardinality assertion.** δ may override.

### Q4 — Where does the migrated artifact ship, and how is the "operator approval gate" expressed?

Issue Work Shape: *"Substantial. Requires α draft, β review, **operator approval gate**."* Three sub-questions:

- **Q4a — Repo of record for the new SOUL.md.** Sigma's actual SOUL.md lives in cn-sigma (out of MCP scope this session). Plausible patterns: (i) α writes the new artifact to a path inside cnos (e.g. `docs/alpha/sigma/SOUL.md` or attached to the PR diff as a new file under a sigma-namespaced path), and δ copies it into cn-sigma after merge; (ii) α writes to cn-sigma directly via a separate harness; (iii) α writes the proposed text to the cnos PR body, and the cn-sigma update is δ's downstream action.
- **Q4b — When is the operator approval gate?** SOUL.md's continuity rule (sigma SOUL §3.6 per issue's Active Constraints; also `configure-agent/SKILL.md` §3.1 "Constitutive self rule") forbids the agent from writing its own constitutive self outside configuration mode. β's normal review approval (CDD §1.4 step 8 → 9) is the artifact-quality gate. The "operator approval gate" the issue names is presumably an *additional* gate after β approves and before the artifact replaces sigma's actual SOUL.md. γ's reading: this is δ's gate, not γ's. δ approves the migration content before it lands in cn-sigma's main. **Default proposal pending δ: δ owns the post-β operator approval gate; γ records it in the close-out triage if it's exercised.**
- **Q4c — Cycle release-vs-non-release shape.** #278 was non-release (no version directory, close-outs at `.cdd/unreleased/{role}/278.md` indefinitely). Is #277 the same shape (non-release, sigma-side artifact change), or does it warrant a cnos release because the migration encodes UIE-V as a primitive that downstream cnos cycles inherit? **Default proposal pending δ: non-release for the cnos repo (sigma-only artifact change); δ may override if a cnos-side release is warranted (e.g. a `cnos.core/skills/agent/soul/` skill ships per Q2, or doctrine-essay or template patches land).**

### Q5 — How should γ interpret the brief's broader framing relative to the issue's narrower scope?

The dispatch brief frames the cycle as "operationalizing UIE-V as the constitutive structure of every cnos agent" with α/β/γ as "the visible roles of UIE-V phases at the agent boundary". The issue text is sigma-specific (AC12 forbids template modification; Non-goal #1 forbids generic-template rewrite; constraints name "Sigma-specific: do not merge with generic template"). γ's reading: the broader claim is the cycle's *meta* purpose (sigma's migration makes UIE-V visible as a primitive that downstream cycles can build on, including the writer-package redo that `9dbb107` already encoded as superseding the prior writer-package design). The narrow sigma-only scope is the cycle's *in-scope work*. These are not in conflict if α treats sigma's SOUL.md as the artifact and β verifies AC1–AC12 against the artifact; the broader framing is observational context for γ's PRA later. **Default proposal pending δ: dispatch on issue text; broader framing used by γ for PRA structural-claim verification only.**

## 8. Draft dispatch prompts (HELD — not for harness pickup until §10 status flips)

These follow `CDD.md` §γ-dispatch-prompt-format and `gamma/SKILL.md` §2.5. They are committed here as drafts so δ can review and approve / correct in one round. **They are not live until §10 status flips to "DISPATCH OPEN".**

### Draft α prompt

```text
You are α. Project: cnos.

Load src/packages/cnos.cdd/skills/cdd/alpha/SKILL.md.

Issue: gh issue view 277  (or, since gh is unavailable in this env: read via mcp__github__issue_read method=get owner=usurobor repo=cnos issue_number=277)

Tier 3 skills:
  - src/packages/cnos.core/skills/skill/SKILL.md       (skill-form authority — SOUL.md becomes a skill)
  - src/packages/cnos.core/skills/write/SKILL.md       (prose-quality discipline)
  - src/packages/cnos.core/skills/design/SKILL.md      (Verify-phase structural reasoning: boundary / policy / contract)
  - src/packages/cnos.core/skills/compose/SKILL.md     (one axis per artifact; no overlap with existing skills)

Cross-repo artifact (mandatory read for AC9):
  cn-sigma/spec/SOUL.md is the artifact being migrated.
  This repo's MCP scope is restricted to usurobor/cnos. Read cn-sigma/spec/SOUL.md from <PATH-OR-METHOD-PROVIDED-BY-δ>.
  If that path/method is not present in your env, surface to δ before drafting (per alpha/SKILL.md §2.1 step 4).

Cross-repo evidence:
  cn-sigma/threads/adhoc/20260427-self-verification-gap.md is the issue's named evidence.
  Same access notes as above.

Cycle-inheritance MCAs in force (from #278):
  alpha/SKILL.md §2.1 step 4 — enumerate inline-prose-named artifacts as well as linked entries.
  issue/SKILL.md §2.5.3 — parallel-dependency / constraint-spec references must be linked with a path.

Doctrine inheritance:
  AC7 names CFA, EFA, JFA. The fourth doctrine essay (IFA, shipped 3.60.0) is also part of canonical doctrine.
  Inherit all four by reference per the doctrine README hub at docs/alpha/doctrine/README.md. Do not re-derive.

CTB structural compatibility (AC10):
  docs/alpha/ctb/LANGUAGE-SPEC.md is the canonical CTB language spec. AC10 asks for structural compatibility (would translate to CTB without loss); not for inline CTB syntax. Read §2 (signature surface) before drafting frontmatter.

Out of scope (issue Non-goals + AC12):
  src/packages/cnos.core/templates/SOUL.md must NOT be modified in this cycle.
  Improvements to the generic template may be noted in α's close-out for a future cycle, not applied.

Operator approval gate (issue Work Shape):
  After β approval, the migration content goes through δ's operator approval gate before landing in cn-sigma's main.
  α writes the artifact and the close-out in this cnos cycle; the cn-sigma write is δ's downstream action unless δ instructs otherwise.

Operating environment:
  This is the same MCP-only / git-via-proxy environment as cycle #278. CDD §Tracking applies. Default to MCP for GitHub surfaces and git for branch state. Do not use unauthenticated api.github.com curl (#268 T2, #278 P2 cycle properties).

Branch:
  This cycle's shared branch is claude/migrate-soul-skill-form-t7sIN (operator-designated; γ branch is the same — single-branch convention per #278).
```

### Draft β prompt

```text
You are β. Project: cnos.

Load src/packages/cnos.cdd/skills/cdd/beta/SKILL.md and follow its load order.

Issue: gh issue view 277  (or, since gh is unavailable in this env: read via mcp__github__issue_read method=get owner=usurobor repo=cnos issue_number=277)

Cross-repo notes:
  The artifact being migrated lives in cn-sigma/spec/SOUL.md.
  This repo's MCP scope is restricted to usurobor/cnos.
  α's PR will carry the migrated artifact (path TBD per δ on Q4a — see γ's intake note at .cdd/unreleased/gamma/277.md).
  AC9 verification ("preserve identity / optimization order / memory surfaces / write triggers / boundary invariants / continuity rule / vibe from current SOUL") requires reading the current sigma SOUL — read it via <PATH-OR-METHOD-PROVIDED-BY-δ>; if unreachable, surface to δ before approving.

Cycle-inheritance MCAs in force (from #278):
  alpha/SKILL.md §2.1 step 4, issue/SKILL.md §2.5.3 — relevant when verifying α's intake completeness.

Doctrine retro-coherence check (operator brief — for β's review):
  Verify the migrated SOUL.md does not silently rewrite doctrine. CFA / EFA / JFA / IFA argue for triadic coherence, ethics-as-coherence-mechanism, judgment-as-coherence-action, and inheritance-as-cross-cycle-discipline. The migration should operationalize what they already argued for, not introduce vocabulary that contradicts a doctrine essay's load-bearing claims. If contradiction found, that is a finding.

Skill-cascade check (operator brief — for β's review):
  SOUL.md acquires frontmatter per AC1 (name, artifact_class, governing_question, kata_surface). Per LANGUAGE-SPEC §2, frontmatter declares triggers, scope, calls. Verify α's choices for these fields are structurally justified and do not silently break runtime intake (self-cohere reads SOUL.md as opaque markdown today; verify nothing in the migrated frontmatter shape breaks that).

Operator approval gate:
  β's normal A/RC verdict is the artifact-quality gate. δ owns the post-β operator approval gate before the migration content lands in cn-sigma. β's release skill (release/SKILL.md) governs in this cnos cycle; δ owns the cn-sigma write (and any related release artifacts) unless δ instructs otherwise.

Operating environment:
  Same as α's prompt above. MCP for GitHub surfaces, git for branches; do not use api.github.com curl. CDD §Tracking applies; run the reachability preflight + synchronous baseline pull before starting transition-only polling.

Branch:
  Shared cycle branch: claude/migrate-soul-skill-form-t7sIN (single-branch convention per #278).
```

### Notes on the dispatch prompt drafts

- Both prompts assume δ's resolution of Q1 and Q4a as the *path-or-method* parameter. γ has left placeholder `<PATH-OR-METHOD-PROVIDED-BY-δ>` in both prompts; δ fills before flipping §10 status to OPEN, or instructs γ to fill on δ's behalf.
- Both prompts default to γ's reading on Q3 (all four doctrine essays) and Q4b (δ owns post-β operator approval gate). Either default may be overridden by δ; γ updates the prompts in the same commit that flips §10.
- Q2 (separate `cnos.core/skills/agent/soul/` skill) is not named in the prompts; γ's default reading is "out of scope this cycle" and α/β do not need to know about it.
- Q5 (broader framing) is not named in the prompts; γ's default is "α implements the issue, γ verifies the structural claim in the PRA". α/β do not need to know about the broader framing — including it would risk α expanding scope.
- Tier 3 skill list mirrors the issue's `## Tier 3 Skills` section (4 skills) with full paths. Verified each path exists by `find`.
- Per the brief's instruction to add Tier 3 only ("Do not add Tier 1/2 skills — those load automatically"), γ has not added cnos.cdd lifecycle skills or eng/* bundles to α's Tier 3 list. They auto-load per CDD §4.4.
- The prompts intentionally do not restate the algorithm or repeat what loaded skills already say (per `gamma/SKILL.md` §3.3 — prompt cleverness is not a substitute for issue / skill quality).

## 9. Operating-environment summary for δ

For δ's convenience when resolving Q1–Q5 and gating dispatch:

- γ is in Claude Code on the web; MCP scope is `usurobor/cnos` only. No `gh`, no shell network beyond git proxy, no `cn-sigma` reach.
- γ's git identity is configured: `gamma` / `gamma@cdd.cnos`.
- γ branch: `claude/migrate-soul-skill-form-t7sIN`. No commits beyond what's on main yet; this commit will be γ's first.
- γ has not pushed; this is the first commit on the γ branch.
- α and β sessions are presumably starting (or already started) in their own harness sessions; γ does not know their environments. δ may know.
- Polling: γ will use MCP `*_read` synchronously between cycle steps, plus `git fetch` + branch-SHA inspection under `Monitor` for transition-only stdout when α/β commit. The `Monitor` loop will start after dispatch flips §10.

## 10. Status

**DISPATCH HELD pending δ resolution of Q1–Q5 in §7.**

When δ resolves (by editing this file directly, or by adding an `## Operator response` section, or by any other channel γ can observe), γ:

1. updates §8 prompts to fill the placeholders and incorporate any δ overrides,
2. flips this section to "DISPATCH OPEN — α prompt at §8 / β prompt at §8 / dispatched at <commit-sha>",
3. commits the update,
4. begins active polling on α and β surfaces (branch-head SHAs + PR-state via MCP).

Until then, γ does not start the polling loop (no work to wake to) and does not write further sections.

---

**γ identity for this commit:** `gamma@cdd.cnos`.
**γ commit policy:** this file accumulates incrementally; section IDs (§1–§10) are stable; new sections append at the end. No section is rewritten silently — visible amendments only.

---

## 11. Operator response (captured 2026-04-28, post-`4234cbb`)

δ updated issue #277 (now at `updated_at: 2026-04-28T01:54:48Z`) and replied: *"We don't need sigma soul we have base soul in cnos. Issue updated."* Followed by:

> Target is the cnos SOUL, not Sigma's — incorporating Sigma's learnings upstream
> No longer a template — it's the canonical agent skill (AC12)
> Sigma's SOUL derives from it as a configured specialization (AC13)
> Path changes from templates/ to skills/agent/ (or equivalent)
> #281 explicitly gated on this in Related Artifacts

### What changed in the issue (vs. the version γ read at intake):

- **Problem reframed.** Target is now the cnos canonical SOUL.md (`src/packages/cnos.core/templates/SOUL.md` is the *current* location, but per AC12 it stops being a template and per AC1 it relocates to `src/packages/cnos.core/skills/agent/SOUL.md` or equivalent canonical path). Sigma's SOUL is reframed as a downstream derivation, not the migration target.
- **AC1 path changed:** explicit `src/packages/cnos.core/skills/agent/SOUL.md (or equivalent canonical path)`.
- **AC9 reframed.** Was: "preserve identity / optimization order / memory surfaces / write triggers / boundary invariants / continuity rule / vibe from current SOUL." Now: "Learnings from Sigma's operational SOUL incorporated: memory surfaces, write triggers, multi-agent invariants, boundary invariants, ambiguity tie-breaks." All five named items are *already* present in the current `templates/SOUL.md` (verified by re-reading: §2.4 memory + write triggers; §3.3 multi-agent; §3.4 boundary; §3.5 ambiguity). γ's reading: AC9 is satisfied if α preserves these sections in the migrated form, drawing on the current cnos template as the source-of-truth and Sigma's SOUL only as supplementary context if α's environment has access; α does **not** need to read cn-sigma to satisfy AC9.
- **AC11 sharpened.** PLUR is now an explicit AC ("PLUR either derived from boundary mechanics or pointed to CBP/EFA"), not just an example.
- **AC12 inverted.** Was: "Generic template at `src/packages/cnos.core/templates/SOUL.md` is NOT modified in this cycle." Now: "The file is no longer a template. It is the canonical agent skill. Agent-specific configuration (identity, operator preferences) is handled via a separate mechanism (configure-agent skill, or agent-specific spec that inherits from the canonical SOUL)." So the file *does* move/transform; the relocation is in scope.
- **AC13 added.** "Sigma's `cn-sigma/spec/SOUL.md` is updated to derive from the canonical version, carrying only Sigma-specific configuration (identity, vibe, operator relationship)." See Q1' below — γ's reading on whether α executes the cn-sigma update or it's δ's downstream action.
- **Active Constraints changed.** Removed: "Continuity rule (current SOUL §3.6)" and "Sigma-specific: do not merge with generic template" (both made obsolete by the reframe). Added: explicit "CFA, EFA, JFA, **IFA**" inheritance — **resolves Q3 (yes, all four doctrine essays).**
- **Non-goals shifted.** Removed: "Rewriting the generic SOUL template (separate cycle)". Added: "Rewriting cnos.cdd or cnos.writer — their relationship to UIE-V is downstream of this".
- **Work Shape simplified.** Removed: "operator approval gate" — **resolves Q4b (β's normal A/RC verdict is the only gate; no separate operator approval gate).** Added: "The cnos SOUL becomes the foundation that both CDD and writer specialize."
- **Related Artifacts.** #281 added: "Writer-package redo (blocked on this): #281". γ has read #281 — it explicitly cites #277 as a dependency, AC1 framing cnos.writer as a composition of (UIE-V agent + write skill + prose-shape lifecycle artifacts) where each role skill becomes a delta over the core UIE-V agent skill. This validates the brief's framing about UIE-V being constitutive across cnos agents.

### Resolution of Q1–Q5 (mapped to the updated issue):

| Question | Status | Resolution |
|---|---|---|
| Q1 (cn-sigma read) | **mostly resolved → Q1'** | The migration target is the cnos canonical (in this clone). Read access to cn-sigma is no longer required for AC9 (the cnos template already contains the named learnings). Q1' (below) captures the residual: AC13's cn-sigma write. |
| Q2 (loader/verifier skill) | **resolved as deferred** | Operator did not name a loader/verifier skill addition. γ's default holds: out of scope this cycle. The migration will land the canonical SOUL as a skill at `src/packages/cnos.core/skills/agent/SOUL.md`; the existing `self-cohere` runtime intake continues to read it as opaque markdown. Cnos.cdd / cnos.writer reframe per UIE-V is downstream (per the updated issue's Non-goals + #281). |
| Q3 (IFA inheritance) | **resolved: yes, all four** | Active Constraints now lists "CFA, EFA, JFA, IFA". AC7 still says "triad" — γ reads this as legacy phrasing; the more specific Active Constraints governs. |
| Q4a (repo of record) | **resolved: cnos** | AC1 names `src/packages/cnos.core/skills/agent/SOUL.md (or equivalent canonical path)`. α decides the exact path within cnos.core (with β review). γ's bias: literal `src/packages/cnos.core/skills/agent/SOUL.md` if no structural reason against; α may choose `src/packages/cnos.core/skills/agent/agent/SOUL.md`-style if frontmatter naming demands. |
| Q4b (operator approval gate) | **resolved: removed** | Updated Work Shape removes "operator approval gate". β's A verdict is the gate. |
| Q4c (release vs non-release) | **γ's call → release-shaped** | This cycle changes the canonical agent SOUL.md that every cnos agent loads, relocates the file, and changes its skill-form classification. Downstream issues (#281; cnos.cdd reconciliation) are explicitly gated on this. γ's bias: release-shaped (version bump from 3.60.0; CHANGELOG row; tag) per CDD §5.3a Artifact Location Matrix. **β decides the version bump magnitude (likely 3.61.0 or 4.0.0 depending on how β reads the impact graph)** and authors the CHANGELOG / RELEASE.md prose. γ flags this in the β prompt rather than overriding β's release-skill judgment. |
| Q5 (brief vs issue) | **resolved: aligned** | Updated issue's Problem section + Work Shape ("The cnos SOUL becomes the foundation that both CDD and writer specialize") aligns with the brief's framing. γ's broader-framing observation moves from "cycle's meta purpose" to "cycle's stated purpose"; PRA structural-claim verification still applies. |

### Q1' (residual) — How does AC13 (Sigma's SOUL update) get executed?

The cnos cycle ships the canonical artifact in cnos. AC13 requires Sigma's `cn-sigma/spec/SOUL.md` to be updated to derive from it. Two possibilities:

- **(a)** α writes the canonical SOUL.md to cnos AND illustrates the Sigma-derivation pattern via a worked example (could be inline in the SOUL.md kata, or a separate `docs/alpha/examples/...` artifact). The actual cn-sigma file write is δ's downstream action (analogous to #278's deferred D1).
- **(b)** α has separate access to cn-sigma (different harness, broader scope) and writes the derivation directly. Cycle ships in two places.

γ's default: **(a)**. Sigma's SOUL update lands as δ's downstream action after this cycle merges. α's PR carries the canonical SOUL plus a worked-derivation example showing the pattern Sigma will follow. β verifies AC13 by checking that the derivation pattern is concretely specified (not just "Sigma should derive from this"). δ executes the cn-sigma write in a separate δ session after this cycle closes. **δ overrides if (b) is the intended path.**

### Sigma's operational evidence (AC9 source, Sigma-side context)

The issue cites two evidence artifacts in cn-sigma (`cn-sigma/spec/SOUL.md` and `cn-sigma/threads/adhoc/20260427-self-verification-gap.md`). γ does not have MCP access to cn-sigma; α may not either. Per the updated AC9 phrasing ("memory surfaces, write triggers, multi-agent invariants, boundary invariants, ambiguity tie-breaks"), all five items are **already in `src/packages/cnos.core/templates/SOUL.md`** (verified by re-reading) and α can satisfy AC9 from the cnos template alone. The Sigma evidence is supplementary context for *why* these matter; it is not a load-bearing source α must read. γ surfaces this as guidance in α's prompt rather than as a blocker.

## 12. Live dispatch prompts (supersedes §8 drafts; corrected after §14)

Per `CDD.md` §γ-dispatch-prompt-format and `gamma/SKILL.md` §3.3–§3.4: name role + project + skill-to-load + issue. α gets Tier 3. Nothing else. The skills tell each role what to load, how to poll, how to behave; the issue tells them what to do. γ's intake observations and ambiguity readings (Q1–Q5, Q1') live in this γ-side file for γ's own use, **not** in the prompts.

### α prompt (LIVE)

```text
You are α. Project: cnos.
Load src/packages/cnos.cdd/skills/cdd/alpha/SKILL.md.
Issue: gh issue view 277
Tier 3 skills: cnos.core/skills/skill, cnos.core/skills/write, cnos.core/skills/design, cnos.core/skills/compose
```

### β prompt (LIVE)

```text
You are β. Project: cnos.
Load src/packages/cnos.cdd/skills/cdd/beta/SKILL.md and follow its load order.
Issue: gh issue view 277
```

## 13. Status — DISPATCH OPEN

**Status flipped to DISPATCH OPEN at this commit.**

Live α prompt: §12 / α prompt (LIVE).
Live β prompt: §12 / β prompt (LIVE).

γ's polling resumes immediately after this commit lands:

- branch-head SHA polling for `claude/migrate-soul-skill-form-t7sIN` via `git fetch` + `git rev-parse origin/{branch}` (transition-only stdout under `Monitor` when α/β commit)
- PR-state polling via `mcp__github__list_pull_requests` and `mcp__github__pull_request_read` once α opens a PR
- issue activity via `mcp__github__issue_read get_comments` synchronously between cycle steps
- per-role records: γ watches `.cdd/unreleased/alpha/277.md` and `.cdd/unreleased/beta/277.md` for α/β commits (file-presence + diff via git)

γ does not author further sections until α or β commits to the cycle (or δ commits a directive). When that happens, γ appends §15, §16, … with mid-cycle observations, drift checks, role-separation verification, and PRA-input data per `gamma/SKILL.md` §2.5–§2.10.

## 14. γ-side process miss — overlong dispatch prompts (operator correction)

**What happened.** γ's first draft of §12 (live dispatch prompts) ran ~80 lines for α and ~60 lines for β. δ caught it before commit and corrected: *"Per CDD.md §γ-dispatch-prompt-format: the prompt names the role, the project, the skill to load, and points to the issue. The CDD skill tells each role what to load and what to do. If you find yourself wanting to enumerate steps in the prompt, the algorithm is not clear enough — fix the skill, not the prompt."* §12 was rewritten in place (uncommitted at the time of the operator catch); §14 records the miss for the cycle iteration / PRA.

**What γ wrote into the long prompts (now removed):**

- α prompt: cycle scope, source artifact path, out-of-scope note, doctrine-inheritance note, CTB structural-compatibility note, AC13 interpretation, MCA-1/MCA-2 reminder, cycle-shape (release/non-release) bias, operating-environment note, branch name.
- β prompt: cycle scope (for verification), MCA reminder, doctrine retro-coherence check direction, skill-cascade check direction, cnos.cdd / cnos.writer reframe note, AC13 interpretation, release-shape decision direction, operating-environment note, branch name.

**Why each was wrong:**

| What γ added | Where it actually belongs | Why γ added it (failure mode) |
|---|---|---|
| Cycle scope, source artifact path, out-of-scope, doctrine inheritance, CTB compatibility | issue body (Problem / Related Artifacts / Non-goals / Active Constraints / AC10) | γ duplicated the issue contents in the prompt as a "convenience" — exactly the *prompt compensation* failure mode `gamma/SKILL.md` §1.3 names. |
| MCA-1 / MCA-2 reminder | `alpha/SKILL.md` §2.1 step 4 + `issue/SKILL.md` §2.5.3 (already loaded by α) | γ assumed α might forget; this is exactly what the skill loading tier is for. Repeating in the prompt hides where the rule actually lives. |
| AC13 interpretation, release-shape bias, doctrine retro-coherence directives, skill-cascade check directives | If they belong anywhere, they belong (a) in the issue if the AC needs sharpening (γ should request a δ edit), or (b) in γ's own intake notes for γ's later PRA — *not* in the prompt that biases α/β before they form their own reading. | γ smuggled γ's reading into the prompt instead of letting α/β read the issue and form independent readings. This is the *role leakage* failure mode `gamma/SKILL.md` §1.3 + §3.5 names — γ-as-coordinator must transfer artifact facts only, not γ's hidden reading. |
| Operating environment, branch name | The role's skill discovers the env via §Tracking reachability preflight + cycle convention (the role can fetch the branch list and see the cycle branch). | γ assumed the role would not know; the skill explicitly handles it. |

**Loaded skills that should have caught this (and did, via δ):**

- `gamma/SKILL.md` §2.4 ("Do not compensate for a weak issue by making the prompt longer. Fix the issue instead.") — γ read this at intake, applied it to detect possible α/β failure modes, did not apply it to γ's own prompt drafting.
- `gamma/SKILL.md` §3.3 ("Make the issue executable before dispatch — Prompt cleverness is not a substitute for issue quality.") — same pattern.
- `gamma/SKILL.md` §3.4 ("Name only Tier 3 skills in the issue — Tier 1 and Tier 2 are already mandatory. Repeating them hides the real issue-specific constraints.") — γ extended this principle into the prompt by repeating MCA-1 / MCA-2 explicitly.
- `gamma/SKILL.md` §3.5 ("Preserve epistemic separation — γ transfers artifact facts only.") — γ violated by including γ's reading on AC13 / release-shape / retro-coherence direction in β's prompt.
- `CDD.md` §γ-dispatch-prompt-format ("γ does not enumerate skills or steps in the prompt — that is the skill's job. If the prompt needs to restate the algorithm, the algorithm is not clear enough — fix the skill.") — γ enumerated skills *and* steps *and* MCA reminders.

**Self-blindness pattern.** §11 (operator response) explicitly captured γ's reading of Q1' / Q3 / Q4c with a "γ's default pending δ override" structure. That pattern is correct for the γ-side living record. γ's failure mode was carrying that same prose forward into the dispatch prompts as if the prompt were the right channel. The right channel was: keep γ's reading in §11, ask δ to update the issue if any reading needed to land in α/β's view, otherwise let α/β form independent readings from the issue alone.

**Pattern name (γ-side):** *prompt cargo-cult.* γ knew the dispatch prompt format is short (read it at intake; recited it back in §12's intro paragraph) and *still* wrote 80-line prompts because γ was carrying γ's intake research forward into the wrong artifact. The general failure shape is: *understanding the rule does not prevent applying the violation.* The corrective discipline is: **write the prompt by literally instantiating the four-line template, then stop. If anything else feels necessary, that signals an issue patch or a skill patch, not a prompt addition.**

**Surfaces affected (PRA input):**

- §12 of this file — corrected in place (uncommitted at correction time; rewrite is therefore not a silent edit per the §10 commit policy, since the original was never visible on origin).
- γ's PRA §9.1 cycle-iteration trigger 4 ("loaded skill failed to prevent a finding"): pre-fired by this miss. The relevant loaded skill (`gamma/SKILL.md`) covers the rule but γ's application failed; this is **skill-application miss**, not skill-authoring gap. No skill patch warranted; the lesson is γ-discipline, captured as a project MCI candidate at PRA time.

**MCA candidate for PRA triage:** consider whether `gamma/SKILL.md` §2.5 (dispatch step) should add a literal four-line template snippet that γ instantiates verbatim, with a "stop here" line. Current §2.5 has the template but presents it as an example shape; making it a **fill-in-the-blank** that γ copy-pastes might mechanically prevent the cargo-cult failure. **γ does not patch now** — N=1 occurrence; observe whether this miss recurs with future γ sessions before promoting to skill-authoring change.

---

**γ identity for this commit:** `gamma@cdd.cnos`.

