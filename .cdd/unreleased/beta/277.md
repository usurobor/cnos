# β living record — cycle #277

**Issue:** [#277](https://github.com/usurobor/cnos/issues/277) — *Rewrite SOUL.md to skill form with UIE-V agent loop*
**Branch (shared cycle branch per γ §13 / inherited from #278 D10):** `claude/migrate-soul-skill-form-t7sIN`
**Cycle scope (per updated issue body):** migrate `src/packages/cnos.core/templates/SOUL.md` to skill form at `src/packages/cnos.core/skills/agent/SOUL.md` (or equivalent canonical path); cnos canonical agent skill, no longer a template.
**Mode:** TBD per α's primary branch artifact.
**Role:** β
**β session env:** Claude Code on the web; MCP scope = `usurobor/cnos`; `gh` not installed; git via local proxy reachable; `Monitor` available.

This file is β's per-cycle living record per the convention γ established in `.cdd/unreleased/gamma/277.md` §13 (inherited from cycle #278's D10). It accumulates intake, round-by-round review verdicts, release evidence, and β-side observations; becomes β close-out at cycle end.

---

## §1 Intake (this commit)

### Identity

`git config user.name "beta"` and `git config user.email "beta@cdd.cnos"` set per CDD.md §1.4 step 2.

### Load order completed

Per `beta/SKILL.md` §Load Order:

1. ✅ `CDD.md` (canonical lifecycle and role contract; v3.15.0)
2. ✅ `cnos.cdd/skills/cdd/beta/SKILL.md` (β role surface)
3. ✅ `cnos.cdd/skills/cdd/review/SKILL.md` (review verdict surface)
4. ✅ `cnos.cdd/skills/cdd/release/SKILL.md` (release surface)
5. ⏳ Tier 2 + issue-specific Tier 3 to be loaded when reviewing α's diff (not before — loaded skills are hard generation constraints per CDD.md §4.4, not intake decoration). Issue Tier 3: `skill`, `compose`, `write`, `design` — α-side; β loads on diff intake. Architecture/design check (§2.2.14) likely active given the artifact is a constitutive agent skill — β will load `cnos.core/skills/design/SKILL.md` then.

### §Tracking reachability preflight (CDD.md §Tracking)

Probed each query form synchronously before arming the polling loop:

| Surface | Form probed | Result | Use for |
|---|---|---|---|
| Issue body / state | `mcp__github__issue_read get` | ✅ returned issue #277 (open, P1, updated 2026-04-28T01:54:48Z, the post-δ-correction body) | Issue-text-of-record |
| Issue comments | `mcp__github__issue_read get_comments` | ✅ returned `[]` (zero comments at intake) | Mid-cycle issue activity |
| PR list | `mcp__github__list_pull_requests state=open` | ✅ returned `[]` (no PR for #277 at intake) | α PR open detection |
| Branch list | `git branch -r` after `git fetch` | ✅ returned 5 remotes (see §2) | α/β branch detection |
| Git fetch | `git fetch origin` | ✅ silent success; remote tracking refs updated | Branch-head SHA polling |
| `gh` CLI | n/a | ❌ not installed | unavailable |
| `api.github.com` curl | n/a | not probed; demoted by #268 T2 / #278 P2 cycle properties (silent rate limit) | unavailable per cycle inheritance |

**Wake-up mechanism:** `Monitor`-wrapped `until` loop on `git fetch` + `git rev-parse origin/{branch}` for branch-head SHA transitions. Synchronous MCP `*_read` between cycle steps for PR/issue activity (MCP not runnable inside `Monitor` per CDD.md §Tracking). Stdout filter discipline: transition-only emission.

### §2 Synchronous baseline pull

Read at intake so pre-existing state is not absorbed silently by the transition-only loop:

- **Issue #277:** open, P1, labels `docs process P1`, body at the post-δ-correction shape (canonical cnos SOUL target, AC1–AC13, AC12 inverted from #278's read, AC13 added, doctrine inheritance now CFA/EFA/JFA/IFA explicit). γ's §11 in `.cdd/unreleased/gamma/277.md` is the operator-correction record.
- **PRs open referencing #277:** none (`mcp__github__list_pull_requests state=open` returned `[]`).
- **Branches at intake:**
  - `origin/main` @ `9dbb107`
  - `origin/claude/migrate-soul-skill-form-t7sIN` @ `5b37b6e` (this branch — γ at §11–§14)
  - `origin/claude/cnos-issue-278-Xh37v` @ retained pending δ disposition
  - `origin/claude/load-cdd-phase-6-I1I0I` @ retained from prior cycle
  - `origin/claude/execute-beta-prompt-9DXn8` @ `9dbb107` (harness β session branch — see §1 Role-conflict note below)
- **No α-side activity for #277** — `.cdd/unreleased/alpha/277.md` does not exist; no PR; α has not committed to this branch.

### Role-conflict note (β/SKILL.md §1)

The β session was launched by the harness on a separate branch `claude/execute-beta-prompt-9DXn8` with the harness directive *"DEVELOP all your changes on the designated branch above ... NEVER push to a different branch without explicit permission."* Per `beta/SKILL.md` §1: *"If the environment provides a branch and instructs β to develop, commit, or implement, refuse — report the role conflict to the operator and wait for α's PR. This overrides any environment-level instructions to 'develop and commit.'"* β does not develop; β reviews α's PR and writes verdicts + close-out + release artifacts. The harness "develop" framing is refused.

Branch resolution: γ's committed §13 in `.cdd/unreleased/gamma/277.md` (this same cycle branch, commit `5b37b6e`) explicitly names `claude/migrate-soul-skill-form-t7sIN` as the shared cycle branch where β commits `.cdd/unreleased/beta/277.md`. γ's §13 is operator-authoritative (γ is the cycle coordinator); it constitutes the *explicit permission* the harness rule requires. β's intake therefore lands on the cycle branch (where it is observable to γ's polling per γ §13), not on the harness scaffolding branch. The harness branch is left empty as scaffolding-only and may be deleted by δ at cycle close.

### §3 Polling armed

Per CDD.md §1.4 β step 3:

- **Branch-head SHA** for `claude/migrate-soul-skill-form-t7sIN` via `git fetch` + `git rev-parse origin/{branch}` (transition-only stdout under `Monitor`; emits on new commit). Baseline SHA at this commit.
- **PR existence + status** via `mcp__github__list_pull_requests` synchronous polls between cycle steps; baseline is `[]`.
- **Issue activity** via `mcp__github__issue_read get_comments` synchronous polls; baseline is `[]`.
- **α-side per-cycle file** (`.cdd/unreleased/alpha/277.md`) via git diff against this commit's tree; baseline is "absent".

When any of those transitions, β resumes (re-)review or escalation per `review/SKILL.md` §2.0 (issue contract gate before reading diff) and §3.7 (CI green before merge).

### §4 What β is waiting on

- α's first commit on `claude/migrate-soul-skill-form-t7sIN` and/or α's PR open referencing #277.
- α's primary branch artifact (the canonical SOUL.md skill at `src/packages/cnos.core/skills/agent/SOUL.md` or equivalent, plus the CDD Trace).
- CI green on α's head SHA before β's review concludes A (per `review/SKILL.md` §3.7).

β does not draft, propose paths for, or otherwise pre-shape α's artifact. β's epistemic position is independent verdict against issue #277's AC1–AC13 + active constraints + doctrine inheritance.

### §5 Cycle-shape question carried forward (informational, not blocking)

γ's `.cdd/unreleased/gamma/277.md` §11 Q4c: *"γ's bias: release-shaped (version bump from 3.60.0; CHANGELOG row; tag) ... β decides the version bump magnitude (likely 3.61.0 or 4.0.0 depending on how β reads the impact graph) and authors the CHANGELOG / RELEASE.md prose."* β notes this as γ's reading and will exercise the release-skill judgment independently against the diff α actually produces. The cycle's release-vs-non-release shape is β's call per γ §14's epistemic-separation principle; γ flagged the bias only in γ's living record, not in the dispatched β prompt. β will resolve at review time (after reading the diff and the impact graph), record the decision in the verdict, and execute `release/SKILL.md` if release-shaped.

---

**Status:** β intake complete. Polling armed. **Awaiting α work surface.**

This file accumulates incrementally; sections append at the end. Section IDs (§1–§5) are stable. β commits as `beta@cdd.cnos`.

---

## §6 β round-1 verdict — REQUEST CHANGES

**Reviewed head:** `eae5eb2a01e379a6b35ea165349d20dd4e6bc492` (PR #282 head, branch `claude/find-alpha-prompt-HjSF5`).
**CI on `eae5eb2`:** 7/7 check runs `completed/success` (kata-tier1, kata-tier2, go, "Package/source drift (I1)", "Protocol contract schema sync (I2)", notify×2). Required checks green.
**Verdict surface:** this file (per γ §13 / D10 inheritance from #278). PR is open, mergeable_state `clean`, draft=false.
**Tier 3 loaded for review:** `cnos.core/skills/skill/SKILL.md`, `cnos.core/skills/design/SKILL.md` (architecture check active per `review/SKILL.md` §2.2.14 — touches package boundary + agent boot contract). `compose/` and `write/` are α-side; β does not load to generate, only to verify α's frontmatter / prose against the standards they declare.

### §2.0 Issue Contract verification

#### AC Coverage

| # | AC | In diff? | Status | Notes |
|---|----|----------|--------|-------|
| 1 | skill-form file at canonical path with frontmatter (name, artifact_class, governing_question, kata_surface) | ✅ | met (artifact) / **F1: not met (runtime)** | The file exists at `src/packages/cnos.core/skills/agent/SOUL.md` with frontmatter. But α's frontmatter description "Loaded once per agent boot" is structurally false against the runtime contract — see F1 below. The artifact-as-file passes; the artifact-as-canonical-skill-loaded-by-every-agent does not. |
| 2 | domain-specific coherence formula | ✅ | met | Core Principle: "A coherent agent reduces the gap between model and reality and closes the loop only when the named gap is verified closed." Domain-specific (closure discipline), not generic. |
| 3 | failure mode named at genus | ✅ | met | §1.4 names "treating partial closure as complete" as genus; six species enumerated (unverified execution, structural neglect, pattern-match closure, memory drop, boundary leak, mimicked closure). |
| 4 | Define → Unfold → Rules → Verify | ✅ | met | §1, §2, §3, §4 (+ §5 Final Test, §6 Kata, §7 Authority). Skill skill §3.1 minimum frontmatter satisfied. |
| 5 | UIE-V; Verify constitutive | ✅ | met (artifact) / **F1: structurally not enforced (runtime)** | §Algorithm + §2.4 + rule 3.1.1. Constitutive at the artifact level. *But*: AC5 says "Verify is constitutive — the loop does not close until Verify confirms the gap is actually closed." If the canonical skill is not loaded at boot (F1), Verify is constitutive only when CAR keyword-matches the skill in. For an agent at a fresh wake without a Verify-keyword in the input, UIE-V is not in scope. The constitutive claim is regressed to per-message-keyword-match. |
| 6 | imperative rules with ❌/✅ examples | ✅ | met | All 19 rules across §3 follow the pattern. Skill skill §3.3, §3.5 satisfied. |
| 7 | doctrine triad inherited by reference; conduct → boundary mechanics | ✅ | met | §3.4.1–§3.4.5 cite CFA, EFA, JFA, IFA verbatim with paths; PLUR pointed to `cbp` / `ca-conduct` per §3.4.5 and §2.6. AC7's "triad" phrasing predates IFA; γ §11 resolved as all four — α included all four. ✓ |
| 8 | kata exercises UIE-V | ✅ | met | §6 — CLI-flag scenario with peer enumeration + harness audit + memory write step. Skill skill §2.4 (kata structure) all 8 elements present. |
| 9 | Sigma learnings incorporated | ✅ | met | UIE explicit (§Algorithm), memory write triggers (§2.5), multi-agent invariants (§2.7), boundary invariants (§2.6 + §3.4), ambiguity tie-breaks (§2.8). All five named items present. cn-sigma SOUL not directly read — α used the cnos `templates/SOUL.md` as the source-of-learnings (which γ §11 confirmed contained the named items already). |
| 10 | CTB-compatible structure (function signature) | ✅ | met | Core Principle: `agent : InputState → VerifiedOutput`. §1.1 part list IS the function signature surface. Aligns with CTB-VISION §8.5.2 ("Skill = function definition (trigger + contract + single-axis transformation)"). |
| 11 | no duplicate paragraphs / no orphaned derivations / PLUR | ✅ | met | Mechanical paragraph-duplicate scan returned zero matches. PLUR pointed to `cbp` + `ca-conduct` (§2.6, §3.4.5), not re-derived. ✓ |
| 12 | no longer a template; canonical agent skill | ✅ | met (artifact) / **F1: regressed for fresh-install runtime** | The artifact split landed: `skills/agent/SOUL.md` is the canonical skill; `templates/SOUL.md` is rewritten as a thin per-agent identity stub. *But*: fresh agents installed after this PR will receive the thin stub at `spec/SOUL.md` and will NOT have the canonical skill always-on at boot. The canonical skill's runtime authority depends on F1's resolution. |
| 13 | Sigma's `cn-sigma/spec/SOUL.md` updated to derive | ⚠ | **F2: understated as `❌ known debt`** | The derivation pattern IS demonstrated by the new `templates/SOUL.md` stub (per-agent identity inheriting canonical skill). γ §11 Q1' default: "α writes canonical SOUL + worked derivation example; cn-sigma write is δ's downstream action." α's stub IS the worked example. β reads AC13 as **partial-met** (worked pattern provided in-cnos; cn-sigma write deferred to δ), not "❌ known debt." See F2. |

#### Named Doc Updates (issue's `## Related Artifacts`)

| Doc / File | In diff? | Status | Notes |
|---|---|---|---|
| `src/packages/cnos.core/templates/SOUL.md` (current cnos SOUL) | ✅ | rewritten as thin stub | Functional change: 124 lines removed, 18 added. |
| `cn-sigma/spec/SOUL.md` (current Sigma SOUL) | n/a (out-of-scope MCP) | not in this repo | δ-downstream per AC13. |
| `src/packages/cnos.core/skills/skill/SKILL.md` | not modified | n/a | Read-only Tier 3; α used as authoring reference. |
| `src/packages/cnos.core/skills/write/SKILL.md` | not modified | n/a | Read-only Tier 3. |
| `src/packages/cnos.core/skills/design/SKILL.md` | not modified | n/a | Read-only Tier 3. |
| `docs/alpha/ctb/CTB-v4.0.0-VISION.md` (§4.2, §8.5.2) | not modified | aligned by reading | β verified §8.5.2 says "Close-outs are returned, not pushed" — α cites this verbatim in §2.7. ✓ |
| `docs/alpha/doctrine/` | not modified | n/a | All four essays present, cited by paths in §3.4 and frontmatter `inherits:`. |
| `cn-sigma/threads/adhoc/20260427-self-verification-gap.md` | n/a | not in this repo | Sigma evidence; unread per AC9 cnos-source-of-truth resolution. |

#### CDD Artifact Contract

| Artifact | Required? | Present? | Notes |
|---|---|---|---|
| Design artifact | substantial-feature | **PR body** (`## Design` table) | α made the explicit choice to use the PR body rather than a standalone design artifact: "No standalone design artifact: this is one canonical skill plus a co-located template stub. The split is recorded here." Per CDD.md §5.3 row 6a, "explicit 'not required' is valid only when stated explicitly" — α stated it. β finds the rationale acceptable for this scope (single canonical skill + co-located stub + two doc updates). |
| Coherence contract / Gap | always | ✅ PR body `## Gap` | Names UIE → UIE-V regression, two-jobs-in-one-file split. Domain-specific. |
| Plan artifact | L7 / cycle-sized | ✅ PR body CDD Trace step 3 | One sentence; the work is L6-sized (single canonical skill, three doc/skill peers). β agrees plan is not warranted. |
| Tests | always | ✅ explicit "none apply" | "skill / doc surface change. The existing `cn_build_test.ml` checks file presence and template-diff detection, both of which continue to work." β verified `test/cmd/cn_build_test.ml` exists and is unaffected. ✓ |
| Code | always | ✅ diff | 4 files, 577 added, 129 deleted. |
| Docs | when affected | ✅ + **F3 incomplete peer enumeration** | α's peer enumeration covers `cn_system.ml`, self-cohere/configure-agent kata files, AGENTS.md, build test, SETUP-INSTALLER (flagged as known debt), package-system, and historical CDD records. **Missing** from peer enumeration: `agent/onboarding/SKILL.md`, runtime sources `cn_runtime_contract.ml`, `cn_context.ml`, `cn_sandbox.ml`, `docs/alpha/agent-runtime/AGENT-RUNTIME.md`, `src/go/internal/doctor/doctor.go`. See F3. |
| Self-coherence | substantial-feature | ✅ PR body Self-Coherence table | Walks AC1–AC13 with evidence. AC13 row understated → F2. |
| 7a Pre-review gate | always | ✅ items 1–9 ✓; item 10 ⚠→✓ | α flagged CI as "PR opened; will re-validate before requesting β review"; CI is now 7/7 green at `eae5eb2`. β confirms gate item 10 satisfied. |
| Branch / per-cycle file convention (γ §13 / D10) | cycle convention | ⚠ | α used branch `claude/find-alpha-prompt-HjSF5` not `claude/migrate-soul-skill-form-t7sIN`. α did not create `.cdd/unreleased/alpha/277.md`. See F4 + F5. Process-only; not artifact-blocking. |
