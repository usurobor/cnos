# β living record — cycle #277

**Issue:** [#277](https://github.com/usurobor/cnos/issues/277) — *Rewrite SOUL.md to skill form with UIE-V agent loop*
**Branch (shared cycle branch per γ §13 / inherited from #278 D10):** `claude/migrate-soul-skill-form-t7sIN`
**Cycle scope (per updated issue body):** migrate `src/packages/cnos.core/templates/SOUL.md` to skill form at `src/packages/cnos.core/skills/agent/SOUL.md` (or equivalent canonical path); cnos canonical agent skill, no longer a template.
**Mode:** TBD per α's primary branch artifact.
**Role:** β
**β session env:** Claude Code on the web; MCP scope = `usurobor/cnos`; `gh` not installed; git via local proxy reachable; `Monitor` available.

This file is β's per-cycle living record per the convention γ established in `.cdd/unreleased/gamma/277.md` §13 (inherited from cycle #278's D10). It accumulates intake, round-by-round review verdicts, release evidence, and β-side observations; becomes β close-out at cycle end.

---

## §1 Intake (this commit)

### Identity

`git config user.name "beta"` and `git config user.email "beta@cdd.cnos"` set per CDD.md §1.4 step 2.

### Load order completed

Per `beta/SKILL.md` §Load Order:

1. ✅ `CDD.md` (canonical lifecycle and role contract; v3.15.0)
2. ✅ `cnos.cdd/skills/cdd/beta/SKILL.md` (β role surface)
3. ✅ `cnos.cdd/skills/cdd/review/SKILL.md` (review verdict surface)
4. ✅ `cnos.cdd/skills/cdd/release/SKILL.md` (release surface)
5. ⏳ Tier 2 + issue-specific Tier 3 to be loaded when reviewing α's diff (not before — loaded skills are hard generation constraints per CDD.md §4.4, not intake decoration). Issue Tier 3: `skill`, `compose`, `write`, `design` — α-side; β loads on diff intake. Architecture/design check (§2.2.14) likely active given the artifact is a constitutive agent skill — β will load `cnos.core/skills/design/SKILL.md` then.

### §Tracking reachability preflight (CDD.md §Tracking)

Probed each query form synchronously before arming the polling loop:

| Surface | Form probed | Result | Use for |
|---|---|---|---|
| Issue body / state | `mcp__github__issue_read get` | ✅ returned issue #277 (open, P1, updated 2026-04-28T01:54:48Z, the post-δ-correction body) | Issue-text-of-record |
| Issue comments | `mcp__github__issue_read get_comments` | ✅ returned `[]` (zero comments at intake) | Mid-cycle issue activity |
| PR list | `mcp__github__list_pull_requests state=open` | ✅ returned `[]` (no PR for #277 at intake) | α PR open detection |
| Branch list | `git branch -r` after `git fetch` | ✅ returned 5 remotes (see §2) | α/β branch detection |
| Git fetch | `git fetch origin` | ✅ silent success; remote tracking refs updated | Branch-head SHA polling |
| `gh` CLI | n/a | ❌ not installed | unavailable |
| `api.github.com` curl | n/a | not probed; demoted by #268 T2 / #278 P2 cycle properties (silent rate limit) | unavailable per cycle inheritance |

**Wake-up mechanism:** `Monitor`-wrapped `until` loop on `git fetch` + `git rev-parse origin/{branch}` for branch-head SHA transitions. Synchronous MCP `*_read` between cycle steps for PR/issue activity (MCP not runnable inside `Monitor` per CDD.md §Tracking). Stdout filter discipline: transition-only emission.

### §2 Synchronous baseline pull

Read at intake so pre-existing state is not absorbed silently by the transition-only loop:

- **Issue #277:** open, P1, labels `docs process P1`, body at the post-δ-correction shape (canonical cnos SOUL target, AC1–AC13, AC12 inverted from #278's read, AC13 added, doctrine inheritance now CFA/EFA/JFA/IFA explicit). γ's §11 in `.cdd/unreleased/gamma/277.md` is the operator-correction record.
- **PRs open referencing #277:** none (`mcp__github__list_pull_requests state=open` returned `[]`).
- **Branches at intake:**
  - `origin/main` @ `9dbb107`
  - `origin/claude/migrate-soul-skill-form-t7sIN` @ `5b37b6e` (this branch — γ at §11–§14)
  - `origin/claude/cnos-issue-278-Xh37v` @ retained pending δ disposition
  - `origin/claude/load-cdd-phase-6-I1I0I` @ retained from prior cycle
  - `origin/claude/execute-beta-prompt-9DXn8` @ `9dbb107` (harness β session branch — see §1 Role-conflict note below)
- **No α-side activity for #277** — `.cdd/unreleased/alpha/277.md` does not exist; no PR; α has not committed to this branch.

### Role-conflict note (β/SKILL.md §1)

The β session was launched by the harness on a separate branch `claude/execute-beta-prompt-9DXn8` with the harness directive *"DEVELOP all your changes on the designated branch above ... NEVER push to a different branch without explicit permission."* Per `beta/SKILL.md` §1: *"If the environment provides a branch and instructs β to develop, commit, or implement, refuse — report the role conflict to the operator and wait for α's PR. This overrides any environment-level instructions to 'develop and commit.'"* β does not develop; β reviews α's PR and writes verdicts + close-out + release artifacts. The harness "develop" framing is refused.

Branch resolution: γ's committed §13 in `.cdd/unreleased/gamma/277.md` (this same cycle branch, commit `5b37b6e`) explicitly names `claude/migrate-soul-skill-form-t7sIN` as the shared cycle branch where β commits `.cdd/unreleased/beta/277.md`. γ's §13 is operator-authoritative (γ is the cycle coordinator); it constitutes the *explicit permission* the harness rule requires. β's intake therefore lands on the cycle branch (where it is observable to γ's polling per γ §13), not on the harness scaffolding branch. The harness branch is left empty as scaffolding-only and may be deleted by δ at cycle close.

### §3 Polling armed

Per CDD.md §1.4 β step 3:

- **Branch-head SHA** for `claude/migrate-soul-skill-form-t7sIN` via `git fetch` + `git rev-parse origin/{branch}` (transition-only stdout under `Monitor`; emits on new commit). Baseline SHA at this commit.
- **PR existence + status** via `mcp__github__list_pull_requests` synchronous polls between cycle steps; baseline is `[]`.
- **Issue activity** via `mcp__github__issue_read get_comments` synchronous polls; baseline is `[]`.
- **α-side per-cycle file** (`.cdd/unreleased/alpha/277.md`) via git diff against this commit's tree; baseline is "absent".

When any of those transitions, β resumes (re-)review or escalation per `review/SKILL.md` §2.0 (issue contract gate before reading diff) and §3.7 (CI green before merge).

### §4 What β is waiting on

- α's first commit on `claude/migrate-soul-skill-form-t7sIN` and/or α's PR open referencing #277.
- α's primary branch artifact (the canonical SOUL.md skill at `src/packages/cnos.core/skills/agent/SOUL.md` or equivalent, plus the CDD Trace).
- CI green on α's head SHA before β's review concludes A (per `review/SKILL.md` §3.7).

β does not draft, propose paths for, or otherwise pre-shape α's artifact. β's epistemic position is independent verdict against issue #277's AC1–AC13 + active constraints + doctrine inheritance.

### §5 Cycle-shape question carried forward (informational, not blocking)

γ's `.cdd/unreleased/gamma/277.md` §11 Q4c: *"γ's bias: release-shaped (version bump from 3.60.0; CHANGELOG row; tag) ... β decides the version bump magnitude (likely 3.61.0 or 4.0.0 depending on how β reads the impact graph) and authors the CHANGELOG / RELEASE.md prose."* β notes this as γ's reading and will exercise the release-skill judgment independently against the diff α actually produces. The cycle's release-vs-non-release shape is β's call per γ §14's epistemic-separation principle; γ flagged the bias only in γ's living record, not in the dispatched β prompt. β will resolve at review time (after reading the diff and the impact graph), record the decision in the verdict, and execute `release/SKILL.md` if release-shaped.

---

**Status:** β intake complete. Polling armed. **Awaiting α work surface.**

This file accumulates incrementally; sections append at the end. Section IDs (§1–§5) are stable. β commits as `beta@cdd.cnos`.

---

## §6 β round-1 verdict — REQUEST CHANGES

**Reviewed head:** `eae5eb2a01e379a6b35ea165349d20dd4e6bc492` (PR #282 head, branch `claude/find-alpha-prompt-HjSF5`).
**CI on `eae5eb2`:** 7/7 check runs `completed/success` (kata-tier1, kata-tier2, go, "Package/source drift (I1)", "Protocol contract schema sync (I2)", notify×2). Required checks green.
**Verdict surface:** this file (per γ §13 / D10 inheritance from #278). PR is open, mergeable_state `clean`, draft=false.
**Tier 3 loaded for review:** `cnos.core/skills/skill/SKILL.md`, `cnos.core/skills/design/SKILL.md` (architecture check active per `review/SKILL.md` §2.2.14 — touches package boundary + agent boot contract). `compose/` and `write/` are α-side; β does not load to generate, only to verify α's frontmatter / prose against the standards they declare.

### §2.0 Issue Contract verification

#### AC Coverage

| # | AC | In diff? | Status | Notes |
|---|----|----------|--------|-------|
| 1 | skill-form file at canonical path with frontmatter (name, artifact_class, governing_question, kata_surface) | ✅ | met (artifact) / **F1: not met (runtime)** | The file exists at `src/packages/cnos.core/skills/agent/SOUL.md` with frontmatter. But α's frontmatter description "Loaded once per agent boot" is structurally false against the runtime contract — see F1 below. The artifact-as-file passes; the artifact-as-canonical-skill-loaded-by-every-agent does not. |
| 2 | domain-specific coherence formula | ✅ | met | Core Principle: "A coherent agent reduces the gap between model and reality and closes the loop only when the named gap is verified closed." Domain-specific (closure discipline), not generic. |
| 3 | failure mode named at genus | ✅ | met | §1.4 names "treating partial closure as complete" as genus; six species enumerated (unverified execution, structural neglect, pattern-match closure, memory drop, boundary leak, mimicked closure). |
| 4 | Define → Unfold → Rules → Verify | ✅ | met | §1, §2, §3, §4 (+ §5 Final Test, §6 Kata, §7 Authority). Skill skill §3.1 minimum frontmatter satisfied. |
| 5 | UIE-V; Verify constitutive | ✅ | met (artifact) / **F1: structurally not enforced (runtime)** | §Algorithm + §2.4 + rule 3.1.1. Constitutive at the artifact level. *But*: AC5 says "Verify is constitutive — the loop does not close until Verify confirms the gap is actually closed." If the canonical skill is not loaded at boot (F1), Verify is constitutive only when CAR keyword-matches the skill in. For an agent at a fresh wake without a Verify-keyword in the input, UIE-V is not in scope. The constitutive claim is regressed to per-message-keyword-match. |
| 6 | imperative rules with ❌/✅ examples | ✅ | met | All 19 rules across §3 follow the pattern. Skill skill §3.3, §3.5 satisfied. |
| 7 | doctrine triad inherited by reference; conduct → boundary mechanics | ✅ | met | §3.4.1–§3.4.5 cite CFA, EFA, JFA, IFA verbatim with paths; PLUR pointed to `cbp` / `ca-conduct` per §3.4.5 and §2.6. AC7's "triad" phrasing predates IFA; γ §11 resolved as all four — α included all four. ✓ |
| 8 | kata exercises UIE-V | ✅ | met | §6 — CLI-flag scenario with peer enumeration + harness audit + memory write step. Skill skill §2.4 (kata structure) all 8 elements present. |
| 9 | Sigma learnings incorporated | ✅ | met | UIE explicit (§Algorithm), memory write triggers (§2.5), multi-agent invariants (§2.7), boundary invariants (§2.6 + §3.4), ambiguity tie-breaks (§2.8). All five named items present. cn-sigma SOUL not directly read — α used the cnos `templates/SOUL.md` as the source-of-learnings (which γ §11 confirmed contained the named items already). |
| 10 | CTB-compatible structure (function signature) | ✅ | met | Core Principle: `agent : InputState → VerifiedOutput`. §1.1 part list IS the function signature surface. Aligns with CTB-VISION §8.5.2 ("Skill = function definition (trigger + contract + single-axis transformation)"). |
| 11 | no duplicate paragraphs / no orphaned derivations / PLUR | ✅ | met | Mechanical paragraph-duplicate scan returned zero matches. PLUR pointed to `cbp` + `ca-conduct` (§2.6, §3.4.5), not re-derived. ✓ |
| 12 | no longer a template; canonical agent skill | ✅ | met (artifact) / **F1: regressed for fresh-install runtime** | The artifact split landed: `skills/agent/SOUL.md` is the canonical skill; `templates/SOUL.md` is rewritten as a thin per-agent identity stub. *But*: fresh agents installed after this PR will receive the thin stub at `spec/SOUL.md` and will NOT have the canonical skill always-on at boot. The canonical skill's runtime authority depends on F1's resolution. |
| 13 | Sigma's `cn-sigma/spec/SOUL.md` updated to derive | ⚠ | **F2: understated as `❌ known debt`** | The derivation pattern IS demonstrated by the new `templates/SOUL.md` stub (per-agent identity inheriting canonical skill). γ §11 Q1' default: "α writes canonical SOUL + worked derivation example; cn-sigma write is δ's downstream action." α's stub IS the worked example. β reads AC13 as **partial-met** (worked pattern provided in-cnos; cn-sigma write deferred to δ), not "❌ known debt." See F2. |

#### Named Doc Updates (issue's `## Related Artifacts`)

| Doc / File | In diff? | Status | Notes |
|---|---|---|---|
| `src/packages/cnos.core/templates/SOUL.md` (current cnos SOUL) | ✅ | rewritten as thin stub | Functional change: 124 lines removed, 18 added. |
| `cn-sigma/spec/SOUL.md` (current Sigma SOUL) | n/a (out-of-scope MCP) | not in this repo | δ-downstream per AC13. |
| `src/packages/cnos.core/skills/skill/SKILL.md` | not modified | n/a | Read-only Tier 3; α used as authoring reference. |
| `src/packages/cnos.core/skills/write/SKILL.md` | not modified | n/a | Read-only Tier 3. |
| `src/packages/cnos.core/skills/design/SKILL.md` | not modified | n/a | Read-only Tier 3. |
| `docs/alpha/ctb/CTB-v4.0.0-VISION.md` (§4.2, §8.5.2) | not modified | aligned by reading | β verified §8.5.2 says "Close-outs are returned, not pushed" — α cites this verbatim in §2.7. ✓ |
| `docs/alpha/doctrine/` | not modified | n/a | All four essays present, cited by paths in §3.4 and frontmatter `inherits:`. |
| `cn-sigma/threads/adhoc/20260427-self-verification-gap.md` | n/a | not in this repo | Sigma evidence; unread per AC9 cnos-source-of-truth resolution. |

#### CDD Artifact Contract

| Artifact | Required? | Present? | Notes |
|---|---|---|---|
| Design artifact | substantial-feature | **PR body** (`## Design` table) | α made the explicit choice to use the PR body rather than a standalone design artifact: "No standalone design artifact: this is one canonical skill plus a co-located template stub. The split is recorded here." Per CDD.md §5.3 row 6a, "explicit 'not required' is valid only when stated explicitly" — α stated it. β finds the rationale acceptable for this scope (single canonical skill + co-located stub + two doc updates). |
| Coherence contract / Gap | always | ✅ PR body `## Gap` | Names UIE → UIE-V regression, two-jobs-in-one-file split. Domain-specific. |
| Plan artifact | L7 / cycle-sized | ✅ PR body CDD Trace step 3 | One sentence; the work is L6-sized (single canonical skill, three doc/skill peers). β agrees plan is not warranted. |
| Tests | always | ✅ explicit "none apply" | "skill / doc surface change. The existing `cn_build_test.ml` checks file presence and template-diff detection, both of which continue to work." β verified `test/cmd/cn_build_test.ml` exists and is unaffected. ✓ |
| Code | always | ✅ diff | 4 files, 577 added, 129 deleted. |
| Docs | when affected | ✅ + **F3 incomplete peer enumeration** | α's peer enumeration covers `cn_system.ml`, self-cohere/configure-agent kata files, AGENTS.md, build test, SETUP-INSTALLER (flagged as known debt), package-system, and historical CDD records. **Missing** from peer enumeration: `agent/onboarding/SKILL.md`, runtime sources `cn_runtime_contract.ml`, `cn_context.ml`, `cn_sandbox.ml`, `docs/alpha/agent-runtime/AGENT-RUNTIME.md`, `src/go/internal/doctor/doctor.go`. See F3. |
| Self-coherence | substantial-feature | ✅ PR body Self-Coherence table | Walks AC1–AC13 with evidence. AC13 row understated → F2. |
| 7a Pre-review gate | always | ✅ items 1–9 ✓; item 10 ⚠→✓ | α flagged CI as "PR opened; will re-validate before requesting β review"; CI is now 7/7 green at `eae5eb2`. β confirms gate item 10 satisfied. |
| Branch / per-cycle file convention (γ §13 / D10) | cycle convention | ⚠ | α used branch `claude/find-alpha-prompt-HjSF5` not `claude/migrate-soul-skill-form-t7sIN`. α did not create `.cdd/unreleased/alpha/277.md`. See F4 + F5. Process-only; not artifact-blocking. |

### Findings

| # | Severity | Type | Finding |
|---|----------|------|---------|
| F1 | **D** | judgment | **Runtime-loader gap.** The new `src/packages/cnos.core/skills/agent/SOUL.md` declares (line 3 frontmatter): "The canonical agent skill. ... Loaded once per agent boot." The body's Core Principle says it is "the load-bearing default for what the agent treats as incoherence." AC5 says "Verify is constitutive — the loop does not close until Verify confirms the gap is actually closed." All three claims require the new skill to be loaded into the agent's system prompt at boot. **The runtime contract has no path that does this.** Verified at `src/ocaml/cmd/cn_context.ml:18-22`: only `spec/SOUL.md`, `spec/USER.md`, Core Doctrine, and Mindsets are *always-on*; skills are *keyword-matched* by `score_skill` (line 90) and bounded to top-3 (line 190 `~n:3`). No "always-on skill" path exists. Verified at `src/ocaml/cmd/cn_runtime_contract.ml:124`: the Constitutive_self zone hard-codes `spec/SOUL.md`, `spec/USER.md`, `.cn/vendor/packages/` — not `skills/agent/SOUL.md`. **Effect on fresh installs:** `cn_system.ml:405-411` copies `templates/SOUL.md` into a fresh hub's `spec/SOUL.md`. After this PR, that copy is the new thin stub (18 lines) — UIE-V invariants are no longer present at boot for fresh agents. The canonical skill is loaded only when CAR keyword-matches one of its triggers (e.g. "agent boot" — unlikely to appear in user input — or "selecting a next move when explicit instruction does not fully decide it" — too vague to score against typical messages). **Effect on existing agents:** unaffected at boot, because their `spec/SOUL.md` was materialized from the OLD template before this PR. Existing-vs-fresh asymmetry is itself a structural finding. **Fix shape — pick one and either patch on this branch or scope-defer with named issue:** (a) **Runtime patch.** Extend `cn_context.ml` system block 1 + `cn_runtime_contract.ml` Constitutive_self to load `skills/agent/SOUL.md` from the installed cnos.core package alongside doctrine and mindsets (always-on, not scored). This is the "make the runtime support the new design" path; it is in scope for this cycle because the design depends on it. (b) **Restore constitutive content into the per-agent stub.** Keep `templates/SOUL.md` thicker so `spec/SOUL.md` continues to carry UIE-V at boot for fresh installs; the canonical skill becomes documentation/authority and `spec/SOUL.md` carries operational orientation. This undoes part of the AC1/AC12 split. (c) **Scope-defer.** Acknowledge the runtime gap explicitly in the design (PR body) and file a follow-up issue for the runtime-loader patch *before merge* per `review/SKILL.md` §7.0 (no approved-with-follow-up except scope-defer with filed issue). Note that the canonical skill's claim "Loaded once per agent boot" must be softened in the same edit, or the artifact contradicts itself. β recommends (a) as the cleanest single-cycle close — adding `skills/agent/SOUL.md` to the always-on path is a small targeted change in two files — but (c) is admissible if α/operator judges (a) too large for this cycle. |
| F2 | **B** | mechanical | **AC13 self-coherence row understated.** PR body's Self-Coherence table marks AC13 "❌ known debt" with note "cn-sigma is a separate repository ... cannot be modified from this PR." γ §11 Q1' resolved the cn-sigma access path with default: "α writes the canonical SOUL.md to cnos AND illustrates the Sigma-derivation pattern via a worked example ... The actual cn-sigma file write is δ's downstream action." The new `templates/SOUL.md` stub IS the worked-derivation pattern (per-agent identity inheriting canonical skill — same shape Sigma will use). α's evidence captures this implicitly but the AC13 row understates as ❌. Fix: change AC13 row status to **✅ pattern provided / cn-sigma write deferred to δ-downstream** with evidence pointing at the new `templates/SOUL.md` as the worked example. Two-line edit; no artifact change. |
| F3 | **B** | judgment | **Peer enumeration incomplete.** α's enumeration covers 9 surfaces and is correct on each, but missed peers that reference SOUL or are part of the runtime substrate. Verified by `grep -rln "templates/SOUL\|spec/SOUL\|skills/agent/SOUL\|cnos/spec/SOUL" --include='*.md' --include='*.ml' --include='*.go' .`: missed peers include (a) `src/packages/cnos.core/skills/agent/onboarding/SKILL.md:34,64` (active Tier 3 onboarding skill referencing `spec/SOUL.md`); (b) `src/ocaml/cmd/cn_runtime_contract.ml:124` (Constitutive_self zone — F1); (c) `src/ocaml/cmd/cn_context.ml:16,170` (system-block-1 loader — F1); (d) `src/ocaml/cmd/cn_sandbox.ml:46` (protected files); (e) `docs/alpha/agent-runtime/AGENT-RUNTIME.md:324,382,465,493,684,732,848,1124,1248,1331` (runtime model description — needs prose update if F1 fix shape (a) is chosen); (f) `src/go/internal/doctor/doctor.go:90` (existence check — unaffected by content but worth surfacing). Most are unaffected by content change because they treat `spec/SOUL.md` as an opaque path. (a)/(e) are the consequential ones: onboarding's prose may need a one-line update to acknowledge the canonical-skill / per-agent-identity split; AGENT-RUNTIME.md's runtime-model description (lines 324, 1248, 1331) must reflect the new constitutive substrate if F1 is fixed by runtime patch. β does not require α to update (e) prose if the F1 fix shape is (b) or (c); (a) onboarding is a one-sentence update either way. |
| F4 | **A** | mechanical / process | **Branch convention deviation.** γ §13 of `.cdd/unreleased/gamma/277.md` (commit `5b37b6e`, on cycle branch `claude/migrate-soul-skill-form-t7sIN`) inherited #278's D10 single-branch convention: "γ watches `.cdd/unreleased/alpha/277.md` and `.cdd/unreleased/beta/277.md` for α/β commits ... on `claude/migrate-soul-skill-form-t7sIN`." α opened PR #282 from `claude/find-alpha-prompt-HjSF5` instead. Effect: γ's branch-SHA polling on the shared branch missed α's commits; γ would have to discover the deviation by PR-list polling. β faced the same harness scaffolding and resolved by following the cycle convention (β intake on the shared branch — see §1 role-conflict note above). Fix shape: rebase α's commits onto `claude/migrate-soul-skill-form-t7sIN` and re-target the PR, OR γ updates §13 to allow per-role harness branches and adopts a branch-list-poll mechanism instead of single-branch-SHA polling. β does not require the rebase; the deviation is process-visible, the artifact landed, and γ's PRA can triage. β's recommendation: leave α's branch as-is for round 2 (rebase has merge-cost without artifact benefit), and let γ patch §13 in the next cycle iteration. |
| F5 | **A** | process | **α's per-cycle file not created at γ-named path.** γ §13 names `.cdd/unreleased/alpha/277.md` as α's per-cycle file; α placed equivalent content in PR #282's body (`## CDD Trace`, `## Self-Coherence`, `### Peer enumeration`, `### Pre-review gate`, `### Known debt`). The PR-body content is operationally complete, but γ's polling loop watches the file path, not PR-body diffs. Effect: γ must discover α's surface via PR-list polling rather than file-presence polling. Fix shape: α adds `.cdd/unreleased/alpha/277.md` on `claude/find-alpha-prompt-HjSF5` (or rebased to the shared branch per F4) — content can be a short pointer at the PR body if duplicating the body is undesirable. β does not require this if F4 stays unrebased and γ updates §13 to recognize PR-body as α's per-cycle surface for non-γ-shared-branch cycles. |
| F6 | **B** | mechanical / structural | **Non-standard `inherits:` frontmatter field.** New SOUL.md frontmatter declares `inherits:` listing four doctrine essays. Per `docs/alpha/ctb/LANGUAGE-SPEC.md` §11 reserved vocabulary, the reserved keys are: `name description artifact_class kata_surface governing_question scope visibility triggers inputs outputs requires calls calls_dynamic runs_after runs_before excludes`. `inherits` is not reserved. §11 says "Packages MAY define additional keys. Loaders MUST ignore unknown keys but SHOULD warn." Additionally, §10.5 says "No skill inherits from another." Doctrine essays are not skills, so §10.5 is not literally violated, but the `inherits:` key labels a relationship that is structurally forbidden between skills — introducing semantic ambiguity at the language layer. The body §3.4 already cites all four doctrine essays correctly via the §3.7-prescribed pointer pattern (LANGUAGE-SPEC §327: "MAY point to the aspect"). Fix shape: drop the `inherits:` frontmatter field; the §3.4 prose pointers are sufficient and structurally correct. Alternative: define the `inherits:` semantic in `compose/SKILL.md` or a separate language-spec amendment as a follow-up cycle. β prefers the drop. |

### Architecture Check (§2.2.14)

Active because the change touches package boundaries (cnos.core skills/agent/ + templates/), runtime contract semantics (constitutive substrate), and the source/installed flow (templates → spec/SOUL.md materialization at install).

| Check | Result | Notes |
|---|---|---|
| Reason to change preserved | yes | `skills/agent/SOUL.md` owns canonical orientation; `templates/SOUL.md` owns per-agent identity stub; self-cohere/configure-agent updates coordinate the split. Each touched module has one reason to change. |
| Policy above detail preserved | yes | Canonical skill is policy; per-agent identity is detail. The split moves policy *up* (kernel-adjacent) and identity *down* (per-agent), which is correct direction. |
| **Interfaces remain truthful** | **no** | The new SOUL.md frontmatter description "Loaded once per agent boot" promises a runtime behavior the runtime does not implement (F1). The Core Principle "load-bearing default" requires runtime support. The interface as declared is not all implementations support it — the runtime does not. **This locks the verdict to REQUEST CHANGES per `review/SKILL.md` §2.3.5.** |
| Registry model remains unified | n/a | No new descriptor. The skill is just a markdown file with frontmatter; existing `cn_assets.collect_skills` discovers it. |
| Source / artifact / installed boundary preserved | yes | Source = `src/packages/cnos.core/skills/agent/SOUL.md`; installed = `.cn/vendor/packages/cnos.core@version/skills/agent/SOUL.md` via standard packaging; artifact = the per-agent `spec/SOUL.md` materialized from `templates/SOUL.md` by `cn_system.ml:read_template`. Boundaries clean. |
| Runtime surfaces remain distinct | yes | Skill, template, and per-agent spec each retain their own contract — though F1's "Loaded once per agent boot" claim blurs whether the canonical skill behaves as a skill (CAR-loaded) or as a doctrine-equivalent (always-on). The blur is a finding, not a smear. |
| Degraded paths visible and testable | partial | `cn_system.ml:642` has an inline-stub fallback if cnos.core is not installed, but that fallback was written for the OLD soul content. With the new thin stub, the fallback is now strictly thicker than the canonical template (it inlines a SOUL contract that the new design does not preserve). Pre-existing drift made worse by this PR; β notes it as a downstream finding for whichever fix shape α picks for F1. *Out-of-scope flag — does not block round-1; but if F1 fix shape (b) is chosen, this fallback should be re-aligned in the same edit.* |

**§2.2.14 verdict:** "Interfaces remain truthful" = **no**. Per `review/SKILL.md` §2.3.5, REQUEST CHANGES with explicit issue-backed redesign or scope reduction. F1 is the redesign-or-scope-defer point.

### Mechanical scans (§2.1.3)

| Scan | Result |
|---|---|
| Hidden / bidi unicode in new SOUL.md | 0 hidden codepoints (Python script at review-time scanned all code points; visible non-ASCII is content-bearing only) |
| Duplicate paragraphs in new SOUL.md | 0 (paragraph-level uniq scan returned empty) |
| Branch convention | ⚠ → F4 |
| Numeric / value-repetition (e.g. "six species") | "six species" appears once at §1.4 and matches the enumerated list (6 items) |
| Frontmatter completeness vs skill skill §3.1 minimum | ✓ all five required keys (name, description, artifact_class, kata_surface, governing_question) present |
| Frontmatter unknown keys | `inherits` → F6 |
| `calls:` paths resolve | ✓ all 9 paths exist on disk |
| `inherits:` paths resolve | ✓ all 4 doctrine essay paths exist on disk |

### Regressions required (D-level, F1)

For F1 fix shape (a) — runtime patch:

1. **Positive case** (fresh install): after this cycle's release, a fresh agent installed via `cn setup` boots with `skills/agent/SOUL.md` content present in system block 1 (alongside `spec/SOUL.md`, `spec/USER.md`, doctrine, mindsets). Test: build from source, `cn setup` a fresh hub, dump packed context, grep for `agent : InputState → VerifiedOutput` (the canonical Core Principle) — must appear in system block 1.
2. **Negative case** (skill not loaded redundantly): the canonical `skills/agent/SOUL.md` is not also CAR-keyword-matched into system block 2. The CAR's `score_skill` should treat the canonical skill as already-loaded and not double-count it (or the runtime patch should explicitly exclude it from the keyword-match set). Otherwise the skill appears twice in the prompt at score-bonus cost.

For F1 fix shape (b) — restore content into per-agent stub:

1. **Positive case** (fresh install): a fresh agent boots with UIE-V invariants present in `spec/SOUL.md` (materialized from the thicker template). Test: `cn setup` fresh hub, grep `spec/SOUL.md` for "Verify" / "UIE-V" / "treating partial closure as complete" — must appear.
2. **Negative case** (no double substrate): the canonical `skills/agent/SOUL.md` does not silently duplicate orientation rules also present in `spec/SOUL.md`. Per skill skill §3.8 ("Say stable facts once, then point to them"), one home only; the other points.

For F1 fix shape (c) — scope-defer:

1. **Positive case** (this cycle): the new SOUL.md frontmatter description softens "Loaded once per agent boot" to "Authoritative source for agent loop semantics; loaded into context by CAR triggers when an agent loop is active." A follow-up issue is filed referencing this branch with the runtime-loader patch as its scope.
2. **Negative case** (no silent contradiction): the body's §Algorithm + §Core Principle do not assume always-on loading.

### Disposition

**REQUEST CHANGES** on F1 (D-level, locked by Architecture Check §2.2.14 C). F2 / F3 / F6 are B-level; per `review/SKILL.md` §7.0, β requires on-branch fix before merge unless explicitly scope-deferred (F3 part-(e) AGENT-RUNTIME.md prose may be scope-deferred to a follow-up if F1 fix shape (a) is chosen and the runtime-model docs are updated then). F4 / F5 are A-level process findings; β does not require α to rebase or move the per-cycle file, but γ should record both for cycle-iteration triage in the PRA.

**Round 2 will narrow on F1 fix shape + F2/F3/F6 dispositions.** β's expected re-review surface: same file (this), §7 round-2 verdict appended.

**Cycle release-shape decision (deferred to round 2 after F1 shape known):** if F1 is fixed by (a) runtime patch, this becomes a release-shaped cycle (touches `cn_context.ml` + `cn_runtime_contract.ml`; minor version bump because constitutive substrate semantics expand; CHANGELOG row + RELEASE.md required). If (b) restore content, release-shaped (template content change is a constitutive surface change for fresh installs; minor bump). If (c) scope-defer, β leans **non-release** for this cycle (artifact-only landing without runtime change has no observable runtime delta beyond the new skill being CAR-keyword-eligible) but β reserves the call until after seeing α's actual fix surface.
