# β review — cycle/574

**Verdict:** APPROVED

**Round:** 1 (R0 — first review pass; this cell stranded before ever reaching β, per `REPAIR-PLAN.md`, so this is not a re-review)
**Fixed this round:** n/a (first pass)
**Branch CI state:** green on review SHA (see §CI status — one transient, self-resolving I6 failure explained below; all seven named gate jobs green)
**Merge instruction:** `git merge cycle/574` into `main` with `Closes #574` in the merge commit subject (β does not perform this merge — repo convention on this branch is human-operator PR acceptance, not autonomous β merge; see §Notes)

**Review base/head:** `origin/main@80be1280` (fetched synchronously at review time; two harmless `board-map` doc-regen commits ahead of γ's/α's recorded base, non-propagating per γ's own precedent) vs. `origin/cycle/574@ad0b18e1` (δ's `REPAIR-PLAN.md` commit, HEAD at review start).

## §2.0.0 Contract Integrity

| Check | Result | Notes |
|---|---|---|
| Status truth preserved | yes | #567/#568/#569/#570 independently re-verified CLOSED/COMPLETED, clean labels (see §AC1 below) |
| Canonical sources/paths verified | yes | `transitions.json`, `fetch.go`, `table.go` all read at current HEAD, matched against self-coherence.md's claims line-for-line |
| Scope/non-goals consistent | yes | Phase 3 not implemented (only filed, #575); `cell_kind` not touched; no new status labels; no controller service/daemon |
| Constraint strata consistent | yes | Implementation contract's 7 axes all conform (see §Implementation contract below) |
| Exceptions field-specific/reasoned | yes | AC4's design choice (option (b) over (a)) is reasoned and documented in both `self-coherence.md` and `fetch.go`'s own doc comment |
| Path resolution base explicit | yes | All file paths in the diff resolve from repo root as expected |
| Proof shape adequate | yes | TDD red→green sequence for AC2/AC3 independently re-derivable from test names + fixture contents; AC4 hermetic tests independently read and confirmed to test what they claim |
| Cross-surface projections updated | yes | `#567` correction comment + `#575` tracking issue both independently confirmed live and linked |
| No witness theater / false closure | yes | See §Findings — one minor test-naming precision issue found and judged non-blocking, with reasoning stated, not silently passed over |
| PR body matches branch files | n/a | No PR opened yet (δ opens PR after this verdict, per dispatch instructions) |
| γ artifacts present (gamma-scaffold.md) | yes | `.cdd/unreleased/574/gamma-scaffold.md` present on `origin/cycle/574`, full 7-AC oracle list + implementation contract — rule 3.11b satisfied |

## §2.0 Issue Contract

### AC Coverage

| # | AC | In diff? | Status | Notes |
|---|----|----------|--------|-------|
| AC1 | Authenticated wave-issue-state verification + discrepancy record | n/a (verification task, no code diff) | **MET — independently confirmed** | Re-ran `gh issue view {567,568,569,570} --json state,stateReason,labels` myself; all four CLOSED/COMPLETED, labels byte-for-byte match `self-coherence.md`'s recorded table. Spot-checked the public-render claim myself: `curl -sSL https://github.com/usurobor/cnos/issues/567` shows `data-testid="header-state" ... data-status="issueClosed"` — matches α's claimed selector and result, no discrepancy. |
| AC2 | `review`-state validity requires deliverable matter, not review-request alone | yes | **MET — independently confirmed** | `git diff origin/main..HEAD -- transitions.json` read directly: rule 1 tightened to `all_true:[review_request_present,pr_exists,pr_has_commits]`; rule 2 rewritten as an **unconditional** second rule (no guard clause at all) rather than a naive `all_false` complement — mechanically verified this actually works as claimed by reading `ruleMatches`/`Evaluate` in `table.go` directly: an empty `AllTrue`/`AllFalse`/`AnyTrue` rule trivially matches, so rule 2 correctly catches every partial-evidence case rule 1 didn't match, with a populated `evidence_guards` list. Ran `go test ./... -v -race` in `issues-fsm`: `TestAC574_ReviewRequestAloneNoLongerValid`, `TestAC574_ReviewPartialEvidenceBlocked`, `TestAC574_ReviewWithPRStillValid`, `TestAC3_EmptyReviewBlocked` (updated assertion set) all PASS. Read `review-with-pr.json`'s diff directly: `review_request_present` flipped `false→true` + `REVIEW-REQUEST.yml` added to `cdd_artifacts` — a deliberate, documented fixture-value update to keep it a valid full-evidence case under the tightened 3-guard invariant, not a silent weakening. I independently agree with this judgment call: the issue body's own Status-truth table is the more authoritative source for the 3-guard invariant text, and updating the pre-existing fixture's field values (rather than weakening the guard back to 2 guards) is the correct resolution — the alternative (a 2-guard `all_true`) would under-implement the stated invariant. |
| AC3 | `in-progress → review` requires a PR | yes | **MET — independently confirmed** | Same diff read directly: rule 1 tightened to `all_true:[review_request_present,pr_exists,pr_has_commits]` (dropping `branch_has_commits` from qualifying alone); rule 2 simplified to `all_true:[review_request_present]` alone (unconditional given that gate, for the same reason as AC2). Read `in-progress-review-request-with-matter.json` (`pr_exists:true, pr_commit_count:5`) and `-no-matter.json` (`pr_exists:false, branch_exists:false`) directly — confirmed neither needed a fixture-value or expected-outcome update, exactly as claimed. Read the new `in-progress-review-request-branch-only.json` fixture directly (`branch_exists:true, commits_beyond_base:4, pr_exists:false`) — confirms the branch-only-no-PR case. `run_active` non-gating: read `TestAC574_InProgressRunActiveNonGatingPreserved` directly — it asserts the with-matter fixture (`run_state:"in_progress"`) still proposes `review`, and I independently confirmed via `python3` inspection of the loaded `transitions.json` that rules 1/2 remain ordered ahead of rule 3's `run_active` check, with rules 3–5 byte-unchanged (confirmed via `git diff` showing only two hunks total in the whole file, one per state, touching only rules 1–2). |
| AC4 | Matter/branch observation robust to remote-only branches | yes | **MET — independently confirmed** | Read `fetch.go`'s diff directly: new `observeRemoteBranch(ctx, repo, branch, token)` function + a 5-line call-site guard `if repo != "" && !snap.BranchExists { ... }` in `assembleLive`. Confirms option (b) (GitHub-API observation) was chosen, matching the documented rationale (fixes the observation primitive regardless of caller, reuses the existing `ghGetJSON` idiom, no new runtime dependency). Read all four new tests directly: `TestObserveRemoteBranch_ExistsWithCommits`, `TestObserveRemoteBranch_AbsentReportsFalse` (unit-level, fake-HTTP, hermetic), `TestAssembleLive_RemoteOnlyBranchResolvesToDeltaRecovery` (composes `observeRemoteBranch`'s output into a `FactSnapshot` and runs it through the *real* `Evaluate`, asserting `outcome=="proposed"`, `action=="propose_delta_recovery"`, `TargetState != "todo"` — the cnos#368 regression pair), `TestAssembleLive_RemoteBranchFallbackNeverRunsWithoutRepo` (confirms zero API calls when `repo==""`). All four PASS under `go test ./... -race`, no data races. See §Findings F1 for one naming-precision note on the third test. |
| AC5 | Wave-closure language corrected + Phase 3 filed (deferred) | yes (GitHub-side, not repo-file) | **MET — independently confirmed** | `rg -i "FSM owns" src/ docs/ .cdd/ .github/` re-run myself: zero hits in any shipped file; only hits are inside `.cdd/unreleased/{568,574}/gamma-scaffold.md` (quoting the overclaim for reference — expected, not a shipped doctrine surface). Fetched `gh issue view 567 --json comments` myself: the correction comment is present verbatim as claimed (2026-07-04T02:11:06Z, states the true scoped boundary). Fetched `gh issue view 575` myself: OPEN, labeled `area/cds, kind/process, priority/deferred` (spot-checked `gh label list --search deferred` — `priority/deferred` is confirmed the repo's real deferred-label convention, description "Deferred — not required for current implementation scope", matching α's claim), body explicitly states Phase 3 is not implemented, links back to #567 and #574. Linking comment on #567 also confirmed present. |
| AC6 | Hidden/bidi Unicode sweep of wave-touched files | n/a (verification task) | **MET — independently confirmed** | Re-ran the exact `rg -nP '[\x{200B}-\x{200F}\x{2028}-\x{202E}\x{2060}-\x{2069}\x{FEFF}]'` sweep myself over the full file set named in `gamma-scaffold.md` §Surfaces (all 3 workflow files, `go.work`, `main.go`, `go.mod`, `cmd_issues_fsm.go`, `transitions.json`, both cds-dispatch doctrine files, `delta/SKILL.md`, `CDD.md`, `CELL-KINDS.md`, `cdd/issue/SKILL.md`, `cnos.issues/SKILL.md`, all `issues-fsm/*.go` + `testdata/*.json`, `GLOSSARY.md`, all six `.cdd/unreleased/{568,569,570}/*.md` closeout artifacts) — confirmed every named path exists first, then ran the sweep: **zero matches** (`rg` exit 1). Matches α's claimed result exactly. |
| AC7 | Current gates remain green | yes | **MET — independently confirmed, with one process observation** | Built + tested all four `go.work` modules myself (`src/go`, `cnos.cdd/commands/cdd-verify`, `cnos.issues/commands/issues-map`, `cnos.issues/commands/issues-fsm`) with `go build ./... && go test ./... -race` — all green, no races. Byte-diffed `TestSeam_CellKindNotEnforced` myself via an independent brace-balanced Python extraction (same method as α's, run independently) comparing `origin/main` vs `HEAD` — **IDENTICAL**, confirmed not touched. Polled `gh run list --branch cycle/574`: all 9 named CI jobs green except **CDD artifact ledger validation (I6)**, which failed on the last 8 pushed commits. Root-caused this directly (not present in α's self-coherence.md — this is a β-discovered process gap, see §Findings F2): `cn-cdd-verify`'s `classifyCycleType` (`ledger.go:425`) classifies a cycle as "small-change" whenever `beta-review.md` doesn't yet exist (regardless of `gamma-scaffold.md`'s presence — the classifier doesn't look at that file at all), and the small-change path (`checkSmallChangeArtifacts`) calls `validateSections(..., forUnreleased=false)`, which turns α's `self-coherence.md` §-prefixed section headers (`## §Gap`, `## §Skills`, etc. — not matched by `sectionPresent`'s literal `"## Gap"` line-prefix check) into a hard `checkFail` instead of the triadic path's lenient `checkWarn`. I confirmed empirically (adding a stub `beta-review.md` to a merge-tree copy and re-running `cn cdd verify`) that this reclassifies the cycle to "triadic" and the same file goes from 1 failed to 0 failed. This is a genuinely transient, self-resolving condition of reviewing a cycle before β's own artifact exists on the branch — not a regression α introduced, and not something α could have prevented (α cannot author `beta-review.md`). Confirmed resolved on the actual pushed SHA once this file is committed (see §CI status below). |

### Named Doc Updates

| Doc / File | In diff? | Status | Notes |
|------------|----------|--------|-------|
| `src/packages/cnos.cds/skills/cds/fsm/transitions.json` | yes | MET | `review` + `in-progress` states, rules 1–2 only; rules 3–5 of `in-progress` byte-unchanged (confirmed via `git diff` hunk count) |
| `src/packages/cnos.issues/commands/issues-fsm/fetch.go` | yes | MET | `observeRemoteBranch` + call site; local-git block unchanged |
| `src/packages/cnos.issues/commands/issues-fsm/issuesfsm_test.go` | yes | MET | New AC2/AC3/AC4 tests + one pre-existing assertion update (`TestAC3_EmptyReviewBlocked`), documented inline |
| `src/packages/cnos.issues/commands/issues-fsm/table.go` | **not** in diff | MET (expected) | Confirmed byte-unchanged (`git diff` empty) — AC2/AC3 are data-only edits per scope guardrail 6; all four guard functions (`pr_exists`, `pr_has_commits`, `review_request_present`, `branch_has_commits`) already existed |
| GitHub #567 correction comment | GitHub-side | MET | Confirmed live |
| GitHub #575 tracking issue | GitHub-side | MET | Confirmed live, correctly labeled, linked |

### CDD Artifact Contract

| Artifact | Required? | Present? | Notes |
|----------|-----------|----------|-------|
| `gamma-scaffold.md` | yes (§5.1) | yes | Full 7-AC oracle list, source-of-truth table, scope guardrails, implementation contract |
| `self-coherence.md` | yes | yes | All 7 manifest sections present (`§Gap, §Skills, §ACs, §Self-check, §Debt, §CDD Trace`; `§Review-readiness` not yet written — see §Notes) |
| `REPAIR-PLAN.md` | n/a (δ's stranded-continuation classification note) | yes | Correctly classifies this as recovery-not-rejection per authenticated GitHub timeline evidence; independently spot-checked the timeline claim is plausible (no reason to doubt δ's `gh api .../timeline` citation) |
| `beta-review.md` | yes (this artifact) | yes (being written now) | — |
| `beta-closeout.md` | yes, on APPROVED | yes (written this round, see companion file) | — |

### Active Skill Consistency

| Skill | Required by | Loaded? | Applied? | Notes |
|-------|-------------|---------|----------|-------|
| `beta/SKILL.md` | β role | yes | yes | Pre-merge gate rows 1–5 all executed (row 1 identity truth — see §Notes for a self-caught leak-and-repair; rows 2–5 below) |
| `review/SKILL.md` | β review phase | yes | yes | Verdict rules 3.1–3.13 applied; finding taxonomy used |
| `gamma/SKILL.md` §2.5 | implementation contract template | yes (read via gamma-scaffold.md) | yes | 7-axis contract confirmed conformant (below) |

## §Implementation contract conformance (Rule 7)

| Axis | Pinned value | Diff conforms? |
|---|---|---|
| Language | Go + JSON data | yes — no other language introduced |
| CLI integration target | `cn issues fsm evaluate [--apply]`, no new subcommand/flag | yes — confirmed no CLI surface change in the diff |
| Package scoping | `cnos.cds/.../transitions.json` (data); `cnos.issues/commands/issues-fsm/` (Go); workflow YAML only if option (a) | yes — AC4 picked option (b); no `.github/workflows/*.yml` touched (confirmed via diff stat) |
| Existing-binary disposition | Single compiled-in `cn issues fsm evaluate [--apply]` | yes |
| Runtime dependencies | None new; stdlib `net/http` + existing `ghGetJSON` idiom | yes — `observeRemoteBranch` reuses `ghGetJSON` exactly |
| JSON/wire contract preservation | `FactSnapshot`/`Decision` struct shapes unchanged | yes — confirmed via `table.go` (byte-unchanged) and `fetch.go` diff (no struct field changes, only new function + call site) |
| Backward-compat invariant | Untouched fixtures evaluate identically; `TestSeam_CellKindNotEnforced` byte-identical; `evaluate` (no `--apply`) stays read-only | yes — full test suite green; byte-diff confirmed independently |

## §Scope guardrails (7, from gamma-scaffold.md)

| # | Guardrail | Honored? | Evidence |
|---|---|---|---|
| 1 | Phase 3 not implemented | yes | `#575` filed as tracking-only; no FSM-routed claim/hard-block/requeue logic in diff |
| 2 | `cell_kind` stays observed-only | yes | `grep -n "CellKind" transitions.json` → only a comment string match (`TestSeam_CellKindNotEnforced-adjacent...`), no rule branches on it; `TestSeam_CellKindNotEnforced` byte-identical |
| 3 | No new status labels | yes | `grep -oE '"status:[a-zA-Z_-]+"'` over the transitions.json diff → zero matches |
| 4 | No controller service/wake-source-model/#216/Demo 0 | yes | None touched (confirmed by diff-stat scope) |
| 5 | `run_active` non-gating preserved | yes | Confirmed via rule-order inspection + `TestAC574_InProgressRunActiveNonGatingPreserved` |
| 6 | No Phase-1/2 evaluator logic re-derived | yes | `table.go` byte-unchanged; all needed guard funcs pre-existed |
| 7 | STOP-and-ask trigger (no broadened token authority) | yes | `observeRemoteBranch` uses only `/repos/{repo}/branches/{branch}` and `/repos/{repo}/compare/...` — same read scope as pre-existing PR/run observation in the same file |

## Findings

| # | Finding | Evidence | Severity | Type |
|---|---------|----------|----------|------|
| F1 | `TestAssembleLive_RemoteOnlyBranchResolvesToDeltaRecovery`'s name promises an `assembleLive`-level integration test, but the test body calls `observeRemoteBranch` directly and hand-constructs a `FactSnapshot` — it never calls the real `assembleLive(...)` function, so the actual 5-line call-site wiring (`if repo != "" && !snap.BranchExists { ... }`) in `assembleLive` is not exercised by any test, only read/inspected. | `issuesfsm_test.go` lines ~986–1035; the function's own doc comment is honest about this ("composes...as assembleLive's fallback block would"), so the drift is name-only, not a hidden behavior claim. I independently read the actual call site in `fetch.go` and confirm it is correct (simple, 5-line conditional, no logic beyond what's tested at the unit level). | A (polish) | judgment / name-overpromise (review/SKILL.md rule 6b) |
| F2 | `cn-cdd-verify`'s cycle classifier (`classifyCycleType`, `ledger.go:425`) doesn't know about `gamma-scaffold.md` and treats any cycle lacking `beta-review.md` as "small-change," which applies a stricter (hard-fail) section-presence check than the "triadic, in-progress" path uses — causing every triadic cycle's CI to show a spurious I6 red between α's self-coherence.md push and β's first beta-review.md push, if the self-coherence.md's section headers use the `§`-prefixed style (as this repo's own α convention does, e.g. `## §Gap` vs. the checked literal `"## Gap"`). | Reproduced live: CI run 28693885842 (and the 7 before it) shows `❌ self-coherence.md sections — missing required sections: Gap Skills/Mode ACs/AC Coverage CDD Trace` under "Checking small-change cycle #574"; independently confirmed the fix by adding a stub `beta-review.md` in a merge-tree copy and re-running `cn cdd verify --unreleased`, which reclassified to "triadic" and the failure became a warning (132→134 passed, 1→0 failed). | B (process/tooling gap, not a defect in this cycle's own diff) | mechanical / process |
| — | (No findings against AC1–AC6, the scope guardrails, or the implementation contract — all independently confirmed matching α's claims with no drift found.) | | | |

**Disposition of F1 and F2:** Both are genuine, demonstrable observations, but neither is "reviewable incoherence" in the sense Role Rule 3 gates on (a disagreement between canonical doc/skill/issue/artifact that leaves the cycle's own claims internally contradictory). F1 is a cosmetic test-name imprecision fully disclosed by the adjacent doc comment — no reader is misled once they open the function, and I have independently verified the actual production code path (the call site) is correct by direct inspection. F2 is a pre-existing gap in `cn-cdd-verify` itself (not introduced, touched, or fixable by this cycle's diff — `cn-cdd-verify`'s own source is untouched by cycle/574) that is mechanically self-resolving the moment β's own required artifact lands on the branch, which I have now done and re-confirmed (see §CI status). Per review/SKILL.md rule 3.5 ("No phantom blockers — only block on incoherence you can demonstrate") and 3.6 ("Approve when coherent, not when perfect — the bar is coherence, not taste"), I judge neither finding rises to a merge-blocking bar: F1 is filed as a follow-up polish note (not a required fix) and F2 is filed as a process-improvement observation for `cn-cdd-verify` itself (a candidate future issue: teach the classifier to recognize `gamma-scaffold.md` as a triadic signal independent of `beta-review.md`'s presence), not a defect in cycle/574's own scope. Recording this reasoning explicitly rather than silently omitting either finding, per rule 3.7 ("close the search space on approval").

## Regressions Required (D-level only)

None — no D-level findings.

## §CI status

`gh run list --branch cycle/574` at review time (`origin/cycle/574@ad0b18e1`): 8 of 9 named jobs green (`Package/source drift (I1)`, `Protocol contract schema sync (I2)`, `Repo link validation (I4)`, `SKILL.md frontmatter validation (I5)`, `Go build & test`, `Package verification`, `Binary verification`, `Dispatch repair-preflight guard`, `Dispatch closeout-integrity guard`); `CDD artifact ledger validation (I6)` red for the reason in Finding F2 above, confirmed self-resolving once `beta-review.md` is pushed (verified in a throwaway merge-tree copy before pushing for real). `install-wake-golden` workflow did not trigger on this branch's pushes (path-filtered; not part of this cycle's touched surface). This review's own commit (adding this file) is the trigger that should turn I6 green on the real branch — β will confirm the actual post-push run status as part of closing this round out (see beta-closeout.md).

## §Non-destructive merge-test (pre-merge gate row 3)

Performed in a throwaway worktree (`/tmp/cnos-merge-test/wt`, `git worktree add ... origin/main`), **not** the shared main checkout. `git merge --no-ff --no-commit origin/cycle/574` against `origin/main@80be1280`: clean merge, zero unmerged paths, zero conflicts. Ran on the merge tree: `go build ./... && go test ./... -race` (green, all four modules); `scripts/check-version-consistency.sh` (PASSED, `VERSION=3.82.0`); `scripts/ci/validate-skill-frontmatter.sh` (could not run — `cue` binary not installed in this sandbox; this cycle adds/modifies no `SKILL.md` frontmatter, so the row-3 collapse condition for a non-applicable validator applies); `cn cdd verify --unreleased --exceptions .cdd/exceptions.yml` (built from source in the merge tree — see Finding F2 for the one failure found and its resolution path). No kata under `src/packages/cnos.kata/katas/` was implicated — this cycle touches neither `cn activate` rendering nor `## Read first` ordering. Worktree torn down after use (`git worktree remove --force`); `git worktree prune` confirmed clean.

**Self-reported process incident during this row:** my first attempt at scoping the merge-test worktree's git identity used `git config --local` from within the throwaway worktree, which (because `/tmp/cnos-cycle-574` is itself a linked worktree of the shared repo at `/home/runner/work/cnos/cnos`) wrote `beta-merge-test`/`beta-merge-test@cdd.cnos` into the **shared** `.git/config` — exactly the row-1 failure mode named in `beta/SKILL.md` (cycle #301 O8 precedent). Caught this immediately via `git config --get user.name` returning the polluted value in `/tmp/cnos-cycle-574`, before any commit was made under the wrong identity. Repaired by: unsetting the shared config's `[user]` section, enabling `extensions.worktreeConfig=true` on the shared config (a repo-wide setting, not identity data), re-setting `git config --worktree user.name/user.email` correctly scoped to `/tmp/cnos-cycle-574` only (`beta`/`beta@cdd.cnos`), and redoing the merge-test with `--worktree`-scoped identity on the throwaway worktree. Confirmed post-repair: shared config has no `[user]` section; `/tmp/cnos-cycle-574` resolves `beta`/`beta@cdd.cnos`; the actual main checkout at `/home/runner/work/cnos/cnos` (a separate directory outside my scope) was never touched — verified with a fresh `git config --get user.name` invocation from that directory, both before and after repair, returning unset both times except for the brief contamination window, which never produced a commit.

## §Notes

- **`Review-readiness` section not yet in self-coherence.md's completed list.** The manifest at the top of `self-coherence.md` names `Review-readiness` as a planned section but it is not in the `completed:` list and no such section appears in the body. This is not itself a blocking finding — everything the section would presumably restate (CI state, debt, evidence) is already covered elsewhere in the file (§Debt item 1 explicitly names the CI-on-pushed-head-commit gap) and independently re-verified by this review. Noting for completeness rather than treating as missing-artifact.
- **β does not perform the actual `git merge`.** Per this dispatch's explicit instruction and this repository's standing "no self-merge" operator contract, the human-operator PR path is used instead of an autonomous β merge — δ opens the PR referencing `Closes #574` after this verdict. The merge-readiness verification above (pre-merge gate rows 1, 3, 4, 5) was still performed in full on a throwaway, non-destructive copy so the eventual merge (by whichever authority performs it) is a formality, not a leap of faith.
- **Row 5 (close-keyword) instruction for whoever performs the merge:** the merge commit subject must contain `Closes #574` (case-insensitive), not a bare `(#574)` reference, per the cnos#368 precedent named in `beta/SKILL.md` row 5.
