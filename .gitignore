# Dependencies
node_modules/

# Instance-specific directories (belong in hub, not template)
memory/
media/

# Databases and logs
*.db
*.sqlite
*.log

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.idea/
.vscode/
*.sublime-*

# Environment and secrets
.env
.env.*
*.pem
*.key

# Claude Code local config + harness scratch
.claude/settings.local.json
.claude/worktrees/

# Build artifacts
# dist/ is cn build + release output (tarballs, index.json, checksums.txt, per-target cn-* binaries).
# Never committed — see docs/reference/packages/DESIGN-266-dist-out-of-git.md.
dist/
build/
coverage/
*.tgz

# Peer sync cache
.peer-cache/

# Test harness build cache (tests/cdd/test_cn_cdd_validate_receipt.sh)
.cdd-cache/

# OCaml/Dune build output
_build/

# Built binaries
src/cn
src/go/cn
cn
