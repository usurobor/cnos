# Dependencies
node_modules/

# Instance-specific directories (belong in hub, not template)
memory/
media/
# Skill source under cnos.core/skills/agent/memory/ is package content,
# not hub-instance memory (#100 memory faculty).
!src/packages/cnos.core/skills/agent/memory/
!src/packages/cnos.core/skills/agent/memory/**

# Databases and logs
*.db
*.sqlite
*.log

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.idea/
.vscode/
*.sublime-*

# Environment and secrets
.env
.env.*
*.pem
*.key

# Build artifacts
# dist/ is cn build + release output (tarballs, index.json, checksums.txt, per-target cn-* binaries).
# Never committed — see docs/alpha/package-system/DESIGN-266-dist-out-of-git.md.
dist/
build/
coverage/
*.tgz

# Peer sync cache
.peer-cache/

# OCaml/Dune build output
_build/

# Built binaries
src/cn
src/go/cn
cn
